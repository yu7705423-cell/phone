# 小手机 Mini Phone - 架构设计

版本: v1 (draft)
状态: 待确认

---

## 0. 这是什么

一个跑在浏览器里的仿手机前端。外壳是手机，里面装各种 app。核心场景是 AI 角色扮演:
角色卡 + 世界书 + 记忆 + 聊天 + 朋友圈，形成一个自洽的闭环。

### 硬性约束

1. **零 emoji**。所有图标一律 SVG。由 CI 脚本扫描源码强制执行，不靠自觉。
2. **简约大方**。统一设计令牌，禁止硬编码颜色与尺寸。
3. **无构建**。不需要 npm install，不需要打包器。
6. **可扩展到几十个 app 而不互相污染**。这是整套架构最主要的设计目标。

### 非目标

- 不追求像素级复刻某款真机
- 不做多用户 / 账号体系
- 不做服务端(第一阶段)

---

## 1. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 模块系统 | 原生 ES Modules | 静态依赖图,天然模块隔离,无全局命名冲突 |
| 渲染 | Preact + htm (vendor 本地文件) | 约 12KB。流式追加、长列表、输入焦点保持这些场景需要成熟的 diff 算法 |
| 样式 | 原生 CSS + CSS 变量令牌 | 主题切换、深浅色、安全区全靠变量 |
| 状态 | 自写轻量 store + hooks | 数据域集中,订阅式更新 |
| 存储 | IndexedDB (小配置走 localStorage) | 朋友圈与头像需要存图片 Blob,localStorage 5MB 上限不够用 |
| 图标 | 自维护 SVG path 表 | 24px 网格, stroke-width 1.5, currentColor |

### 运行方式

ES Modules 在 `file://` 协议下会被 CORS 拦截(origin 为 null),因此**不能双击 index.html 打开**。
需要一个静态服务器:

```
python3 -m http.server 8000
# 或
npx serve .
```

部署到 GitHub Pages / 任意静态托管即可直接访问。

---

## 2. 分层

依赖方向严格单向,不允许反向,不允许跨层跳跃。

```
  shell      设备外壳: 机身、屏幕、状态栏、Home 指示条、安全区
    |
    v
  system     内核: 数据域、AI 引擎、注册表、导航、Intent、存储、通知
    |
    v
  sdk        应用接口: app 唯一能接触到的系统入口
    |
    v
  apps       应用: 每个 app 一个目录,完全自包含
```

横向基建(不依赖任何层,被各层使用):

- `ui/`     设计系统原语
- `icons/`  SVG 图标
- `styles/` 设计令牌与全局样式

### 铁律

```
apps/*  只能 import:  sdk/, ui/, icons/
apps/a  永远不能 import  apps/b
apps/*  永远不能 import  system/  或  vendor 之外的任何外部地址
system/ 可以 import: ui/, icons/, vendor/
```

违反这条就是后期 bug 的源头。新增 app 时对照检查。

---

## 3. 数据域 (最重要的一节)

**跨 app 共享的数据不归任何 app 所有,一律提升到内核。**

常见错误是把角色卡做成"联系人 app 的私有数据",然后聊天 app 要用就互相引用,
朋友圈要用又引用一遍,三个月后没人敢动。

正确做法: 数据在 `system/db/`,app 只是这些数据的视图。
删掉世界书 app,世界书数据还在,聊天照常注入。

### 3.1 characters 角色卡

```js
{
  id: 'char_xxx',
  name: '角色名',
  avatar: '<dataURL or url>',
  cover: '<主页背景图>',
  signature: '个性签名',
  persona: '人设描述,进入 system prompt 的主体',
  scenario: '场景设定',
  firstMessage: '开场白',
  altGreetings: ['备选开场白'],
  exampleDialogue: '对话示例',
  lorebookIds: ['lb_xxx'],      // 关联的世界书
  tags: [],
  pinned: false,
  createdAt, updatedAt
}
```

### 3.2 lorebooks 世界书

```js
{
  id: 'lb_xxx',
  name: '世界书名',
  description: '',
  global: false,                 // true = 对所有角色生效
  entries: [
    {
      id: 'e_xxx',
      comment: '条目备注(仅供人看)',
      keys: ['关键词1', '关键词2'],
      secondaryKeys: [],         // 有则需二次命中
      content: '注入到 prompt 的内容',
      enabled: true,
      constant: false,           // true = 常驻,不需关键词触发
      priority: 100,             // 预算不足时从低优先级开始丢
      order: 0,                  // 同优先级内的排列顺序
      position: 'system',        // system | beforeChat | afterChat | atDepth
      depth: 4,                  // position 为 atDepth 时,插在倒数第几条消息前
      caseSensitive: false,
      probability: 100           // 命中后的触发概率
    }
  ],
  createdAt, updatedAt
}
```

### 3.3 memories 记忆

沿用 rainyword 的 rank + category + keywords 三件套,补上 scope 分层。

```js
{
  id: 'mem_xxx',
  scope: 'global' | 'character:<id>' | 'chat:<id>',
  content: '简洁的第三人称陈述',
  category: 'fact'|'emotion'|'pending'|'pattern'|'relation'|'profile',
  rank: 'S'|'A'|'B'|'C',
  keywords: ['关键词'],        // B 级必须有,用于命中判定
  source: 'auto' | 'manual',
  createdAt, updatedAt,
  // 下面这些是记忆重构（4.103）加的。老数据没有，读的时候都有默认值
  weight: 1,                  // 当时双方反应有多大，0 到 2。提取时给
  about: 'user'|'char'|'both'|'',   // 关于谁。召回给「关于你的」留保底名额
  slot: 'occupation'|'location'|'birthday'|'school'|'family'|'contact'|'',
                              // 天生只有一个值的槽位，新的来了旧的让位
  dueAt: 'YYYY-MM-DD' | '',   // 待办挂的日子。过了就不再当「还没了结」
  pinned: false,              // 钉住：每轮常驻，不再参与召回
  taboo: false,               // 忌讳：常驻，写明不主动提起
  useCount: 0, lastUsedAt: 0, // 真的被送出去过几次、上次是什么时候
  supersededBy: '' ,          // 被哪一条取代了。取代不是删除
}
```

六类语义(针对陪伴型对话设计,不要换成通用分类):

| category | 含义 |
|---|---|
| `fact` | 关键事实: 姓名、年龄、职业、学校、喜好、经历 |
| `emotion` | 情绪印记: 表达过的情绪、什么让人开心/难过、有效的安慰方式 |
| `pending` | 未完结事项: 提到但没结果的事,如考试、面试、等结果 |
| `pattern` | 互动模式: 说话习惯、称呼偏好、聊天风格偏好 |
| `relation` | 关系阶段: 与角色的关系状态和重要转折 |
| `profile` | 用户画像: 沟通风格、性格特点、价值观 |

`pending` 是让角色能主动追问"上次说的面试怎么样了"的来源,不要省略。

**注入规则**：S 级压进关系底色常驻（4.91），钉住与忌讳的常驻（4.103），
其余按九项打分每轮挑几条（4.103）；C 级不参与召回，仅在记忆管理页可见。
早先的「S / A 全量注入、B 靠关键词」已经不是现状。

**记忆缓冲区** `memoryBuffer[chatId]`: 尚未总结的原始对话,与 messages 分开存,
总结完成后清空。上限 200 条,超出丢弃最旧的。

### 3.4 chats / messages 会话与消息

```js
// chat
{
  id: 'chat_xxx',
  characterIds: ['char_xxx'],    // 数组,为群聊预留
  title: '',
  lastMessageAt,
  unread: 0,
  pinned: false,
  muted: false,
  summary: '',                   // 压缩后的历史摘要
  summarizedUpTo: 'msg_xxx',     // 摘要覆盖到哪条消息
  createdAt
}

// message
{
  id: 'msg_xxx',
  chatId: 'chat_xxx',
  role: 'user' | 'char' | 'system',
  authorId: 'char_xxx' | 'me',
  kind: 'text' | 'image' | 'voice' | 'sticker' | 'typing',
  content: '文本',
  attachments: [],
  turnId: 'turn_xxx',            // 同一次生成拆出来的若干条共用
  raw: '模型原文',                // 只挂在整轮第一条上
  swipes: ['版本1', '版本2'],     // 重 roll 产生的多个版本
  swipeIndex: 0,

  // 引用。双向的：你能引他的，他也能引你的或者自己早先说过的
  quoteId: 'msg_xxx' | null,     // 引的哪一条
  quoteText: '一小段快照',        // 原话删了还要看得见，所以存一份
  quoteRole: 'user' | 'char',
  quoteAuthorId: 'char_xxx' | 'me',

  stamp: '2026-01-01 周三 14:30', // 模型自己写的当地时间，界面上不显示，见 4.67

  status: 'sending' | 'done' | 'error',
  error: null,
  createdAt
}
```

#### 消息操作：长按那一条

编辑、修格式、引用、复制、多选、删除全在气泡的长按菜单里（桌面右键）。
没有一项进「设置」，也没有第二个入口 —— 见 CLAUDE.md 第 5 条。

**重新生成**只在**最后一轮**的角色消息上出现。重生成中间某一轮，
模型看到的历史里还带着它后面那些消息 —— 那些本来是对旧回复的回应，
拿它们当上下文去生成「旧回复」，出来的东西自相矛盾。
入口原先在右上角菜单，叫「重新生成上一条」；挪进长按菜单之后
不用再说「上一条」了，你长按的就是它。

**编辑**改的是「这条的正文」，但正文在哪个字段要看 `kind`：
文字改 `content`，图片改 `prompt`，语音改 `voiceText`，
后两者改完那份媒体要重新生成（`reply.regenMedia`）。

**多选删除**要顺手挪一下 `chat.memoryUpTo`。它指向「已经总结到这条」，
被删掉之后 `pendingOf` 拿 `findIndex` 找不到锚点会返回 -1，
于是整段对话被当成一条都没总结过，下次总结全部重来。
所以删之前先记住锚点的时间点，删完往前挪到还在的那条上。

**引用**存的是 `quoteId` + 一份文本快照。只存 id 的话，原话一删引用就空了；
只存快照的话点不回去。两个都留：原消息还在就跟着它走（可能被编辑过），
删了就退回快照。进上下文时也按这个顺序取 —— 不然模型看到一句「是啊」
根本不知道在应哪句。


### 3.5 moments 朋友圈

```js
{
  id: 'mo_xxx',
  authorId: 'char_xxx' | 'me',
  text: '动态正文',
  images: ['img_xxx'],          // 图片 id 数组,数据本身在 images 表
  location: '',
  createdAt,
  likes: ['char_xxx', 'me'],
  comments: [
    { id, authorId, text, replyTo: null | commentId, createdAt }
  ]
}
```

### 3.6 persona 我是谁

```js
{
  name: '我的名字',
  avatar: '',
  cover: '',
  signature: '',
  description: '我的人设,进入 prompt'
}
```

### 3.65 stickers 表情包

```js
{
  id: 'stk_xxx',
  name: '开心',
  keywords: ['开心', '笑'],   // 输入时按这些词推荐
  group: '默认',
  imageId: 'img_xxx' | null,  // 缓存到本地后有值
  url: 'https://...' | null,  // 远程链接，二者至少有一个
  useCount: 0,
  createdAt, updatedAt
}
```

三种导入方式,都先进确认页选分组再入库:

- **批量选图** — 一次多张,文件名当名称
- **txt** — 每行一个,支持 `名称|链接`、`名称,链接`、`名称 链接`、纯链接,
  `#` 开头跳过。名称里的分隔符同时拆成关键词
- **docx** — docx 就是 zip,用 `system/unzip.js` 解开:
  正文 `word/document.xml` 按段落取文字后同 txt 解析,
  `word/_rels` 里的外部超链接一并收进来,
  `word/media/` 下的内嵌图片直接入库。
  解压用浏览器自带的 `DecompressionStream('deflate-raw')`,不引第三方库

链接类的先按原样存,页面上提供一键「缓存到本地」;跨域取不回来的保留链接,
并在结果里明确报数,不静默失败。

### 3.7 images 图片

朋友圈配图、角色头像、背景图全部由用户本地上传,不走网络。

```js
// images 表
{ id: 'img_xxx', blob: Blob, w, h, bytes, createdAt }
```

规则:

- **以 Blob 存 IndexedDB,不存 base64。** base64 体积涨约 33%,
  且会让承载它的 JSON 记录变得极大,解析明显变慢
- 上传即压缩: canvas 缩放到长边 1280,`toBlob('image/webp', 0.82)`;
  头像长边 256。原图不保留
- 展示用 `URL.createObjectURL(blob)`,组件卸载时必须 `revokeObjectURL`,
  否则每次滚动朋友圈都在泄漏内存
- 业务记录里只存图片 id,取用时按 id 拉 Blob
- 朋友圈单条最多 9 张
- 设置页提供存储占用统计与清理孤儿图片的入口

### 3.8 layout 主界面布局

主界面的网格摆放、挂件尺寸、分页、Dock 内容都要持久化。

```js
{
  pages: [
    { id: 'p1', cells: [
        { id, kind: 'widget',      ref: 'header',       x: 0, y: 0, w: 4, h: 1 },
        { id, kind: 'app',         ref: 'lorebook',     x: 0, y: 1, w: 1, h: 1 },
        { id, kind: 'app',         ref: 'memory',       x: 1, y: 1, w: 1, h: 1 },
        { id, kind: 'widget',      ref: 'recent-chats', x: 2, y: 1, w: 2, h: 2 },
        { id, kind: 'placeholder', label: '待开发',      x: 0, y: 2, w: 1, h: 1 },
        { id, kind: 'placeholder', label: '待开发',      x: 1, y: 2, w: 1, h: 1 },
        { id, kind: 'widget',      ref: 'moments-peek', x: 0, y: 3, w: 2, h: 2 },
        { id, kind: 'placeholder', label: '待开发',      x: 2, y: 3, w: 1, h: 1 },
        { id, kind: 'placeholder', label: '待开发',      x: 3, y: 3, w: 1, h: 1 },
        { id, kind: 'placeholder', label: '待开发',      x: 2, y: 4, w: 1, h: 1 },
        { id, kind: 'placeholder', label: '待开发',      x: 3, y: 4, w: 1, h: 1 }
    ]},
    { id: 'p2', cells: [] }
  ],
  dock: ['chat', 'settings', 'contacts', 'notes'],
  currentPage: 0,
  wallpaper: { home: 'img_xxx', lock: 'img_xxx' }
}
```

坐标显式存储,`x` 取值 0-3。不使用 CSS 自动流: 混合尺寸下自动流的结果不可预测,
且拖拽排序本来就需要显式坐标。

**自愈规则**(与 4.2 的 `getInjectOrder()` 是同一类问题,同样必须有):

- 单元引用了已不存在的 app 或挂件 -> 转为 placeholder,保住版式不塌
- 已注册但未出现在任何页面、也不在 Dock 里的 app -> 优先占用最近的
  placeholder 位置,没有则追加到最后一页空位,不够则新建一页
- 单元重叠或越界(`x + w > 4`)-> 按顺序重新落位
- Dock 引用了不存在的 app -> 转为 placeholder;Dock 不足 4 个 -> 补 placeholder
- 空页面自动删除,第一页除外;`currentPage` 越界 -> 归零

没有这套自愈,以后每删一个 app 或每加一个挂件,主界面就可能出现空洞、
重叠或漏图标,而且是静默的。

### 3.9 存储与迁移

**主存储是 IndexedDB。** localStorage 仅用于极小的配置(当前主题、上次打开的 app)。

早前曾计划 localStorage 先行、后续再切 IndexedDB,该方案已作废:
本地上传图片使得容量需求远超 localStorage 的 5MB 上限,而超限是抛异常,
会导致该域下所有写入一起失败,不是可以推迟处理的问题。

- 键名统一 `phone:<collection>` 与 `phone:app:<appId>:<key>`
- 全局 `schemaVersion`,每次数据结构变更写一个 `migrate_N_to_N+1` 函数
- 适配层接口固定为 `get/set/remove/list/query`,业务代码不直接碰 IndexedDB API
- 角色卡、世界书支持单文件 JSON 导入导出;
  含图片时打包为 zip 或内联 base64(仅导出时转换)

### 3.95 读写性能：三件事

聊得越久越卡，卡的地方和「这个会话有多长」没关系 —— 都是全库扫描和整屏重画。
三处各自处理：

**一、二级索引。** `makeCollection(name, prefix, { indexBy })` 顺手维护一张
`值 -> Map<id, row>`。消息按 `chatId` 建了一张。没有它的时候，翻一个会话要把
全库过一遍，消息列表一屏二十行就是二十遍全库扫描。

每个桶还各自带一个版本号，`messagesOf` 拿它当缓存键：桶没变就直接给上一次
排好序的那份。改 A 会话的消息不会让 B 的缓存作废。

**注意 `messagesOf` 返回的是共享数组，不要就地改它。** 要排序要截断先 copy
一份。现有调用方都是先 `filter` 再动手，天然安全。

**二、写入按行合并。** `idb.js` 把要写的行攒到下一个宏任务，同一行只留最后
一版，一次事务写完。一轮 JS 里改多少行都只开一个事务 —— 导入两千条记录从
两千个事务变成一个。

攒的窗口是**一个宏任务**，不是几百毫秒：落盘仍然紧跟着这一轮，不拿数据换吞吐。
真正高频的那种另有办法，见下。另外 `pagehide` 和切到后台时各补一次 flush。

只有 collection 的行走这条路。图片和音频写完之后会立刻从 idb 读回来
（`images.blob`），还是走原来的 `write()`。

**三、该不落盘的就别落盘。** `create` / `update` 收 `{ persist: false }`：
只改内存镜像和界面。流式回复的「正在输入」占位用它 —— 一秒改几十次，
回复完成后整条删掉，写下去的每一版都是马上作废的东西。

页面中途挂掉，丢的只是这个占位。旧版本留下的占位在开机时清掉
（`messages.removeWhere(m => m.kind === 'typing')`），否则就是一条永远停在
那儿、点不开也删不掉的空气泡。

**四、列表项记忆化。** `lib.js` 的 `memo()`：props 逐项比过，一样就不往下走。
消息气泡和会话行都包了一层。流式回复时只有最后那条在变，先前几百条没必要
跟着重画 —— 每个气泡都要取头像、切一遍正文、算一次引用，加起来就是打字时的卡顿。

**代价是传进去的函数属性必须身份稳定。** 每次渲染现造一个箭头函数，就永远
判不出相等，包了也白包。`Conversation` 用一个 ref 兜住当前这次渲染的闭包，
对外暴露的那层永远不变。同理，现造的对象（不入库的开场白那条）也要钉住。

`memo` 只给**没有 children 的叶子组件**用：children 每次渲染都是新数组。

测下来（8000 条消息 / 20 个会话，桌面浏览器；手机上按 5 到 10 倍看）：

| | 之前 | 之后 |
|---|---|---|
| 消息列表来一条新消息 | 0.53 ms | 0.13 ms |
| 会话页流式来一段 | 4.76 ms | 0.28 ms |
| 两千条记录落盘 | 1529 ms | 138 ms |
| 一条回复改五百次 | 51 ms 的事务 | 一次都不写 |

流式那一项是关键：每来一段就是 4.76 ms，一秒几十段，在手机上就是从头卡到尾。

### 3.96 搜聊天记录：十几万条也不卡

`system/search.js`，入口两个（都通向同一个页面，只差一个 `chatId`）：

| 想搜什么 | 从哪儿进 | 路由 |
|---|---|---|
| 所有对话 | 消息列表顶上的搜索条 | `/search` |
| 这一段对话 | 会话右上角菜单 - 搜索聊天记录 | `/search/<chatId>` |

**不建倒排索引。** 中文没有词边界，要建就得建二元组，十几万条是几百万个
posting，内存和维护成本都不值。靠三件事撑住这个规模：

**一、不另存一份。** 每个会话的消息本来就有一份按时间排好的缓存（3.95 的
`messagesOf`），搜索直接扫它。抄一份十几万条的全局快照本身就要一百多毫秒，
而且随便哪条消息一变就得重抄。

**二、按时间归并着扫。** 几十个会话各自有序，用一个堆从尾往前归并，
扫到的天然就是最新的那条。于是**够数就能停** —— 「晚」这种到处都是的词，
扫几百条就凑满一屏，不必把十几万条都过一遍。

代价是不报总数：报总数就得把十几万条全过一遍，为了一个数字白扫一遍不划算。
凑满就停的时候界面写「仅显示最近 200 条结果」，全扫完了才写「共 N 条」。

用堆而不是每次在几十个会话头部挑最大的：罕见词要把十几万条都过一遍，
每条都挑一遍就是几百万次比较。

**三、分片，按时间切。** 一片跑满 8ms 就让出主线程。片长按时间算而不是按
条数算，所以**手机慢就多切几片，每一片始终只占一帧** —— 这一条是整个方案
的地基，换个更慢的设备结论不变。

让出主线程用 `MessageChannel` 而不是 `setTimeout`：后者有 4ms 下限，
切几十片就白等好几百毫秒。

第一片是**同步**跑的，所以常见词一次都不用让，敲完就出结果。

**结果随扫随出。** 先扫到的就是最新的，一片一片往上加，罕见词也能立刻见到
第一条，不必等整轮扫完。敲字和开扫之间留 140ms，否则每敲一下就开一轮、
扫到一半又被下一下取消，白扫的比扫完的还多。

十五万条消息 / 40 个会话，桌面浏览器实测：

| | 全程 | 首屏 | 最长一片 |
|---|---|---|---|
| 常见词（够数即停） | 0.6 ms | 0.6 ms | 0.6 ms |
| 单字（够数即停） | 0.2 ms | 0.2 ms | 0.2 ms |
| 罕见词组（扫全库） | 76 ms | 8.1 ms | 15.4 ms |
| 搜不到（扫全库） | 33 ms | — | 8.2 ms |

「最长一片」量的是两次回调之间的间隔，中间还夹着浏览器自己的绘制 ——
它没超过一两帧，就是「扫得再久界面也不停」。

### 3.97 会话只画最近一段

聊了两万条的会话，一次性把气泡全铺出来在桌面上要 1.8 秒，手机上十几秒。
所以 `Conversation` 只画最近 `PAGE = 200` 条，上面留一个「查看更早的消息」，
一次再要 200 条。加载更早的那一下用 `useLayoutEffect` 把滚动位置钉住，
不然上面多出内容视口就跳走了。

从搜索结果跳进来的走 `/chat/<id>@<消息 id>`：会话页先把窗口开到装得下那一条，
再滚过去闪一下。**不能只是退回去然后找 DOM** —— 几千条之前的那一条根本不在
DOM 里，找不到就什么都不会发生。限定搜索退回时用 `nav.replace` 换成这条路由，
免得同一段对话在栈上叠两层。

「全选」只选当前画出来的这一段：屏幕上没有的不该被悄悄选中。

| | 之前 | 之后 |
|---|---|---|
| 打开两千条的会话 | 301 ms | 102 ms |
| 打开一万条的会话 | 981 ms | 13 ms |
| 打开两万条的会话 | 1824 ms | 27 ms |

---

## 4. AI 引擎

### 4.1 任务类型

引擎不只会"回一句话",它按任务类型工作:

| 任务 | 说明 | 输出 |
|---|---|---|
| `chat.reply` | 角色回复消息 | 流式文本,可含图片与语音标记 |
| `chat.regenerate` | 重 roll,产生新 swipe | 流式文本 |
| `chat.summarize` | 上下文超预算时压缩历史 | 文本 |
| `memory.extract` | 从近期对话提取长期记忆 | JSON 数组 |
| `moment.create` | 角色主动发朋友圈 | JSON |
| `moment.comment` | 角色评论某条动态 | JSON |
| `moment.reply` | 角色回复用户的评论 | JSON |
| `profile.update` | 角色更新签名/状态 | JSON |

### 4.2 三块结构

**A. ContextProvider 上下文提供者** - 每种上下文一个模块,只负责产出自己那一段:

```
persona     我是谁
character   角色卡(人设、场景、对话示例)
lorebook    世界书激活结果
memory      相关记忆检索结果
history     最近消息
moments     最近朋友圈
time        当前时间、距上次对话的间隔
```

**B. TaskSpec 任务定义** - 声明式,一个任务一个文件:

```js
export const momentCreate = {
  id: 'moment.create',
  context: ['persona', 'character', 'memory', 'history', 'moments', 'time'],
  instruction: '...',
  output: 'json',
  schema: { text: 'string', mood: 'string', imagePrompt: 'string|null' },
  budget: { context: 8000, output: 600 }
};
```

**新增一种 AI 行为 = 新增一个 TaskSpec 文件,引擎代码零改动。** 这是防返工的核心机制。

**B2. 可排序注入与容错**(继承自 rainyword 的 PROMPT_BLOCKS)

`chat.reply` 的上下文区块顺序对用户可配,存在 `settings.injectOrder`。
两条防御性写法必须保留,它们是"加功能不炸老数据"的实际保障:

```js
// 1. 读顺序时做清洗: 丢掉不认识的 id,补上配置里缺失的
function getInjectOrder() {
  const known = Object.keys(CONTEXT_BLOCKS);
  const seen = new Set(), out = [];
  (settings.injectOrder || []).forEach(id => {
    if (known.includes(id) && !seen.has(id)) { seen.add(id); out.push(id); }
  });
  DEFAULT_ORDER.forEach(id => { if (!seen.has(id)) { seen.add(id); out.push(id); } });
  return out;
}

// 2. 每个区块单独 try/catch,一个区块出错不拖垮整个 prompt
for (const id of getInjectOrder()) {
  let text = '';
  try { text = CONTEXT_BLOCKS[id].build(ctx) || ''; } catch (e) { text = ''; report(id, e); }
  if (text) prompt += text;
}
```

没有第 1 条,以后每新增一个区块,老用户的 `injectOrder` 里就缺一项,
该区块永远不注入,而且是静默失败,最难排查。

**B3. 骨架的固定段落**

身份开场与风格收尾不参与排序,固定在首尾。

```
开场:
你不是AI助手。你不是在扮演角色。你就是<角色名>，正在用手机和<用户名>发消息。

收尾:
你的每次回复由 1 条或多条消息组成，用空行分隔。
有时候一条很长，有时候几个字就发出去。分几条、每条多长，你自己定。

这是手机聊天，不是写文章。
你有自己的情绪和立场，反应忠于你这个人，不迁就对方。
```

关于收尾这段的取舍,前后改过两轮:

**第一轮**。rainyword 原文是"一条消息就是一个念头"。它确实压住了长段落,
但把模型推向了另一个极端——每条消息都成为干净、完整、有信息量的单元,
气泡长度趋于一致,每条都在推进对话。真人聊天的本质特征是**不均匀**,
以及**不周到**(对方说三件事,真人常常只接住一件)。于是改成一段更长的描述,
专讲这两点。

**第二轮**。自然表达协议(下一节)进来之后,那段长描述和它大面积重叠:
"不均匀"对应协议指令二,"只接住一件"对应指令五与指令八,几乎一字不差。
更要命的是"废话、语气词、没说完的半句，都正常"和协议指令三**直接打架**——
一边说语气词正常,一边禁止为制造口语感硬加语气词。两条相反的指令同时在场,
模型只会各挑一半执行。

所以收尾收回到只剩这一段独有的三件事:

1. **分条的格式契约** —— 用空行分隔。协议完全不管这个,它假设输出是一整块。
2. **场景框定** —— 手机聊天,不是写文章。
3. **身份锚点** —— 忠于你这个人,不迁就对方。

协议关掉时收尾要能单独站住,所以"有时候一条很长,有时候几个字就发出去"
这一句留着 —— 它是分条契约唯一的具体锚点,抽象地说"由你定"模型撑不住。

87 token,原先 150 出头。省下的不多,重点是不再自相矛盾。

**B3.5 自然表达协议**

收尾后面还接一段更长的行文约束 `skeleton.style`，管的是同一件事：
这一条回复该怎么写。九条指令加一份最终检验 —— 句式去重、句长打散、
禁止固定回应模板、允许情绪错位与漏听、关系校准、留白不升华、整体优先，
最后一条是「自然优先于规则」：某条执行下来显得刻意就暂缓。

它是用户带进来的一份成品 prompt，原样收进模板，没有改写。
为什么单独成一段而不是并进 `skeleton.closing`：

- **一千一百多 token，每轮都要花**。这个量级必须让用户看得见、关得掉，
  所以「上下文与记忆」里有开关，副标题直接写出实时估算的 token 数。
- 收尾那段讲的是「消息怎么分条」，这段讲的是「句子怎么写」。
  两件事分开放，用户改其中一个不会连带动到另一个。

它照样存在 `promptTemplates` 里（第 11 条），在「Prompt 模板」页可以整段改写，
也可以恢复默认。

**B4. prompt 模板可编辑**

骨架的开场、收尾,以及每个 TaskSpec 的 instruction,**一律不写死在代码里**,
而是作为默认值存入 `settings.promptTemplates`,在设置页可编辑,并提供"恢复默认"。

```js
settings.promptTemplates = {
  'skeleton.opening': '...',
  'skeleton.closing': '...',
  'task.memory-extract': '...',
  'task.moment-create': '...'
}
```

这类 prompt 需要长期反复调整,写死意味着每次改语气都要改代码。
代码里只保留 `DEFAULT_TEMPLATES`,运行时一律从 settings 读取,缺失时回落到默认值。

**C. Provider 适配层** - 统一请求与流式解析:

- Anthropic 原生 (默认模型 `claude-opus-5`)
- OpenAI 兼容 (自定义 baseURL,覆盖绝大多数中转服务)

统一的流式接口: `onDelta(text)` / `onDone(full)` / `onError(err)`。
Anthropic SSE 事件流按 `content_block_delta` 逐字追加。
浏览器直连 Anthropic 需要额外请求头 `anthropic-dangerous-direct-browser-access: true`
才能通过 CORS(实现时需实测确认)。

### 4.3 AIQueue 请求队列

**所有 AI 请求必须过队列。** 直接在组件里 fetch 是禁止的。

队列负责:
- 并发上限(默认 1-2,可配)
- 去重(同一 chat 的 reply 不允许重复入队)
- 取消(离开页面时 abort)
- 重试与退避
- 统一错误上报到通知中心

朋友圈下拉刷新可能一次触发 5 条动态生成 + 3 条评论,没有队列会瞬间打爆并发限制。

### 4.4 世界书激活算法

1. 收集候选: 角色关联的世界书 + 标记为 global 的世界书
2. `constant` 条目直接入选
3. `selective` 条目: 在扫描窗口(最近 N 条消息拼接的文本)中匹配 `keys`;
   配了 `secondaryKeys` 则需二次命中;应用 `caseSensitive` 与 `probability`
4. 按 `priority` 降序排序,按 token 预算从低优先级开始丢弃
5. 按 `position` 插入 prompt 的对应位置

世界书 app 必须提供**激活预览**: 输入一段文本,显示会激活哪些条目、各占多少 token。
没有这个调试视图,后期排查 prompt 问题只能盲猜。

### 4.5 记忆注入与提取

**注入**(`context/memory.js`):

1. 按 scope 收集: `global` + `character:<当前角色>` + `chat:<当前会话>`
2. S / A 级直接入选
3. B 级在**扫描窗口**中匹配 `keywords` 才入选
   - 扫描窗口 = 最近 N 条消息拼接的文本,与世界书共用同一个窗口
   - rainyword 原实现只扫最后一条用户消息,窗口过窄:
     上一句说"下周面试"、这一句说"好紧张",记忆就命中不了。必须改
4. C 级不注入
5. 按 rank -> 更新时间排序,**截断到 token 预算内**
   - rainyword 原实现无预算控制,记忆攒到两百条时 prompt 会溢出
   - 记忆管理页需显示"当前注入约 N token"

输出格式：上面这五步是最早那一版。现在挑哪几条见 4.103（九项打分），
写成什么样也见 4.103（按性质分三栏，不再平铺）。最早那个抬头
「请自然地运用这些信息」为什么是错的，见 4.91。

**提取**(`tasks/memory-extract.js`):

- 触发: 每累计 N 轮 AI 回复(`settings.autoSummarizeInterval`,0 为关闭),
  或用户在会话菜单手动"立即总结记忆"

  轮数是**自己填的**,不是从滑块里挑。原先是 0 到 30 的 range,
  上限卡死在 30,而且拖不准 —— 想设 45 根本设不了。
  现在改成数字输入框,配一个独立的开关,0 不再兼任"关闭"这个语义。

  输入过程中必然经过"空"和"0"这些中间状态,直接写进设置会把自动总结
  顺手关掉。所以编辑期间先放在组件本地的 draft 里,只有解析出 >= 1 的
  整数才落盘,失焦再回到真实值。关掉开关时记住上一次填的数,再打开时填回去。

  `memoryEnabled` 关掉时**不再自动提取**。以前它只拦注入,后台照样在提,
  提出来也不注入,白烧接口 —— 而界面上早就写着"关闭后不注入记忆,
  也不执行自动总结",代码却没做到。手动"立即总结"不受这条限制:
  那是明确的手动动作,有人会先把记忆攒起来,回头再打开注入。
- 输入: 记忆缓冲区的原始对话 + **已有记忆档案全文**
- 要求模型以客观第三方视角分析,明确声明"你不是对话中的任何一方"
- 去重靠两条: 提示词里要求"与已有记忆重复的不要输出",
  以及模型可返回 `updateId` 指定更新某条已有记忆而非新增
- `pending` 已有结果的标注"（已完结）",更新过的内容标注"（更新）"
- 输出严格 JSON:

```json
{"memories":[{"content":"","category":"fact","rank":"A","keywords":[],"updateId":""}]}
```

**JSON 解析必须比原实现稳。** rainyword 用 `raw.match(/\{[\s\S]*\}/)` 贪婪匹配,
模型若在 JSON 前后各写一段含大括号的文字就会连带吞掉。正确做法:
先尝试整体 `JSON.parse`,失败再扫描第一个括号平衡的对象,再失败才报错重试。

### 4.6 朋友圈闭环

```
聊天记录 + 记忆 + 角色卡
    |  moment.create
    v
角色发动态
    |  用户点赞 / 评论
    v
moment.reply  角色回复
    |
    v
动态内容写回 memories
    |
    v
影响下一次聊天的 prompt
```

触发时机: 手动下拉刷新 / 聊天结束后按概率触发 / 模拟时间流逝。

朋友圈配图由用户本地上传,不由 AI 生成。`moment.create` 产出的是文字,
是否配图、配什么图由用户在发布时自行选择。

### 4.5 记忆只按角色分，没有全局

记忆挂在 **一个角色 + 一个身份** 上，就这两个维度。
原来还有「全局 / 角色 / 会话」三档 scope，砍掉了：

- **全局** 和「全局世界书」重了。跨角色永远成立的世界设定属于世界书，
  那边本来就有 `global` 开关，还带关键词激活和优先级。
- **会话** 和「角色 + 身份」重了。有了账号体系之后，一个身份和一个角色
  之间就只有一段对话，再按会话分是同一件事说两遍。

数据上是 `memories.charId`。迁移 3 从老的 `scope` 推出来：
`character:X` 直接取 X，`chat:Y` 查那段会话的角色，`global` 留空。

`charId` 留空的老记录**仍然对所有角色生效**（`belongsTo` 里放行），
记忆 app 会单独列一个「没绑定角色 N」的筛选条，点进去挑个角色就能归位。
新写入的记忆一律带 `charId`，不会再产生没归属的。

### 4.55 从文字导入记忆

`system/ai/tasks/memory-import.js`。从别处复制一大段文字粘进来，拆成一条条结构化记忆。

**拆分靠聊天模型，不是向量接口** —— 向量接口只负责把文字变成向量。
两件事在界面上也说清楚了，免得误会。

- **分段**：按空行切段再攒到接近 2400 字；单段就超长的按句号切，不在句子中间硬断。
  太长喂给模型它会偷懒漏东西。
- **去重**：把已有记忆和前面几段已经拆出来的都算进去，按「去掉空白后转小写」比对。
- **先预览再入库**：`parse()` 只解析不写库，界面上列出来让人勾掉不要的，
  再 `commit()` 一次性写进去。一大段文字拆出几十条，总有几条是废话。
- 写进去之后 `touchVec()` 排队补向量，配了向量接口就自动索引。

入口在记忆 app 的右上角，以及空列表时的那个按钮 —— 记忆库自己的事放在记忆库里。

### 4.5 主用与副用：后台活儿走副用

`services.chat` 里可以存多个接口，指定一个主用、一个副用。副用原本只是
主用报错时的兜底，现在还多了一个用途：**把后台活儿分流过去**。

分界线是「用户会不会盯着屏幕等这个结果」：

| | 走哪个 |
|---|---|
| 聊天回复、主动消息、朋友圈动态与评论 | **主用**（你正等着看） |
| 自动总结记忆、历史压缩、从文字导入记忆、导入角色卡、批量生成 NPC、角色琢磨开小号 | **副用**（慢一点无所谓） |

代码上是 `BACKGROUND_TASKS` 这个 id 集合，`runJSONTask` / `runTextTask`
按 taskId 选 runner：后台走 `withSpareFirst`（副用优先，挂了退回主用），
其余走 `withFallback`（主用优先，挂了顶给副用）。两个方向共用 `tryBoth`。

三条规矩：

- **主用和副用可以是同一个预设。** 只有一个中转站的人也该能把后台活儿
  分出去（虽然分了也还是同一条线，但语义上说得通，也方便以后换）。
- **半填的预设当没配。** `config()` / `fallbackConfig()` 只认填全了密钥和模型的，
  否则每个后台任务都要先往它撞一次墙再退回来，白白慢一倍还刷一屏报错。
- **两个开关都能单独关。** 主用关掉就是「没配接口」，聊天发不出去，这是
  合法状态；副用关掉就没有兜底。不做「必须选一个」的强制单选。

报错里会带上是哪个预设挂的（`预设名：具体错误`），不然一句「请求失败」
根本没法查。

没配副用、或者在「接口」页把开关关掉，就全部退回原来的行为。
副用一般更便宜也更慢，这么分既省钱，又不会让整理记忆挡住聊天。

### 4.6 向量记忆

记忆检索从「硬碰关键词」换成「按意思找」。`system/ai/embed.js` + `system/ai/memvec.js`。

- **接口**：OpenAI 那套 `/v1/embeddings`，OpenAI 本体、大多数中转站、本地 Ollama
  都是这个形状。配置在 `services.embed`，属于「服务」，和聊天/语音/生图一样放全局设置。
- **向量存在记忆记录自己身上**（`m.vec`），类型是 `Float32Array` ——
  IndexedDB 的结构化克隆原生支持，比存 JSON 数字数组小一半还多。
- **存之前归一化**，检索时余弦相似度就退化成点积，省一半计算。
- **建索引是批量的**，接口本来就收数组，9 条记忆一次请求发完。
- **查询向量按文本缓存**（`embedQuery`），同一个扫描窗口连发几轮不会重复算。

向量现在只是召回打分里「线索」那一项的三条来路之一（关键词命中、向量相似度、
本地二元组相似度取最大），见 4.103。`selectByVector` 与 `select` 两条老路已经撤了。

**回落照旧不影响聊天**：接口没配、还没补向量、这一轮取向量失败，线索那一项就少
向量这一路，本地二元组仍在。`queryVecFor()` 只警告不抛。

换了嵌入模型，旧向量和新查询就没法比了，`isStale()` 按 `vecModel` 判定，
界面上直接显示「差 N 条」。

`buildChatSystem` 是同步的，算向量要发请求，所以查询向量由 `queryVecFor()`
在外面先算好、作为 `opts.queryVec` 传进去。

开关分两处，按 CLAUDE.md 第 5 条：接口在 `设置 - 向量`（全局服务），
怎么用它在 `会话菜单 - 上下文与记忆 - 怎么找记忆`（这段对话的检索行为）。

### 4.645 功能说明按需注入：先给目录，用上了再给细则

`system/ai/capabilities.js`。

十来段功能说明堆在人设后面，问题不只是 token —— 是**注意力**。模型的注意力被
一堆「你还可以发转账、可以打电话、可以送礼物」摊薄了，聊天本身反而写不好。

所以每个能力有两种形态：

| | 是什么 | 什么时候给 |
|---|---|---|
| **目录** | 一行，说清它叫什么、写哪一行会生效 | 平时 |
| **细则** | 整段，讲格式、边界、什么时候别用 | 这一轮真沾边了 |

「沾边」= 最近 8 条消息里用过这个标记，或者这段对话里挂着未处理的状态
（一笔没收的钱、一件没拆的礼物）。

**目录那一行必须自带正确的写法。** 冷着的时候模型拿不到细则，照着目录写出来的
**第一次**就得能落地；落地之后这个能力就热了，下一轮细则跟上。所以目录行里
写的是 `[转账：金额 留言]` 这种可以直接照抄的东西，不是「你可以转账」。

**两个例外常驻**：时间戳和译文是**每条消息的义务**，不是「想用再用」的功能，
冷着注入等于把它们关掉。表情也算半个例外 —— 名字单子本身就是内容，一个不给
等于没这功能，所以它常驻，只是冷着时只列 12 个名字，热了才列 60 个。

实测（全部能力打开、不含自然表达协议）：每轮 1396 token 降到 776，省 620。

开关在「上下文与记忆 - 按需注入」，默认开。

### 4.65 一轮回复如何落成多条消息

真人是一条一条发的,所以一次生成不再是一个大气泡。

**分条的依据是换行，一个换行就够**。早先按空行切，模型写的单换行全被并进一条，
于是几句话黏成一大段气泡。现在 `splitReply` 按 `/\n+/` 切，
连着几个换行和一个换行是一回事，模板里也只写「换行就是分条」这一条规则。

**标记**。回复里可以出现这几种标记,由模型自己决定什么时候用:

```
[图片：画面的描述]      交给生图接口
[语音：要说的话]        交给语音接口
[表情：名字]            从表情库里挑一个发出去
[引用：那句话的一小段]   单独成行，挂到它下面那一条上
[时间：2026-01-01 …]    单独成行，剥掉不显示，见 4.67
[译文：这条消息的译文]   单独成行，挂到它上面那一条上，见 4.673
[转账：88.00 生日快乐]   落成一张待收款的转账卡，见 4.675
[收款] / [退回]          单独成行，处理对方转过来的那一笔，自己不占气泡
[位置：东京塔 港区芝公园] 落成一张位置卡，第一个空格前是地点名，见 4.677
[来电] / [视频来电]       单独成行，对方那边响起来，不占气泡，见 4.678
[礼物：封面 | 拆开是什么]  竖线前是拆开前看到的，后面是里面装的，见 4.676
[拆开] / [拒收]          单独成行，处理对方送来的礼物，不占气泡
[一起听]                 单独成行，拉对方一起听歌，不占气泡，见 4.679
[点歌：歌名]             听的时候换一首，认不出来就当没点过
[建歌单：歌单名]         建一个属于这个角色的歌单
```

中英文冒号、半角与全角方括号都认。图片和语音只在对应接口配好、且角色卡上开了
对应开关时,才会把说明加进 system prompt——没配却让它发,只会生成失败。

表情不需要接口，但要把**表情名原样列给模型**，它才知道可以写什么。
名字对不上就退回关键词、再退回包含匹配；还是找不到也照样发出去，
气泡上显示它想发的是哪个 —— 悄悄吞掉一条消息比显示一个陌生名字更糟。
表情库可能有几百个，全列出来光这一段就把预算吃掉了，所以按使用次数取前 60 个。

**时间行必须能认出没有标签的写法**。模板让模型写 `[时间：…]`，
但它常常只丢一个 `[2026-01-01 周三 14:30]`，标签说掉就掉。
认不出来那一行就当正文渲染出去了 —— 而且它挡在最前面，
后面那行 `[引用：…]` 也跟着剥不掉，整条消息全乱。已经这么出过一次。

所以时间行走的是**独立的一道预处理**：整行就是一个时间戳的，
不管在第几行都摘掉，只留第一个挂到整轮第一条上。
模型经常每条都写一遍，那样白白多花 token 也没有额外信息 ——
一轮回复就是一个时刻。

判定往保守了写：括号里必须只由数字和时间用字构成，
且确实带着钟点或日期的样子（有冒号，或者形如 2026-01-01），
免得把「（3秒）后他抬起头」这种正常内容当成时间戳切掉。

引用不一样：它不占气泡,也不需要任何接口。读到就记下来,挂到紧随其后的那一条上。
模型引的是原话里的一小段,`resolveQuote` 拿它回头在最近 40 条里找出处
(去掉空白之后互相包含即算命中,取最近的一条)。认不出来也不丢 ——
原样存进 `quoteText`,气泡照样显示,只是点不回去。

一次落成多条时,`materialize` 是唯一的落库口子。「修格式」重新分条走的是同一条路,
不然两边各写一遍,迟早在字段上走岔。

**拆分与送达**。流式先打进一条临时的「正在输入」,结束后删掉它,
把原文按空行和标记拆成若干条,**逐条插入并按长度停顿**,读起来像真人在打字。

**一轮**。同一次生成的所有消息共用 `turnId`,原文与候选只挂在这一轮的第一条上。

- 重新生成:整轮删掉再生成,新原文追加进候选
- 左右切换候选:**不再调接口**,拿存下来的原文整轮重放,且不停顿

**媒体是异步补上的**。图片和语音消息先以 `media: 'pending'` 落库,
生成完再回填 `imageId` / `audioId`;失败则记 `media: 'error'` 并保留原因,
界面上直接显示为什么没出来,不静默失败。

音频存在独立的 `files` 仓库(images 那边会压缩,音频不能压),
每条语音可以单独存到本地。

### 4.655 历史截到哪儿：按条数还是按轮次

`buildHistory` 有两种截法，在「上下文与记忆 - 历史范围」里选
（`settings.historyMode`）。

| | 取什么 | 什么时候用 |
|---|---|---|
| 按条数 | 最近 `historyLimit` 条消息 | 长度可控，但可能只截到一轮的后半段 |
| 按轮次 | 最近 `historyTurns` 轮 | 整轮进入或整轮不进入，不会把一轮劈开 |

**一轮 = 用户连着发的那几条，加上角色接着回的那几条**，用户重新开口的地方
就是下一轮的起点（`takeTurns`）。本项目的回复本来就是一次生成落成好几条
（4.65），用户这边也常常连发三四条，所以「条」和「轮」差得很远：
按 20 条截，很可能正好从角色那轮回复的中间切开，角色看不到自己这轮
开头说了什么，也看不到用户这轮开头问的是什么。

`currentTurn()` 是同一套切分的另一半 —— 角色还没回的那几条，
即当前这一轮。识图的 `chat` 档用它决定哪几张图要带（4.665）。

两种截法之后都还要再过一遍 `contextBudget`，预算是硬上限。

### 4.66 修格式：模型降智时的确定性补救

模型偶尔会把格式写歪：标记换成别的括号、开头多报一遍自己的名字、
整段裹一层引号、换行打成字面的 `\n`、动作用 `*星号*` 而不是中文括号。
这些都是**确定性**的问题，不值得再调一次接口重生成一遍 ——
重生成还会把内容也换掉，而你要的只是格式。

`system/ai/repair.js`。长按那条消息 → 修格式。

- `fixesFor(msg)` 只返回**这一条真用得上的**修法，每条附一份「改完长什么样」。
  一条都认不出来时，长按菜单里根本不出现「修格式」这一项 —— 不留死入口。
- `applyFix(msgId, id)` 应用一条；`applyAll(msgId)` 按顺序叠着全修一遍。
- 顺序要紧：先还原 `\n`，再去名字、去包裹引号、换星号，**最后才重新分条** ——
  换行没还原出来，分条不知道该在哪儿断。

「重新分条」这条会先把歪掉的标记扶正（`normalizeMarks`，整行是媒体标记
但括号或冒号写错的，统一改写成标准写法），再交给 `splitReply`。
拆出来的消息用**小数递进的 createdAt** 插在原位：排序照旧，
又不会挤到下一条消息后面去。

判定都往保守了写，宁可漏也不要误伤：

- 「去掉开头的名字」只在开头那个名字**确实等于这个角色的名字**时才认，
  不然会把「老板：明天开会」这种正常内容切掉半句
- 「去掉包住整段的引号」要求首尾成对，**且中间不再出现一次结尾符** ——
  否则「他说“好”，然后走了」会被拦腰截断

### 4.665 用户发来的图片与语音：先读成文字

聊天接口只收文字。用户发的图片，角色本身是看不见的；用户录的语音，
角色也听不见。所以这两样都要先经过一道「读成文字」，
读出来的东西写回消息的 `content`，随消息一起进上下文。

```
用户选图  -> images 域（压缩存本地）-> 识图接口 -> content = [图片：描述]
用户录音  -> files 域（原件不压）   -> 识别接口 -> content = [语音：转写]（听起来…）
```

媒体本身永远留在本地，只有读出来的那段文字进 prompt。

**生图提示词分两层**：全局那份在「设置 - 生图」，管画风；角色那份在角色卡上，
管这个角色长什么样。拼的顺序是**画面描述、角色提示词、全局提示词** ——
画面在最前面，因为那是这一张要画什么，后面两段是「一直都这么画」。

**锁脸**（`system/ai/imageprompt.js`）解决同一个角色每次生成长得都不一样。两条路：

| | 怎么做 | 代价 |
|---|---|---|
| 读成外貌描述 | 脸图交给能看图的模型读成一段话，缓存在角色卡上 | 任何生图接口都能用，只花一次识图调用 |
| 直传参考图 | 脸图塞进生图请求（OpenAI 兼容的 `images/edits`，multipart） | 像得多，但接口得支持 |

两条同时开就**先试直传，接口不认再退回描述** —— 退回去也比画成另一个人强。
接口支不支持是**接口的属性**，所以开关在生图预设上；脸图是**角色的属性**，
所以在角色卡上。

什么算「涉及脸部」：`off` / `涉及本人时` / `始终` 三档，默认中间那档，
按自拍、正脸、镜子这类词加上角色自己的名字来判断 —— 模型写「阿岚站在窗边」
画的就是她本人。判不准就调到「始终」。

换了脸图，缓存的那段外貌描述会一起清掉，不然新脸配旧描述。

**识图是选开的，共三档**（设置 - 识图，`services.vision.mode`）。
不少聊天模型自己就能看图，再单独配一套识图接口纯属多花一次调用。

| 档位 | `mode` | 图怎么到模型那儿 |
|---|---|---|
| 关闭 | `off` | 不读图。照样能发，消息上标一句「角色看不到这张图」 |
| 交给聊天模型 | `chat` | 不调识图接口。发起回复时，把**当前这一轮**发的图以 dataURL 挂在对应那条消息上 |
| 单独的识图接口 | `api` | `system/ai/vision.js` 先读成 `[图片：描述]` 写回 `content` |

**`chat` 档按轮次带图，带过一次就换成文字。** `engine.js` 的 `imagesFor()`
只取 `currentTurn()` 里还没有描述的图；`buildHistory` 把它们放进
`{ role, content, image }`，各 provider 再转成自己的内容块形状
（OpenAI 是 `image_url`，Anthropic 是 `source.type = 'base64'`）。
带图的两条消息不做相邻合并，合并会把图对应的那句话挪走。

回复流结束后 `describeCarried()` 接着跑：用同一个聊天模型把刚看过的图写成一段
描述，`content` 改成 `[图片：描述]`、`vision` 标 `done`。从下一轮起这条消息就是
纯文字，不必再传图。不 await —— 描述是给以后几轮用的，这一轮模型已经看过原图了。

按轮次而不是「最近 N 张」：一张压缩过的图也有几十上百 KB，每轮重传一遍，
越聊越慢也越聊越贵，而模型看到的还是同一张。反过来，当轮的图必须是原图 ——
这一档存在的理由就是让模型自己看，退成描述的话它和 `api` 档就没区别了。

描述失败时标 `vision: 'error'`，气泡下面写明原因。这一轮模型是看过图的，
丢的只是往后几轮的上下文；补救办法是长按消息改那段文字。

三档都不把图片上传到任何中转存储，`chat` 档也只是把 dataURL 写进请求体。
没配置也照样能发图 —— 发出去却不告诉用户对方看不见，比发不出去更糟。

**语音识别**（`system/ai/asr.js`，设置 - 语音识别）。分两档：

| 档位 | 走哪个端点 | 拿到什么 |
|---|---|---|
| 仅转写文字 | `audio/transcriptions` | 文字 |
| 同时识别语气 | `chat/completions` + `input_audio` | 文字、语调、情绪、语速 |

进阶档要求模型本身能收音频（gpt-4o-audio-preview 那一类），
普通的 whisper 端点填进来会报错。识别方式是一个用户可选的档位，
不是自动降级 —— 两者的模型名不一样，猜不出来。

模型没按 JSON 格式回的时候，把它吐出来的文字当转写用上，不整条丢掉。

**没配接口时的保底是浏览器自己的识别**（`system/audio.js` 的 `listenLocally()`，
Web Speech API）。不花钱、不出设备，代价是只有文字，没有语调，而且 Safari 之外
支持得并不齐。所以它是保底，不是默认：接口配好了就走接口。
`asr.js` 的 `canSendVoice()` 把两条路并成一个判断，界面只问「能不能发语音」。

再往下还有一档 **写成语音**：用户直接输入文字，存成 `kind: 'voice'` 但不带音频。
气泡和真录的一样，角色读到的也一样。

**它不单独占面板入口**，就挂在「语音」这一个入口下面：录音条上有「改为输入」，
录不了或者识别不了时也直接落到这条路上。对角色来说收到的都是一条语音，
没有区别；为同一件事在面板上摆两个格子，只是让用户先做一道不必要的选择题。

**为什么要先转 wav**。`MediaRecorder` 在各浏览器上给出的格式不一样
（Chrome 是 webm/opus，Safari 是 m4a），而 `input_audio` 只认 wav 和 mp3。
所以存下来的是原件，送去识别前用 WebAudio 临时转一份 16k 单声道 wav
（`system/audio.js`）—— 解码、重采样、写 PCM 头，不引第三方库（规约第 9 条）。
wav 只在请求期间存在，不入库。

**编辑的语义按谁发的分**。角色发的图片和语音是按描述生成出来的，
改了描述就得重新生成一份；用户发的是自己选的图、自己录的音，
媒体本身不动，改的只是角色读到的那段文字。这两件事看着像，
但走的是完全相反的路，`MsgMenu` 里按 `role` 分开处理。

### 4.675 转账：两边都能发，收到的一方决定收还是退

`system/transfer.js`。状态只有三个，存在消息自己身上：`pending` 发了还没处理、
`taken` 收下了、`returned` 退回了。**处理的人永远和发的人相反**，所以界面上
只有对方发来的那一张点得动，自己发的只能等（或者长按删掉）。

用户这边从面板的「转账」发起；角色那边写一行 `[转账：金额 留言]`。
反过来，角色处理用户转来的那一笔就写一行 `[收款]` 或 `[退回]`，
用户处理角色转来的那一笔就点气泡选。两条路最后都汇到 `settle()`。

**`[收款]` 必须带方括号**才算指令。不带的话，「退回」两个字单独成行的正常
句子也会被当成指令 —— 这类误判比漏判难查得多。金额读不出来的 `[转账：一百块]`
整条丢掉：凭空造一笔金额不明的转账比少发一条更糟。

**处理完要落一行提示**（`kind: 'notice'`，居中一行灰字，不占气泡）。
转账那一条可能早翻上去了，状态在原地改一下没人看得见；而且上下文里也得有
这么一句，否则对面只能从括号里那三个字反推刚刚发生了什么。

**提示行就是这件事的记录，删了它就当没处理过。** `dropMessage` 看到
`notice.settledId` 就把那笔转账退回 `pending`。重新生成角色那一轮时整轮清空，
走的也是这里 —— 于是「模型上一轮收了，重 roll 之后可以改成退回」是自然成立的，
不用另写一套回滚。

金额一律 `Math.round(n * 100) / 100` 收口再落库，免得 0.1 + 0.2 那种东西跑到
界面上。上限一百万，纯粹是防手滑多打几个零。

开关在**角色卡**上（`canTransfer`，第 5 条：属于这个角色的事放在这个角色身上）。
注入不按「聊过才讲」省 token —— 那样角色永远迈不出第一步，只能等用户先转一笔，
这个能力对它就成了单向的。规则本身在 `skeleton.transfer`，嫌它话多可以自己改。

#### 币种

`system/currency.js`。十来个常见币种，另加一档「不显示符号」。
**只影响显示和小数位，不做任何汇率换算** —— 一段对话里两个人用的是同一种钱，
换算没有意义。

小数位跟着币种走：日元韩元本来就不写小数，写成 88.00 一眼就假。
收口在**落库之前**，不然界面上四舍五入过的数字和库里那个对不上。

**币种记在每一笔转账上**，不是只看当前设置。改了设置只影响此后发出的，
已经发出去的不动 —— 那笔钱是按当时那个币种谈的，改显示等于事后篡改。

入口在转账面板里那一行（和翻译一样：全局的东西，放在用它的地方）。
上下文里给模型的一律是纯数字，币种通过 `skeleton.transfer` 里一句话告诉它，
省得它把符号当成金额的一部分再抄回来。

### 4.677 位置：发的是虚拟定位

`system/place.js`。一个地点名加一行地址，**不读设备 GPS，也不查任何地图接口**。

这不是偷懒。用户的真实坐标不该为了一句台词交出去；角色那边更是压根没有坐标 ——
它「在东京」是设定，不是事实。既然两边都只需要这两行字，就别去接一个
既拿不到又不该拿的东西。气泡上也不画一块假的地图底纹，只有图标。

模型写 `[位置：地点名 地址]`，**第一个空白之前是地点名**，后面是地址。
中文地名里一般没有空格，所以整行没空白就当成只写了地点名。

**角色报的地点要落在它自己待的地方。** 时区设了就把
`clock.zoneLabel(char.timezone)` 塞进 `skeleton.location` 当锚点
（「（你在日本 · 东京）」）；没设就只写「和你的设定对得上」，
不替它瞎猜一个城市 —— 猜错了比不说更糟。开关同样在角色卡上
（`canSendLocation`）。

### 4.678 通话：把回合藏在一个电话界面后面

`system/call.js` + `apps/chat/pages/CallScreen.js`。

**这不是实时语音，也不装成实时语音。** 一轮要走完「识别、模型、合成」三步，
最快也要一秒多，真人打电话的停顿是零点几秒 —— 差一个数量级。所以整件事按
**回合**做：说完一句等对方，对方说完再轮到你。真人打电话本来也是这样，
只要每一步的等待是可见的，回合制并不出戏。

三条让它不至于难熬：

**一、按句切。** 模型流式吐字，遇到句末标点就把这一句排进播放队列，不等整段。
等待只剩第一句的时间，后面的句子在前一句播放期间就备好了。这一条不做，
通话就是「按一下，等六秒，听一段」，是对讲机不是电话。

**二、字幕是主角。** 默认不发声，只出字幕。没配语音接口的人照样能用，
而且一分钱不多花。想听声音在通话界面上点开喇叭 —— 配了语音合成且角色有音色
就走接口（那才是这个角色的声音），否则退回浏览器自带的 `speechSynthesis`：
机械，但不花钱也不用等。和识图、语音识别是同一套「配了就走接口，没配有保底」。

**三、system 只拼一次。** 世界书、记忆、人设在通话期间不会变，每轮重算一遍
纯属白花时间，还要多跑一次向量检索。回复上限也压到 400 token —— 电话里说一两句
就停，给多了反而诱导它一口气讲完。

**两边都能打字，也都能说话。** 我这边默认打字，点麦克风改成说；识别常开，
靠**静音断句**（`audio.listenStream`，结果停止增长 1.3 秒就算一句说完）——
通话里不能让人按住说话，真人打电话没有这个动作。我一开口就把它正在播的掐掉。

**接不接通不花接口调用。** 直接看角色卡上那个免打扰时段（那本来就是这个人的
作息），睡着的时候接不到电话是天经地义的，再掺一点随机免得成了闹钟。

**整通电话只落一条消息。** 通话中的每一轮都不落库，挂断时合成一条 `kind: 'call'`
的记录：方向、结果、时长、全文。二十轮对话不该把会话列表冲垮，而角色下次聊天
照样要记得电话里说了什么 —— 所以正文里带着整段对白，气泡上只画那张记录卡，
点开看全文。太长了由已有的注入预算和历史压缩接管，不用新写一套。

**角色可以打进来**：一行 `[来电]`（要带画面就写 `[视频来电]`）。不占气泡 ——
电话是一件事，不是一条消息。`reply.js` 用动态 `import` 拿 `call.js`：call 要用
engine，engine 又要用 reply，静态引会成环。

**电话盖在所有东西上面**，所以通话界面挂在 `screens/CallLayer.js`，由外壳渲染，
不属于聊天 app —— 在主界面、在别的 app、在锁屏上接到都是同一回事。层级
`--z-call: 95`，压过 dock、横幅和所有浮层。

这一层在 ErrorBoundary **外面**，它炸了整个外壳跟着白屏。所以没在打电话的时候
它一个模块都不碰（hook 照常调，顺序不能变）—— 空转时多查一样东西，就多一处
能把外壳拖垮的地方。这条是测试逼出来的：`accounts.current()` 会读 settings，
而自愈那组测试正好会把 settings 弄坏。

喇叭、摄像头、麦克风是**通话控件不是设置项**，记住上次怎么用的就行，
一个设置页都不加。能不能通话在角色卡上（`canCall`）。

#### 视频通话

对方那一面铺满整个屏幕：角色卡上传了「视频通话画面」就用它，没有就拿头像顶上，
两样都没有（头像本来就是个首字）就退回语音那套版式 —— 铺一块空白不如老老实实
把那个首字放中间。

**我这一面可以是虚拟的，也可以是真的。** 默认虚拟：小窗里放我的头像。点摄像头
键换成真实画面，`system/camera.js` 开 `getUserMedia`。

**角色能不能看见我，取决于识图那一档，而且只认「交给聊天模型」。** 另配一套识图
接口也能读图，但那是先调识图拿描述、再调聊天接口 —— 通话里每一轮多一次往返，
等得人想挂电话。这一档聊天模型自己就能看图，画面跟着那一轮请求一起过去。
看不见的时候界面上直说，不含糊。

一轮最多带一帧，且离上一帧至少 8 秒：你来我往说得快的时候，每句话传一张图贵得
没道理，画面也没怎么变。帧压到长边 512、jpeg 0.6 —— 「对方在干什么」这种判断
不需要高清。前置摄像头看到的是镜像，界面上也镜像显示，**传出去那一帧要翻回来**，
否则模型看到的字全是反的。

**摄像头那一帧一个字都不落库。** 只在那一次请求里出现，用完就没了；挂断、
切回虚拟、离开通话都立刻关流。视频通话是演出来的，但摄像头是真的 ——
真的东西按真的规矩办。

### 4.676 礼物：拆开之前，里面装什么一个字都不进上下文

`system/gift.js`。封面写「限量款球鞋」，拆开是一张纸条 —— 这个反差就是礼物
好玩的地方。所以这里有一条硬规矩：

> **没拆开之前，正文里没有里面是什么。**

不是藏在界面里而已。落进消息 `content` 的那段字里根本没有它，模型读到的是
`[礼物：限量款球鞋（未拆封）]`，想猜也没得猜。拆开那一刻才改写正文，把里面的
东西补进去，同时落一行提示。

**两个方向都瞒，包括角色自己送的那件。** 角色写的时候当然知道里面是什么，
但**正文是会被搜索读到的** —— 搜索结果显示的就是 `content`，只要它在正文里，
用户搜一下就提前看见了。一条规矩管两头最省心。这条是测试抓出来的。

拆开用 `[拆开]`，拒收用 `[拒收]`，和转账的 `[收款]`/`[退回]` **分成两套词**：
一段对话里可能同时挂着一笔钱和一件礼物，共用一个词就分不清在处理哪一个。

保密可以关（送礼物面板里那个开关）。关掉之后角色收到的同时就知道里面是什么，
反应更连贯，但也就没有惊喜了 —— 界面上把这句话直接写出来。

删掉那行提示就当没拆过，整轮重新生成走的也是这里（和转账同一套 `unsettle`）。

### 4.679 一起听：边聊边听，不另开一页

`system/music.js`（曲库与歌单）+ `system/listen.js`（一场一起听）。

**不像通话那样把会话盖住**，只在会话顶上加一条播放条。听歌本来就是背景，
两个人该聊什么还聊什么 —— 这是一起听和打电话最根本的区别，界面得照着这个来。

**歌是用户自己传的，而且必须真的有东西能响**：一个直链地址，或者一个存在本机
的音频文件。不做「只填歌名」的纯虚拟歌 —— 一起听要真的有东西在放，计时才有
意义；光有个名字，两个人对着空气坐着。歌词按 LRC 存原文，播放时按时间轴对出
当前这一句，对不上就当没有。

**统计分两层**：本次挂在 store 上（这一场听了多久、几首），累积加在 chat 上
（`listenSeconds` / `listenCount`），不因为这一场结束而清零。结算时落一条
`kind: 'listen'` 的记录，正文带着这一场的曲目 —— 情侣空间要的就是这些记录，
不必另建一张表。

**秒表只在真的在放的时候走**：暂停的时间不算进一起听的时长。

角色这边：`[一起听]` 拉你听，`[点歌：歌名]` 换一首，`[建歌单：名字]` 建一个
属于它自己的歌单（歌单分主人，它的品味不该和用户的混在一张表里）。点歌认不出
名字**就当没点过** —— 凭空冒出一首曲库里没有的歌，界面上根本放不出来。

**角色拉起来的那一场不自动出声**：没有用户动作，浏览器本来也不让自动播，
与其弹一个「被拦下了」的错，不如老老实实停在那儿等人按播放。

`skeleton.listen` 里那三条是这个功能的要害，冷着注入等于白做，所以
**正在一起听的时候这个能力必定是热的**：不要逐句解读歌词、不要报歌名歌手、
大部分时候安静听着，每首最多说一两句。

两个由测试逼出来的细节：`load()` 要**同步**记下「现在是这一首」再去解地址
（解本地文件地址是异步的，等它回来才更新的话，中间那一下是一场没有歌的一起听，
`next()` 也找不到自己在队列里的位置）；当前这首不在队列里时（角色临时点的一首）
放完就停，不要莫名其妙跳回歌单第一首。

#### 接网易云

`system/netease.js`。接的是**自己部署的** NeteaseCloudMusicApi，地址在
「设置 - 音乐服务」里填。为什么不内置：那个服务要跑 Node，浏览器里起不来；
而且所有人共用一个出口 IP 会被网易云限流 —— 谁想用谁自己部署一份。
音频流不走那台服务器，接口只返回 CDN 地址，浏览器直接去 CDN 拿歌。

**两个账号**。用户自己的号存在设置里，角色那个号（其实是用户的第二个号）
存在角色卡上。这个接口支持把 cookie 当查询参数逐次传进去，所以两个号可以在
同一台设备上各走各的，不需要来回登录。

**关于「真的一起听」这件事，先把话说清楚**：网易云自己那个一起听是
**需要双方在线的实时房间**（socket 房间），而且相关接口在 api-enhanced 那个
分支里才有。角色那边没有第二个客户端，房开了也没人进；硬让同一台机器用第二个
账号进房是自己骗自己，还容易被风控。所以这里做的是**让两个号的听歌数据都真的动**：

| 做什么 | 走哪个 | 结果 |
|---|---|---|
| 听歌打卡 | `/scrobble` | 两个号的听歌记录、年度报告里都有这首歌 |
| 歌单同步 | `/playlist/create` + `/playlist/tracks` | 各自多一个「和某某一起听」歌单 |

这是这个场景下「真的一起听」唯一落得了地的含义。歌单同步**默认关**：
往用户真实的歌单里写东西是件重的事，得他自己点头；打卡不受这个开关影响。
一首歌放够 30 秒（或者放完）才打卡，刚点开就切走的不该记进听歌记录。

**登录只做扫码**。手机号那条路要用户把密码交出来，不做。扫码那个组件放在
`ui/qrlogin.js` 并且**不认识任何具体服务** —— 三个动作由调用方传进来。
因为登号的地方有两处（设置里登自己的、角色卡上登角色的），而这两处分属不同的
app，不能互相 import（规约第 8 条）。

**网易云的歌不存播放地址**，那个地址会过期，每次要放的时候现取。库里只留
`neteaseId`。

角色点歌时曲库里找不到，就**替它去网易云搜一首回来**加进曲库 ——
这就是「角色可以自己搜歌加进来」。两处都没有才当没点过。

**cookie 就是账号权限**，比接口密钥还敏感，只存在这台设备的浏览器里。
登录页把这句话直说，不替用户含糊。

### 4.67 时间感知

`system/time.js`。

**为什么要让模型自己把时间写出来**。在 system prompt 里写一句「现在是下午三点」，
模型常常视而不见 —— 那只是一句它扫过去的话。真正管用的是**要求它每条回复
第一行先写一行 `[时间：2026-01-01 周三 14:30]`**：写过一遍，它才算真的看见。

这一行**显示时过滤掉**（`reply.js` 的 `STAMP_LINE`，和引用行同一条路子剥），
但**记在消息的 `stamp` 上，回头塞回上下文**（`engine.buildHistory`）。
于是历史本身就是一条时间线，模型每次都能顺着往下推。

用户这边没法要求他写。隔得久了（默认 30 分钟）由我们替他补一句时间 ——
隔了三小时才回和秒回不是一回事，这个差别模型该看见。

两个开关，都在「上下文与记忆 - 时间感知」：

| 开关 | 关掉之后 |
|---|---|
| 时间感知 | 时间块一个字不注入，历史里也不带时间，也不要求它写 |
| 让她自己写出时间 | 时间照常注入，只是不再要求它写那一行 |

**虚拟时间**。存的不是一个死时刻，而是「设定的那一刻 `timeVirtualAt`」加上
「设定时的真实时刻 `timeSetAt`」，两者之差就是偏移。所以虚拟时间**会自己往下走**，
不是钉死在一个点上。要钉死另有 `timeFrozen`。
历史消息的 `createdAt` 也过一遍 `toWorld()`，整条时间线一起平移。

**时区**。分两边：

- **我在哪儿** → `settings.timeZoneUser`，在时间感知页
- **她在哪儿** → `characters[].timezone`，在**各自的角色卡**上

分开放是因为它们属于不同的对象（CLAUDE.md 第 5 条）。角色没单独设就跟你同城 ——
绝大多数情况本来就是。两边时差为 0 时**一个字都不提**，同城还唠叨一句反而是噪音。

时差用 `Intl.DateTimeFormat` 把同一时刻在两个时区的「墙上时间」都取出来，
各自当成 UTC 反推，相减即得。**夏令时因此自动是对的** —— 不用自己维护换算表，
也不用引第三方库（第 9 条：无构建）。

### 4.673 翻译：原文在上，译文收在同一个气泡里

`设置 - 翻译` 在会话菜单里（属于这一段对话，不是整机行为，CLAUDE.md 第 5 条）。
选定语言后写进 `chat.translateTo`，engine 就把 `skeleton.translate` 拼进 system prompt，
要求模型每写一条消息就紧跟一行 `[译文：…]`。

```
今日はちょっと疲れた
[译文：今天有点累]
```

`splitReply` 读到译文行，**按出现的先后一一配**：第 n 句译文配第 n 条还没有译文的
正文（引用行挂下一条，方向正好相反）。早先是「挂到刚落下的那一条」，那只在原文与
译文交替出现时才对，模型另一种常见写法是先写完几条正文再把译文一起补在后面，
那时每句译文都盖在最后一条上，前面几条全空 —— 看起来就是一直在掉翻译。
译文写在原文上面的先记着，下一条正文落下来时补上。认法有多宽见 4.104。

渲染上不另起气泡，也**不放「点击翻译」按钮**：译文就是原文气泡底下多出来的一行
（`.bubble-trans`），点气泡本身展开或收起。多一个按钮就多一次命中判断，
而每条消息都要判一次。

默认展开还是点击展开，是整机的阅读偏好，放全局 `settings.translateOpen`
（`'tap'` | `'always'`）—— 它跟具体哪段对话无关，换一段对话也不该重设一遍。

翻译走的是同一次回复请求，不额外调接口：另起一次翻译请求要多花一轮往返，
还会让译文和原文对不上号（模型不知道自己上一条到底分成了几句）。

### 4.68 主动消息

角色不等你开口，自己挑时间发消息来。`system/ai/proactive.js`。

调度是一个 20 秒一次的轮询，跟着外壳的生命周期跑，所以开着哪个 app 都在数时间。
下一次的落点存在 `localStorage`（设备本地的临时状态，不进 IndexedDB）。

- **随机**：落点在 `平均间隔 x 0.5` 到 `x 1.5` 之间掷。定死间隔会像闹钟，随机才像人。
- **免打扰**：撞进时段里就把落点推到时段结束，不是丢掉。起止填成同一个数表示不设。
- **挑人**：单人会话、角色没关掉 `proactive`、当前没在生成、堆积未读没超上限，几个条件里随机选一个。
- **不灌**：堆了 `maxUnread` 条没看就先停，等你看了再继续。
- **页面关着不跑**。重新打开时如果早就该发了，挪到十几秒到一分钟之后再发，
  像是刚好这会儿想起你，而不是开机就炸一串。

生成走 `task.proactive` 模板，接在正常的聊天 system prompt 后面，
历史末尾补一条「现在由你主动开口」的 user 消息，再用 `renderTurn` 按一条条的节奏送达。
送达后累加 `chat.unread` 并发一条通知；会话开着的时候 Conversation 会立刻把未读清零。

开关按角色单独配，入口在**会话右上角菜单的「主动找我」**，不在全局设置里
（见 CLAUDE.md 第 5 条）。每个角色自己一个落点，存在 `localStorage` 的一张
 charId 到时间戳的表里；角色删掉，落点跟着清。

### 4.69 通知横幅与提示音

`notify()` 只管发出一条通知并广播 `EVENTS.notify`，它不知道会被怎么呈现。
听这个事件的有两处：`shell/NotifyBanner.js` 负责从顶上掉下来的横幅，
`system/sound.js` 负责响一声。锁屏上的通知列表另外直接读 `notifications` 这个 store。

- **提示音不放音频文件**。预设一律用 WebAudio 现场合成（起音 8ms、指数衰减，
  听着像敲出来的而不是蜂鸣器），想要真实录音就自己传一个，存进 `files` 域。
  iOS 上 AudioContext 必须由一次真实触摸唤醒，`installUnlock()` 在外壳挂载时
  就绑好第一次 touch/click。
- **正开着那个会话就不弹**，人就在看，真机也是这么做的。
  判断收在 `reply.js` 的 `notifyMessage()` 里（一条消息一条通知，不是一轮一条）：栈顶正好是 `/chat/<id>`
  且页面可见才算「在看」。退回消息列表、切到别的 app、锁屏、切后台，
  都照弹。早先只有主动消息走通知，正常回复反而是静悄悄的 ——
  切出去等回复，回来才发现人早就回了。
- 点横幅或点锁屏上的通知，都走 `EVENTS.notificationOpen`，
  由 `system/intents.js` 统一转成 `open(appId, payload)`。通知自己不认识路由。
- 横幅的安全区要在 `.banner-host` 上再让一次：绝对定位是贴着 `.root` 的
  padding box 算的，量不到 `.root` 自己的 `padding-top`。

开关在 `设置 - 通知`（横幅、提示音、音量、试一条）。这是整机行为，不属于某个角色，
所以放全局设置里不违反 CLAUDE.md 第 5 条。

### 4.695 系统通知与 Web Push

`sw.js` 是整个项目唯一的 Service Worker，**故意没有 fetch 处理函数，一个字节都不缓存**。
无构建方案已经被 HTTP 缓存坑过一次（所以才有「强制更新」），再叠一层 SW 缓存只会更难查。
它存在的唯一理由是 Web Push 规定必须要有 SW。

三条通知路径，互不替代：

| | 谁弹 | 什么时候有效 |
|---|---|---|
| 应用内横幅 | `shell/NotifyBanner.js` | 页面在前台 |
| 系统通知 | `sw.registration.showNotification()` | 页面还活着（含刚退到后台的几秒） |
| Web Push | 推送服务把 `push` 事件送到 SW | **app 完全关着也行，但必须有服务器发** |

`system/push.js` 是客户端这一半：注册 SW、申请权限、订阅、退订、点击回跳。
页面不在前台（`document.visibilityState !== 'visible'`）且开了「交给系统弹」时，
`EVENTS.notify` 就转给系统通知，不再弹应用内横幅。

几个踩过的点：

- iOS 只给**添加到主屏幕之后的 PWA** 发系统通知。标签页里授权了也不会响，
  所以设置页会检测 `display-mode: standalone` 并直说。
- **收到 push 却没弹通知，iOS 会直接把订阅作废**。所以 `sw.js` 里三种负载
  （正常 JSON、解析失败、完全为空）都保证走到 `showNotification`。
- iOS 上不能用 `new Notification()`，只有 SW 的 `showNotification` 有效。
- 换了 VAPID 公钥必须先 `unsubscribe` 再订，否则服务器推不动。

**服务器那一半本项目不提供。** 要真正在 app 关着时叫醒手机，需要：
生成一对 VAPID 密钥，公钥填进设置页，订阅对象交给服务器保存，
服务器用 `web-push` 之类的库往 `subscription.endpoint` 发加密负载
（`{ title, body, route, appId }`）。设置页留了「订阅上报地址」和「复制订阅」两条路。

### 4.697 保活：循环播放一段静音音频

`system/keepalive.js`。移动端浏览器一旦切到后台就把页面的定时器压到几乎不走，
主动消息的 `tick` 也就跟着停了。有音频在播的标签页会被系统当成还在干活，
不会被这么压 —— 所以这里循环播一段 1 秒的静音 WAV。

WAV 是现场用 `DataView` 拼出来的（44 字节头 + 全零 PCM），转成 object URL，
不放音频文件、不引第三方库（规约第 9 条）。

**默认关着**，开关在 `设置 - 保活`。它有实打实的代价：耗电、占一个音频焦点、
在有些系统上会让锁屏出现播放控件。这些代价该由用户自己决定要不要付。

iOS 的自动播放策略要求第一次 `play()` 由真实触摸触发。开关本身就是一次点击，
所以开的那一下一定能播。上次退出时开着、这次启动还没人碰过屏幕的情况下
`play()` 会被拦，`installRetry()` 挂一个一次性的 `pointerdown`，等第一次点击补上。

保活只是让定时器继续走，页面还活着。**它不能替代 Web Push** —— app 彻底关掉，
这段音频也就没了。

### 4.7 群聊

一个会话可以挂多个角色(`chat.characterIds` 为数组)。

**生成方式**: 默认每个角色独立调用,各自只看到自己的角色卡。

| | A. 独立调用(默认) | B. 一次生成整段 |
|---|---|---|
| 人设纯粹度 | 高,角色之间不串味 | 低,角色容易同质化 |
| token 成本 | 高,N 个角色 N 次请求 | 低 |
| 节奏 | 由调度器决定谁开口 | 由模型自行安排 |

选 A 为默认: 角色人设不互相污染是角色扮演的核心价值,
B 生成出的多个角色往往说话像同一个人。B 作为设置项保留,不改变数据结构。

（落地时按第 15 条改成默认一次调用写整轮，谁开口由模型定，见 4.162。以下为最初设计）

**发言调度器** (`system/ai/scheduler.js`) 决定一轮里谁开口、开口几个、顺序如何:

- 被 `@` 或被点名的角色必定开口,优先级最高
- 其余角色按话题相关度(关键词命中角色卡与记忆)加随机权重抽取
- 单轮开口角色数有上限,默认 1-2 个,可配
- 同一角色不连续开口两轮,除非被点名

**群聊 prompt 附加**:

- 注入"当前群里有谁"(其他成员的名字与一句话印象,不注入完整角色卡)
- 最近若干条消息需带说话人姓名,角色才知道在跟谁对话
- 记忆 scope 用 `chat:<id>`,群里发生的事不污染与该角色的私聊

---

### 4.85 角色资料、导入与关系网

**联系首页是卡片墙**，不是列表：两列方形封面 + 名字 + 一句签名，
有小号的角标写着几个，NPC 角标写 NPC。

**点开先看资料，不是一屏输入框。** `ProfilePage` 只放**名片级**的东西：
头像、名字、一句签名、年龄/性别/生日三个小块，加上关系列表。

**人设、情境、开场白、说话示例一概不出现在资料页**，那是一大段长文，
放上来就又变回「一打开满屏都是人设」。要看要改都点「编辑资料」进 `EditPage`。

角色新增了 `age` / `gender` / `birthday` 三个纯展示字段，
它们不进 prompt 主体 —— 主体仍然是 `persona`。

**导入角色卡**（`task.card-import`）：txt / md / docx 都行。
`system/doctext.js` 负责把文字抠出来（docx 走 `unzip.js` 解 `word/document.xml`，
`<w:br>` 当换行）。解析完先列给你看再建角色，
模板里反复强调**资料里没写的字段留空，不许自己编**。

**关系**存在角色自己身上：`relations: [{ charId, label }]`，
label 的含义统一成「在我眼里，这个人是我的什么」。`link()` 一次写两边，
所以两个方向的称呼可以不一样（女儿 / 妈妈）。

**批量生成关联 NPC**（`task.npc-batch`）：模型围着一个角色写一批人，
每个带 `relation`（在他眼里主角是什么）和 `reverse`（反过来）。
同样先预览再入库，建出来的是独立角色，能单独聊天。

**关系网**手画 SVG，不引图库 —— 节点就这么几个，力导向是杀鸡用牛刀。
中心一圈、外面一圈，`graphAround` **必须按广度走**：深度优先的话，
一个既是直接关系、又能从别人那儿绕到的人会被先记成第二层，圈就全乱了。
边上的标签放在 0.45 处（越过中心圆但离外圈的名字还远），免得压住名字。

### 4.9 账号与小号

用户人设从「只有一个」变成一棵两层的树。`system/accounts.js`。

```
大号 A ─┬─ 小号 A2        同一个人的两个身份
        └─ 小号 A3
大号 B                    另一个人，和 A 完全无关
```

数据上：`personas` 集合，`parentId` 为空就是根账号（大号）。
`chats` 和 `memories` 都带 `personaId`。迁移 2 把原来那份单一 `persona` KV
收成根账号，已有会话和记忆全部挂到它名下，等于原封不动地成为大号。

**会话属于「身份 + 角色」这一对**，不是只属于角色。换成小号去找同一个角色，
开的是一段新对话，两边互不相干。

记忆的可见性只有两条规则：

1. **不同根账号之间一条都不给。** 按 `rootIdOf` 过滤，另一个大号的事完全看不到。
2. **同一个根下面互相可见，但会点名是关于谁的。** 大号的记忆对小号可见，
   因为角色还是同一个角色，它确实记得那些事；但注入时会标成「（关于大号）」，
   而不是「关于你现在在聊的这个人」。

和小号说话时只多一句：`你们是刚认识的，还不熟。` 到此为止。

**这里踩过一个坑，别再改回去。** 最早的写法是给跨身份的记忆标上「（关于大号）」，
再写一整段解释「你认识另一个人但别把他俩联系起来」。结果适得其反 ——
一提大号，模型就开始琢磨眼前这人是不是大号，越描越黑。

现在：记忆原样注入，不标是关于谁的；关系说明里一个字都不提大号。
记忆内容本来就是第三人称带名字的（「阿园喜欢喝三分糖的奶茶」），
谁是谁靠内容自己说清楚，比外加标签可靠得多。

这也更接近真实：认识新朋友时，我们自己的过去全都在，
只是对眼前这个人一片空白 —— 而不是心里默念「别把他和某某搞混」。

**角色的小号是她自己开的，用户开不了。** `ai/tasks/char-alt.js`。
触发挂在主动消息的调度上：轮到她主动开口时，有一定概率她开的不是口，而是一个新号。

条件卡得比较死，免得刚认识就冒出一堆马甲：
角色自己的开关要打开、和当前身份聊满 30 条、最多同时挂 2 个、小号自己不会再开小号。
`blockedBy()` 会把不满足的那一条原样返回，界面直接显示，不用猜。

名字、签名、人设、理由都由模型自己想（`task.char-alt`）。
人设要求**用第二人称写给她自己看**，里面点明「你是林晓、你为什么开这个号、
你打算怎么说话」—— 这是角色对自己的交代，不是给用户看的。
开完号会排一条一到五分钟后的主动消息，否则开了也没下文。

马甲和本体是两个独立的对话对象，记忆各算各的（`scope` 本来就是 `character:<id>`）。
联系 app 里只能看、不能建。

切换入口在「联系」app 首页，小号在各自账号的页面里开。
未读徽标、消息列表、主动消息都按当前账号过滤 —— 换了账号，
别的身份那边的会话不该突然冒出新消息。

### 4.8 「联系」app：人设归它管

`apps/contact/`。一个专门写人设的 app，因为人设是一整页长文，塞在角色卡里
一打开满屏都是它，别的设置全被挤到看不见。

界限是这样划的：

| | 在哪 | 装什么 |
|---|---|---|
| 这个人是谁 | **联系** app | 头像、名字、签名、人设、情境、开场白、说话示例；我的人设也在这儿 |
| 她在对话里怎么表现 | 会话右上角的**角色卡** | 音色、语速、发语音、发图片、主动找我、关联世界书 |

角色卡顶上留一行跳进「联系」，联系里也写明行为设置在哪儿，两边互相指得到。

跨 app 一律走 Intent（`phone.intent.open('contact', { route })`），
聊天 app 不 import 联系 app —— CLAUDE.md 第 8 条。

同一个入口只有一处（第 5 条）：原来「主页」tab 里那个「我的人设」弹层、
联系人长按菜单里的「编辑角色卡」都改成跳转，不再各自复制一份编辑界面。

### 4.81 「情侣空间」app：绝大部分内容不另存一份

`apps/space/`、`system/space.js`。

**一段单人会话就是一个空间。** 关系本来就长在会话上，人设不同会话也不同，
所以「每个人设各有一个空间」是白来的，不必再拿 人设 × 角色 拼一张表。
群聊不算空间 —— 里面没有「我们俩」这回事。

关键决定是**什么都不复制**。空间里能看的东西，绝大部分已经是聊天记录里的消息：

| 空间里看到的 | 其实是 |
|---|---|
| 礼物墙 | `kind: 'gift'` 的消息 |
| 一起去过的地方 | `kind: 'location'` |
| 一起听 | `kind: 'listen'`，累积数落在 `chat.listenSeconds/listenCount` |
| 通话记录 | `kind: 'call'` |
| 约定 | `kind: 'pact'`，`pact` 字段是 open / done |
| 已寄出的信 | `kind: 'letter'` |
| 在一起多少天 | `chat.loveStartAt` |

这样做换来三件白给的好处：它们天生就在上下文里（模型读历史就读到了）、
天生跟着会话一起删、天生不会和聊天记录对不上。反过来说，
**空间不是一个新的数据源，它是聊天记录的另一种看法。**

真正需要自己存的只有两样，放在新的 `spaceItems` 域里（按 chatId 建了索引）：

- **纪念日**（`type: 'day'`）会话里没有对应的消息，它就是一条纯粹的设定；
- **还没寄出的信**（`type: 'draft'`）寄出之后它就变成一条消息了。

#### 约定和转账、礼物是同一套骨架

`[约定：那件事]` 落一条 pact 消息；`[约定完成：那件事]` 把它标完成、
改写正文、并落一行「约定已完成」的提示。删掉那一行提示，约定就回到未完成 ——
和「已收款」「已拆开」完全同构：**提示行本身就是这件事发生过的唯一凭据**
（见 4.675、4.676）。模型写「完成」时给的是一小段原话，拿它回头去认领；
认不出来就什么都不做，凭空标完一条别的约定比不标更糟。

`MARK` 里 `约定完成` 必须排在 `约定` 前面。交替是从左往右试的，
反过来写「约定完成：早点睡」会先被「约定」吃掉，剩下「完成：早点睡」当成内容。

#### 信分「留在信箱」和「寄出」

这是「偷偷寄信留言」那件事的落点。留在信箱的信是 `spaceItems` 里的一行，
**只有自己看得见，不进上下文，角色不知道它存在**；寄出之后它变成一条
`kind: 'letter'` 的消息，这时候角色才读得到。想好了再寄，或者永远不寄，
都由用户决定。

#### 注入默认是关的

`ai/context/space.js` 是一个新的注入块，写三样：在一起第几天、
三十天以内要到的纪念日、还没完成的约定。**礼物、地点、信的正文一概不重复**
—— 它们本来就是消息，历史里已经有一份，再抄一遍只是把同一段话交两次。

这一段每轮都占位置，所以默认不注入，开关在空间页上（`chat.spaceInject`）。
按第 5 条它绑在这段关系上，所以不进「设置」；按第 13 条它花钱，所以必须能关，
且界面上写清楚开了会多什么、关了会少什么。

### 4.82 「日常」app：撞上什么是运气，运气归随机数管

`apps/daily/`、`system/events.js`、`system/draw.js`。

「今天她遇上了什么」分成两半，各归各的：

| | 谁决定 | 花不花钱 |
|---|---|---|
| **日程** 她自己安排的 | 模型按人设生成，一天一次 | 花 |
| **随机事件** 撞上的 | 本地骰子从事件库里抽 | 不花 |

分开的理由就一句：**撞上什么是运气，运气该由随机数决定。**
让模型「随便想一件事」，它会一直想出同一类事 —— 那不是随机，那是它的偏好。
和距离用 haversine 算、点数用本地 RNG 掷是同一条原则：**数字不是模型的活。**

（批 2a 只做了库与生成这半边。日程、今日事项、吃饭记录在批 2b。）

#### 库按两个轴分格，第三个轴只管权重

- **领域** 这件事落在哪一面：环境 / 人际 / 运气
- **色彩** 好事 / 坏事 / 不好不坏
- **分量** 平常（基础权重 10）/ 少见（基础权重 1）

分量**不参与分格** —— 少见的词条和平常的待在同一格里，只是被抽中的机会
小一个量级。这就是「小概率事件」，不必为它单开一档。
三个表都是表驱动的，加一行就多一档。

库是**全局**的：「路上堵车」不属于任何一个角色，它是可能落在任何人头上的一件事。
某个角色要不要遇上随机事件，按第 5 条放在那个角色的角色卡上（`char.eventsOn`）。

#### draw.js：抽法单拎出来，因为三件事必须一起做

```js
pick(items, { weightOf, recent, cooldown, rng })
```

1. **按权重抽** —— 常见的事本来就该常发生；
2. **刚抽过的压一压** —— 同一件事连着出现三次就假了；
3. **压一压，不是封杀** —— 罚分随名次退回 1，从不为 0。

第三条是这个模块存在的理由。用户的原话是「可以喜欢的一直吃，但是问 n 次
全都是一模一样的食物我觉得不 OK」—— 要的不是禁止重复，是**别总是同一个**。
所以罚分是 `(i+1)/(cooldown+1)`：上一次刚抽中的罚得最狠，越往后越轻，
出了范围完全恢复，池子里只剩它时照样抽得到。吃饭（批 2b）用的是同一套。

`rng` 可以从外面传。测试里换成定死的序列，于是「抽中了什么」本身是可测的。

#### 大运是一个有惯性的慢变量

`drift(cur, { keep, span, range })`：每天挪一小步，而且记着上一次在哪儿。
每天重掷一个随机数的话，今天大吉明天大凶 —— 那不叫运势，那叫噪声。

它同时影响两件事：**撞上事情的概率**和**抽中什么色彩**（顺的时候好事权重高）。
偏向永远压不到 0：再背也不是不可能有好事。

概率那一侧**只往上加，不往下减**：用户填的是基础概率，填了 100% 就该是
每天都有，不该被运势悄悄打个折。运势对「抽中哪一条」的影响在 `toneWeight`
那儿，那才是「大运影响抽取」该待的地方。

今天发不发生 = `基础概率 × 大运 × 距上次几天`，掷的是本地随机数。
大部分日子应该什么都没有 —— 那也是一种结果。

#### 批量生成：一格一次单独调用

`ai/tasks/event-batch.js`。和聊天**没有任何关系** —— 不带人设、不带历史、
不占会话的上下文预算。用户在九宫格里逐格填条数，填了数的格子各跑一次。

分格生成比一次要一大堆强得多：同一次调用里混着好事和坏事，
模型会不自觉地凑成对，写出来的全是「虽然……但是……」。

去重做两道：**生成前**把这一格已有的发过去明说别重复，
**落库时**再按去标点的形比一遍。第二道才是兜底的那一道 ——
模型换个说法照样会重。发多少条过去可以在「用量与上限」里调，默认全给。

生成结果先摆出来让人看一眼再决定存不存，这是 CLAUDE.md 第 6 条里那个例外。

### 4.83 角色的一天：花钱的只有一件

批 2b。`system/day.js`、`system/food.js`、`ai/tasks/day.js`、
`ai/tasks/recipe-batch.js`、`ai/context/day.js`。

一天里有四样东西，**只有第一样花钱**：

| | 谁决定 | 每天几次调用 |
|---|---|---|
| 日程 她打算做什么 | 模型按人设排 | 1 |
| 撞上的事 | 本地骰子（4.82） | 0 |
| 大运 | 本地慢变量（4.82） | 0 |
| 三顿吃什么 | 本地从食谱库抽 | 0 |

开关在角色卡上（`char.dayOn`），**默认关**。开了之后，每天第一次回复前
`streamReply` 里会先把当天排出来 —— 排完再拼上下文，否则「你今天」那一段
要等到下一条消息才出现。

#### 注入分两层，这是整件事的要害

```
一整天的摘要        当天常驻   她当然知道自己今天要干什么
当前时段的具体事项   时段到了才给
撞上的那件事        它绑的那个时段到了才给
```

中午十二点就知道「今晚会停电」，那不叫过日子。所以随机事件抽中之后
**绑一个时段**（绑在哪儿也是掷出来的），`brief()` 拿当前时段的下标去比，
没到就一个字都不提。没到的时段只出现在摘要里，而且摘要只写打算做什么，
不写做得怎么样。

大运**不写数字**，只写倾向（「最近不太顺，耐心比平时短一点」）。
写「今天运势 +1.4」，模型会把它当设定念出来。

#### 事项的状态：三档，不是四档

`plan / done / drop`。「进行中」不存 —— 它是「这一条落在当前时段」推出来的。
存一份就要有人负责在时间走过去的时候改它，那是一个永远会忘的活。

模型可以写 `[事项完成：那件事]`、`[事项取消：那件事]`。和约定同一条规矩：
给的是一小段原话，拿它回头去认领，**认不出来就什么都不做**。
这两行不落消息 —— 它们改的是那一天，不是这段对话。

时段认不出来的事项在 `save()` 里**整条丢掉**，不挪到一个默认时段：
挪过去等于凭空替她安排了一件她从没说过的事。

#### 吃什么：问十次得到十次同一样，是因为模型没有记性

用户的原话是「可以喜欢的一直吃，但是问 n 次全都是一模一样的食物我觉得不 OK」。

这件事不归模型管：**吃什么**本地从食谱库抽（draw.js 的冷却），
**吃过什么**落一条记录，下次抽的时候它就是「最近」。
冷却是压一压不是封杀，所以爱吃的那几样照样反复出现，只是不会连着十顿都是它。

食谱库按**地区**分，地区留空的是哪儿都有的那些。角色吃到的是
「它所在地区那一批 + 通用那一批」—— 别把别的地区的菜端上来，
「在东京的人今天吃了毛血旺」一眼就假。所在地区填在角色卡上。

**库是空的就返回 null，不编。** 凭空造一道菜出来，下次问又是另一道，
又回到了「每次都不一样但每次都对不上」的老问题。

`history()` 排序要先 reverse 再按时间稳定排。只按 `at` 排是不够的：
补录、导入、同一秒里落了好几条，时间戳就是一样的，顺序一乱，
「最近吃过什么」这件事本身就不成立了。

#### 食谱库两档，配了哪档走哪档

和识图、语音那几处是同一个套路：

- **普通接口** 按地区写常见的家常菜、早点、外卖。够用，不会离谱。
- **会联网搜索的接口**（`services.search`，设置里单独配）才问得出
  「这个地方真的有哪几家店」。这一档写出来的条目带店名，上下文里就成了
  「午饭在陈记面馆吃的担担面」。这是它唯一值钱、也唯一需要多花一份钱的地方。
  提示词里明写**查不到就去掉，不要编**。

这套接口不在 chat 预设列表里，所以走 `runJSONWithPreset` 而不是
`runJSONTask` 的主用 / 副用那一套。它的 `provider` 固定是 openai ——
不写的话会默认去敲 Anthropic 的端点，一次就炸。

### 4.84 关系小件：心声、拍一拍、特别关心、骰子、头像

批 3。`system/extras.js`、`system/avatar.js`、`ai/tasks/inner.js`、
`ai/context/avatar.js`。五样东西都很小，凑在一起说。

#### 心声：默认藏着，点头像才看

两种产出方式，差别**只在花不花第二次钱**：

| | 怎么来的 | 每轮几次调用 |
|---|---|---|
| 随回复一起 | 她在同一次回复里多写一行 `[心声：…]` | 0（不额外调） |
| 单独生成 | 整轮说完后另起一次，只问「刚才那几句话背后你在想什么」 | 1 |

第二档贵，但写出来的东西更像背面那一层 —— 同一次生成里顺手带出来的心声，
多少会和台词互相迁就，说什么心里就想什么。默认关。

三档的说明**一次列全**，不是只讲当前那一档：「开了会多花什么」得在
**开之前**就看得见（第 13 条）。切过去之后才告诉用户要多花钱，等于没说。

心声挂在**她最后说的那一条**上，不是第一条 —— 挂第一条会抢在台词前面被读到，
那就不是「背后那一层」了。

#### 拍一拍：后缀归被拍的那一方

别人拍我，显示的是**我**给自己设的那一句。所以拍角色时读角色卡上的，
角色拍我时读我自己的。两处入口分开：我的在会话菜单的「互动」里，
角色的在角色卡上，各自写明是给谁用的。

单击头像看心声、双击拍一拍，只能等 260ms 才分得清是哪一个。心声那一下
因此会慢一拍，能接受；反过来把拍一拍做成单击，误触的代价大得多。
头像带 `.no-callout`（第 12 条）。

#### 骰子：点数不是模型的活

让模型自己写一个数，它会一直写 6 和 1 —— 那不是随机，是它对「戏剧性」
的偏好。和随机事件、距离、金额同一条规矩。

还有一层：掷出来的那一条要到**下一轮**才进历史，所以她写下 `[骰子]` 的
那一刻并不知道点数。提示词里明写了这件事，让她把话停在那儿。
这和礼物拆开之前不知道里面是什么是同构的 —— 信息控制本身就是玩法。

少于两面的骰子落回默认的六面，不夹到两面：夹到两面等于凭空给了用户
一个他没要过的硬币。

#### 特别关心：一个前缀，一处提醒

`starTitle(char, title)` 一处定义，消息、来电、动态三处通知都过这一道。
没开就原样返回，调用方不必自己判断。

动态那一条只有特别关心的角色才弹 —— 朋友圈本来就是「你想起来才去看」
的东西，每条都弹就成了骚扰。

#### 头像：只说看得见的，不替用户解释

她看得见你的头像。换了就在**下一轮**提一句，附一句客观描述；
说过一次就记下是哪一张，下一轮它就不新了（`chat.seenAvatar`）。
常驻一句「他的头像是深蓝色的」没有意义 —— 头像一直在那儿，值得一提的是它变了。

描述是**本地按像素算的**，不走识图接口：缩到 16×16 读一遍，
算感知亮度和饱和度。换头像这件事每次都要描述一句，为它调一次识图太贵；
而且这里要的本来也不是「图上有什么」，是「大致什么样」。

**一张纯黑头像写成「几乎全黑」，不写「看起来心情不好」。**
用户提过「纯黑纯白等于不开心」这层潜规则，但那恰恰不该写死 ——
写死了它就变成一条生硬的设定，她会照着念出来。让她自己琢磨才是对的。

描述在**换的那一刻**算好存进人设（`persona.avatarNote`），注入时只读一个
字段：注入块是同步的，不能在那儿等一个 canvas。

反过来她也能换自己的：用户先传几张进头像库（角色卡上），
她写 `[换头像：名字]` 从里面挑。认不出名字就不换 —— 凭空换成另一张，
用户看到的是一个她根本没提过的头像。库是空的就整个能力都不注入。

**让她自己上网找头像做不到。** 纯浏览器里没有那个能力：跨域抓图会被 CORS
挡住，MCP 也需要一个进程去连。真要做，得先有一个自己部署的、开了 CORS 的
HTTP 端点 —— 那和 Web Push、后台中转是同一件事（见 13.2 的 N3、N4）。

### 4.85 面板自定义、点外卖、共享位置

批 4。`system/panel.js`、`system/takeout.js`、`system/geo.js`、
`ai/context/geo.js`。

#### 面板：顺序和「在不在面板上」分开存

格子多了一屏放不下，所以分两层：常用的留在面板上，其余收进「更多」。
哪些留下、什么顺序，全由用户自己定，代码只给一个默认。

两个字段分开存（`panelOrder` 和 `panelMore`），不是存一个「面板上的清单」：
那样从更多里挪回来时它只能落在末尾，用户上次排好的位置就丢了。
分开存，挪进挪出都不动顺序。

`order()` 和注入区块的 `resolveOrder` 同一个道理（见 4.2 B2）：
丢掉不认识的 id，补上配置里缺失的。没有这一步，以后每新增一格，
老用户的面板里就少一格，而且是静默的。

`panelMore` 没设过（`undefined`）才用默认；设成**空数组**是
「我全都要放面板上」，不是「没设过」。第 13 条：放几个不封顶。

#### 点外卖：和转账同构，但有两个方向

| | 谁吃 | 谁付 | 对方要表态吗 |
|---|---|---|---|
| 自己点 | 我 | 我 | 不用 |
| 请客 | 对方 | 我 | 收下 / 谢绝 |
| 代付 | 我 | 对方 | 付 / 不付 |

「给对方点还让对方付」没有意义 —— 那不叫点外卖，那叫叫他自己点，
所以这一档不存在。

其余和转账完全同构（见 4.675）：一条消息带状态、收到的一方表态、
落一行提示、提示行被删就当没表过态。

**处理词不能和转账共用。** 一段对话里转账、礼物、外卖可能同时挂着，
所以三套词必须互不重叠：转账是「收款 / 退回」，礼物是「拆开 / 拒收」，
外卖是「要了 / 不要」。这条规矩之前只写在注释里，这次真踩了 ——
外卖原本也用「收下」，结果模型写 `[收下]` 时被转账那边先接走，
而那边没有待处理的转账，于是**什么都没发生，还不报错**。
提示词里也补了一句「别写成 [收款] 或 [收下]」。

#### 共享位置：那个数字不能让模型编

用户的原话是「担心 AI 乱编」。问模型两地多远，它会给一个听起来很像的数，
而且每次都不一样。所以距离**本地按 haversine 算**，算完连同单位写进上下文，
再明说一句「照它写，不要换成别的数，也不要自己估」。

哪一端没坐标就**返回 null，不猜**；注入块这时改口说「说不出具体多远 ——
不要编一个数出来」，而且整段里一个数字都不出现。

坐标两端都是自己填的，不读设备 GPS（和 4.677 一个道理）。想用真实定位的，
界面上有一个按钮单独去取，取不到就报错，**不悄悄退回一个编的坐标**。
`CITIES` 是一张可扩展的小表，点一下一键填入。

踩过一个坑：`clean()` 幂等性。`Number(null)` 和 `Number('')` 都是 **0**，
而 0 是个合法的经纬度 —— 所以对一个已经 clean 过的位置再 clean 一次，
`lat: null` 会变成 `lat: 0`，「只填了地名」的那一端就成了几内亚湾的
(0, 0)，凭空算出上万公里。空值必须先挡掉再转数字。

### 4.86 时间性行为：她什么时候回

批 5。`system/pace.js`、`system/autoreply.js`，外加 `space.js` 的定时寄信
与 `ai/proactive.js` 的深夜那一档。

这一批解决的是同一件事的四个面：**消息不该总是秒回，也不该总要按按钮**。

#### 三档节奏，默认还是原来那档

| 档位 | 什么时候回 | 花不花钱 |
|---|---|---|
| 按按钮才回 | 你按发送键右边那个按钮 | 按一次花一次 |
| 发完就回 | 消息一发出去就生成 | 每条都花 |
| 过一会儿才回 | 等一段时间再生成 | 每条都花，只是晚一点 |

默认是第一档，和这一批之前一模一样。后两档都要用户自己去开，
所以第 13 条的「花钱的必须能关」在这里是「默认就是关的」。

#### 等多久由本地算，绝不能问模型

问它，它会写「我过了二十分钟才回你」，然后**立刻把这句话发出来**。
所以时长在 `delayFor` 里算，算完就是一个定时器。
和距离、点数、金额一样：**数字不是模型的活**（见 4.678、4.82、4.85）。

四个能在本地读到的因素相乘，最后乘一个 0.6 ~ 1.6 的抖动：

| 因素 | 怎么影响 | 从哪读 |
|---|---|---|
| 几点了 | 深夜慢五倍，白天最快 | `day.slotNow` |
| 她这会儿有没有事 | 当前时段排了日程就慢一倍 | `day.brief`（见 4.83） |
| 运势 | 走背字的时候慢一点 | `events.luckOf`（见 4.82） |
| 这条要紧不要紧 | 带问号快一半，写得长快一些 | 标点和长度 |

「要紧不要紧」只看标点和长度，很粗。但它要防的是「每条都一样快」，
不是真去理解这句话 —— 那才是模型的活。

上限默认十分钟，**填 0 就是不封顶**（第 13 条）。

#### 到点的时刻记在会话上，不是记在内存里

页面一关定时器就没了，而「她该回了」这件事不该因为你退出去看了眼别的 app
就取消。所以 `pacePending` 存在会话行上，两个地方读它：

- **会话页**自己起一个定时器，管你正开着的那一段，顺带显示倒计时；
- **全局那个 tick**（二十秒一轮）调 `runDue()`，扫所有到点的会话。
  人在别处、或者页面刚打开的时候靠的是这一条 ——
  「她趁你没看的时候回了一句」要成立，就得有它，而且要走通知。

`runDue` 先清 pending 再生成：生成要好几秒，这期间 tick 会再跑一轮，
不先清就会连发两条。已经在生成的会话直接跳过（`isReplying`）。

#### 自动回复：两道闸，而且都要主动关掉并说一声

两个方向都有：**我这边**（我不在，她发来时替我回一句）和
**她那边**（她在忙或在睡，我发消息时回一句固定的，**不调接口**）。
后者顺带省钱 —— 一句「在开会，晚点回你」不该花一次生成。

这件事最容易出的毛病是**忘了自己开着**，然后一整天都在自动回复。
所以条数和时限两道闸一起上，到了**主动关掉并落一行提示**，
而不是默默不回 —— 默默不回的话，用户只会觉得坏了。

条数填 0 是不限（第 13 条）。计数在发完当场就加，加完到顶立刻关：
等下一条才发现超了，那一条已经发出去了。

#### 定时寄信：不靠定时器，靠时间戳

信箱里的草稿可以填一个投递时刻（`sendAt`，0 表示不定时）。
`deliverDue()` 由全局 tick 调，扫出到点的几封，落成正式的信再删草稿。
`createdAt` 按写好的那一刻算，不是按投递那一刻 —— 这样它在列表里
落在你写它的位置。

这一条**排在 `isConfigured()` 前面**：寄信不需要接口，没配接口也该寄出去。

#### 深夜那一档：不能变成固定节目

夜里睡不着冒出来的那种话，和常规主动消息是两件事，各有各的开关和节奏。
上次发送的时刻单独记在 localStorage 里，默认至少隔五天 ——
每晚都来一次，它就从「今天格外难过」变成了一个固定节目。间隔填 0 是不限。

两个坑：

- **配置读不到。** `configOf` 是逐字段列出来的，新加的四个字段忘了列进去，
  于是开关永远是 `undefined`，整档功能不可达，而且不报错。
  新增配置项必须同时改 `DEFAULTS` 和 `configOf`。
- **被免打扰吃掉。** 免打扰默认 0 点到 8 点，正好把深夜时段整个盖住。
  所以这一档的判断要**排在免打扰前面**：它本来就发生在夜里，
  有自己的时段和自己的开关。界面上写明了「不受免打扰时段限制」。

#### 开关放在哪儿

节奏和自动回复跟着会话走，所以在会话右上角菜单里进「节奏与自动回复」
（`/pace/:chat`）；深夜那一档跟着角色走，所以在角色的「主动发起对话」
页里。都不进全局设置 —— 第 5 条。

会话页顶上那一行横幅同时写两件事：自动回复开着没有、还要等多久。
点它直接进设置页，不用再翻菜单。

### 4.87 主界面：跨页挪动、真移除、文件夹

批 6。`screens/home/layout.js`、`screens/home/FolderView.js`。

#### 跨页挪动：格子要在整本布局里找，不是在当前页里找

从前每个写操作都先拿 `lay.pages[pageIdx]`，再在这一页的 `cells` 里按 id 找。
而「移动到别处」最常见的用法恰恰是**选中、翻一页、再点目标** ——
这时源头压根不在当前页上，于是找不着，报一句「位置不存在」，
看上去就像整页没了。

改成 `locate(lay, cellId)` 在所有页里找。跨页交换换的是「页 + 坐标」：
两个格子各自搬到对方那一页去。Dock 与网格互换时，空出来的那一格
也要留在**源头那一页**上，不能跟着当前页跑。

#### 真移除：heal 会把移除的 app 再摆回来

`heal()` 有一条「已注册但没摆出来的 app：找个空位放上」——
它是为了新增 app 时老用户也能看见。但它分不清「还没摆」和「用户不要」，
于是移除完刷新一下就复活。

加一张 `lay.removed` 名单：移除时记上，heal 跳过名单上的。
反过来，**摆着的比名单说了算** —— 用户自己又把它放回主界面时，
heal 把它从名单上划掉，否则下一次自愈又把它收走。
移除的 app 在「设置 - 主题」里列出来，点一下放回去。

#### 文件夹：就是一个装着 app id 的格子

`{ kind:'folder', name, apps:[appId] }`，永远 1x1。不另起一张表 ——
它没有脱离主界面单独存在的意义，跟着格子一起挪、一起删最省事。

heal 里文件夹和 app 共用同一个 `seen` 集合，所以装进文件夹的 app
不会被当成「没摆出来」又在外面摆一份。剔空的文件夹直接去掉。
移除一整个文件夹等于把里面的 app 一起移除，它们一起进 `removed`。

本项目的整理模式是**点选**而不是拖拽（手机上拖拽跨页太难控），
所以文件夹不靠「把一个图标拖到另一个上面」生成，而是：
空位菜单里「新建文件夹」勾选若干 app，或者 app 菜单里「装进文件夹」。
不做文件夹套文件夹 —— 一共几十个图标，套两层只会更难找。

### 4.88 世界书：两部分，各自分深度

批 7。`ai/context/lorebook.js`、`engine.insertLore`、迁移 4。

#### 原来那三档是假的

老字段 `position` 有三个值：`system` / `beforeChat` / `afterChat`。
名字骗了人 —— 三者**都落在同一个设定区里**，只是排序不同，
谁都没真正进过对话。

现在拆成两个都真的字段：

| 字段 | 取值 | 管什么 |
|---|---|---|
| `part` | `before` / `after` | 在角色卡**之前**还是之后 |
| `depth` | 0 / N ≥ 1 | 0 留在设定区；N 插进对话，倒数第 N 条之前 |

两部分里各自再分深度，所以两个字段永远都有意义，不是三选一。
世界观、时代背景这类「先有世界再有人」的设定放角色前；
「这个人在这个世界里的处境」放角色后。

#### 深度要解决的是「聊久了就不算数」

整段设定堆在 prompt 最前面，聊上几十轮之后它就是远处的背景音。
一条插在最后一句前面的规则，模型几乎没法忽略。所以「这一条要不要被无视」
交给用户按深度自己定，而不是由代码统一决定。

插入时**以 `system` 身份**，不混进任何一方的发言 ——
混进去模型会把它当成谁说过的话，甚至回一句「好的我记住了」。

三个边界：

- **从深到浅插。** 先插深的，浅的那一条的「倒数第 N 条」才还是原来那一条。
- **先合并再插。** `mergeAdjacent` 会把相邻同角色的消息并成一条，
  插完再合并就把刚插进去的位置又挪了。
- **深过头不丢。** 只剩三条却填了深度十，落在最前面。丢掉更糟：
  用户明明写了一条规则，却因为聊得还不够长就不生效。

#### 一轮只激活一次

设定区和对话里各要一份命中结果。各算各的，带概率的条目会被掷两次骰子，
于是「设定区里有、对话里没有」这种鬼事就会出现。所以 `streamReply`
先激活一次，把同一份 `lore` 同时传给 `buildChatSystem` 和 `buildHistory`。

#### 注入顺序也要跟着拆

区块从一个 `lorebook` 变成两个（`lorebook` / `loreAfter`）。
`resolveOrder` 只把新区块补在**末尾**，所以光加一个区块不够 ——
老用户的「角色前」会排在角色卡后面，名不副实。迁移 4 顺手把顺序改对：
`lorebook` 挪到 `character` 前一位，`loreAfter` 插在后一位。

#### 位置总览

`/map`。按实际注入顺序列出**全部**条目，命不命中都列。
和「激活预览」是两件事：预览回答「这段话会激活谁」，
总览回答「这一堆东西到底按什么顺序、落在哪儿」。
「上下文与记忆」里有入口，顺带写明现在有几条、几条插在对话里。

### 4.89 请求记录：这一轮到底发了什么

批 8a。`ai/trace.js`、`engine.send()`、设置的 `/trace`。

排查 prompt 只有两条路：对着代码数，或者把真正发出去的东西摆出来。
前者数不清 —— 注入区块有十个，世界书按深度插进消息数组，能力目录按
最近用没用过决定给目录还是给细则。光读代码说不准这一轮实际长什么样。

#### 一道门

原先六处各自调 `getProvider(c.provider).stream/complete(...)`：聊天、通话、
JSON 任务、文本任务、指定预设、连接测试。没有任何一个地方能同时看见它们。

现在统一走 `send(taskId, c, payload, kind)`。它只做两件事：记一笔，然后
转交给 provider。加这道门的唯一目的就是有地方可以记。

#### 默认关着，关着时一条都不记

`trace.begin()` 在关闭状态下返回一个空壳（`done` / `fail` 都是空函数），
调用方不必到处写 `if`。**用户平时不该知道有这东西**，所以入口收在
「存储与备份」最下面的「排查」一节。

记录只在内存里。它回答的是「刚才那一轮发了什么」，不是日志档案；
落库还要额外的清理与配额，不值当。刷新即清空，关闭开关也清空。

两个细节：

- **图片只记类型，不抄 dataURL。** 一张压缩过的图也有几十上百 KB，
  抄进记录里，翻两页就把内存吃光了。
- **保留条数可填 0 表示不限**（第 13 条），界面上写明长期开启会持续占内存。

### 4.90 骨架重写：性别锚点、消息规则、示例、自检

批 8b。`ai/templates.js` 的骨架部分、`engine.buildChatSystem`、`reply.stripThink`。

依据是三家公开的提示词工程材料里反复出现、且互相印证的四条：
关键指令在长上下文的首尾各放一遍；指令之间的矛盾是不遵守的首要原因；
示例对格式一致性的作用大于规则；用「做什么」代替「不要做什么」。

#### 性别锚点：唯一一处绝不能错的事实

`gender` 从前只是资料页上的展示字段，`tasks/card.js` 里明写着
「不进 prompt 主体」。于是**角色的性别从来没有进过 prompt**，
用户那边更是连这个字段都没有。

现在两处都补上，并且**首尾各注入一次** —— 长上下文里只说一遍的东西
会被忽略，两头都说才钉得住。

只写填了的一方，两边都没填就整段不出现。不替用户猜：猜错比不写更糟。

#### 消息规则：两段合一，去掉矛盾

从前是 `skeleton.closing`（分条格式）与 `skeleton.style`（自然表达协议）
两段，各说各的，对「一条消息长什么样」口径不一致，其中一处还直接打架：
收尾说「语气词都正常」，协议指令三说「禁止硬加语气词」。

合成一段 `skeleton.rules`，八节，原协议九条指令的约束一条不少，
体积压到三分之一。始终注入，不给开关 —— 它是消息如何成形的契约。

其中两节按用户的修正重写：

- **理解与回应**。原来写的是「只回应最扎的那一件，其余不逐条回应」。
  这是错的。真人的做法是：先把对方这一轮全部读完，判断哪件事影响更大，
  **本轮只推进那一件，另一件押后**，等话题将要结束时自己回头提起。
- **不追问已作说明的铺垫**。对方先发一个「唉」，后面已经解释了原因，
  就不要再问「怎么突然叹气」。

#### 示例：两个，都只演示形式

一个演示押后与回头提起，一个演示日常小事顺着接。开头写明
「仅演示分条与回应方式，不代表你的语气与人设」，避免把所有角色
带成同一个调子。按 token 计费而不额外调接口，所以给开关，默认开着。

#### 收束三件套

取舍顺序、核心设定、性别，全部贴着输出放。靠后的位置模型读得最重，
而这三件正是不能被前面任何一段压过去的。

核心设定（`char.core`）是人设里最不能偏离的几点，三到五行，
在开头的人设之外于末尾再注入一次。这是「首尾各一遍」的直接应用。

#### 自检：<thinking> 里先检查再说话

**这一段和上面的「示例」都已经删掉了**（CLAUDE.md 第 16 条：内置提示词只陈述
客观事实，不替角色作判断；九章消息规则、示例、自检整套删过两次）。留着下面
这些字是为了记住当初为什么做、又为什么不对。`stripThink` 还在 —— 有的模型
自己会吐 <thinking>，剥掉它仍然是必要的。

`skeleton.think` 让模型逐条回答五个问题再输出。第 4 条专治乱召回 ——
让它自己先筛一遍，而不是在检索侧硬调阈值。

`stripThink` 在三处生效：拆条之前、流式预览的每一帧、以及整轮落库时
（自检挂在第一条的 `think` 字段上，排查时回看得到）。

流式那一处不能漏：不剥的话，整张检查清单会先在「正在输入」的气泡里
滚一遍，等收尾才消失。

漏写闭合标签时，从开标签到结尾整段都当作自检丢掉。代价是这一轮可能整个
变空并报「模型返回了空内容」；另一种做法是把检查清单原样发给用户，那更糟。

#### 文风

规则文本一律书面、中性、祈使：不用破折号作插入语，不用「省得 / 干脆 /
反正」一类口头连接，不打比方，不用「那一条」这类指代。
`style.mjs` 里有一条检查钉着这件事。

CLAUDE.md 第 7 条原本只管界面文案。prompt 也是写给人看的产品文本，
同样适用。

### 4.91 记忆分两层：底色常驻，召回下沉

批 8c。`system/bond.js`、`ai/context/memory.js`、迁移 5。

#### 问题出在措辞，不在检索

从前召回块的抬头是「对话记忆 —— 基于历史对话的客观分析结果，
**请自然地运用这些信息**」。这句话是在命令模型把召回的东西用上，
于是每一条噪音都被硬塞进回复。

检索本来就不可能完美。能改的不是精度，是**检索结果的定位**：
它是候选，不是必须使用的事实。新的抬头明说「供参考」「不相符则跳过」。

#### 两层

| | 是什么 | 什么时候在 | 放哪儿 |
|---|---|---|---|
| 关系底色 | S 级记忆压成的几句关系现状 | 每轮都在 | 设定区，角色卡之前 |
| 一直记着 | 钉住的与忌讳的（4.103） | 每轮都在 | 设定区，底色之后 |
| 本轮相关 | 按九项打分挑出的具体条目 | 每轮几条 | 对话历史，倒数第一条之前 |

从前 S 级是**每轮钉死全量注入**的。S 级恰恰是「关系的重大转折」，
于是随口问一句今天吃什么，满眼也都是那几件大事。现在它们压成底色常驻，
具体条目等到相关时再召回。抽取模板里 S 的定义也跟着收紧：
只给关系的重大转折，日常事实一律不给 S。

等级字母也不再写给模型看。`[S/事实]` 里的 S 对它没有任何含义 ——
它不知道 S 比 A 重要在哪儿、该怎么用。当时换成了 `【事实】` 这样的标签；
后来连类别标签也撤了，改成按性质分三栏（4.103）：类别 id 对使用没有指导意义，
模型不知道拿 pattern 该怎么办。

#### 底色按根账号分开存，手写之后不被覆盖

`char.bonds[rootId]`，记着正文、来源签名、生成时间与是否手写。
同一个角色被两个大号分别聊，两段关系不是一回事；小号与大号共用
（和 `memory.listFor` 同一条规矩）。

签名是 S 级记忆的 `id:updatedAt` 拼起来的。增、删、改任意一种都会让它变，
变了才重压 —— 每压一次是一次接口调用，整项可以在「用量与上限」里关掉。

手写之后 `manual` 置位，自动那条路不再覆盖它。点「恢复自动生成」时
**签名一并清空**：点这个按钮的人要的是重新生成一份，
而不是等下一次 S 级变动。

#### 召回下沉：为什么不放在设定区

召回每轮都在变。放在设定区靠前的位置，它后面所有稳定内容的
prompt 缓存每轮都会作废，费用与延迟都上去了。而且离当前对话越远，
模型越容易忽略它。

所以默认 `memoryDepth = 1`，复用批 7 的深度注入机制，插在最后一条消息之前。
填 0 则回到设定区，「用量与上限」里写明了这样做的代价。

同一深度上世界书与召回可能同时存在，各自带着自己的抬头，中间空一行，
不会粘成一段（`insertLore` 里的 `raw` 分支）。

#### 一轮只算一次

和世界书同一个道理：设定区与对话里用的必须是同一份，否则带概率的条目
两边对不上；而且向量检索本身要花一次接口调用。`streamReply` 先算好，
同时传给 `buildChatSystem` 与 `buildHistory`。

#### 核心设定

`char.core`，人设里最不能偏离的三到五行，导入角色卡时自动压一遍
（多一次接口调用，可在「用量与上限」里关掉），也可以随时手写。
它在 prompt 末尾再注入一次，是「首尾各一遍」的直接应用（见 4.90）。

生成失败不抛：角色已经建好了，不该因为这一步没成就整个失败。

### 4.92 五处修正：模板落库、时区、时差措辞、间隔、示例

用户对着请求记录看新 prompt 时发现的。

#### 改了默认提示词，老库一个字都吃不到

`DEFAULT_SETTINGS.promptTemplates` 从前是 `{ ...DEFAULT_TEMPLATES }` ——
**整套模板在第一次启动时就被抄进 settings 并落库**。而 `template()`
优先读 settings，于是：

- 代码里改 `DEFAULT_TEMPLATES` 对任何跑过一次的库都无效；
- 模板页里每一条都显示「已修改」，虽然用户从没动过。

这直接违反 CLAUDE.md 第 11 条 —— 代码里那份是**回落**，不是初始值。
改成 `promptTemplates: {}`，只存改过的那几条。

迁移 6 把已经落库的那一套整体挪到 `promptTemplatesLegacy`。
新的内置版本立刻生效，而此前的文本一个字没丢：模板编辑页里多一个
「载入升级前的版本」，逐条找得回来。

#### 「对方在本地」

`zoneLabel(LOCAL)` 返回「本地」。这句话对模型毫无信息量 ——
它不知道「本地」在哪儿，也就没法判断那个点对方在做什么。

`resolveZone()` 把「跟随设备」解析成真实的 IANA 时区再查表，
于是写出来的是「中国 · 北京、上海」。表里没有的直接写 IANA 名。

`zoneDiff` 也改成先解析再比：一边填「跟随设备」、一边显式填了同一个地方，
按 id 比会当成两个时区，凭空多出一句时差。

#### 「比你晚 1 小时」

中文里「早 / 晚」既能指时区的前后（北京比伦敦早八小时），
也能指钟面上的先后（一点比两点早）。两种读法方向正好相反，
所以这两个字在这里不能用。

改成「与你相差 N 小时」，两边的钟点都写出来，方向自明。

#### 「你们刚刚还在聊」

`gapText` 拿的是最后一条消息的时间。而最后一条往往就是用户**刚发出的
这一句**，于是永远算出「刚刚」，哪怕两个人三天没说过话。

改成量到**上一轮结束**为止：跳过结尾那一串连续的用户消息，
取它们之前的那一条。

#### 示例里不填真名

示例原先用 `{{charName}}` / `{{userName}}` 填成双方的真名。填了之后这两段
看上去就像这两个人真说过的话，模型会把示例的语气当成角色的语气，
也会把面试、火锅当成发生过的事。

改用「对方」「你」这种代称，并在开头明说「示例中的语气、用词与事件均与你
无关」。示例要演示的只有形式。

### 4.93 她在听什么：读角色账号的真实播放记录

批 9a。`netease.recent` / `pullRecent` / `pullIfDue`、`ai/context/music.js`。

#### 推翻了 4.6 里的一句话

`netease.js` 原先写着：「网易云自己那个一起听是要双方在线的实时房间，
角色那边没有第二个客户端，房开了也没人进。」

这句话漏掉了一个人：**用户自己就是那个客户端**。在电脑上用角色的账号
登录真正的网易云客户端，那个号的播放记录就是真的在动。

#### 两条路，按拿得到的信息从多到少试

| 接口 | 拿到什么 | 能说什么 |
|---|---|---|
| `/record/recent/song` | 最近播放，带时间戳 | 「刚刚在听 X」 |
| `/user/record` | 本周听歌排行，无时间戳 | 「最近常听 X」 |

第一条不是每个 NeteaseCloudMusicApi 分支都有，所以 404 就退到第二条。
两条都没有就是这份部署不支持，界面上直说，不假装有。

退到排行榜时注入的措辞跟着变，并且明写一句「记录中没有时间，
不要说成正在听」—— 拿周榜当「正在听」是编造。

#### 过期的不注入

三天前那首歌写成「她正在听」比不写更糟：用户一问就穿帮，
而且模型会顺着这个假前提往下编。

所以两层过滤：整份记录超过 `musicFresh` 分钟就整段不注入；
带时间戳的那条路还会**逐首**判断，只留够新的。填 0 表示不限时效。

#### 什么时候拉

不起定时器。不聊天的时候没必要一直压着自己部署的那个接口。

- **发消息时顺带看一眼**，距上次不足 `recentGap` 分钟就跳过。填 0 表示
  不自动拉，只在手动点的时候拉。
- **会话菜单里一个手动入口**，点了就拉，不看间隔 —— 点它的人就是想现在知道。

这件事只请求音乐接口，不调模型，所以它不在「用量与上限」里，
而是跟着音乐服务那一页走。

### 4.94 分条与人设：prompt 保证不了的那部分

用户反馈「还是一整句」「不贴人设」之后加的三件。

先确认了机制本身没问题：造一个 `schemaVersion` 停在 3、`promptTemplates`
里躺着整套旧模板的库，重新载入，迁移链跑完之后发出去的确实是新 prompt。
所以问题不在「没升上来」，在「升上来了模型也不照做」。

#### 格式那一问要放在最后

自检是模型读到的**最后一段**，位置最靠近输出。原先五条检查里没有一条问
「这一段分几条」—— 而这正是最容易被忽略的一条：它只写在中间的消息规则里，
读到结尾就淡了。

补两问：分成几条、每条在哪里断（要求写出每条首尾）；以及「换成另一个角色
说是否同样成立」，这一条对着人设。收尾改成「按第 5 条的结论逐条输出」。

#### 角色自带说话示例时，内置示例让位

示例的力气很大，这正是它被三家列为最有效手段的原因，也是它的代价：
**两套示例同时在场，内置那套的语气会把角色自己的语气盖过去。**
一个话少的角色会被带成例子里那种语气。

所以 `char.exampleDialogue` 非空时不注入内置的那两段，形式由角色自己的
示例演示。用户当初担心「这不就污染人物性格了吗」，担心得对。

#### 兜底分条

规则写了「换行即分条」，但模型有时候就是回一整段。prompt 保证不了这件事，
所以本地补一刀：**整轮只拆出一条纯文字、而且超过设定字数**时，
照句末标点断，断完还长的再照逗号断。

三条边界：

- **只在「一行」时动手。** 模型自己分好条的一个字都不碰 ——
  它分得比标点准，那是它的判断，不该被规则推翻。
- **带方括号标记的整条不碰。** `[图片：…]` 被切开就废了。
- **句号断完去掉，问号叹号留着。** 聊天里多半不写句号，但语气要留。

字数可填，填 0 表示不处理，模型回什么就显示什么。

### 4.95 流式开关，以及开关放在看得见的地方

#### 流式 / 一次返回

回复一直是流式的，没给过开关。加上 `streamMode`：

- **流式**：字一个个出来。但**自检那一段也是流式吐的**，剥掉之后开头几秒
  气泡是空的，看着像卡住。
- **一次返回**：等整段生成完再显示，中间只有「正在输入」。

两种发出去的请求内容完全相同，费用也相同，区别只在何时显示。
另有一类实际用处：部分接口不支持流式返回，关掉就能用。

开关起初放在会话的「上下文」页里，藏得太深。它说的是「和接口之间怎么传」，
用户想改它的时候人在「设置 - 接口」（多半是刚换了一个不支持流式的中转站），
所以挪到那一页、主用接口下面。上下文页那一份删掉，只留一个入口（第 5 条）。

#### 「当日日程在哪开」

开关在角色卡里，位置是对的（第 5 条：属于某个角色的开关放在那个角色身上）。
问题在**找不到**：会话菜单里「角色卡」那一行的副标题只写了
「人设、开场白、对话示例、关联世界书」，谁也想不到日程开关在里面。

两处都补：副标题写全，以及 —— 更重要的 —— 「今天」那一页在角色没开时
原本只说「请去角色卡里开」，现在**就地给一个开启按钮**。
第 5 条的判断标准是「用户想改这件事的时候人在哪儿」，
他正站在这一页上看着「还没有开启」，那开关就该在这里。

### 4.96 三处修正：说话冲、时区、横幅在后台过期

都是用户拿着一轮真实回复找出来的。那一轮的自检里模型自己写着
「面对撒娇式调侃会从容回应，甚至反撩」，可输出还是硬的 ——
说明不是它没读人设，是**规则把它推硬了**。

#### 规则里三处在把话往硬里推

| 原文 | 问题 |
|---|---|
| 不为了制造口语感而添加语气词 | 语气词正是口语里的缓冲。一律禁掉，剩下的就只有硬话 |
| 你的反应不必符合对方的期待 | 被读成「态度也不必客气」，于是玩笑一律顶回去 |
| （四、语气 只说了与人设一致） | 没说清这些规则**不管**分寸，于是它们自己成了分寸 |

改法不是删约束，是**给每条划定范围**：

- 语气词那条改成「同一个语气词不要连着几条反复使用」，并补一句
  「语气词本身按你的说话方式自然使用」。约束的是重复，不是语气词。
- 「不必符合期待」限定成**内容**：可以不给对方想要的答案，但不因此把话说硬。
  并补一句：对方开玩笑、撒娇、示好时，按你们的关系回应，不要一律顶回去。
- 四、语气 开头写明：分寸由人设与当前关系决定，**不由本节规则决定**；
  本节各条都是形式上的约束，不是让你更冷淡、更简短或更具攻击性。

自检也加一问：语气是否与人设和当前关系相符，偏硬或偏冷则改。

#### 角色没设时区时，不要替它认领一个地方

4.92 把时间那一段改成了「你在<地名>，现在是…」。但角色的时区**没设过**时，
`charZone` 跟的是用户的设备时区 —— 于是一个日本角色被告知「你在中国」，
它会照着认下来，报出的时间也是用户这边的。

改成：**明确设过时区才写地名**，没设过就只给钟点。
角色卡上那一行也从「与本人相同」改成「尚未设置，暂时跟随你所在的时区」，
并写明不设置的后果。

#### 横幅在你看不见的时候自己过期了

收到通知就起一个 4.6 秒的定时器。保活状态下角色半夜发来消息，
横幅在页面藏起来的时候弹出、又自己消失，切回来什么都没有 ——
用户看到的现象是「在别的页面不弹，或者只弹出第一条」
（那一条正好是他在看的时候来的）。

两处改：

- **藏起来时不计时。** `visibilitychange` 上停掉定时器，回到前台再从头计。
- **藏起来时攒着，不互相顶掉。** 前台仍然只显示最新那条；藏起来时排队，
  最多攒三条，回来一条条补完。

### 4.97 「每句话都感觉顶回来」：问题在一轮的构成

第二轮反馈。这一次模型的自检写得很好 ——「轻松调侃，带点从容的撩，
符合恋爱后的人设」—— 输出却仍然是顶的。把四条并排看就清楚了：

```
サボってねーし                否认
午後の授業ないって言っただろ   纠正
bad boyって何                 反问
…なら毎日悪い子だな           反驳式俏皮
```

**四条全是冲着对方的反驳动作，一条都没有顺着对方说的。**
「顶」不在用词，在于**整轮的结构就是一场辩驳**。每一句单看都不重，
合起来就是四次推回去。

还有一层更根上的：对方说「哼哼你这个 bad boy」是**玩笑**，不是**主张**。
模型把它当成一个需要纠正的事实，纠正了两遍。

#### 补两条

**读意图**（并入二、理解与回应）：

> 判断对方这一轮要的是什么：一个答案、一个反应，还是一句玩笑。
> 要的是反应或玩笑时，回应那份情绪，不要去纠正其中的事实。

**一轮的构成**（新的第三节）：

> 连续地否认、纠正、反问，读起来就是在顶回去，哪怕每一句都不重。
> 确实需要纠正时，先顺着对方的说法说一句，再纠正，并且只纠正一次。
> 一轮之中至少有一条不是在纠正：或者顺着对方的话说，或者把话题往前推。

自检里加上对应的一问，而且要求**逐条标注**：

> 逐条标出每一条是哪一种：否认、纠正、反问、顺着说、往前推。
> 若其中没有一条是顺着说或往前推的，重写。

标注比「检查一下语气」有用得多：它把一个模糊的感觉变成一件能逐条核对的事。

#### 和已有示例不打架

用户给的示例一里，角色连着问了四句（「啊？/ 为什么 / 是没发挥好吗 /
还是问的难」）。那也是同一种动作，但它们是**关切的提问**，不是纠正。
所以第三节写的是「不要整轮都在反驳」，不是「不要整轮都是同一种动作」——
后者会和示例直接冲突，而 prompt 内部的矛盾是不遵守的首要原因。

#### 顺带：防膨胀那条线抬到 900

原来的自然表达协议一千三百多 token，压完六百出头。补了这两节之后八百多。
涨的是真约束不是废话，所以把 `style.mjs` 里那条线抬到 900，
并在注释里写清楚为什么抬 —— 再涨就要先删东西。

### 4.98 一次发好几张图：摞成一叠

`apps/chat/pages/ImageStack.js`。

一张图一个气泡，五张就是五个气泡、三屏高，中间说过的话全被顶出屏幕。
摞起来只占一张的位置。

#### 什么算一叠

**连着发的、同一个人发的**那几张。中间夹了一句话就断开 ——
那两张之间隔着一句话，本来就不是一组。

**带着状态的那几张不并进来**：还在生成的、出错的、还在识别的、
识图没开的。它们下面各自挂着一行说明（「识图未开启，角色看不到这张图」
之类），摞进叠里那行说明就没了，而那正是当时最该看见的一句话。
这一条是靠 `mediaui` 那个套件挂掉才发现的。

#### 三件事

- **背后要漏出图**，而且要摊得开。压紧了就只剩一道彩色的边，
  看不出下面还有照片 —— 那不叫一叠，那叫毛边。偏移给到十几个像素，
  再绕底边转一点，像一摞随手放下的照片。
- **点这一叠换最上面那张。** 不必展开就能挨个看。右下角一个「3 / 5」的角标，
  既说明还有几张，也说明这东西点得动。
- **展开就是平常的图片气泡**，一张一个，最后一张下面挂「收起这 N 张」。
  试过做成九宫格相册，不对：那是另一套交互，而这里要的只是「摊开看」。

#### 选择模式下不摞

摞着就没法单独挑其中一张。进多选时整叠自动摊开，退出再摞回去。

### 4.99 顺手一点：正在输入、回到最新、边缘返回、命中区

#### 正在输入不是转圈

等回复时那个气泡里转的是通用的加载圈。**转圈是「系统在忙」，
这里要说的是「对面在打字」**，在聊天里不是一回事。
气泡里改成「正在输入」四个字，标题下面也写一行 —— 真机就是写在那儿的。

中间试过三个依次起落的点，那是另一种惯例，但这个项目的取向是简约
（第 4 条），字比动画干净。

#### 看旧消息时，新消息不该把人拽走

原先每来一条消息就 `scrollTop = scrollHeight`。往上翻着看旧消息时，
正读的东西会被一把拽到底。

改成：**自己发的一定滚到底**（那是你刚做的事），
别人发的只在你本来就贴着底部（离底 80 像素以内）时才滚。
不贴着的时候，右下角出一个「回到最新」。

那个按钮压在输入框正上方 —— 拇指本来就在那儿。第一版把它定位到整个
`.conv` 上，结果**盖住了发送键**，所以中间加了一层 `.conv-main`，
让它只浮在消息区之上。

#### 从左边缘划回去

返回键在左上角，那是右手拇指最够不着的一个角。真机上谁也不点它，都是从边上划。
所以这个手势不是锦上添花，它是主路。做在 `Page` 里，所有页面一次都有。

两个门槛，少一个都不行：起手必须在**左边缘 24 像素以内**，
横向位移要大于 64 像素**且**明显大于纵向。否则页面里任何一次斜着的滑动
都会把人划出去。

#### 画出来 34，摸得着 44

两家的人机指南都给到 44 至 48 像素，而手指的接触面本来就有十来毫米宽。
但按钮画那么大不好看 —— 所以只把**命中区**撑开：一个看不见的 `::after`
往外扩几像素，看得见的那一块不动。

输入栏那几个（发送、加号、表情）是按得最多的，往**上**扩得比往下多：
拇指从下方够过来，落点总是偏下。

### 4.100 不可选是默认，可选才是例外

用户反馈「长按有些地方还是会出现系统的复制浮层」。

第 12 条只要求**挂了长按的元素**带 `.no-callout`，那几处也确实都带了。
但提示行、日期行、按钮旁边的空白长按一样弹系统浮层，和本项目自己的
长按菜单撞在一起。这里是手机，不是网页 —— 本来就没有「选中一段文字」这回事。

所以反过来：**整个外壳默认不可选**，只给输入框放开。
要复制在自己的菜单里有「复制」，这正是第 12 条自己写的办法。

真正的祸根在 `.bubble` 上：它自己声明了 `user-select: text`，
而气泡恰恰是最会被长按的东西。查出来是一次 sed 的误伤 ——
那次要给「请求记录」那一段放开选中，匹配的是
`white-space: pre-wrap; word-break: break-word;`，
而这一串在三个块里都有，`.bubble` 和修复预览一起被带上了。

（`-webkit-touch-callout` 只有 iOS Safari 认，Chromium 连解析都不解析，
`cssRules` 里根本没有这一条。所以测试查的是样式文件的原文，不是算出来的值。）

### 4.101 接口页：不让它无限蔓延

预设一多，「已保存 / 主用 / 副用」三份列表各列一遍，整页被撑成一条长走廊，
底下的东西全被推到看不见的地方。

`List` 加一个 `cap`：列表在区域内自己滚，页面本身的滚动不受影响。
高度取「四行半」—— 露半行出来，一眼就知道下面还有。

另外两处按手上的习惯调了：**新建放到最上面**（要加接口的人是奔着它来的，
不该先滚过一长串已有的），**填写顺序改成先地址后密钥**
（地址决定了这个接口是什么，密钥是跟着它的）。

## 5. App 契约

系统不认识任何具体 app,只认 manifest。

```js
export const manifest = {
  id: 'chat',                    // 全局唯一,同时是 storage 命名空间与路由前缀
  name: '聊天',
  icon: 'message',               // 指向图标注册表,不是图片路径
  accent: 'var(--accent-green)', // 来自令牌
  entry: () => import('./App.js'),   // 懒加载,每个 app 独立 chunk
  permissions: ['ai', 'storage', 'notify'],
  intents: ['open:chat', 'pick:character'],   // 声明能处理什么
  settings: { /* 声明式 schema,系统自动生成设置页 */ },
  showOnHome: true
};
```

`settings` 用 schema 声明而不是每个 app 手写设置页,省掉大量重复代码和样式不一致。

**新增 app = 新建目录 + 在 `apps/index.js` 加一行。其他文件零改动。**

---

## 6. App 之间怎么打交道

只有三条合法通道,没有第四条。

### 6.1 Intent 跳转与请求

```js
phone.intent.open('chat', { chatId });            // 打开别的 app
const c = await phone.intent.request('pick:character');  // 要一个结果,带返回
```

调用方不知道谁来处理,由 manifest 声明。换实现、删 app 都不会连锁崩。

### 6.2 系统服务 共享数据不归任何 app

见第 3 节。角色卡、世界书、记忆、会话、朋友圈全在 `system/db/`。

### 6.3 事件总线 只广播系统级事件

锁屏、解锁、网络变化、通知被点击、主题切换。
业务事件一律不走总线,否则又变成隐式耦合。

---

## 7. 导航模型

双层栈。一开始就做对,否则加返回手势和多任务时必返工。

**系统层栈**: `LockScreen -> Home -> App -> App 内页面`

- Dock 是快捷方式,不构成导航层级,从 Dock 进 app 与从网格进完全一样
- 后台 app 保留状态不卸载,上限 5 个,超出按 LRU 回收
- 覆盖层不入栈: AppSwitcher / Sheet / Modal / Toast
- 通知点击可直达某 app 的某页面

**应用层栈**: 每个 app 自己的页面栈

```js
phone.nav.push('/character/char_xxx')
phone.nav.pop()
phone.nav.replace('/')
phone.nav.home()          // 回到主界面
```

- 返回手势只在当前 app 的栈内退,退到栈底再返回则回主界面
- 每个 app 的栈独立保存,切走再切回保留原位置

chat app 内部是 tab + stack 混合:

```
chat
 |- 消息    列表 -> 会话详情
 |- 联系人  列表 -> 角色主页 -> 编辑角色卡
 |- 朋友圈  信息流 -> 动态详情
 |- 主页    我的人设 -> 我的动态 -> 编辑
```

chat 的四个 tab 是**应用内**的分区,与系统层无关,各自保留滚动位置与页面栈。
角色主页与我的主页复用同一个 Profile 组件,只是 subject 不同。

---

## 8. 系统界面

手机壳这一层是用户第一眼看到的东西,单独定义清楚,不要留给实现时临时发挥。

整体取向: **黑白,不用灰作为填充色。**
层次靠留白、发丝线和阴影做;次级文字用黑或白降低不透明度,不引入灰色相。
应用图标一律纯白底 + 黑色 SVG,带一点点阴影。大量留白,简约来自克制而非装饰。

### 8.1 全屏根容器

**不画机身外框、不画刘海、不做投影。** 界面直接铺满视口,桌面与移动端一致。

**整个项目只有一处使用视口单位**,在根容器上,而且它只是兜底:

```css
.root { height: var(--app-h, 100vh); }
```

手机浏览器里视口单位算的是地址栏收起后的高度,比实际可视区高一截,
底部内容会被压到屏幕外且无法滚动到(`body` 是 `overflow: hidden`)。
所以由 `shell/Root.js` 量出真实可视高度写进 `--app-h`,
**只在宽度变化(横竖屏切换)时重算**;地址栏显隐只改高度,一律忽略。
这样既不会被切掉,也不会像 `dvh` 那样随滚动抽动。

其余所有高度由 flex 与 grid 按比例分配,不再出现任何视口单位。

```css
.root      { height: 100vh; display: flex; flex-direction: column; }
.statusbar { height: var(--statusbar-h); flex: none; }
.screen    { flex: 1; min-height: 0; overflow: hidden; }
.dock      { height: var(--dock-h); flex: none; }
```

`min-height: 0` 不能省。flex 子项默认 `min-height: auto`,
内容一旦超高就会把容器撑破,导致 Dock 被挤出屏幕。

禁止 `dvh` / `svh` / `lvh`,理由与强制手段见 `CLAUDE.md`。

### 8.2 StatusBar 状态栏

左侧真实时间(HH:MM),右侧信号、Wi-Fi、电量三个 SVG 图标。

**可开关,默认自动。** 移动端浏览器本身就带状态栏,再画一条就是双份。
设置项 `statusBar` 取三个值:

| 值 | 行为 |
|---|---|
| `auto`(默认) | 触摸设备(`pointer: coarse`)自动隐藏,桌面端显示 |
| `on` | 始终显示 |
| `off` | 始终隐藏 |

- 时间每分钟更新,且对齐到下一个整分,不做每秒轮询
- 电量优先读 `navigator.getBattery()`,不可用时不显示数字
- 前景色由当前页面声明: `<Page statusBarStyle="light|dark">`,
  壁纸深浅不同时状态栏需要反色,这个不能写死

### 8.3 LockScreen 锁屏

- 壁纸(用户上传,走 images 域)
- 大时钟 + 日期,字重轻,居上
- 通知列表: 角色发来的消息聚合展示,点击直达该会话
- 上滑解锁。无密码,这不是安全功能,只是一个入口仪式

### 8.4 HomeScreen 主界面

**不是 iOS 式的等距图标海。** 采用横条 + 方形挂件混排的卡片式布局。

```
┌──────────────────────────────┐
│ 状态栏                        │
├──────────────────────────────┤
│ [       横条挂件           ]  │  4 x 1
├─────────────┬────────────────┤
│  app   app  │                │
│             │   方形挂件      │  左 2x2 放四个 app
│  app   app  │                │  右 2x2 一个方形
├─────────────┼────────────────┤
│             │  app   app     │
│  方形挂件    │                │  下一块左右对调
│             │  app   app     │
├─────────────┴────────────────┤
│  [app]  [app]  [app]  [app]  │  底部 Dock,四个 app
└──────────────────────────────┘
```

**网格**

- 4 列,`grid-template-columns: repeat(4, 1fr)`
- 行高由容器均分,`grid-auto-rows: 1fr`,**不写死像素也不用视口单位**
- 单元支持跨格: app 为 1x1,方形挂件 2x2,横条挂件 4x1
- 使用**显式坐标**而非 `auto-flow: dense`。自动流在混合尺寸下结果不可预测,
  且拖拽排序本来就需要显式坐标

**挂件(widget)**

挂件是主界面上的活内容,不是装饰:

| 挂件 | 尺寸 | 内容 |
|---|---|---|
| `player` | 4x2 | 播放器版式:一侧圆角方形封面,旁边三行可各自选字号的文本 |
| `note` | 2x1 / 2x2 / 4x1 / 4x2 | 纯文字块,两行,各自选字号,可切衬线 |
| `photo` | 2x2 / 4x2 / 2x1 | 整块图片,底部可加一行说明 |
| `clock` | 2x1 / 2x2 / 4x1 | 时间与日期 |
| `recent-chats` | 2x2 / 4x2 | 最近会话与未读 |
| `moments-peek` | 2x2 / 4x2 | 最新一条朋友圈预览 |
| `memory-count` | 2x2 / 2x1 | 记忆条数与最近新增 |
| `love` | 2x2 / 4x4 / 4x3 | 头像 + 一行字 + 整月日历，今天标成音符 |
| `custom` | 2x1 / 2x2 / 4x1 / 4x2 / 4x4 | 自己传进来的一个 HTML，关在 sandbox 里跑 |

每个挂件声明自己支持的尺寸列表(`sizes: [[w, h], ...]`),
放置时按尺寸逐一列出,同一个挂件可以以不同大小出现。

挂件与 app 一样由 manifest 声明(见 5. App 契约的 `widgets` 字段),
主界面不认识具体挂件,只按 id 渲染。新增挂件不改主界面代码。

**挂件可以有自己的配置**,存在所属单元的 `cell.config` 里,`render(cell)` 时读取。
声明 `editable: true` 的挂件在主界面上直接点击即可打开编辑器。
顶部横条就是这样:封面本地上传,三行文字与封面左右位置都可改。

**`config.bare` 去掉卡片底。** 白底和投影是格子给的（`.cell-widget`），
不是挂件自己画的，所以这个开关对所有挂件通用，放在编辑器共用的那一段里。
开了之后挂件直接落在壁纸上，圆角和裁剪照旧按格子走。
浅色壁纸配深色文字会糊成一片，编辑器里写明了这一点，但不替用户拦 ——
壁纸是什么样只有用户自己知道。

**`love` 的两处讲究**。

日历**按当月实际占几行铺满剩余高度**，不用定高也不补到六行。
原稿是 420 定高加固定间距，赶上跨六周的月份最后一行直接被切掉；
补到六行又会在五行的月份底下留一条永远空着的带子。
按剩余空间分行两头都躲开了。

颜色默认**不写死**：`--love-ink` 回落到 `var(--text)`，深色模式下自动翻过来。
用户在编辑器里选了颜色才写进 `cell.config.color`，以内联样式盖住那个变量。
内部一律用 `currentColor`，所以文字、均衡器、头像描边、日期一起变。

**`custom` 挂件与安全边界**。

自定义组件是用户自己传进来的 HTML，一律关进 `iframe`，
`sandbox="allow-scripts"` 且**绝不给 `allow-same-origin`**。

这一条是硬的。给了 `allow-same-origin`，那个 HTML 就和本应用同源，
能直接读 `localStorage` 与 IndexedDB —— 接口密钥就存在那里面。
一个随手从别处拷来的组件文件不该够得着它。不给，它拿到的是不透明源，
读不到本应用的任何数据，代价是它也只能自己画自己的。

文件走 `files` 域（不压缩），上限 512 KB，`useFile` 取 object URL 喂给 iframe。
编辑器只接受 `.html`，说明里把「读不到本应用的数据，也无法访问已保存的接口密钥」
明写出来 —— 这是用户该知道的边界，不是实现细节。

**`love` 的三处讲究**。

日历**按当月实际占几行铺满剩余高度**，不用定高也不补到六行。
原稿是 420 定高加固定间距，赶上跨六周的月份最后一行直接被切掉；
补到六行又会在五行的月份底下留一条永远空着的带子。
按剩余空间分行两头都躲开了。

**2x2 是主力尺寸**，4x4 只是备选。2 格宽约 188px，整月塞进去每格只剩十来个像素，
密得看不清。小号改成**只铺两周**：本周与下周，今天必在第一行 ——
日常真正要看的就是眼前这几天。腾出来的高度反过来让留白比原稿更松。

星期在小号只留首字母（一行塞不下 `Sun Mon Tue`），均衡器留着但减到 14 根。
判定用 `cell.w <= 2` 在 JS 里做（要换文案与条目数量，CSS 管不着），
样式挂 `.is-compact`。两周视图会带出下个月的头几天，那些淡一档（`.is-out`）。

颜色默认**不写死**：`--love-ink` 回落到 `var(--text)`，深色模式下自动翻过来。
用户在编辑器里选了颜色才写进 `cell.config.color`，以内联样式盖住那个变量。
内部一律用 `currentColor`，所以文字、均衡器、头像描边、日期一起变。

**`custom` 挂件与安全边界**。

自定义组件是用户自己传进来的 HTML，一律关进 `iframe`，
`sandbox="allow-scripts"` 且**绝不给 `allow-same-origin`**。

这一条是硬的。给了 `allow-same-origin`，那个 HTML 就和本应用同源，
能直接读 `localStorage` 与 IndexedDB —— 接口密钥就存在那里面。
一个随手从别处拷来的组件文件不该够得着它。不给，它拿到的是不透明源，
读不到本应用的任何数据，代价是它也只能自己画自己的。

文件走 `files` 域（不压缩），上限 512 KB，`useFile` 取 object URL 喂给 iframe。
编辑器只接受 `.html`，说明里把「读不到本应用的数据，也无法访问已保存的接口密钥」
明写出来 —— 这是用户该知道的边界，不是实现细节。

**app 图标可以覆盖**。`settings.appIcons[appId]` 存 `{ icon }`,
`system/look.js` 的 `appLook()` 把 manifest 默认值与用户自定义叠加,
主界面、Dock、多任务卡片统一走它。

**用户自定义 CSS**。`settings.customCSS` 注入到一个独立的 `<style id="user-css">`,
覆盖默认外观,随时可清空恢复。设置页里列出常用类名,并支持从文件导入。
它是最后一道样式,不受令牌约束——这是有意的口子,让你能彻底改外观而不用改代码。

**app 图标**

- 圆角矩形(圆角为边长的 24%),**纯白底 + 黑色 SVG**,带一点点阴影
- SVG 占图标宽度的 58%,比之前明显放大
- 下方名称 11px 单行省略
- 未读角标在右上角,数字超过 99 显示 99+

三项在主题设置里可调,通过根元素的 CSS 变量与标记位统一生效:

| 设置 | 作用 |
|---|---|
| `iconColor` | 所有 SVG 的描边颜色,写进 `--icon-color` |
| `iconShadow` | 图标阴影开关,写进 `data-icon-shadow` |
| `iconLabels` | 图标名称显示与否,写进 `data-icon-label` |

每个 app 还能单独换成图标表里的任意一个(`settings.appIcons[appId].icon`)。
不再有每个 app 各自的底色。

**编辑模式**

长按进入,图标与挂件一起抖动。点任意位置弹出该位置的菜单:

- **放一个小组件** — 按尺寸列出全部挂件
- **放一个应用** — 列出全部已注册的 app
- **和别的位置交换** — 接着点另一个同样大小的位置
- **移除** — 原地留下等大的占位格

放大尺寸时会吃掉被覆盖的占位格;若压到非占位的格子则拒绝并提示。
移除的是主界面上的位置,不删数据。

放好之后,声明了 `editable` 的挂件在**非编辑模式下**直接点它即可改图和文字。

**分页**

- 主界面可以有多页,横向滑动切换
- 页面指示点在网格下方、Dock 上方,当前页高亮
- 编辑模式下拖拽到屏幕边缘停留可翻页;拖到最后一页右缘可新建一页
- 页面为空时自动删除(第一页除外)

**占位 app**

第一版会有大量位置还没有对应的 app。放 `placeholder` 类型的单元占位:
灰色底、虚线描边、点击提示"待开发"。这样主界面从第一天就是完整的版式,
以后往里填真 app 时只是替换单元的 `ref`,布局不动。

### 8.5 底部 Dock

**系统级常驻的四个 app 图标**,不是 tab 栏,不承担导航分区的职责。

- 固定高度 `--dock-h`,不随分页横滑,始终显示同样四个 app
- **背景透明、无边框**,与页面同底。早前给它加过一层衬底与上边框,
  结果在主界面底部切出一条明显的色带;导航栏当时用 `--surface` 也有同样问题,
  在每个 app 页的顶部切出一块白板。两处都改为与页面同底
- 容量固定 4 个。编辑模式下可与网格里的 app 互换;
  拖入第五个时最右侧那个退回网格
- 未读角标同样显示

Dock 只是快捷方式,点击进入 app 的行为与从网格点进去完全一样,
不产生额外的导航层级。

### 8.6 壁纸

- 用户从本地上传,走 images 域,压缩规格同 3.7
- 主界面与锁屏可分别设置
- 内置若干纯色与极简渐变作为默认,不内置照片类壁纸

### 8.7 后续(P3)

控制中心(上滑/下拉面板: 主题切换、字号、勿扰)、通知中心、桌面小组件。
这三项的入口手势在 8.1 的外壳层预留,实现推迟。

---

### 8.75 自定义字体

`system/fonts.js`。字体文件存进 `files` 域（二进制，不压缩），
用 `FontFace` 直接喂 `ArrayBuffer` 注册 —— 比 blob URL 稳，不用猜 `format()`，
ttf / otf / woff / woff2 一视同仁。family 名用记录 id（`uf-<id>`），
保证不和系统里同名字体打架。

两个槽位：`--font`（正文）和 `--font-serif`（挂件里勾了「衬线」的那几行）。
选中之后立刻把 CSS 变量设上去，加载还没完成时浏览器自己会回落到后面的系统字体，
不会白屏；加载失败就把变量摘掉。

- 传进来先 `FontFace.load()` 验一遍，坏文件直接报错，不存进库。
- 删掉正在用的字体会自动把槽位清空并回落，不会指向一个不存在的 family。
- `fontBody` / `fontSerif` 在 `LOOK_KEYS` 里，外观预设会带上。
- 中文字体动辄十几 M，界面里直接标出体积。

入口在 `设置 - 主题`，和别的观感设置在一起。

### 8.8 外观预设

`system/looks.js`。把整套装修拍个快照存进 `looks` 数据域（IndexedDB，DB_VERSION 提到 4），
随时切回去。

存的是**外观**，不是内容：`layout` 整份（图标位置、挂件、底部那一排、壁纸引用）
加上 `settings` 里 `LOOK_KEYS` 列的那几项（深色、图标颜色/阴影/名称、底部上移、
自定义 CSS、状态栏、锁屏）。接口密钥、prompt 模板、上下文参数一概不拍。

两个要当心的地方：

- **预设只存图片 id，不存图片本身。** 所以「清理无引用图片」必须把
  `looks.allImageIds()` 也算进已用集合，否则一清理，存好的预设全成空壳。
  `apply()` 里还兜了一层：图确实没了就当没设过，不会把界面搞成空白，
  并在 toast 里说明少了几张。
- 应用时 `layout` 用 `replace` 整份覆盖，不是 `set` 合并 —— 合并的话
  旧版式里多出来的格子会残留下来。

入口在 `设置 - 主题`，和它存的那些东西在同一页，符合 CLAUDE.md 第 5 条。

## 9. 设计系统

### 9.1 布局原语

所有页面必须包在 `<Page>` 里。`<Page>` 负责 NavBar、滚动容器、安全区,
以及**应用内** TabBar(如 chat 的四个分区)。系统级的 Dock 不归 `<Page>` 管,
它在根容器层,见 8.5。
**禁止 app 自己写 overflow 和安全区计算**,这是样式 bug 的主要来源。

```
Page / NavBar / TabBar / List / ListItem / Sheet / Modal
Button / IconButton / Input / Textarea / Switch / Slider / Segmented
Avatar / Badge / Toast / EmptyState / Spinner / Skeleton
```

### 9.2 设计令牌

颜色、圆角、间距、字号、动效曲线全部在 `styles/tokens.css`。
禁止在组件里写死颜色值。深浅色主题靠覆盖变量实现。

### 9.3 尺寸与安全区

- 全屏铺满,不存在机身外框
- 根容器 `height: 100vh`,是全项目唯一的视口单位
- 其余高度由 flex 与 grid 分配,flex 子项记得 `min-height: 0`
- 安全区用 `env(safe-area-inset-*)` 换算为 `--safe-top` / `--safe-bottom`,
  由根容器统一计算,页面与 app 不自己算
- 顶部那一条:网页自己画了状态栏时(安卓安装包)由根容器让;没画时(iOS、手机浏览器)
  根容器不让,改成 `--top-inset` 交给 `.screen` 里各层 —— 主界面、锁屏、没有顶栏的页面
  整层往下让,有顶栏的页面由顶栏把自己加高这一条,底色铺到屏幕顶边垫在系统状态栏底下,
  字和按钮仍在安全区以下。悬浮返回键同样下移 `--top-inset`
- 浏览器标签页(安卓 Chrome 这一类)里网页盖不到地址栏和系统状态栏。`system/fullscreen.js`
  在触摸时请求 Fullscreen API,两样一起藏掉,网页自己画状态栏;开关在「主题 - 浏览器中全屏」,
  默认开,只在触摸屏、非苹果设备、没加到主屏幕时出现。`theme-color` 跟着应用自己的 `--bg`
  (`system/look.js` 的 `syncThemeColor`),不按系统深浅色分。全屏期间 `html[data-browser-full]`
  把 `--safe-top` / `--safe-bottom` 清零:安卓 Chrome 全屏时系统状态栏临时滑出来一次,
  报上来的安全区就停在状态栏高度不回去,照它让会在顶上留一条空白。自己画的状态栏在全屏时
  至少和系统的一样高(`--cutout-top`,即浏览器报的顶部安全区原值),时间和电量落在摄像头那一行,
  页面从它下面开始。全屏期间不要在 resize 里改 viewport:改完会再触发 resize,整屏一直闪。
  唯一的例外:进全屏后量出可视区比屏幕矮一截(Chrome 没把页面画进摄像头那一行,那一行留白),
  把 viewport-fit 换成 auto 再换回 cover,让 Chrome 重新决定;连着最多换两次,换的过程中来的
  resize 不理,量到铺满才重新计数
- **苹果设备不走这一套**(`drawsBars` 先排除苹果):苹果把加到主屏幕的应用也报成
  `display-mode: fullscreen`,但系统状态栏一直都在。.120 到 .130 没排除,iPhone 上安全区被清零,
  主页钻进状态栏底下、悬浮返回键点不到。`viewport.mjs` 里有一段专门模拟这个情形
- 加到主屏幕、以全屏打开(`display-mode: fullscreen`)与上面请求来的全屏同一个处境,
  安全区清零、状态栏加高、留白重排三样照做(`fullscreen.js` 的 `drawsBars`)。状态栏划出来
  几秒后自己收回去,所以 resize 之后等 3.5 秒再量。桌面全屏版启动时与从后台切回来时,
  系统状态栏先露一下再收起,有的机器量不出矮一截、那一行照样留白:等 2.5 秒无条件重排一次,
  由计时器触发,只一次
- `?diag` 打开一次就记在 localStorage(`?diag=off` 关),主屏幕那一份没有地址栏也能看读数;
  读数里另记刚启动与 3 秒后两组
- 地址加 `?diag` 时屏幕中间显示布局读数(`system/diag.js`),排查真机上空出来的那一条

### 9.4 图标

统一入口:

```js
<Icon name="message" size={24} />
```

- 全部 SVG,24px 网格,`stroke-width: 1.5`,`currentColor`
- path 数据集中在 `icons/paths.js`
- **零 emoji**。`scripts/check-no-emoji.mjs` 扫描全部源码的 emoji 码点,接 CI 与 pre-commit

---

## 10. 工程护栏

| 措施 | 挡住什么 |
|---|---|
| 每个 app 外包 ErrorBoundary | 一个 app 崩溃不会整机白屏,只显示"应用已停止" |
| storage 命名空间 + schema version + migration | 数据结构变更不炸历史数据 |
| 懒加载分包 | app 数量增长不拖慢首屏 |
| 依赖边界检查脚本 | 杜绝 app 互相 import |

**边界检查器只按字符串找是不够的。** 原先它拿 `apps/([^/]+)/` 去匹配 import
里那个字符串，于是 `../../settings/MusicPage.js` 这种爬出去的相对写法一个都抓
不到 —— 已经这么漏进去过一次（一起听要在两个 app 里用同一个扫码组件）。
现在先把相对路径解析成仓库里的位置再判断。跨 app 复用的组件请往 `ui/` 放，
服务当参数传进去，别让 UI 层认识某个具体服务。
| `scripts/new-app.mjs` 脚手架 | 每个 app 结构一致 |
| 开发模式 manifest 校验 | id 重复、图标缺失、权限未声明直接报错 |
| AIQueue 统一出口 | 并发打爆、请求泄漏、无法取消 |
| 世界书激活预览 | prompt 问题可调试而非盲猜 |
| 图片上传强制压缩 + objectURL 回收 | 存储膨胀与滚动列表的内存泄漏 |
| prompt 模板与代码分离 | 调语气不用改代码,也不会误伤逻辑 |
| 主界面布局自愈 | 增删 app 或挂件后出现空洞、重叠、漏图标 |
| 视口单位检查脚本 | dvh 混入导致地址栏显隐时界面抽动错位 |
| 死导出检查脚本 | 写了没人调的函数一直躺着，改 API 时悄悄坏掉 |

---

### 10.04 Esc 归谁管

浮层和外壳都想响应 Esc：浮层要关自己，外壳要「返回」。

原先各自挂一个 `window` keydown，同一次按键两边都跑 —— 外壳先注册所以先执行，
于是浮层还没关，人已经被退出会话了。命令式的 `confirm` / `prompt` 更糟，
它们压根没处理 Esc，按下去弹窗留在原地，底下的页面却退掉了。

改成**全场只有一个监听器**，在 `shell/Root.js` 里。
`ui/overlay.js` 只维护一个关闭函数的栈，外壳按下 Esc 时先问它：

```js
if (e.key === 'Escape') {
  if (!closeTopOverlay()) back();
}
```

关掉了最上面那层就到此为止，一层都没有才轮到「返回」。
栈是后进先出，所以浮层套浮层时一次只关一层。

`Sheet` / `FullSheet` / `Modal` 走同一个 `useCloser`，
命令式的 `confirm` / `prompt` 在创建时压栈、`done` 时出栈。
不要再往 window 上挂第二个 Esc 监听 —— 两个监听器就一定会各跑各的。

### 10.05 长按与系统的选中浮层

挂了长按手势的元素必须带 `.no-callout`（CLAUDE.md 第 12 条）。
不加的话 iOS 长按时会弹出系统自己的选中与「拷贝」浮层，
和本项目的长按菜单同时出现，两边抢同一个手势。

`.no-callout` 定义在 `styles/ui.css` 一处，不在各自样式里重抄那三行。
目前挂着长按的：`.msg`、`.msg-row`、`.home`。

`scripts/check-longpress.mjs` 粗扫：文件里既有 `onTouchStart`
又有 300 到 1200 毫秒的定时器字面量（人手长按的时长区间），
却找不到 `no-callout`，就报错。判定是粗的，但要防的就是
「新写了个长按，忘了禁系统浮层」这一种错，而这种错一定落在同一个文件里。

禁掉系统的选中不等于不能复制，只是复制的入口归自己管 ——
系统那套选的是「一段文字」，本项目要的是「这一条消息」，本来就不是一回事。

### 10.06 一次能做多少：0 表示不限

CLAUDE.md 第 13 条落到代码上就是一条约定：**凡是「一次能做多少」的数，
存进 `settings` 的 0 一律表示「不限 / 全都要」。** 读的那一侧自己兜底，
不在 UI 层拦：

```js
const cap = n > 0 ? n : Infinity;     // 或者显式分支
```

这样做的好处是界面不必为「不限」另设一个开关 —— 输入框空着就是 0，
placeholder 写明不限时会发生什么，一个控件把两件事都说清楚了。

已经这么改过的（原先都是写死的常量或带 max 的滑块）：

| 设置项 | 管什么 | 0 是什么行为 |
|---|---|---|
| `callMaxTokens` | 通话每轮回复上限 | 退回接口本身的上限 |
| `callFrameGap` | 视频通话两帧间隔（秒） | 每轮都带一帧 |
| `stickerCold` / `stickerHot` | 注入的表情名单长度 | 全部列出 |
| `proactiveMaxUnread` | 未读多少条就停止主动发送 | 不受未读数限制 |
| `chatPage` | 会话一次渲染多少条 | 一次渲染全部 |
| `searchLimit` | 搜索结果条数 | 全部返回，扫完全库 |
| `scrobbleAfter` | 播放多久才上报（秒） | 一开始播放就上报 |
| `historyLimit` / `historyTurns` | 历史截取的条数 / 轮数 | 整段对话都进上下文 |
| `scanWindow` | 世界书与 B 级记忆的扫描窗口 | 在整段对话中匹配 |
| `contextBudget` | 注入内容的 token 预算 | 不按 token 截断 |
| `memoryTopK` | 语义检索取前几条 | 超过门槛的全部取用 |

前七项集中在「设置 - 用量与上限」（`apps/settings/LimitsPage.js`）。
后四项属于上下文，按第 5 条留在会话右上角的「上下文」页里，不另开第二个入口。

界面上统一用 `ui/basic.js` 的 `NumberInput`：一个输入框加一个单位，
输入过程中以草稿为准（否则把 `12` 删成空再回填成 0，光标会跳到开头），
**不设 `max`** —— 上限由用户决定。

`transfer.js` 原先那个一百万的金额上限直接删了。它挡的不是性能也不是费用，
纯粹是「我觉得不会有人转这么多」，而这正是第 13 条要反对的那种判断。

### 10.1 缓存里的旧代码

无构建方案没有文件指纹，浏览器会把 js 一直留在 HTTP 缓存里。真正麻烦的
不是「全旧」，而是**新旧混着**：这次新加的文件从没被缓存过，一定是新的；
旧文件却可能还是上一版。于是新页面调用了旧模块里还不存在的函数，
直接炸在运行时，报一句 `xxx is not a function`。

已经这么炸过两次（`images.js`、`services.js`），所以做了三层：

1. **启动自检**。`index.html` 里有一行 `<meta name="build">`，`src/version.js`
   里有同一个值。`main.js` 启动时比对，对不上就把代码全换一遍再重开。
   HTML 是导航请求，浏览器对它的重新验证比对子资源积极得多，
   所以拿它当「真实版本」。每个会话只自愈一次（`sessionStorage`），
   免得两个值真配不上时来回刷。

2. **崩溃页上的出口**。「更新代码并重开」原先只在设置页里，
   而设置页本身正是最容易被这么崩掉的页面之一 ——
   恢复手段藏在会坏的东西里面，等于没有。所以 `AppHost` 的崩溃页上也放一个。

3. **提交前拦一道**。`scripts/check-build.mjs` 检查两处构建号一致。
   忘了改其中一个，要么永远检测不出旧代码，要么每次启动都重开一遍。

换代码走 `system/refresh.js`：把本页实际加载过的同源 js/css 用
`cache: 'reload'` 重新取一遍，顺手清掉 Cache Storage，再刷新。
不维护文件清单 —— 清单总会漏，`performance.getEntriesByType('resource')`
拿到的是这一次真正加载过的东西。

## 11. 目录结构

```
phone/
├─ index.html
├─ ARCHITECTURE.md
├─ README.md
├─ vendor/
│  ├─ preact.mjs
│  ├─ hooks.mjs
│  └─ htm.mjs
├─ styles/
│  ├─ tokens.css
│  ├─ base.css
│  └─ shell.css
├─ scripts/
│  ├─ doctor.mjs           一次跑完下面全部检查
│  ├─ check-no-emoji.mjs
│  ├─ check-units.mjs      拦截 dvh / svh / lvh
│  ├─ check-boundaries.mjs
│  ├─ check-tokens.mjs     拦截硬编码颜色
│  └─ new-app.mjs
└─ src/
   ├─ main.js
   ├─ shell/
   │  ├─ Root.js              100vh 根容器,全项目唯一视口单位
   │  ├─ StatusBar.js         时间、信号、Wi-Fi、电量
   │  ├─ Dock.js              系统级底部四个 app
   │  └─ Gestures.js          上滑、返回手势
   ├─ system/
   │  ├─ registry.js          app 注册与 manifest 校验
   │  ├─ runtime.js           生命周期、前后台、ErrorBoundary
   │  ├─ nav.js               双层导航栈
   │  ├─ intents.js
   │  ├─ bus.js
   │  ├─ notify.js
   │  ├─ store.js
   │  ├─ db/
   │  │  ├─ storage.js        localStorage 适配层
   │  │  ├─ schema.js         版本与迁移
   │  │  ├─ characters.js
   │  │  ├─ lorebooks.js
   │  │  ├─ memories.js
   │  │  ├─ chats.js
   │  │  ├─ moments.js
   │  │  ├─ images.js         图片 Blob 读写与压缩
   │  │  ├─ layout.js         桌面布局
   │  │  └─ persona.js
   │  └─ ai/
   │     ├─ queue.js
   │     ├─ engine.js
   │     ├─ scheduler.js      群聊发言调度
   │     ├─ templates.js      prompt 默认模板
   │     ├─ tokens.js         粗略 token 估算与预算分配
   │     ├─ context/
   │     │  ├─ persona.js
   │     │  ├─ character.js
   │     │  ├─ lorebook.js    激活算法
   │     │  ├─ memory.js      检索算法
   │     │  ├─ history.js
   │     │  ├─ moments.js
   │     │  └─ time.js
   │     ├─ tasks/
   │     │  ├─ chat-reply.js
   │     │  ├─ chat-summarize.js
   │     │  ├─ memory-extract.js
   │     │  ├─ moment-create.js
   │     │  ├─ moment-comment.js
   │     │  └─ moment-reply.js
   │     └─ providers/
   │        ├─ anthropic.js
   │        └─ openai-compat.js
   ├─ sdk/
   │  └─ index.js
   ├─ ui/
   ├─ icons/
   │  ├─ Icon.js
   │  └─ paths.js
   ├─ screens/
   │  ├─ LockScreen.js        时钟、通知、上滑解锁
   │  ├─ home/
   │  │  ├─ HomeScreen.js     4 列网格,显式坐标
   │  │  ├─ Pager.js          多页横滑与指示点
   │  │  ├─ AppIcon.js        图标、角标
   │  │  ├─ WidgetHost.js     按 id 渲染挂件,不认识具体挂件
   │  │  ├─ EditMode.js       长按拖拽、改尺寸
   │  │  └─ layout.js         布局读写与自愈
   │  └─ AppSwitcher.js       多任务卡片
   └─ apps/
      ├─ index.js             唯一一处列出所有 app
      ├─ chat/
      │  ├─ manifest.js
      │  ├─ App.js
      │  └─ pages/
      │     ├─ MessagesTab.js
      │     ├─ ContactsTab.js
      │     ├─ MomentsTab.js
      │     ├─ ProfileTab.js
      │     ├─ Conversation.js
      │     ├─ CharacterProfile.js
      │     └─ CharacterEdit.js
      ├─ lorebook/
      ├─ memory/
      └─ settings/
```

---

## 12. SDK 接口

app 能接触到的全部系统能力:

```js
const phone = usePhone();

phone.nav.push(path) / pop() / replace(path) / setTitle(s)

phone.storage.get(key) / set(key, v) / remove(key)      // 自动命名空间

phone.db.characters / lorebooks / memories / chats / moments / persona
   .list(filter) / get(id) / create(obj) / update(id, patch) / remove(id)
   .subscribe(fn)

phone.ai.run(taskId, params)          // 入队并返回可取消的句柄
phone.ai.stream(taskId, params, { onDelta, onDone, onError })

phone.intent.open(appId, params)
phone.intent.request(intentId, params)

phone.notify({ title, body, icon, appId, payload })

phone.ui.toast(msg) / sheet(opts) / confirm(opts) / haptic()

phone.settings.get(key) / set(key, v)

phone.search.searchMessages(q, { chatId, limit, onBatch }) -> { cancel, done }
phone.search.excerpt(text, q) / splitHit(text, q)

phone.transfer.send({ chatId, role, authorId, amount, note }) / settle(id, take)
phone.transfer.pendingFrom(chatId, role) / format(n, code) / display(n, code)
phone.currency.LIST / current() / set(code) / round(n, code)
phone.place.send({ chatId, role, authorId, place, address }) / parse(body)
phone.gift.send({ chatId, role, authorId, cover, inner }) / settle(id, open)
phone.music.addSong / findSong / createList / addTrack / tracksOf / parseLyric
phone.listen.start({ chatId, listId }) / play(id) / next() / stop() / totals(chatId)
phone.netease.qrStart() / qrCheck(key) / saveLogin(cookie, charId) / search(q)
phone.netease.songUrl(id) / scrobble(id, sec, charId) / syncPlaylist(ids, charId, name)

phone.call.dial(chatId) / ring(chatId) / accept() / decline() / hangUp()
phone.call.say(text) / toggleSpeak() / toggleMic() / toggleSelf() / charCanSee()
phone.camera.start() / stop() / attach(el) / grab() —— 帧只进请求，不进库

phone.pace.modeOf(chat) / setMode(chatId, mode) / setPace(chatId, { base, max })
phone.pace.delayFor(chatId, text, { rng, at }) —— 等多久，本地算
phone.pace.schedule(chatId, text) / pendingOf(chat) / leftOf(chat) / clear(chatId)
phone.pace.dueChats(now) / runDue(now) —— 全局 tick 扫到点的那几段
phone.autoReply.configOf(chat, side) / setConfig(chatId, side, patch)
phone.autoReply.shouldReply(chatId, side) / fire(chatId, side) / bannerOf(chat)
```

---

## 13. 路线图

原来那份 P0–P4 是开工前拍的，现在已经跑完并且长歪了不少（向量记忆、多账号、
「联系」app 都不在原计划里）。这一节按**实际做完的**重写，后面接**还没做的**。

### 13.1 已经落地的

**骨架**
- shell（Root / StatusBar / Dock / NotifyBanner）+ screens（home / LockScreen / AppSwitcher）
- 主界面 4 列网格、多页翻页、编辑模式、长按改图标与名字、布局持久化与自愈
- 全屏外壳：`html / body / #app / .root` 四层统一 `100vh` + `viewport-fit=cover`
  + `black-translucent`，壁纸 `position: fixed` 独立于布局
- 令牌系统 `styles/tokens.css`，单色 SVG 图标表 `icons/paths.js`

**数据层**
- IndexedDB 九个集合：`characters` `lorebooks` `memories` `chats` `messages`
  `moments` `stickers` `looks` `personas`
- KV 三个：`settings` `persona` `layout`
- 内存镜像（同步渲染）+ 每 store 一条写队列 + 按行合并的批量落盘
- 消息按 chatId 建二级索引，列表项记忆化（见 3.95）
- `DB_VERSION = 5` / `DATA_VERSION = 3`，迁移从 0 起跑，全新安装也走一遍
- 图片压缩存取、文件存取、存储占用统计

**AI 引擎**
- AIQueue：并发上限、去重、取消、指数退避重试
- 双 provider：Anthropic / OpenAI 兼容；多预设，主用 + 副用，主用报错自动顶上
- 半填的预设（缺密钥或模型）一律当没配，报错带预设名
- 上下文块装配（`context/` 下 basic / lorebook / memory），`resolveOrder` + 逐块 try/catch
- 五个任务：`card` `char-alt` `memory-extract` `memory-import` `moments`
- 后台活儿（整理记忆、导卡、生 NPC、角色开小号）默认走副用，可开关
- prompt 模板全存 `settings.promptTemplates`，代码只留 `DEFAULT_TEMPLATES`

**记忆**
- S/A/B/C 四级 + 六分类，`updateId` 增量更新，扫描窗口 + token 预算
- 只按角色分（不再有全局/会话之分），跨身份按大号隔离
- 向量记忆：OpenAI 兼容 `/v1/embeddings`，`Float32Array` 存在记忆行上，
  归一化后点积即余弦，查询向量按文本缓存；失败三层回落到关键词
- 从一大段文字导入记忆（粘贴 / docx / txt）

**聊天**
- 流式回复、重 roll 多版本、编辑、删除重发
- 分条回复：`[图片：…]` `[语音：…]` 标记切分，换行即分条，错峰投递
- 礼物：封面与内容可以不一样，拆开前内容不进上下文也搜不到（见 4.676）
- 一起听：自建曲库与歌单，边聊边听，本次与累积两层统计（见 4.679）
- 可接自己部署的网易云接口：搜索、播放、两个账号各自打卡与同步歌单
- 功能说明按需注入：平时只给目录，用上了才给细则（见 4.645）
- 生图提示词分全局与角色两层；锁脸两条路，直传参考图或读成外貌描述
- 长按消息：编辑、修格式、引用、复制、多选删除
- 搜聊天记录：全部对话或单段对话，结果按时间倒序，点一条直达原文（见 3.96）
- 会话只画最近 200 条，往上翻再要一段（见 3.97）
- 引用双向（`[引用：…]` 标记），带出处进上下文，原话删了还看得见
- 修格式：模型降智写歪的格式按规则本地修，不重新调接口
- 时间感知：让模型自己写出当地时间再过滤掉；虚拟时间；双方时区各自设
- 用户可以发图片与语音；图片经识图、语音经识别读成文字后进上下文
- 转账：两边都能发，收到的一方收下或退回，处理结果落一行提示（见 4.675）；
  币种可选，记在每一笔上
- 位置：虚拟定位，地点名加地址；角色报的地点跟着它自己的时区设定（见 4.677）
- 通话：回合制，默认只出字幕可开发声，两边都能打字也能说话；
  角色可以打进来，任何页面都弹，整通电话只落一条记录（见 4.678）
- 视频通话：对方用上传的画面或头像铺满；我这边可虚拟可真实，
  开摄像头且识图走聊天模型时角色能看见我的画面，帧不落库
- 识图三档（选开）：关闭 / 交给聊天模型自己看 / 另配一套识图接口；
  交给聊天模型这一档只传当轮的图，看过之后存成描述，往后不再重传
- 历史范围可按条数，也可按轮次截取，轮次不会把一轮劈成两半
- 语音识别两档：仅转写文字，或连语调、情绪、语速一起读；
  没配接口时回落到浏览器自带的识别，也可以直接把文字写成一条语音
- 翻译：按语言让模型顺带给出译文，收在原文气泡里，点一下展开
- 角色可以发表情包，表情名随对话提供给模型，按角色单独开关
- 表情包、语音、上下文预览、模板编辑、主动消息（按角色单独配频率）

**联系（角色人设中心）**
- ins 卡片式网格、资料页（只有姓名/年龄/性别/生日/签名，**不露人设**）、编辑资料页
- 角色卡导入（PNG / JSON / docx / txt）、批量生成关联 NPC、手绘风关系网（BFS）
- 多账号与小号：用户可以有多个人设，同一人设可开小号；角色也能自己开小号
- 小号只带「关于这个人的记忆」，不做任何跨身份提示

**系统能力**
- 通知横幅 + 合成铃声（WebAudio，5 套预设）；角色回复与主动消息都走通知
- Service Worker + Web Push 客户端（服务端还没有）
- 保活：循环播放一段静音音频，切后台时定时器少被冻结（默认关）
- 外观预设、自定义字体、壁纸
- 桌面挂件可去掉卡片底，直接落在壁纸上
- 用量与上限集中成一页，所有「一次能做多少」都能填 0 表示不限（见 10.06）
- 情侣空间：纪念日、约定、信、礼物墙、一起去过的地方、一起听与通话统计（见 4.81）
- 随机事件库：领域 × 色彩九宫格、分量权重、大运、按格批量生成（见 4.82）
- 角色的一天：日程按时段分层注入、事项三态、吃饭记录与按地区的食谱库（见 4.83）
- 关系小件：心声、拍一拍、特别关心、骰子、头像联动（见 4.84）
- 面板自定义排序与「更多」、点外卖、共享位置（见 4.85）
- 回复节奏三档（时长本地算）、两个方向的自动回复、定时寄信、深夜消息（见 4.86）
- 主界面跨页挪动、真移除（可在设置里放回）、文件夹（见 4.87）
- 世界书分角色前/角色后两部分，各自可设注入深度，位置总览（见 4.88）
- 请求记录：默认关闭的排查工具，记下每一轮真正发出去的 system 与消息（见 4.89）
- 骨架重写：性别锚点首尾各一次、消息规则八节、两个示例、核心设定收束、thinking 自检（见 4.90）
- 记忆分两层：关系底色常驻、本轮召回下沉到对话末尾、核心设定自动生成（见 4.91）
- 修正：模板不再整套落库（否则改了默认值老库吃不到）、时区写地名、时差不用早晚、
  间隔量到上一轮、示例不填真名（见 4.92）
- 她在听什么：读角色网易云账号的真实播放记录写进 prompt，过期的不注入（见 4.93）
- 分条与人设：自检末尾补格式那一问、角色自带示例时内置示例让位、本地兜底分条（见 4.94）
- 流式 / 一次返回开关；当日日程就地开启（见 4.95）
- 修正：规则不再把话推硬、角色没设时区就不写地名、横幅在后台不过期（见 4.96）
- 「顶回来」：补读意图与「不要整轮都在反驳」两节，自检逐条标注动作类型（见 4.97）
- 一次发好几张图摞成一叠，点它换最上面那张，展开是平常的图片气泡（见 4.98）
- 顺手一点：正在输入、回到最新、从左边缘划返回、命中区撑到 44（见 4.99）
- 整个外壳默认不可选，只给输入框放开（见 4.100）
- 接口页：新建在最上、先地址后密钥、预设列表在区域内滚动（见 4.101）
- 联系人分组：NPC 单列一栏，分组可新建、改名、解散
- 音乐 app：首页、搜索、我的、曲库；数据读自用户自己的网易云账号
- 一起看：字幕、分段提纲、节奏判断、播放控制（见 4.102）
- 记忆重构：常驻三层、九项打分、本地二元组、写入防矛盾、易变块下沉、
  Anthropic 缓存声明、召回解释页（见 4.103）
- 译文行认得宽、点通知补跳、表情包分组（见 4.104 – 4.106）

**护栏**
- `scripts/doctor.mjs` 十三项：视口单位、零 emoji、导入导出、hook 顺序、依赖边界、
  硬编码颜色、长按禁选、提示词书面语、界面文案书面语、接口调用次数、备份完整性、
  死导出、构建号
- `scripts/smoke.mjs`：一百三十多条路由全开一遍，抓 ErrorBoundary

### 4.102 一起看

**它是「她陪你看」，不是两块屏幕对时间。** 角色那边没有第二个播放器，
所以不做房间、不做同步 —— 一起这件事发生的地方就是你这一台设备。

模型看不见画面。让它「在看」的不是画面，是**字幕**：

| 给它什么 | 花多少 | 怎么来的 |
|---|---|---|
| 最近几句台词 | 不花钱 | `subtitle.parse` 认 SRT / ASS / WebVTT |
| 到此刻为止的剧情提纲 | **一部片一次**调用 | `tasks/watch-outline.js` 读完整份字幕后按时间切段 |
| 画面 | 每帧一次调用 | 这一版不做 |

三处要害：

**提纲必须按进度截断。** 整份提纲是全片的，原样注入她就会说出还没演到的事，
而且是「第一次看却知道结局」那种穿帮。`video.outlineSoFar` 只放行
`from <= 当前时刻`的那几段。角色卡上另有一个「这部片它看过」的开关，
开了才让它知道后面会发生什么。

**什么时候该开口，本地算。** 字幕的疏密就是戏的疏密：
`subtitle.density` 往前后各看二十秒，数这一带有几句台词。对手戏正密的时候
插话最讨嫌，空窗才是开口的时机。这一判断不花一分钱，也不问模型。
间隔由「设置 - 用量与上限」里那一项定，填 0 就是只在你说话时才回。

**字幕从哪儿来。** 三条路，按代价排：

1. 外挂 SRT / ASS / VTT，导入即可，不花什么。
2. MP4 自带的字幕轨：`system/mp4subs.js` **自己拆 ISO 盒子**（moov → trak →
   stsd/stts/stsc/stsz/stco），只读字幕那几个样本，全程 `Blob.slice`，
   不把整个文件读进内存。不用 textTracks —— 内封轨的 cue 是跟着解码进度来的，
   想拿全片就得从头放到尾，而且 Chrome 不暴露 MP4 里的 tx3g 轨。
3. MKV 那一类浏览器根本放不了（`canPlayType('video/x-matroska')` 给空串），
   所以 `system/ffmpeg.js` 用 vendor 里的 ffmpeg.wasm **换壳**：`-c copy`
   把音视频原样搬进 MP4，字幕转成 mov_text，换完第 2 条那套就读得到了。
   三十二兆的核心**只在用户点了才加载**，平时一个字节都不读。
   上限七百兆：MEMFS 在内存里，wasm32 的地址空间就那么大，
   超了当场说不行，不要跑到一半崩掉。

字幕对不上是常事，所以有整体偏移（`video.offsetOf`），**只在读出来的时候加**，
不改原文。片库里能填，一起看那一屏上也能当场调 —— 对不对得上只有正在看的
时候才发现。

**播放控制是真的。** `[暂停]` `[继续]` `[倒回：12:30]` 三行会作用到
页面上那个 `<video>` 上（`watch.attach` 把元素交给 system 层）。
「等下，刚那句我没听清」然后画面真的倒回去 —— 这个瞬间的存在感，
比任何画面理解都强。

### 4.103 记忆重构：常驻三层、九项打分、写入防矛盾、位置与缓存

六批。`system/textsim.js`（新）、`system/memcheck.js`（新）、`ai/context/memory.js`、
`ai/context/index.js`、`ai/providers/anthropic.js`、`ai/tasks/memory-extract.js`、
`apps/memory/{LastPage,CheckPage}.js`。

#### 起因：恋爱特别在意记忆，而它疼在七处

默认一条都不记（自动总结出厂关着，第 15 条）；常驻层只装得下大事（底色只吃
S 级，「不要叫她全名」这种 A 级靠召回碰运气 —— **大事记得，小事一次次冒犯**）；
默认档按字面找不按意思找；只能让她记得，不能让她避开；分不出关于她的和关于你的；
待办会过期成地雷；没有「这句你给我记住」。外加两条召回本身的病：一次召回一堆
没有轻重，互相打架的两条一起回来。

#### 先修一个 400

`insertLore` 把召回与世界书以 `system` 角色插进 messages，而 Anthropic 的 messages
只认 user 与 assistant。直连 Anthropic、库里又有记忆能召回时这一轮根本发不出去，
而 `memoryDepth` 默认就是 1。走 OpenAI 兼容端点的人碰不到，所以一直没被发现。
provider 里 `toUserTurns()` 转成 user，用 `<context>` 裹住（不裹它读起来就是用户
说的话，模型会回一句「好的我记住了」），再合并相邻同角色。OpenAI 那边照旧收 system。

#### 位置：稳的连成一段，变的下沉

prompt 缓存只认完全相同的前缀。时间块每分钟都变，从前排在设定区第八位，它下面的
消息规则、整份能力清单、核心设定每一轮都重新算钱。`context/index.js` 的 `VOLATILE`
列出八块（时间、今天、一起听、一起看、换头像、出行、账本、健康），`assemble` 把它们
拆到 `volatile` 里，`buildHistory` 插到对话末尾，和召回同一条，召回排最后。
两头都合算：设定区成了稳定前缀；末尾是注意力最强的位置之一（Lost in the Middle）。

通话与主动发起不走 `buildHistory`，那两处把 `volatile` 接回 system。

Anthropic 的 system 带 `cache_control`（`settings.promptCache`，默认开，开关在
「用量与上限」）：命中按十分之一计价，没命中那一次按一点二五倍 —— 两头的账都写在
说明上。OpenAI 是自动前缀缓存，开关不影响。

**只有对话、通话、主动发起这三件带**（`engine.js` 的 `CACHED_TASKS`）。一次性任务
（总结记忆、生成相册、排行程……）的 system 每次都不同，声明了也命中不了，只会按
写入价多付两成五；`send()` 在交给 provider 之前把它们的 `cache` 关掉。

#### 召回块：分栏，不平铺

```
[相关记忆]
Notes that may bear on the present conversation, for reference. Ignore any that
do not fit. Where two entries conflict, the one with the later date is current.

Still unresolved:
- 2026-09-10 (10 days ago) 她答应周末来看你 (due 2026-09-23)

Always true:
- 她在成都做编辑

That happened:
- 2026-05-02 (4 months ago) 提到前任时她沉默了很久
```

三栏是记忆自身的性质，不是替角色排优先级（第 16 条删掉的「冲突时的取舍」排的是
人设与世界设定谁让谁，那才是判断）。「冲突以日期新的为准」是解析规则，依据的是
日期这个事实。「多久以前」本地算好写进去：日期相减和距离、点数一样不是模型的活。
`memoryTopK` 默认 6：六条分得清主次，十二条开始平均用力；没有向量接口的那一档
从前只有 token 预算一道闸，现在同样封顶。填 0 不限。

#### 打分：九项，全本地

| 项 | 算什么 |
|---|---|
| 线索 | 三路取最大：字面命中关键词、向量相似度、本地二元组相似度 |
| 新近 | 半衰期 45 天，锚点取「记下来」与「上次被想起」里更近的 |
| 强度 | `log1p(useCount)`。线性会正反馈成死循环 |
| 分量 | 提取时给的 `weight`；老数据按类别与等级估 |
| 未了结 | pending 且没过期。**不需要线索也上场** |
| 时段 | 与记下来时相差两小时以内 |
| 手写 | `source === 'manual'` |
| 最早 | 这段关系最早的五条 |
| 疲劳（减） | 上次被送出去在二十分钟内 |

权重 `WEIGHTS` 有默认值，`settings.memoryWeights` 逐项覆盖，「用量与上限」里改。
不给旋钮的话「她更该记得什么」就成了我替用户定的。

候选池两条来路：线索过门槛的，以及不需要线索也该上场的（未了结的）。人想起一件事
常常不是因为对方刚好提到了它。本地档的门槛只有 0.04 —— 那个数是「这条记忆有多少
字落在刚才那段话里」，天生小，主要靠排序挑，门槛只挡完全不沾边的。

**去冗余就是去矛盾**：互相打架的两条几乎必然在说同一件事，字面高度接近，落在同一
簇里，只出分高的那条。**保底名额**：至少一条具体发生过的事（emotion / relation /
pending），至少一条关于你的；只在候选池里换，不硬塞。

`markRecalled` 在请求真的发出去之后才记 `useCount` / `lastUsedAt`。

**本地二元组**（`textsim.js`）：词、二元组、单字混在一个集合里，逆文档频率压掉
常见字。单字只算三成 —— 「胃病」和「胃不舒服」一个二元组都不共享，这条路得靠单字
接上，而单字太泛。分母固定是记忆那一边（`covers`）：用户只回一句「在吗」时，按较短
那边算会让任何蹭到一个字的记忆都得高分。

#### 写入：三道防矛盾（`memcheck.js`）

槽位：职业、常住地、生日、学校、家庭、联系方式，同一槽位新的来了旧的让位。只列最
容易冲突的几个，槽位表一长就成了第二套角色卡。像到七成五以上：自动让位，按记的
时间算谁新（补总结半年前的历史时，后落库的反而是旧事）。五成到七成五：只标出来，
「记忆体检」页由人挑 —— 「她喜欢猫」与「她喜欢狗」字面也很像，自动合会误伤。

**让位不是删除。** `supersededBy` 标上之后不参与召回，留在库里，可撤回。

#### 常驻三层

关系底色（S 级压成）、**钉住的**、**忌讳的**。后两样在记忆条目上开关，互斥。钉住的
不再去挤召回名额（`poolFor` 排除）。忌讳单独一段写明不主动提起，对方先提起可以回应
—— 这是用户自己钉的设定，不是内置提示词替角色作判断。`pinnedMax` 默认 8，0 不限。

#### 入口

消息长按「记住这句」：不调接口，内容带上是谁说的，等级 A，来源手写，存完
`intent.open('memory', { back: true })` 跳到编辑页，退回来还是这段对话。
记忆页第一眼写着自动总结开没开、各会话积压多少条、关着的后果，一键开。

#### 看得见的账

「记忆 - 上一轮召回」列出候选几条、注入几条、每条的分由哪几项凑出来、谁因为
「同一件事」让了位。九项权重全靠感觉调等于盲调。只给人看，不进 prompt。
`lastRecall()` 是函数不是变量：sdk 那一层 `{ ...memoryCtx }` 摊平进去，摊的那一下
就把值定死了。

#### 没做、以及为什么

- 自动总结默认仍然关着。改成默认开违反第 15 条，走的是「把账摆出来」这条路。
- 概要化（把多条具体事件压成画像）要单独调用，没做。
- 记忆与角色卡冲突不处理：那是用户自己写的两份设定打架。

### 4.104 译文行：认得宽一点，宁可认错也不要漏

认不出来的译文行会当正文渲染出去，屏幕上原文后面紧跟一条一模一样意思的消息 ——
用户看到的就是「莫名其妙重复发了一遍译文」。`reply.js` 的 `transOf()` 认下模型真会
写的五种走样：方括号掉了、自己加了序号、标签单独一行（正文在下一行）、右括号落在
下一行（接着读，最多三行）、`[译文：]` 是空的。冒号和方括号总得有一个，两样都没有
的「翻译」是角色说的话。

单独那套翻译接口（`ai/translate.js` 的 `rowsOf`）收得住四种返回形状：整个是数组、
换了键名、整批挤在一条里用换行分开、每行带序号。

**掉翻译的另一半在历史里。** 界面把 `[译文：…]` 从消息里剥出来单独存进
`translation`，于是历史里角色自己的旧消息一条译文都没有 —— 模型最听自己前几轮的
样子，几十条「不必翻」的示范压过设定区里那一段规则。`buildHistory` 在行内译文开着
时把那一行拼回去（`原文\n[译文：…]`），并在最后一条消息之前插一句
`skeleton.translate-tail`：格式要求开头说一遍、末尾再说一遍，各家的长上下文指引
都是这一句。`skeleton.translate` 本身也按「先说要做什么、给一个结构示例、再说
边界」重写，示例用占位符不用具体语言（第 16 条：不替角色定语言），并写明
「一条消息加它的译文行算一条」，不与 3 到 5 条那条规则打架。

### 4.104b 不要写这些：两道都不牢靠，所以两道都要

`system/ban.js`。用户自己列的禁写词，**默认一条都没有，内置也不给一份** ——
内置一张「AI 味词表」就是替所有角色定文风，第 16 条禁的正是这件事。

| 这一道 | 干什么 | 靠得住吗 |
|---|---|---|
| 送过去 | `skeleton.ban` 拼在设定区收尾段，贴着输出 | 不。否定式指令本来就弱，而且把那句话摆进上下文还会抬高它的出现概率 |
| 查回来 | 回复落地时 `scan()` 扫一遍，命中的标在气泡下方 | 是。它不依赖模型听不听话 |

各家指引都偏好正面陈述，可「不要写空气中弥漫着」没有正面的写法，所以第一道只能
这么写，第二道才是确定的那一道。

`*` 是通配符，跨度以十二字为限（`把*揉进骨血里`）—— 八股本来就是一个骨架配不同的
填空，只认死字面量会漏掉一大半；跨太远就不是同一句了，那是误伤。送进 prompt 时
`*` 换成省略号：星号是本地匹配的写法，模型不认识它。词条里的空白当「可有可无」，
从聊天记录里复制出来的词常常多一个空格。

命中之后自动重掷几次由 `settings.banReroll` 定，**默认 0**（第 15 条：每一次都是
一整次接口调用，登记在 `cost.js` 的 `EXTRA_CALLS` 里）。重掷掉的那几版留在候选里，
已经付过钱的东西不悄悄扔掉；重完仍然命中就照实标出来，不假装干净。

### 4.104c 立即模拟抢票：这台手机上的三个月不会真的过去

`grab.simulate()`。和连点「抢票」到底是同一件事 —— 同一套概率、同一套扣款、
同一本账（`tries` 照样记在票上），只有两点不同：不必点上几十下，
**开售时刻不作数**。

第二点才是它存在的理由：一场三个月后的演出，`saleOpen()` 为假，按钮写着
「90 天后开售」，而这台手机上的三个月不会真的过去 —— 那张票就永远点不动。
`grabOnce` 因此收一个 `ignoreSale`，只有 `simulate` 传真。

不设次数上限（第 13 条）：跑到抢到、或者本档售罄为止。循环一定会停，因为
`left = pool × (1 − tries / TRIES)`，第 TRIES 次之后就是零。代码里那个 10000
是防跑飞的保险，不是给用户的门。抢票不调接口，所以不进 `EXTRA_CALLS`。

### 4.104d 待办：两道监督，谁都不能直接入库

`system/todo.js` + 「待办」app。用户**自己的**待办 —— 角色的日程是 `days`，
你们之间的约定是 `spaceItems` 里的 pact，这三样不是一回事。

聊天里说过的事隔天谁都不记得，所以两边各看一眼：

| | 怎么看 | 强在哪 | 弱在哪 |
|---|---|---|---|
| 本地 | 发出去的每句话过一遍线索词 | 不花钱、不延迟、每条都看 | 只认字面：「我想你了」和「我想去看展」在它眼里一样 |
| 角色 | 回复里写 `[待办：…]` | 读得懂那句是不是真要做一件事 | 只在它想起来时写 |

**两道都不准，所以两道都不直接入库** —— 一律落成 `PENDING`，会话页在输入框
上面压一条问一句，点了才算。这是这个功能的全部分寸：它在替你记事，
不在替你做决定。

忽略掉的记为 `DROP` 留在库里，不是为了统计，是为了**不再问第二遍**同一句话。
识别设置里可以清掉，清掉之后同样的话会重新问起。

线索词与排除词都是**用户可改的默认值**（第 13 条）：`todoCues` / `todoSkips`
存 null 时用 `todo.js` 里那两份，改成数组就是用户自己那份，改成空数组等于把
本地这一道关了。排除词是「我想你」这类的出口 —— 它是这个 app 上最常出现的
句子之一，而它一个待办都不是。

角色那一道是能力 `todo`（会话菜单的「能力开关」里按角色开关）。标记搭在本来
就要发的那次回复上，**不多打一次接口**，所以不进 `EXTRA_CALLS`。
`skeleton.todo` 只写这个标记是什么意思，不写「什么时候该用」（第 16 条）。

`[待办：…]` 不占气泡：`materialize` 见到这个 part 就落一条待确认然后返回 null，
走 `renderTurn` 里本来就有的 `if (!msg) continue`。

**新增数据域要登记三处**，不是两处：`db/index.js` 的 `COLLECTIONS`、
`backup.js` 的 `COLLECTIONS`、以及 `schema.js` 的 `STORES` 加 `DB_VERSION`。
第三处漏了整个应用起不来（「One of the specified object stores was not found」），
而报错里看不出是哪个域。`check-backup.mjs` 现在这三处一起查。

「清空全部数据」也吃 `backup.js` 那张表（`wipeAll`）。它从前在存储页里自己
列了十一个域，后来加的二十几个一个都没跟上，点「清空全部」清掉的不到一半，
而界面上写着「已清空」。凡是「对每一个域做一件事」的，都从那张表读，
不要再抄一份。

### 4.104e 系统闹钟：网页这一半做不到「关掉还能响」

`system/alarm.js` + `ios/Sources/AlarmBridge.swift`。一条待办填上时刻，交给
iOS 自己的闹钟去响。

**为什么非要原生。** iOS 上网页没有后台调度，Web Push 还欠一个服务端
（13.2 的 N3）。所以浏览器里定的时刻只在 app 开着时提醒得了 —— 这不是没做，
是做不到。两边都留着：`alarm.start()` 一分钟看一次表，在 app 里弹；系统闹钟
在锁屏上响。两边各响一次，位置不同，不算重复。

**和「健康」那座桥不一样的两点。** 一、AlarmKit **不要 entitlement**，只要
Info.plist 的 `NSAlarmKitUsageDescription` 加一次运行时授权 —— HealthKit 那种
「自己签多半拿不到」的问题这里没有。二、它要 **iOS 26**，而本项目最低支持 15。

**只做固定时刻，不做倒计时。** AlarmKit 的倒计时那一档要配一个 widget
extension 画 Live Activity，没有它系统会把闹钟撤掉；固定时刻的告警由系统自己
画，不需要扩展。这只 app 要的正是固定时刻。

整个 Swift 文件用 `#if canImport(AlarmKit)` 包着：编译用的 SDK 可能还没有这个
框架，没有就整段不编、如实回「这套构建里没有」—— 编不过比少个功能糟得多。
`build.sh` 在日志里说一声这套 SDK 里有没有它。

网页那边的 id 是 `td_xxx`，不是 UUID，所以 `AlarmBridge.uuid(from:)` 按内容
散一个固定的 UUID 出来 —— 同一条待办每次都要散出同一个，撤的时候才找得回。

「待办 - 系统闹钟」那一页只干一件事：**对账**。排了没响是这类功能里最难查的
一种，屏幕上写着「已排入」而到点没动静时，分不清是没排上、没授权、还是被系统
清了。所以把本应用记的数目和系统里实际挂着的数目并排摆出来。

### 4.104f 抢不到的票和查不到的人数

从前搜不到 `capacity` / `demand` 就 `oddsOf` 返回 null，界面写「无法计算」，
整个抢票在那张票上就废了。现在改成**让模型在同一次返回里一并给出**
（不多一次接口），并用 `numsMade` 标明这两个数是编的还是查到的；
没联网那一档整批算编的，不必问模型。

原则没变，变的是做法：从前是「不拿默认值凑一个数」，现在是「凑，但标出来」。
凑了不说才是问题，凑了说清楚是让功能能用。抢票页上那两行标着「（虚拟）」，
概率那一行写明「这一概率同样是虚拟的」。

### 4.104g 「明天七点」：人不会说年月日

`system/when.js`。待办从对话里来，那一头说的永远是「明天七点」「后天下午
三点半」「周四上午十点」—— 相对的、省略的、带时段词的。所以要有一层把它折算
成绝对时刻，不能让人去填 `2026-09-30`。

认这几类：相对日（今天/明天/后天/大后天/N 天后/周 X/下周 X）、具体日期
（9月30号、2026-10-01）、时段词（凌晨/早上/中午/下午/傍晚/晚上）、
时刻（七点/7:30/七点半/七点一刻/八点五分），中文数字到二十四。

三条规矩：

- **按真实时间算，不按对话里的时间。** 时间感知那一套可以把对话停在某一夜，
  而闹钟要在真实世界里响 —— 按停住的故事时间折算，算出来的是一个永远不会到
  的时刻。
- **只说点钟没说哪天，而那个点钟今天已经过去了，就是明天。**「七点」在晚上
  八点说出口，没有人是指十三小时之前。
- **认不出来就回 null，不猜。** 一个猜错的闹钟比没有闹钟糟：没有闹钟你知道
  自己得记着。

`hasTime` 分开报「认出了日子」和「认出了点钟」。**自动挂时刻那条路只认
`hasTime` 为真的** —— 「我想说今天天气不错」里确实有「今天」，解析器如实报它，
但它不该变成一条凌晨零点的闹钟。

输入框里存的是**原话**（`whenText`），不是换算之后那串数字：人写的是
「明天七点」，回头再看到的也该是这句话。下面一行回显折算结果，认错了看得见。

会话里那条确认栏上，计入这一下把闹钟一并排上 —— 点「计入」本来就是
「记着，到点叫我」的意思，不该再让人进一次待办去排。

### 4.104h 备忘：Apple「备忘录」写不进去，只能递过去

`system/note.js` + 「待办」里的第二个标签页。**用户自己的**备忘 —— 角色手机里
那份备忘录（`theirs`）是角色的，两回事。

**Apple 备忘录没有公开的写入接口**：没有 NotesKit，没有可用的 URL scheme。
（查这一条时差点信了一个叫 `com.apple.developer.alarmkit` 的 entitlement，
那东西根本不存在，是别人被 LLM 编出来坑过的名字，Apple 工程师在论坛里专门
澄清过。凡是 entitlement 都要查到 Apple 自己的文档为止。）

所以存在本地，要送过去就走**系统分享面板**（`navigator.share`，WKWebView 里
可用，要 HTTPS 加一次真实点击）：点一下，在面板里选「备忘录」。不是悄悄写进去，
是递过去 —— iOS 上这就是上限，界面上照实写，不假装能做到别的。
面板上按取消抛的是 `AbortError`，那不是出错，是他改主意了，不弹红的。

**和待办分成两个数据域**，不共用一个：待办是「要做的事」，有状态有时刻；
备忘是「要留着的字」，两样都没有。混在一域里，两边的查询会互相绊。

进来的路两条：备忘页新建，或者对话里长按消息「存成备忘」。后者和旁边那条
「记住这句」是两件事 —— 那一条进角色的记忆、会被注入 prompt，这一条只进
我的备忘，角色看不见。

### 4.104i 时刻和事情常常不在同一句话里

    我：明天七点
    我：叫我起来跑步

两条各自都不成立：前一条没有事，后一条没有时刻。所以 `todo.propose` 收一个
`near`（前几句的原文），自己那句认不出时刻就往回翻；反过来那一半是
`todo.attachTime`：一句话里只有时刻、没有线索词时，把它补给这段对话里还等着
确认、又没挂时刻的那一条。

两道闸都要：**隔太多句**不算（中间隔了一段别的话，那个时刻多半在说别的），
**隔太久**不算（半小时前提过的七点和现在这句没关系）。而且这一句自己带线索词时
不去动别人的 —— 它自己会落一条。

### 4.104j 存钱的入口在账户旁边，支出摊开看构成

**存入 / 取出挂在每个账户那一行下面**（第 5 条）：想给某个账户加钱的人正看着
那个账户，不该让他退回「记一笔」再从头挑一遍自己刚点过的东西。两个按钮都走
`EntrySheet`，只是多传一个 `preset`（账户、收支方向、标题）—— 记账的字段只该有
一份，另写一张迟早走岔。抢票要的共同账户也是这么充值的。

**支出构成**单开一页（`ledger.spending`）。首页那张卡只给一个总数，看不出什么。
这一页按分类摊开，每一类三样：金额、**占比**、**笔数** —— 三千块是一次买掉的
还是三十次凑出来的，是两回事，光看金额分不出来。月份自己翻，只列有流水的那几个
（`ledger.months`），翻到空月份没有意义。

### 4.104k 抢不到，有好几种抢不到

真实的抢票不是一次「中 / 不中」。**多数人根本没走到抽签那一步**：点进去一直
转圈，排队排不进去，好不容易进去了提交时票已经没了。只回一句「未抢到」，
把这段经历里最难受的部分抹平了。

所以一次尝试分四关：

| 关 | 栽在这儿是什么样 |
|---|---|
| 进门 | 排队人数过多 / 页面响应超时 —— 连抽签的资格都没拿到 |
| 余票 | 提交的时候这一档刚好没了 |
| 抽签 | 提交成功，没中 |
| 支付 | 订单生成了，支付没在时限内完成，票被放回（不扣钱） |

**每一关的概率都从供需比算出来**（`pressure(ratio) = 1 - 1/ratio`），不是拍脑袋
给的定值 —— 和「抢到的概率」同一个出处。人越多卡在门口的比例越高，这正是那种
「一直加载」的来历：不是网差，是同时有三十万人在点同一个按钮。

栽在哪一关都算用掉一次尝试，**余票照样往下掉** —— 你卡在门口的这几秒，别人
买走了。这一条不能松：松了就成了「慢慢点总能点到」。

掷出来的数一律「小的走运」。不统一的话，同一个 `random` 在这个函数里一会儿
代表运气好一会儿代表运气坏，测起来和读起来都要拐一道弯。

界面上把过程**演出来**：正在连接 / 正在排队 / 正在加载 / 正在提交，每一步停多久
也由挤的程度决定。**演的时长不影响结果** —— 结果是 `grabOnce` 一次算完的，
手速不起作用这一条仍然成立（见 `grab.js` 开头）。「立即模拟抢票」那条路不演，
它要跑几十次。

### 4.104l 画布有硬上限，超了不是画不好，是整个页面没了

`cardshot.raster` 从前对画布尺寸一个上限都没有。而卡片的高度是**跟着选了多少
条走的**：选一百条就是两万像素高，再乘上二倍屏，三千四百万像素。

iOS 上 canvas 的面积超过一千多万像素就画不出来 —— 轻则一张纯白图，重则内存
一紧，渲染进程被系统收走，屏幕整个白掉，看着像 app 退出了。这条线各代机型
并不一样，卡着画等于赌，所以 `MAX_AREA` 取一千两百万，留一截。

压的办法是**降倍率不是放弃**（`fitScale`）：能画到二倍就二倍，画不下一路降到
刚好装得下。糊一点总好过没有，何况**卡片本身不靠这张 png** —— 那份快照
（msgs + 当时的 CSS 原文）早就存好了，相册里照常打得开。

同一类的第二处：卡片里每张图都是内嵌 dataURL，十来张照片就是几兆，转义完
再翻两三倍，整个字符串还要被解码成一张图。所以 `MAX_URL` 也封一道，
超了就不画这张 png。

### 4.104m ipa 的版本号要自己涨

打包那个 workflow 的 `version` 输入从前带着默认值 `1.0.0`，而手动触发时不填就
取那个默认值 —— 于是连打五次，五个包全是 1.0.0。装到手机上分不出哪个是哪个，
也看不出装成功没有。

改成留空时按 `1.0.$GITHUB_RUN_NUMBER`，自己涨，不必记得填。想钉一个版本仍然
可以在输入里写。

**顺带让人看得出手机上装的是哪一版。** 设置里那行构建号是从站点现取的网页版本，
说明不了外壳 —— 而闹钟、通知、健康那几样只在外壳里，只有重装才会变。
所以外壳把自己的版本注进 `window.phoneAppVersion`，设置里两个并排显示。

### 4.104n 语音三家、生图加 NovelAI，以及「照着常见形态写」的代价

MiniMax 那份语音是**照着「常见形态」猜着写的**，从没对过官方文档，两处猜错：

- 默认地址写的 `api.minimax.chat` 已经不是现在的接口地址。现在按账号所在地分
  `api.minimaxi.com`（国内）与 `api.minimax.io`（国际）。
- 把 GroupId 当成必填。新版 `sk-cp-` 那种 key 只用 Bearer 就够，而
  `isVoiceReady()` 没填 GroupId 就判「没配置」—— 于是设置页明明填满了，
  功能却是灰的。

这两条合起来就是「我啥都写了但是连不上」。**教训和 AlarmKit 那次同一个**：
接口格式要查到出处，猜出来的东西在用户那边才炸，而且报错看不出是猜错了。

顺手改成多家：`voice.KINDS` 列 MiniMax / OpenAI 兼容（`/v1/audio/speech`，
中转站基本都实现）/ ElevenLabs（鉴权走 `xi-api-key` 不是 Bearer，音色在路径里）。
`isVoiceReady` 改成按各家的 `needModel` 判，不把可选项算成必填。

生图加 `nai` 一档：`POST {base}/ai/generate-image`，body 是
`{input, model, action, parameters}`，**回来的是一个 zip**，png 在里头
（`system/zip.js` 的 `unzip` 正好用得上）。密钥是网站用户设置里生成的持久
token。参考图那条路（img2img / vibe transfer）参数完全不同，还没接，照实抛错
让上层退回纯文字。

**报错分两种说**：`连不上（地址、网络、跨域）` 与 `接口返回了错（key、参数）`
—— 这两种的下一步完全不同，混成一句「请求失败」等于什么都没说。

### 4.104o 通话里的声音：功能一直都在，缺的是「为什么没有」

打字通话、角色用配好的音色回话，这两半本来就都有：麦克风关掉就是文字输入框，
`playOne` 里 `canUseApi(char)` 为真就走语音接口。可实际用起来是哑的，因为：

- **喇叭默认关着。** 原本的理由是「没配语音接口的人照样能用，而且一分钱不多
  花」—— 对没配的人是对的，对专门配了音色的人就不对了：他配好了一个声音，
  打通电话却是静音的。改成 `callSpeak` 三态：`null` 是没碰过，那时按
  `canUseApi(char)` 决定；按过一次就记住他按的那次。
- **三种「出不来声」全都不吭声。** 接口没配、角色没设音色、角色的语音被关掉，
  从前一律静悄悄退回浏览器自带的合成，听着像机器人而人不知道哪儿没对。
  现在 `voiceWhy(char)` 给一句话，通话界面上写出来。

（这一条之所以拖到现在才暴露，是因为 MiniMax 那份配置本身是坏的，
`isVoiceReady()` 一直是假 —— 见 4.104n。两个毛病叠在一起，看着就像
「通话不支持语音」。）

通话出声按句走一次合成接口，所以在 `EXTRA_CALLS` 里登记一行（`setting: null`，
开关是通话界面上那个喇叭），「用量与上限」那一页摆得出这笔账。

顺带记一条：`streamCall` 写死走流式，**不看 `streamMode`**。那是故意的 ——
按句切播放靠的就是流式，改成一次返回等于第一句也要等整段。

### 4.104p 跨域是浏览器的规矩，不是 HTTP 的规矩

语音接口「连不上」，报的是浏览器那句含糊的 Failed to fetch —— 看不出是地址错了、
网断了、还是被跨域拦了。而很多语音、生图接口压根没考虑过浏览器直连：要对方在
响应头里点头才读得到，**点不点头是对方的事，我们改不了**。

但跨域是浏览器的规矩。外壳用系统自己的网络栈发，同一个请求照样通。所以加
`system/net.js` + `ios/Sources/NetBridge.swift`：装了 app 时把这类请求交给外壳
转发，浏览器里照常直连。

`nfetch` 是唯一出口，回来的东西**长得像 Response 但不是** —— 只做调用方真正
用得上的那几样。**哪些调用走它由调用方决定**：聊天那几家本来就允许浏览器直连，
不必绕；语音和生图这种十有八九不允许的，绕。

NetBridge **只转发，不判断**：不改地址、不加自己的头、不挑哪些能发。状态码、
响应头、响应体原样交回，错误也原样报 —— 这一层要是自作聪明，上面就再也查不出
真实原因，而「查不出真实原因」正是它要解决的问题。响应体走 base64 过桥，
封在 24 MB：几十兆的 base64 字符串会把内存顶穿，那比失败更糟。

**测试连接和试听不是一回事。** 试听只告诉你成没成，成不了的时候说不出卡在哪。
`testVoice()` 分四件事报：走的哪条路、有没有连上、对方回的什么状态、音频取到
没有 —— 每一步的下一步都不一样。连不上时提示还要看路：外壳那条路连不上，
就不该再叫人去折腾跨域。

### 4.104q Failed to fetch 是三件事，浏览器自己分得清

`fetch` 失败时抛的永远是同一句 `Failed to fetch`：域名解析不了是它，连接被拒是它，
跨域被拦也是它。可这三种的下一步完全不同 —— 混成一句「连不上」等于没说。

分辨的办法是再发一次 `mode: 'no-cors'`（`net.js` 的 `reachable`）。那种请求浏览器
不要求对方点头，回来的东西读不了，但**发得出去就说明服务器是活的**：

| no-cors | 结论 | 下一步 |
|---|---|---|
| 成了 | 被跨域拦下 | 换一个允许跨域的中转地址，或装成 app |
| 也败 | 没连上 | 地址、域名解析、网络 |

所以浏览器里也能把话说准，不必等装 app。外壳那条路不做这个探测 —— 它本来就没有
跨域一说，那时报「被跨域拦下」反而是误导。

### 4.104r 原本那张脸，和现在这张

角色能在对话里自己换头像（`[换头像：…]` → `avatar.wear`），而 `wear` 直接覆盖
`char.avatar` —— 换完就找不回原来那张了。「它本来长什么样」和「它现在什么样」
是两件事，前者是你当初给它选的。

`avatar` 仍然一直是**现在在用的**那张：界面上到处都读它，换个字段等于把每一处
显示都改一遍，漏一处就是两张脸。原本那张单独记在 `avatarBase`，没被换过时是空的
（`facesOf` 那时两张返回同一张）。`restoreFace` 换得回去。

用户自己那边同理。这里还顺手修了一个更要命的：从前在主页换头像会**把旧图删掉**
（`images.remove`），于是「换回去」根本无从做起。现在头像那一张留着，封面照旧
换掉就删 —— 封面不是脸。

### 4.104s 语音的风格与语种：各家收的写法不一样

「用什么语气说」和「说哪种语言」，三家给的口子完全不同 —— 所以不能只做一个
字段往下发，那样十有八九被对方退回来。

| | 语种 | 风格 |
|---|---|---|
| MiniMax | `language_boost`，要它自己那套英文名 | `voice_setting.emotion`，**只收固定的几个值** |
| OpenAI 兼容 | 没有这个参数，并进说明里 | `instructions`，自由文本 |
| ElevenLabs | `language_code`，ISO 码，且仅部分模型认 | 没有这种参数 |

界面上给的是中文名，`LANGS` 负责翻成各家认的写法；填一个表里没有的就原样送给
MiniMax —— 它认的名字比那张表长，没道理拦着（第 13 条）。风格那边 `moodOf`
把中英文情绪词映到它的枚举，**映不上就不发** —— 送一个它不认的值，整个请求会被
退回来，那比没有风格糟得多。每一家支持到什么程度，设置页上照实写。

两层：全局在「设置 - 语音」，角色卡上填了就盖过全局（第 5 条那张表）。
`speak()` 不带参数调用时也回落到全局 —— 试听和真正说话必须是同一套，
不然听到的和用到的不是一回事。

### 4.105 点通知那一下，几乎总是跑在网页前面

外壳（`ios/Sources/NotifyBridge.swift`）收到点击之后直接 `evaluateJavaScript`，而
app 被系统结束之后再点，顺序是先启动、建 WKWebView、开始载入，系统这才把这一下
交过来 —— 打在一个马上就要被换掉的文档上，等于没打。人看到的就是「点了通知，
手机只是刷新一遍，还停在原处」。

三处接：外壳 `pending` 记着，页面 `didFinish`、回到前台、`web` 刚挂上来时各兑现
一次，打不出去就放回去；`index.html` 在模块加载之前先挂一个占位的
`phoneNotifyOpen`，存进 `sessionStorage`；`push.js` 起来之后 `drainPending()`。
冷启动那条路（SW 新开窗口）把去处写进地址 `#n=appId|route`，读完抹掉。

自愈重载的记号从 `sessionStorage` 换成按构建号记在 `localStorage`：外壳每次重新
载入都是新 session，记号跟着没了，于是每次进来都要再自愈、再重载一次。

### 4.106 表情包分组：空的也得存在

分组本来是从表情身上折出来的（谁的 `group` 写着什么），折出来的东西空的组不存在
—— 先建一个再往里放这条路根本走不通。自己建的名字单独存一份
`settings.stickerGroups`，`groups()` 取并集，`groups({ onlyUsed: true })` 给发送面板
（空分组是管理页上的半成品）。逐条添加：一次一张，存完当场打开编辑面板。
粘贴导入：剪贴板里的图片当场收下，文字按 txt 那套逐行解析。

**已有的那几百个怎么归类。** 单个改分组本来就能改（点开那一条的编辑面板），
可库里攒到几百个之后，「把已经有的归一下类」就是几百次「点开、选组、关掉」。
所以加了整理模式：点「整理分组」进去，点表情选中、点分组名整组全选，
右上角一次移入。选中的格子描一圈 `.stk-cell.is-picked`。

### 4.107 线下：一本可以翻的册子

线上是一条条短气泡，线下是大段散文。整条链路都是照线上长的，所以线下不是
「聊天加个开关」，是另一条解析与渲染链路，只共用底下的角色卡、世界书、记忆、
人设、时刻。

| 环节 | 线上 | 线下 |
|---|---|---|
| `skeleton.rules` | 每轮分 3 到 5 条发 | 一轮一整段 |
| `splitReply` | 按行切，三十种方括号标记 | 只摘思考块与时刻，正文原样 |
| `autoSplit` | 一行太长照标点断 | 绝不动 |
| 一次输出 | 一条几十字 | 一段几百到几万字 |
| 上下文 | 几十条才顶满预算 | 三段就顶满了 |
| 能力清单 | 转账、外卖、表情（手机上的动作） | 面对面，那些九成不成立 |

#### 两个数据域，不复用 messages

`scenes`（一场戏，按 chatId 索引）与 `beats`（一段正文，按 sceneId 索引）。

不在 `messages` 上加一个 `sceneId` 再到处过滤：`messagesOf(chatId)` 有十几处
调用方，漏掉一处就是「聊天记录里混进一篇两千字」，而且不报错。

也不把 beats 内嵌进 scene 行（`phoneChats`、`trips` 是内嵌的）。那两个内嵌是因为
总是整行一起读写，线下不是 —— 流式时一直在追加最后一段，内嵌意味着每个 delta
都要 put 一个几十 KB 的行。

一场戏挂在一段会话上。线下和线上是同一段关系的两面，共用角色卡、世界书、记忆、
关系底色、人设、当前时刻；`castIds` 里可以再拉别的角色进来演群戏。

#### 翻页：一段至少一张

坐标是 `{ beat, page }`。一段超过 `pageChars` 就续张，那个数在 `stage.js` 里，
**填 0 表示不切，这一张里滚**（第 13 条）。几万字的长回复因此自动变成几十张，
一张一张翻，比无限长的滚动条好读。

**生成中不分页**，就在当前这张里往下长,落定之后再切张。边写边跳页没法看。

翻页效果、点两侧翻页直接接 `reader.js` 那一套，那边四种效果都写好了。

#### 两种版式：翻页，与竖着滑的明信片

`layout` 两档。翻页那一档是上面那套坐标；明信片那一档把同样的「张」竖着排下去，
一张一片，片上带头像。

**头像只在明信片那一档出现。** 翻页那一档试过通栏头像，丑 —— 一整屏只有正文时，
多一张图就是多一样东西。明信片是一片一片的实物，片上有一张图反而对。方的，
一道发丝框，不做圆 —— 圆头像一出现，整页就读成聊天了。

每一片自己是一个组件，因为头像要 `useImage` 解析，而在循环里调 hook 是不行的。

明信片那一档没有页码，也不吃点两侧翻页；只留一件事：铺满屏幕时点一下把界面
收回来，否则铺满之后连菜单都没有，出不去。

#### 外壳那条状态栏也要染

线下是整屏接管的，可外壳的状态栏在 `.page-body` 之外，仍然是 app 的底色 ——
深色主题下上方就留一条白边。挂着的时候把底色写到 `:root` 的 `--sg-chrome` 上，
`shell.css` 认这个变量，离开就撤掉。

顺带补了 `statusBarStyle`：那个 prop 一直在设 `data-statusbar`，但**没有任何
CSS 接着**（锁屏那边也一样没生效）。现在按底色的感知亮度算深浅。

#### 开篇那一页：首字下沉是大件，署名全是小字

一段开头：一行名字（字距 .34em），一行地点、时刻与编号（10px，字距 .22em），
一道发丝线，然后正文的**首字下沉**。明信片那一档在左边再加一张头像。

**大件是那个下沉的首字，不是署名。** 这一处推翻过三版：

| 试过的 | 为什么撤 |
|---|---|
| 通栏出血的头像当插图 | 一整屏只有正文时，多一张图就是多一样东西 |
| 三行横排的圆邮戳 | 圆戳、双圈、歪几度都是「加图形」 |
| 56px 的大编号 | **刊物里的页码（folio）一向是小的、推到页边的** |

第三版错得最典型：查过之后才确认，杂志开篇那一页真正的大件是首字下沉
（2 到 7 行深，顶端与首行对齐）加一行极小的 credit，页码反而要「推开，
与正文拉开距离」。摆一个巨大的数字在开篇是模板做法，不是刊物做法。

首字下沉用 `::first-letter` 加 `float`，不用 `initial-letter`（各家支持不齐）。
两条判断在 `canDrop` 里，CSS 判不了：

- **开头得是字**。正文很常以「或者（开头，放大成两行高的一个引号很难看。
- **头一段得够长**（16 字）。下沉的那个字比一行高，头一段只有一行的话，
  它比整段还大，看着像出了错。

`sign` 三档：`full` 名字加地点加细线、`line` 只一行、`none` 不显示。
`drop` 单独可关。

#### 字体：系统里没有「软」的那一类

**先修一个真 bug。** 外观里一直有「字体链接」这一栏，但线下从来没有人把那份
样式表挂上去 —— 填了什么也不会发生，表现和「设置不生效」一模一样，正是
`reader.js` 开头那段警告过的那种失败。现在走 `reader.applyFont`，阅读器与线下
各用各的标签（`data-reader-font` / `data-stage-font`），互不拆对方的。

然后是选择本身。iOS 系统里只有宋体与黑体，两种都偏硬，而线下是大段散文，
字硬一点整页就紧。所以给几份现成的，**默认那一份仍然不联网**：

| | 链接 | 字体名 |
|---|---|---|
| 系统字体（默认） | 无 | 跟 `serif` 开关走 |
| 霞鹜文楷 | jsDelivr `lxgw-wenkai-screen-web/style.css` | `LXGW WenKai Screen` |
| 思源宋体 | Google Fonts | `Noto Serif SC` |
| 思源黑体 | Google Fonts | `Noto Sans SC` |

链接与字体名都是从各自仓库的文档里抄的，不是猜的 —— 字体名猜错的表现同样是
「设置不生效」。Google Fonts 那两份在部分网络环境下取不到，界面上照实写。
设置页里显示这一份挂上了没有（`reader.fontStatus`）。

署名那一块从前落回系统黑体，而正文是宋体，两种字挤在一起，署名就显得比正文
硬。现在整块跟着 `--sg-font` 走；名字的字距从 .34em 收到 .2em，那一行小字从
.22em 收到 .14em，并去掉 `tabular-nums` —— 这里的数字不排成列，等宽只让时刻
看着像块表。

#### 明信片展不展开

`cardGrow` 开着（默认）每片跟着内容长；关了每片一样大，按真明信片的比例
（105 × 148 毫米），文字在片内滚。**高度不能用 vh**（第 1 条只许外壳那三层用），
所以靠 `aspect-ratio`。

这和 `spread` 不是一回事：`spread` 管的是界面退不退，`cardGrow` 管的是每片
本身的大小。两档版式各自都有 `spread`。

#### 文风由用户定，内置提示词一个字不写

第 16 条不让内置提示词写文风 —— 写了，所有角色的散文就变成同一个人写的。
但「这一场写成什么样」本来就该有人决定，那个人是用户。`system/tone.js`
就是他决定的地方，和「不要写这些」同一个做法：装它的句子由代码提供
（`skeleton.scene-style`），内容全是用户挑的或者写的。

出厂带四份（克制、绵密、剧本式、第二人称），**一份都不默认启用**，每一份
都能改、能删、能新建，删掉的内置项记一个 null 盖住它（否则下次又冒出来），
「恢复出厂」只动内置那几份。正文一律英文（第 14 条）—— 用中文写「句子要短」，
模型交回来的就是说明书腔的短句。

**只有一个入口**：这一场的设定里（第 5 条）。没有全局开关，新建时预填上一次
挑的那一份 —— 那是个默认值，不是第二个开关。

#### 内置提示词仍然只写规则

```
skeleton.scene-opening    You are {{charName}}, present with {{userName}} in a scene.
skeleton.scene-rules      一轮写一段；不要替 {{userName}} 说话或行动；篇幅 {{words}} 字上下
skeleton.scene-setup      [这一场] 地点、时刻、在场的人 —— 数据，不是指令
skeleton.scene-director   [场外指示] 用户下的那一条
```

`skeleton.scene-rules` 和 `skeleton.rules` 同吃 200 字上限（`check-prompt-tone.mjs`）。
那个上限本身就是拦「判断慢慢长出来」的闸，而线下最容易写进去的正是
「描写要细腻」「多用感官细节」—— 一写，所有角色的文风就变成同一个人的。
文风归角色卡、世界书和用户模板（第 16 条）。

判断要有地方让用户自己下，那个地方是**场外指示**：`role: 'director'` 的一段，
作为指令进 prompt，不占页，只在刊头留一个可以点开的标记。

对白与动作**只在展示层分样式**，不进 prompt、不改数据、可关。解析错了最多是
颜色不对，弄不坏正文。

#### token 一处都不卡

| 卡在哪 | 改成 |
|---|---|
| `MAX_OUTPUT` 写死 32000，每次都送 | 线下单独一个数，**默认 0 = 整个 `max_tokens` 字段不送** |
| `contextBudget` 默认 6000 | 线下另给一个，默认十万量级，0 不限 |
| 历史窗口 | 默认全送 |
| `estimate()` 每轮全串重扫 | 缓存 |
| 流式每个 delta 触发 Preact 重渲染 | 直接 append `textContent`，绕开 diff |

`max_tokens` 默认「不送这个字段」而不是填个大数：各家上限不一样，给一个上限
8192 的模型填 200000，接口直接退回来，而报错文案通常看不出是哪个字段的事。
不送，服务端就用它自己的最大值。

几万字流式如果走 Preact，每秒几十次 diff 一个巨大文本节点，界面会僵住。

#### 外观：照 reader.js 复制一份

`stage.js` 和全局主题分开、和阅读器也分开 —— 线下想要的纸色字号，和读书时想要的
不是一回事。这条理由 `reader.js` 的注释里已经写过一次。

三套内置主题，每套四个中性色加一个标记色：

| | 底 | 墨 | 次要 | 细线 | 标记 |
|---|---|---|---|---|---|
| 正文（浅，衬线） | `#FCFBF8` | `#1C1F24` | `#6E737B` | `#E2E0D9` | `#27405E` |
| 夜刊（深，衬线） | `#15171B` | `#E6E3DB` | `#8B9098` | `#2A2E35` | `#C2A15B` |
| 素（无衬线，高对比） | `#FFFFFF` | `#101214` | `#70757C` | `#EBEDEF` | `#1F6F5C` |

刻意避开「暖奶油底 + 衬线 + 赤陶色」那一套。那不是杂志感，那是现在满大街的
生成感配色。

字体全走系统栈不联网（无构建、要能离线）：衬线 `Songti SC / STSong`，
无衬线 `PingFang SC / Heiti SC`。想换走已有的 `fonts.js` 或 `fontUrl`。

正文宽度按「一行 30 到 38 个汉字」卡住（西文那条 65 字符的老规矩）。首行不缩进，
用段距分段 —— 屏幕上缩进远不如段距清楚。

自定义 CSS 和全局 `customCSS` 同一个做法（独立 style 节点），但**只在线下页面
挂载时插入，离开就移除**，不漏到别的 app。

`spread` 开 = 正文铺满整屏，刊头页码都退掉；关 = 正文在固定区域内，四周留界面。

主题全局一套，某一场想单独换（夜戏用暗的）在那一场的菜单里覆盖（第 5 条）。

#### 入口与打通

面板（`panel.js` 的 `ITEMS`）加一格，点进去整屏接管，不是盖一层 Sheet。
新建时选「让角色开场」还是「我自己写」，地点时刻情境可填可空。

#### 两边互通：成本不对称，所以不对称处理

一开始定的是「摘要互通，原文不互通」。**那条太紧了，而且有一半根本没接上**：

- `chat.summary` 只覆盖**更早之前**，刚发的那几条不在里面；
- `scene.summary` 当时只有线下自己读，线上根本看不到。

于是「在手机上说好楼下见，见了面却完全不知道这回事」。用户撞见的就是这个。

改成一个注入区块 `context/bridge.js`，方向由 `ctx.side` 决定，两侧各走各的：

| | 带什么 | 为什么 |
|---|---|---|
| 线上 → 线下 | **最近 N 条消息的原文**（默认 20） | 一条气泡二十来字，二十条也才几百字 |
| 线下 → 线上 | **最近一场的摘要**，按字数截断（默认 400） | 一段正文上千字，原文带不起 |

线下那一场没有摘要时，截最后一段顶上 —— **这一步不调接口**（第 15 条）。
要真摘要就去开「收场摘要」。两个数都在「用量与上限」里，各填 0 就不带。

这个块进了 `VOLATILE`：对面每来一条消息、每落一段正文它就变，留在设定区
会把整段稳定前缀每轮作废。

#### 续写、编辑、重写留多版

线下比线上多出来的三件手上功夫，都挂在一段上：

**续写**（底栏那个向下的箭头）接着最后那一段往下写，不另起一段。prompt 末尾多
一句 `skeleton.scene-more`，落地走 `appendBeat` —— 续的是**当前那一版**，不另开
一版。那一段**必须留在历史里**，摘掉它模型就不知道接什么。

**重写** 反过来：`omitFrom` 把那一段连同它之后的都从历史里摘掉，否则模型看见
「已经写过了」，交回来的是接着它往下写，不是重写。**旧的不删**，`addSwipe` 往后
添一版，正文末尾出现 `‹ 2 / 3 ›` 自己翻。只对最后那一段开放 —— 中间那一段重写了，
后面几段就接不上了，那种情形走「从这一段分叉」。

**编辑** 改的是当前那一版，不是只改行上的 `text`；只改行的话翻走再翻回来就变
回去了。

一段的版本存成对象 `{text, raw, think, at}`，不是裸字符串 —— 每一版各有各的
时刻。老行里存的是裸字符串，`versionsOf` 统一收一遍，不收的话翻到那一版正文
会变成 `undefined`。

#### 线下也要进记忆

`memory-extract` 从前只读 `messagesOf`，线下发生的事进不了记忆 —— 见了一整
场面，回到线上什么都不记得。现在把线下的段落摆成消息的样子并进未总结那一摞，
场外指示不算（那是指令，不是发生过的事）。

**水位线要两条，不是一条。** 消息和正文各是一个域，各自的 id 在对方那条线上
找不到；共用一条的话 `findIndex` 落空，于是每次都从头再吃一遍 —— 账单按轮翻。
所以 `memoryUpTo` 与 `memoryUpToBeat` 各推各的。

触发仍是同一个 `autoSummarizeInterval`，默认 0 就是关着。

两项要登记进 `cost.js` 的 `EXTRA_CALLS`，都默认关：`sceneSummary`（收尾摘要）、
`sceneCompress`（窗口外压摘要）。正文走主用接口（你正盯着屏幕等），这两项走
副用或记忆接口。

### 4.108 「它什么都不记得」：三件事叠在一起

用户报的是「昨天说过在哭、说过几号去哪，今天一问三不知」。跑了一遍诊断，
是三件各自都讲得通的事叠出来的：

| | 诊断 | 性质 |
|---|---|---|
| `memories: 0` | `autoSummarizeInterval` 默认 0，自动总结关着，**一条长期记忆都没生成过** | 第 15 条要求的默认值 |
| 昨天那几句不在 prompt 里 | `historyLimit` 默认 20，更早的不进上下文 | 保守的默认值 |
| 待办里有「15 号去上海」，prompt 里没有 | **没有任何区块读用户的待办** | 真漏了 |

第三条是实打实的漏：检测命中、点了计入、闹钟都排上了，角色仍然一无所知。
补了 `context/plan.js`（`[你记着的事]`），只给已确认未完成的那些，排好时间的
在前，条数在「用量与上限」里，0 为全给。等确认的不给（还不算数），做完的
不给（不必再提）。

前两条都按规矩来，**不能偷偷改默认值** —— `autoSummarizeInterval` 登记在
`EXTRA_CALLS` 里，`check-calls.mjs` 就是拦「新加了一项却默认开着」的。
问题在于三条合起来的后果用户完全看不出来。所以在「上下文与记忆」顶上加一栏
**「它现在记得住什么」**，把三件事各摆一行，照实说后果：

```
长期记忆    库里 0 条。自动总结已关闭，不会再有新的记忆产生。
            「昨天说过的话今天不记得」多半出在这一项上。
历史范围    每次只带最近 20 条消息，更早的不进上下文。
你记着的事  「待办」里有 3 条已计入、还没做的，每次请求都会带上。
```

开关还在原处，这一栏只负责让人看见。**默认值不替用户改，账要摆出来** ——
第 13 条那句话反过来也成立：省钱的默认值不告诉用户，等于悄悄关掉了功能。

### 4.109 召回分档：先固定带一批近期的，再叠召回

上一节把三件事摆出来之后还剩一层：**记忆提取开了、库里有了，昨天的事照样
想不起来。**

原因在候选池。`poolFor` 只收两类：**线索命中的**，和**还没了结的**。
昨天记下「一直在哭，心情很差」，今天开口是「早」—— 一个字都不沾，那条
连候选池都进不去。打分里那个 `recent` 因子救不了它：池子里没有它，
分数无从谈起。

所以另开一档，`[最近记下的]`：**最近 N 条，不问相关不相关，一律带上**。
人对昨天的事本来就不需要谁提起。四档各管各的：

| | 收什么 | 怎么选 |
|---|---|---|
| 关系底色 | S 级压成的几句 | 常驻 |
| 一直记着 | 钉住的与忌讳的 | 用户指定，常驻 |
| **最近记下** | 最近 N 条 | **按时间，不问内容** |
| 相关记忆 | 这一轮检索到的 | 命中才来 |

两处配套：

- **召回排除掉近期这几条**（`ctx.skip`）。不排除的话它们占两份位置，
  而召回本来就只有几个名额。
- **近期这一档不记「被想起过」**，和钉住的一致。记上的话强度虚高，还要
  挨一次疲劳压制，等它滚出窗口反而更难被召回。

条数默认 10，**和「召回最多取几条」摆在同一页**（上下文与记忆）。这一条是被
用户问出来的：那个数本来就能改，但它当时放在「用量与上限」，而它的邻居
`memoryTopK` 在「上下文与记忆」—— 同一类旋钮分在两页，找不到很正常。

顺带挖出另一个：`memoryTopK` 从前藏在「开了向量检索才显示」的分支里，
**可它对关键词匹配一样生效**。默认配置下用户根本看不到召回取几条。也挪出来了。

记忆总数少于这个值时，召回会无可挑选 —— 那不是毛病，那几条本来就全带上了，
界面上写清楚。

**顺带记一条踩坑**：查这件事时一度以为 `scoreOf` 的 `total` 是 NaN、
打分整个没生效 —— 其实是「召回解释」那一页存的字段叫 `score` 不叫 `total`，
是测试写错了字段名。差一点就去改一个没坏的东西。

### 4.110 线下画在哪儿：单开一页，还是就在聊天里

`placement` 两档。单开一页是原来那套（翻页、铺满、明信片都在那儿）；
**就在聊天里**是在会话中划一条线，往下按线下的规则演 —— 同一套提示词、
同一套数据，只是画的地方不同。

#### 整场戏挂在一条消息上

消息流里写一条 `kind: 'scene'` 的锚点，这一场的全部正文渲染在那一条里。
**不把段落混进消息列表** —— 那一页的分页（一屏多少条）、多选、引用、搜索
全按「一条消息」算，混流要各改一遍，而它们都在会话页那个一千七百行的文件里。
会话流里本来就有十几种不是气泡的行（`notice`、通话、一起听、外卖），
线下块是同一类东西。

代价：一整场算一条，窗口化对它不起作用。可接受 —— 它本来就是往下滚的。

翻页与分张在这一档不适用：内联没有「一张」这回事。

#### 输入框整个换一套

`openInline(chatId)` 有值时：发的是正文不是消息，回的走 `streamScene`，
表情那一格换成续写与外观，占位文字改成「写你这一段」，底色也换一换 ——
一眼看得出这一下发出去的是什么。

**场次要现读，不能吃渲染时那一份。** 刚划完线就要开场，而那一下的闭包里
`live` 还是 `null` —— 拿它判断会走到线上那条路上去，往聊天里落一条气泡。
这个 bug 第一版就犯了，测试里钉死了「线下开着时没有往聊天里落气泡」。

#### 悬浮窗改的是全局

内联时没有线下那一页的菜单可挂，常用的几项浮在正文上面：主题、字号、
署名、首字下沉、对白分色，外加一个去完整设置的入口。

**它改全局，不改这一场。** 这是读着读着随手调的旋钮，调完当然该一直是这样；
写成场次级覆盖的话，下一场又变回去，而且每用一次就在那一场上留一份看不见
的覆盖。那一场另设过外观时，悬浮窗上照实说一句「此处的改动被它盖住」。

#### 顺带：两个扫描器在半个项目上是瞎的

`check-ui-tone` 与 `check-prompt-tone` 各有一份 `stripComments`，都不认
模板串里 `${}` 那一段还能再套模板串。而 `class=${`a${x ? 'b' : 'c'}`}`
这种写法本项目到处都是 —— 内层那个反引号把外层提前收掉，从那儿往后引号
状态整个反过来：**注释被当成文案扫，真的文案反而漏掉**。

是写这一节时撞上的：一条普通注释被拦，而同一个文件里故意塞的违规字符串
没被抓到。修好之后提进 `lib.mjs`，两边共用一份，并各验了一遍
「真违规抓得到、注释放得过」。

还剩一个已知的坑：正则字面量里的引号（`/['"]/`）同样会被当成字符串开头。
彻底解决要上真的解析器，本项目里没有那种写法，先记着。

### 4.111 美化：按会话隔离，隔离本身就是安全机制

一份美化挂在若干段会话上，**只在那段会话的页面开着时注入，离开就摘**。
不是全局皮肤。

这一条同时解决了这类功能最大的风险。自由 CSS 一句 `.root{display:none}`
就能让人再也进不去，而它存在 IndexedDB 里，刷新也回不来。按会话隔离之后：
坏掉的那份只坏一个聊天页，而**清除的入口在消息列表上，列表本身不上美化**，
永远够得着。

第二道是崩溃记号：挂上时在 `localStorage` 记一个 id，渲染活过 800 毫秒
才清掉。上次没清掉说明那一份把页面拖垮了，这次进来就不注入。

**这里栽过两次，方向相反。** 第一次 `unmount()` 顺手清记号 —— 那等于守卫
不存在，页面炸掉时 ErrorBoundary 把这一层卸下来，卸载就清记号，下次照样注入。
改成只靠定时器之后栽了第二次：人在美化页改一个数立刻按返回，定时器被清掉，
记号留下，一份好好的美化被当成崩过的，回到会话页是素颜。

现在两个信号都用：错误边界接到异常先叫 `skin.reportCrash()`，卸载时看见它
就留着记号；没看见就是正常离开，清掉。而且只清**这次自己写的**记号 ——
上次崩过、这次没挂上的那份，记号不是这次写的，清了等于每隔一次就再炸一回。

#### 两层，别混

| | 改什么 | 谁编辑 | 能不能逐项回退 |
|---|---|---|---|
| 令牌层 | 已经是 CSS 变量的那些 | 滑杆与选择器 | 能 |
| 自由层 | 用户手写 | 文本框 | 只能整段清掉 |

生成顺序固定：令牌在前，自由在后 —— 自由层永远盖得住令牌层。
没设过的令牌**整条不发**，那一项仍是 app 自己的值。

为此把会话页几个写死的尺寸提成了令牌（`--composer-h`、`--composer-pad`、
`--bubble-gap`、`--bubble-gap-in`、`--bubble-r`、`--bubble-px/py`、
`--bubble-fs`），值和从前一模一样，不改观感。**调不了的东西只能先提成令牌**
—— 这是这套东西的硬边界。

头像形状特殊：`Avatar` 的圆角写在内联样式里，内联盖得过类选择器。所以在组件里
留了个口子 `border-radius: var(--avatar-r, <原值>)`，不然只能靠 `!important`。

#### 不重写用户的选择器

隔离靠「只在那一页挂着」，不靠改写选择器。改写要一个真的 CSS 解析器，
而 `@media`、嵌套、伪元素上每一处想当然都会写错，错了还是静默的。
时间上的隔离够用，而且对用户解释得清楚。

#### 样板间用真组件

美化页顶上那三条气泡是**真的 `Bubble`**，喂假数据，不是另写一份 HTML。

另写一份必然腐烂：真页面改了 class 它不跟，于是在这里调好的东西到聊天页
不生效，而且没人会发现。这一条别图省事改回去。

#### 还没做的

挂载点（换图标、挂 before/after、位移）、头像框、独立的美化 app
（现在只能从某段会话进去，管理不了「我所有的美化」）。
时刻与已读回执已经做完，见 4.112；导入导出已经做完，见 4.127。

**不叫「工坊」。** 这套东西从头到尾叫「美化」，代码、界面、这份文档都是。
再起第二个名字，用户就要学两遍，而两个词指的是同一件事。

### 4.112 时刻与已读回执：绝大多数已读不存在库里

气泡上那行小字，两样东西：这条消息出现的时刻、自己发的那条被没被看见。
默认都关着 —— 它们会改变每一条气泡的样子，不该在用户没要的时候自己出现。

#### 显示哪个时刻

**本机上这条消息出现的时刻**（`msg.createdAt`），不是角色写在正文里的
`[时间：]`。那一个是剧情里的时间，两者常常差着好几天，混在一起谁都读不懂。
当天只写时分，隔天带上日期。

#### 「已读」= 角色开始生成回复的那一瞬

不另存一份状态，也不去猜：她动笔了，就是看见了；她一直没回，就一直是未读
—— 那恰好是想看到的意思。

打点在 `engine.streamReply` 排到的那一刻，**不在界面里**。「角色说话」有好几个
入口（手动发、自动回复、主动找你），它们都从那儿过；打在界面上只能覆盖一个。

#### 绝大多数已读是推出来的

后面已经有角色说过话了，前面那些当然被看过。所以真正写进库的 `readAt`
只有「最后一条角色消息之后发的那几条」，也就是此刻正悬着的那一两条。
再往前的靠 `readUpTo(chatId)`（这段会话里最后一条角色消息的时刻）推出来。

这样做的好处不是省空间，是**开这个开关之前的几万条历史一条都不用回填**，
而且显示出来是对的。逐条写一遍的话，第一次调用就会把整段历史全改一遍，
`updatedAt` 集体跳到今天，库里每一行都被重写一次。

`readUpTo` 由会话页算一次往下传。气泡是 memo 过的，每条各算一遍要把整段
消息扫上几百遍；更要紧的是**这两项设置不能在气泡里读** —— memo 过的组件
不会因为设置变了就重画，只能靠属性变。所以传下去的是三个标量
（`stampAt`、`readOn`、`readUpTo`），不是一个对象：对象每次渲染都是新的，
memo 就废了。

#### 开关放在美化页

两项都是全局的，不随美化切换，但入口只有美化页那一个 —— 要改它的时候
人正在那儿看着气泡（CLAUDE.md 第 5 条）。美化页没挂美化时也显示这一组。

### 4.113 「过去看一眼就回来」：那一趟结束时要收摊

会话菜单里点「每轮的接口调用」，跳的是设置 app 的 `/limits`。这类跨 app 的
「去那边看一眼 / 改一下」全走 `intent.open(app, { route, back: true })`，
`nav.returnTo` 记下出发时那一整条栈，退到底时原样放回去。

**栽过一次，两个毛病连在一起。**

从前带 `back` 的那一跳不给目标 app 垫首页 —— 栈就是 `['/limits']` 一条，
理由是「退到底该是回去，不是落在设置首页上」。可那条回头路只活一趟：
人半路按了桌面、锁了屏、或者从切换器再进来，`returnTo` 就没了，剩下一条
**底下没有首页的栈**。于是从桌面点设置，开的是「用量与上限」，再一按返回
直接回桌面，永远上不去那一级。

现在两头都改：

- **栈底永远垫着首页。** 回去那一下改看 `returnTo.depth`（出发时落在第几层），
  不再靠「栈里只有一条」。栈从此没有孤儿状态，回头路丢了也只是退成普通的
  上一级。
- **那一趟结束时把去过的 app 收回首页**（`endTrip`）。回去了、按了桌面、
  锁了屏、转身去了第三个 app，四处都收。不收的话设置就一直停在被看的那一页
  上 —— 那一页是别人领进去的，人下次自己点设置并不想从那儿开始。

一处不收：从被看的那一页又带 `back` 跳去别处，这条栈马上要当新的回头路用。

### 4.114 图片引用表：删图之前先问一圈

`purge.usedImageIds()` 是「库里哪些图还有人指着」的唯一一张表。两处吃它：
「清理无引用图片」照着它留；删消息、清聊天记录、重生成配图时照着它删
（`releaseImages`）。

**这里栽过一次，两个方向都栽。** 表上漏了相册、歌封面、书封面、书架封面、
阅读页与线下的背景图 —— 点一次「清理」，自己导入相册的照片全没了。反过来，
删消息时直接删图，而「保存到相册」存的是**同一个 id**不是复制一份，相册里
那一张就成了空框。

所以规矩是两条：往 `images` 里存东西的地方，都要在那张表上留一行；
删图一律先把引用它的行删掉，再走 `releaseImages`，让表来决定删不删。
不要在别处直接 `images.remove` 一条别人也可能指着的图。

### 4.115 删会话、删角色、删身份只能从 purge 走

`purge.dropChat(chatId)`、`purge.dropCharacter(charId)`。删身份（`accounts.remove`）
也是逐段会话走 `dropChat`。

从前消息列表、角色资料页、账号页各删各的：都只删会话行、消息和空间，
后来按 chatId / charId 建了索引的域（线下的场次与正文、出行、段评、角色手机、
健康、影评、待办、每一天、吃饭）一样都没跟上，留成孤儿指着一段不在的会话、
一个不在的人。删身份那处连消息都没删。

规矩：**按 chatId 或 charId 建了索引、或者带着这两个字段的域，加进库里时要在
purge 的那两个函数里各补一行。** 图片一律走 `releaseImages`（见 4.114）。

**两处「删除这个角色」是同一件事。** 从前联系 app 那一处只删角色卡，会话留着，
列表里显示「已删除的角色」，点进去是「该会话已不存在」—— 同一句话在两个地方
做两件事，那是这个界面在说自己的实现。现在都走 `dropCharacter`，确认框也写
同一句，把要删的数目摆出来。

相册留着。它是使用者自己归类的地方，和书、片子、歌一样属于这台机器，不属于
角色；`保存到相册` 那条也写着「会话里这一条不受影响」。所以删角色不动它，
而相册指着的那张图也因此不会被 `releaseImages` 回收。

### 4.116 角色包：能带走的全带走

`charpack.js`。一个角色和挂在它名下的一切打成一个 zip，换台设备装回去。
整库那一份是 `backup.js`，两者不是一回事：这一份要能在别人机器上落地。

带走的是两批，和 `purge` 的 `dropChat` / `dropCharacter` 正好对应 ——
**那边管删，这边管带走，加数据域时两边各补一行**：

| 挂在会话上 | 挂在角色上 |
|---|---|
| 消息、线下（场次与正文）、出行、纪念日与没寄出的信、那几段会话用的美化 | 记忆、动态、每一天、吃饭、健康、影评书评、段评、待办、那台手机与手机里的会话 |

不带走的是**使用者自己的库**：书、片子、歌、表情、相册。那些不属于角色。
影评与段评指着书或片子，对方没有那一本时这几条落不下去 —— 落下去也没有
一处显示得到它，所以装的时候跳过并报个数。

#### 装回去要各记一张 id 表

角色 id 撞了就整包换一套 id 另建副本。**每一域都要各记一张表**：线下的段指着
场次、待办指着消息与会话、美化被会话指着、动态的赞与评论指着人。少一张表就是
一堆指向空处的行 —— 而且不报错，要到打开那一页才看得出来。

`me` 那个作者 id 不参与重映射：它在对方机器上仍然是「我」。

世界书和美化是两个例外，**id 撞了用本机那一份，不覆盖也不另建**：这两样都常被
几处共用，盖掉等于替别人改设定、替别的会话改样子；另建则会在同一个包导两次时
多出一份同名的。

### 4.117 我们：长篇与番外，正文那一套整个复用线下的

用户的话是「也是用线下的那一套，但是分为番外和长篇」。所以这不是第二套
散文引擎，是线下那一套的第二个入口：

| 复用的 | 在哪儿 |
|---|---|
| 一段段正文（分段、分版、续写、编辑、钉住、分张） | `system/scene.js` 的 beats 那一半，一行没再写 |
| 展示层（首字下沉、署名、明信片、版本翻页） | `ui/prose.js` |
| 外观（纸色、字号、版式、自定义 CSS、字体） | `system/stage.js`，同一套设置 |
| 文风预设 | `system/tone.js`，同一份库 |
| 正文页那一套手势与流式写法 | 照 `StageRead` 写了一份，见下 |

新写的只有「作品是什么」：`system/work.js` + `system/ai/engine.js` 里
`buildWorkSystem` / `buildWorkHistory` / `streamWork` + `apps/us`。

#### 展示层挪进了 ui/

`StageBits.js` 从前在 `apps/chat/pages/` 下。第二个 app 要用它，而第 8 条
不许 app 引 app —— 所以整个挪到 `ui/prose.js`。

**挪的时候要把 sdk 摘干净**：`ui/` 是横向基建，不依赖任何层。那里唯一碰
sdk 的是署名上那张头像（`useImage` 把图片 id 解析成地址）。改成由调用方
传一个 `Face` 组件进来，这一层只负责摆在哪儿。每个 app 各留三行。

#### 两个数据域，正文仍在 beats 里

`works`（一部作品，按 chatId 索引）与 `chapters`（一篇，按 workId 索引）。
正文**不另开一域**，仍然是 `beats`，`sceneId` 那个字段存的是「哪一篇」——
一场戏或者一章，id 各不相同，不会撞。

为什么不把章塞进 `scenes`：`scenes` 按 chatId 索引，而记忆提取、
线上线下的 bridge、线下列表都按 `ofChat` 取整段关系的场次。章混进去就要
在每一处加一句过滤，漏一处就是「聊天记忆里混进一整部古代 AU」。
和 4.107 里「不复用 messages」是同一条理由。

#### 一部作品挂在一段会话上

因为「带不带原来的记忆」这个开关需要知道是哪段关系。番外尤其如此 ——
小剧场多半是「如果当时……」，不知道原来发生过什么就写不出来。

#### 三个开关，都由用户定（第 16 条）

| | 长篇 | 番外 |
|---|---|---|
| `carry` 带上原来的记忆与关系 | 自己选 | 一直带，不给开关 |
| `solo` 整篇由它写 | 自己选 | 自己选 |
| `opening` 新的一篇由谁开场 | 自己选 | 自己选 |

`carry` 关着时，**这一篇看不见原来那段关系的任何东西**：记忆、关系底色、
当日日程、正在听什么、情侣空间、待办、聊天摘要，一样都不注入，世界书也
只认这部作品自己挂的那几本。「新的身份新的开始」就是这个意思。开着时和
线下一模一样 —— 走同一个 `assemble`，一个区块都不少。

`solo` 开着时把 `skeleton.scene-rules`（不要替用户写）换成
`skeleton.work-solo`（所有人都写），并且贴着输出那句提醒也不发 ——
那正是这一档允许的事。**是换掉，不是两段都出现**：两段一起发，
模型收到的是一对互相矛盾的规则。

`opening` 定在作品上而不是每一篇上。一部作品里每一篇都是同一个写法，
每开一篇再问一次是多余的。选「让它开场」时新建一篇会立刻打一次接口，
所以那一行说明里写清楚了。

#### 换身份换的是名字与人设，不是那张脸

`charAs` / `meAs` 各是 `{ name, persona }`，两样都可以只填一个，
留空就退回角色卡与账号上的那一份。署名上的头像仍然用本人的。

#### 章与章之间靠摘要衔接，没有摘要就截正文

后面一篇的设定区里带 `[前面写到哪儿了]`：每一篇有摘要就用摘要，没有就截
它正文的末尾顶上 —— **那一步不调接口**（第 15 条）。要真摘要就去开「收篇
摘要」（`workSummary`，登记在 `EXTRA_CALLS`，默认关着）。每一篇带多长由
`workPrevChars` 定，填 0 是整篇带上。

#### 作品不进记忆

番外是「如果当时」，长篇是另一条线。两样都不该混进这段关系的记忆里去，
所以 `memory-extract` 照旧只读 `scenes`，不读 `chapters` —— 两个域分开
本身就把这件事保证了，不必再加一句过滤。

### 4.118 气泡那一档：一张方形封面，下面是歌词页

`stage.LAYOUTS` 从两档变成三档。前两档（翻页、明信片）是刊物的排法 ——
一张一张翻、一片一片摆，读的是「一本册子」。第三档不是排版的变体，
是另一种读法：

```
 沈砚
 雨还没停。                  一段一个长气泡
 他站在檐下，手里捏着一张票根。   气泡里按句断行
 「你来得比我想的早。」
```

#### 按句断行是这一档的全部秘密

歌词页之所以看着是歌词页，靠的是**一句一行加行距拉开**，不是靠字号。
所以 `linesOf` 先按换行切，再按句末标点切，一句一个 `<p>`。字号只比正文
大一号（1.18 倍）—— 再大就成了海报。

**断句那个正则后面那个否定前瞻少不了。** 只写「句末标点加一个可选的收口
引号」的话，句号后面和引号后面各断一次：「…早。」被切成「…早。」和一个
孤零零的引号，一整行上就一个符号。

#### 封面不在正文页上，在扉页上

第一版把封面压在正文顶上，读起来是错的：每翻一段都要从那张图下面绕过去，
而它一屏只有一次用处。**封面是「点进这部作品」那一下的事，不是读的时候的事。**

所以封面移到作品页（`apps/us/pages/Bits.js` 的 `Hero`）：点进一部作品先看到
一张大的方形封面、标题、参与的人、体裁与进度，往下就是目录。正文页上面
只有正文。

线下没有扉页这一层（它的索引就是场次列表），所以线下的气泡档从头到尾
只有气泡。

扉页那一块**用这部作品自己的纸色**，不是 app 的底色 —— 它是作品的一部分，
不是列表的表头。没有图时用标题的头一个字排一张：唱片没有封面时也不会给你
一个灰方块。

#### 自己写的那些不填底

一屏全是实心块会糊成一片。角色那边填标记色兑淡的底，自己那边只描一道线。
这是这一档里唯一区分说话人的手段 —— 气泡上那行名字是小字，真正一眼看出
是谁的是底色。

#### 三档共用同一批东西

这一档没有另写一套：正文仍然是 `pagesOf` 出来的那些段（只取每段的第一张，
因为气泡不分张），长按、重写、版本翻页、场外指示全是原来那几个函数。
线下与「我们」同吃这一档 —— 它们共用 `stage.js`。

不适用的两样在这一档不画：页码（没有「张」这回事）、点两侧翻页。

#### 顺带：扫描器终于认得正则了

写这一节时撞上 4.110 末尾记着的那个坑 —— 正则字面量里的引号被当成字符串
开头。断句那个正则里正好带着收口的引号，于是它下面那段注释被当成界面文案
扫，报了两处并不存在的问题。

`lib.mjs` 的 `stripComments` 现在认正则字面量：`/` 前面那个有意义的字符
决定它是除号还是正则开头，这是各家高亮器都在用的那条老启发式。不是真解析器，
但认错的代价只是多扫或少扫一段。补进去之后验过一遍：**正则下面故意塞的
违规字符串仍然抓得到，紧挨着的注释放得过。**

### 4.119 生图那个 400：不发出去，比看懂它的报错快

「生图总报错」，回来的是上游自己那套字段：

```
400: upstream generation failed: HTTP 400:
{"size":"1024x1024","n":1,"prompt":null,"is_style_transfer":false,...}
```

`"prompt":null`。对面收到的请求里没有画面描述。这句报错里一个字也看不出
是本机没填 —— 它甚至不是我们发的那个形状，是中转站转给它上游之后对面回的。

#### 两处，都不是「接口的问题」

**一、`undefined` 会把整个键丢掉。** `JSON.stringify({prompt: undefined})`
出来是 `{}`。重画一条老图片消息时，`m.prompt` 没有这个字段是常事
（那条消息是在这个字段存在之前建的），于是发出去的 body 里根本没有 `prompt`，
对面按「没收到」处理。空字符串稍好一点，但结果一样。

现在 `image.js` 的 `needPrompt` 把这两种都挡在本机：抛
「这一张没有提示词，没有可画的内容」，一个请求都不发。

**拼过之后再看就晚了。** `compose()` 会把全局画风提示词和角色自己的提示词
接在画面描述后面，只要配了任何一句，空描述也会被撑成非空 —— 那时候请求发得
出去，画回来一张谁也没要过的图，钱照花。所以 `reply.js` 的 `generateImage`
在拼装**之前**先验一次原始描述。

**二、`response_format` 不该一直发。** 这个字段原先每次都带
`b64_json`。`gpt-image-1` 根本不认它，直接 400；中转站转到别家模型时同样
常常 400，而报错里看不出是哪个字段。

现在它是生图接口上的一档设置（`FORMATS`），**默认那一档整个字段不发**，
由服务端用自己的默认值。只有一种情形需要手动指定：接口默认给链接，而那个
链接跨域取不回来 —— 取不回来时的那句报错里直接写了去哪儿改。

#### 记一句：默认值不是「最常见的值」，是「最不惹事的值」

这个字段当初写死 `b64_json`，理由是「大多数接口都支持，而且省一次跨域取图」。
两条都对，但它漏掉了第三种情况：**不支持的接口不会说自己不支持**，
只回一个不带字段名的 400。一个可选参数，发与不发的失败代价差这么远的时候，
默认就该是不发。

### 4.120 点通知白屏：交出去的那一下要有回执

装成 ipa 之后，从锁屏或通知中心点一条通知，app 打开是一张白纸，也没跳到那段
会话。4.105 那一版把「跑在网页前面」的时序接住了，这一次是另一件事：
**页面根本不在了。**

#### 进程在后台被回收，回到前台谁都不说

网页进程在后台被系统回收是常事 —— 保活开着时 app 一直活着，WebKit 的内容
进程正是内存紧张时最先被收的那个。回到前台看到的就是一张白纸：`url` 还在，
`isLoading` 也是 false，`webViewWebContentProcessDidTerminate` **不保证会来**
（进程是在后台没的，那一下常常不补发）。从外面什么都看不出来。

而点通知进来正好就是从后台回来。于是 `flush()` 拿着那一下往一张空文档上打：
`window.phoneNotifyOpen && window.phoneNotifyOpen(...)` 在没有这个函数的页面上
求值是 `undefined`，**不抛错**。上一版只看抛不抛错，于是当它交到了，`pending`
清掉，人看到的就是「白屏，也没跳过去」。

#### 两处改法，都是「问一句」

**一、回执。** 网页那头两份 `phoneNotifyOpen`（`index.html` 的占位、`push.js`
的正式那份）都回 `true`。外壳只认这个值：回别的当作「这张页面上没有它」，
放回 `pending`、叫外壳重载，`didFinish` 再交一次。同一下点击最多这样重载两次 ——
站点真坏了不能没完没了地转。跳转本身出了错也回 `true`：那是本机的 bug，
重载救不回来，只会再炸一遍。

**二、回到前台先问「你还在吗」。** `didBecomeActive` 里不再直接 `flush`，先
`evaluateJavaScript("!!(document.body && document.body.children.length)")`：
抛错是进程没了，回 false 是被换上来一张空文档，两种都 `load()`。问得到，
再交点通知那一下。正在载的时候不插手，`didFinish` 会交。

这条不只救点通知：从后台回来看见白纸的每一次都走它。

#### 顺带：白的那几秒别是白的

网页那层是透明的，画不出东西的那几秒露出来的是外壳 `view` 的底色。从前是
`.systemBackground`，纯白。现在跟着 `index.html` 的 `theme-color` 走（浅色
`#F2F3F5`、深色 `#0D0E10`）。重载仍然要等网络，但等的那几秒是页面的底色，
不是一张白纸。

#### 外壳那一半要重新打包

网页那一半推上去就生效；`NotifyBridge.swift` 与 `ShellViewController.swift`
的改动要重新打一次 ipa 才在手机上。设置页顶上的「外壳」版本号说明装的是哪一版。
新网页配旧外壳照常能用（旧外壳不看回执）；新外壳配旧网页会多重载一两次再交到。

### 4.121 生图报 prompt:null，其实是请求体在过桥时没了

4.119 修的是本机不该发空提示词。修完仍然报同一句，因为还有第二条路会变成
「对面收到一个没有提示词的请求」，而且这一条只在**装成 ipa 之后**才出现。

#### FormData 过不了那座桥，而它失败时一声不吭

`net.js` 的 `viaNative` 把请求体转成 base64 交给外壳。它认字符串、
`ArrayBuffer`、`Uint8Array`、`Blob` —— 独独没有 `FormData`。于是：

```js
let bodyB64 = '';
if (typeof body === 'string') …          // 都不命中
// bodyB64 仍然是空串，照发
```

外壳收到一个空的 `body`，`NetBridge` 的 `if !b64.isEmpty` 不成立，
`httpBody` 就不设。**对面收到的是一个没有请求体的 POST。**

生图带参考图那条正是走 `FormData`（`images/edits` 是 multipart）。所以 relay
回的是：

```
{"prompt":null,"is_style_transfer":false,"referenced_image_ids":null,…}
```

外加模型那句「I need a usable image-generation target first. Please upload the
reference image」—— 它说的全是实话，提示词和图**一样都没收到**。而本机这边
看什么都正常：请求发出去了、状态码也回来了。

浏览器里没有这回事：那条路是 `fetch(url, {body: form})`，由浏览器自己生成
分界串并配好 `content-type`。**只有过桥这条路上没人做这件事。**

#### 自己拼 multipart，分界串跟着 content-type 一起交

`formBytes()` 按 RFC 7578 拼字节，回来的 `contentType` 带上现生成的 boundary，
覆盖调用方给的那个（调用方不可能知道它）。验的办法是拿同一个 `FormData`
分别走浏览器和走桥，把两边拆开逐字段比：字段名、值、`filename`、
每段的 `Content-Type`、文件字节，全部一致，中文按 UTF-8 编码。

**认不出的请求体现在抛错，不再静静发一个空的。** 这一条比上面那段修补要紧：
少支持一种类型是小事，「发出去了但里面什么都没有」才是查了两轮都没查到的原因。

#### 顺带查出来的四样

| | 从前 | 现在 |
|---|---|---|
| 只给链接时那一取 | `fetch`，装成 app 也卡在跨域 | `nfetch`，和接口那条走同一条路 |
| `b64_json` 里是整个 data URL | `atob` 抛 `InvalidCharacterError` | 前缀与折行都先摘掉 |
| 链接回来的是登录页 | 存进去，等 `createImageBitmap` 才炸「源图像无法解码」 | 看 content-type，当场说「那个链接回来的不是图片」 |
| 200 却带 `error` | 「接口没有返回图片」 | 把对面那句话原样交出来 |
| 回来的字节存不成图 | 浏览器那句「源图像无法解码」 | `toLibrary` 带上字节数与类型 |
| `data:` / `blob:` 地址 | 也送上桥，而 `URLSession` 取不了 | 不过桥，本机自己取 |

认不出的回包形状会把原文截前 200 字带出来。猜不出形状的时候，原文是唯一
能往下查的东西。

最后那两行是同一个道理的两面：**报错要说得出是哪一步。**「源图像无法解码」
既不提图片也不提接口，而走到那一步的数据全都来自对面 —— 截断的图、
base64 过的 HTML、浏览器不认的格式，都会落在这同一句话上。带上
「多少字节、什么类型」，一眼就分得出是「对面压根没给图」还是「给了但传坏了」。

#### 参考图那条退回去，只在「接口不认」时退

退回纯文字是**第二次调用**。从前的 `catch` 什么都往下咽，于是两种情形也会
再画一张：人按了取消（凭空的一笔账），以及图已经画回来了只是存不进去
（重画一张也一样存不进去）。现在取消原样抛出去，`images.put` 挪到 `try`
外面。取消也不再在气泡上留一句红字 —— 那不是错误。

### 4.122 生图世界书：同一套书，两边互斥

世界书原先只有一种用处：注入对话。可画风、镜头、光线这类设定只对生图有意义，
写进普通世界书就会跟着进聊天 —— 角色开口一股说明书味，正是第 16 条要躲的那种。
反过来，「天台常年上锁」这种剧情设定拼进生图提示词，画出来的是一把锁。

所以给书加一个 `forImage`，**两边互斥**：标了的只进生图提示词，没标的只进对话。
不是「两边都进，各自挑」—— 那样每加一条都要想一遍它会不会漏到另一边。

#### 不是新数据域

只是 `lorebooks` 行上多一个布尔字段。所以备份、导出角色包、删角色那几处
一行都不用改（4.115、4.116 那套登记表都是按域登记的）。**加字段不加域**，
这是这次能只改四个文件的原因。

#### 归属与激活，沿用同一套

「全局生效」和「关联到这个角色」照旧，`booksFor(char, forImage)` 只是在原来的
筛选上多一个条件。命中判定（关键词、二级关键词、常驻、概率）整段抽成 `hits()`，
两边共用 —— 抄一份的话，改了一边忘了另一边是迟早的事。

差别只在**拿什么去扫**：对话那边扫的是扫描窗口，生图这边扫的是**画面描述**
（`[图片：…]` 方括号里那句）。

#### `part` 与 `depth` 在这里没有意义

生图请求里没有「对话历史」可插，所以那两项对生图书不显示 —— 不是禁用，是
不画出来。留着一个永远不生效的输入框，比没有更糟。同理，「激活预览」那一页
说明里写清楚生图书不在其中，「条目总览」把它们单列一组。

#### 拼进去的顺序

```
画面描述 → 这一张命中的生图世界书 → 角色自己的提示词 → 全局提示词
```

生图世界书排在角色与全局之前：它是**按这一张的内容命中的**，比那两个
「一直都这么画」更贴着这一张。

#### 顺带修的：朋友圈的图从前不走 compose

「全局生图提示词」那一栏写的是「每次生成都会拼在画面描述后面」，而
`tasks/moments.js` 直接调 `generate`，绕过了 `compose` —— 朋友圈那张图
既不按全局提示词画，也拿不到生图世界书。界面上写着的话没做到，改成走 compose。

### 4.123 尺寸：列出来的是备选，不是全部

从前是三档 `Segmented`，填不了别的。NovelAI 常用的 832x1216、1216x832
一个都不在里头 —— 而那不是「我们不支持」，是**清单里没有**，正是第 13 条
要拦的那种「代码替用户封顶」。

现在是「常用值点一下就填上 + 自己填」。两家的常用值不一样，所以
`SIZES` 按 kind 分开列，`sizesFor(kind)` 取对应那一份。

#### 自己填就什么都可能进来

`sizeText()` 把填的东西规整成 `宽x高`：全角的 `×` 认，中间的空格去掉，
认不出就退回 1024 见方。**认不出的不原样发出去** —— 把一句「大一点」
发过去，对面回的是一个看不出原因的 400，而那正是这一轮在查的东西。
界面上同时标一行红字说这一条识别不了、会按什么发，不是等它 400 回来才说。

NovelAI 还要求边长是 64 的倍数、总像素有上限。**那一条写在界面上，不在代码里拦**：
各家的限制随模型改，写死在代码里迟早挡住一个本来能用的尺寸。

### 4.124 开机不许回写没读到的那一条

用户报「闪退一次之后所有上传的图标和壁纸都消失了」。丢的偏偏只有这两样，
别的一点没少 —— 因为它们**各在一条 kv 记录里**，而别的数据在各自的对象仓里：

| 丢的 | 存在哪 |
|---|---|
| 应用图标 | `settings.appIcons` |
| 壁纸、图标排布、文件夹、Dock | `layout` |

#### 「读出来是默认值」和「库里真是默认值」，从前分不开

`makeKV.load()` 读不到记录就回落成默认值，一声不吭：

```js
const row = await idb.get('kv', key);
if (!row) { store.replace(structuredClone(fallback)); return; }
```

读**报错**那条路是安全的（`run()` 会 reject，`ready` 跟着 reject，开机页面
写「启动失败」）。真正要命的是**读成功但回来是空的**：那时候内存里是一份
默认值，而库里那条还在（或者只是这一次没拿到）。

接着开机有两处**无条件回写**：

```js
healAndSave()                    // → layout.replace(healed)
migrateLegacy()                  // → settings.set({ services: services() })
```

两处都不问来路。于是「这一次没读到」被当场写成了「用户的设置就是默认的」，
**盖掉库里真实的那一份，永久**。一次瞬时故障被自己的自愈逻辑变成了永久数据丢失，
而且全程零报错。

#### 改法：`stored()`

`makeKV` 记一个 `found`：load 真读到记录才为真，任何一次写入之后也为真。
两处回写各加一道 `if (!stored()) return;`。

**heal 本来就不必在开机时落盘** —— 它是确定的，同一份输入每次愈出同一个结果，
下次开机再愈一遍就是了。真正需要落盘的是用户改过之后那几次，
而那几处都先 `layout.replace` 过（`stored()` 因此为真），照旧存。

#### 这一条的通用形式

**任何开机时跑的自愈、迁移、补全，都不许写一条自己没读到的记录。**

自愈逻辑的输入要是残缺的，它「修」出来的就是一份残缺的真相，而它写得比谁都
理直气壮。读不到就什么都不做 —— 下次读到了再说，这一次的代价只是这一屏是默认的样子。

#### 留下的坑：图片本身还在，但没人引用了

图标与壁纸的**图还在 `images` 里**，丢的只是指向它们的那条记录。于是
`usedImageIds()` 从此数不到它们 —— 「设置 - 存储 - 清理无引用图片」会把它们
**真删掉**。记录在这里：这一类「引用丢了但数据还在」的事故之后，
那个按钮是第二把刀。

### 4.125 点通知闪退：async 版的委托回调不在主线程

4.120 修的是「点通知白屏」，没修好，因为**白屏和闪退是两件事，而我把它们当成了一件**。
白屏是网页还在载，闪退是另一句 —— 前几轮一直在查「为什么没跳过去」，方向从一开始就错了。

崩溃日志一句话点名：

```
SIGABRT · abort() called
faultingThread queue: com.apple.root.user-initiated-qos.cooperative
  -[NSAssertionHandler handleFailureInMethod:…]
  -[UIApplication _performBlockAfterCATransactionCommitSynchronizes:]
  -[UIApplication _updateStateRestorationArchiveForBackgroundEvent:…]
  -[UIApplication _updateSnapshotAndStateRestorationWithAction:windowScene:]
  @objc closure #1 in NotifyBridge.userNotificationCenter(_:didReceive:)
  specialized thunk for @escaping @isolated(any) @async
  completeTaskWithClosure
```

#### async 那一版看着更干净，代价在返回的那一刻

`UNUserNotificationCenterDelegate` 的两个回调各有两版：完成句柄版，和 `async` 版。
从前写的是 async 版：

```swift
func userNotificationCenter(_:didReceive:) async { … }
```

**async 版返回时，Swift 并发在协作线程池上回调那个完成句柄，不是主线程。**
UIKit 收到完成之后要接着跑 `_updateSnapshotAndStateRestorationWithAction:`，
那一步的断言要求主线程 —— 于是 `abort()`。

`await MainActor.run { … }` 挡不住这个：那只保证**我们自己那段**在主线程，
而崩的是**我们返回之后 UIKit 自己那段**。

#### 为什么前台试通知一直是好的

那条走 `willPresent`，不触发响应收尾这一段。于是「授权正常、试通知能收到、
一点就崩」三件事同时成立，看起来像是通知功能好的、跳转功能坏的 ——
实际上坏的是收尾。

#### 改法

两个回调都换成完成句柄版，交付与 `done()` 都放进 `DispatchQueue.main.async`。
`done()` 无论交付成没成都要叫：不叫的话系统一直等着这一次响应收尾。

`deliver` 上那个 `@MainActor` 也去掉了 —— 现在由调用点保证主线程，
标注反而会让人以为编译器替你保证了什么。

#### 这一条要记住的

**「我自己那段在主线程」不等于「这个回调在主线程完成」。** 系统框架的回调
是不是要求主线程完成，看的是它拿到完成通知之后还要做什么，而那部分不在你的代码里。
拿不准的时候，用完成句柄版并在主线程上叫它 —— 那一版没有这个歧义。

### 4.126 外壳只问一句版本，其余走缓存

「直接打开也白屏很久」和闪退不是一回事。量过之后很清楚：

```
本地（3000 条消息 + 400 张图）
  读库合计        37ms
  重载到画出界面   116ms
开机发出的请求    174 个（js 167、css 5、html 1），401KB
```

**慢的不是库，也不是渲染，是那 167 个 js。** 无构建方案的代价在这儿具体化了：
ES Module 边解析边发现依赖，一层套一层，没法一次全发出去。localhost 上零延迟
所以看不出来，移动网络下一个往返 60 到 150 毫秒 —— 最坏十几到二十几秒。

#### 每次回源的只该是 index.html 一个

从前 `load()` 用 `.reloadRevalidatingCacheData`，等于每次把这 167 个全部重新
验一遍。可**真正需要每次是新的只有 `index.html`** —— 它里面那行
`<meta name="build">` 就是这一版的版本号（`src/main.js` 的自愈本来就在读它）。

所以外壳每次载入前只拿它去问一句：

| 问到的 | 怎么载 |
|---|---|
| 和上次一样 | `.returnCacheDataElseLoad`，整站走缓存，一个子资源都不必再取 |
| 不一样 | 先清 HTTP 缓存，再 `.reloadIgnoringLocalCacheData` 整站重取 |
| 问不到（离线、超时、站点挂了） | 用缓存开 —— **离线时反而打得开**，而白屏什么都救不了 |

那一问用 `.reloadIgnoringLocalCacheData` 且 6 秒超时：它自己必须是新的，
否则问到的是旧版本号；而它挂了不能把整个 app 挡在门外。

#### 两条性质都没丢

**「改了网页刷新就生效」还在**：那一问每次都做，新版下一次启动就认得出。

**不会新旧混在一起**：清缓存是整站一起清，不是一个文件一个文件地判。
无构建方案被坑过的那一次正是「新文件是新的、旧文件还是上一版」
（见 `system/refresh.js`），按版本号整体切换从根上避开它。

版本号在 `didFinish` 里才落盘 —— **没载成就不算数**。载失败却记下了，
下次会以为「版本没变」而一直用旧缓存。

#### 还剩一段没省掉

子资源的缓存新鲜度归 HTTP 头管（GitHub Pages 给 `max-age=600`）。十分钟之后
再打开，WebKit 仍会对这 167 个各发一次条件请求 —— 回的是 304、没有响应体，
走 HTTP/2 一条连接复用，比全量下载快得多，但**不是零**。要做到零，得让外壳
把整站下到本地自己伺服（`WKURLSchemeHandler`），那需要一份文件清单，
而无构建方案没有清单可用。等这一版量过再决定值不值得。

### 4.127 美化包：只带这一份自己的东西

一份美化调好了只能留在本机，而美化这种东西的价值有一半在「抄别人的」。
所以导出成一个文件，叫**「美化包」** —— 和已有的「角色包」同一套叫法。

#### 里面有什么，没有什么

```json
{ "kind": "phone-skin", "version": 1,
  "name": "…", "tokens": {…}, "shape": "…", "css": "…" }
```

**不带它挂在哪几段会话上。** 那是本机的事，别人导进去自然要自己挂。
`kind` 那一行是为了认得出「这不是一个美化包」—— 没有它，随便一个 JSON
导进来都会变成一份空美化，而用户只会看到「导入成功」。

#### 只认识的字段才留下

`unpack()` 逐项挑：令牌只收 `TOKENS` 里有的、且能转成有限数字的，
头像形状必须在 `SHAPES` 里。照单全收等于把任意字段写进库里，
以后哪一处读到它都可能出怪事，而那时候早就查不到是从这儿进来的。

坏文件分四种说法，不是一句「文件无效」：不是 JSON、不是对象、
标记不对（**把它写的那个标记原样引出来**）、版本比我们新。

#### 图片：分三类，导出前摆出来

美化行里**没有图片字段** —— 图只可能出现在用户手写的那段 CSS 的 `url(...)` 里。
三种下场完全不同：

| | 会怎样 |
|---|---|
| `data:` | 已经内联在文本里，跟着包走，只是包变大 |
| `http(s):` | 跟着走的只是地址。对方打得开才看得见 |
| 其余（`blob:`、相对路径） | 指的是本机的东西，**分享出去就是空框** |

所以导出前先数一遍摆给人看。不说清楚的话，对方打开是空框，
而分享的人以为自己发出去的和屏幕上长得一样 —— 这种错发现不了，
因为出错的是**别人**的屏幕。

导入时反过来提一句：引用了网络地址的话，打开这段会话会向那台服务器发请求。
只在真有远程地址时才说，没有就不啰嗦。

#### 重名不覆盖

同名的加「（2）」。覆盖掉别人调了半天的那一份，比多出一行糟得多。

### 4.128 画布的面积守住了，边长没守住

相册里点「重新生成图片」闪退。`raster()` 早就写着一段注释讲画布上限、
也确实按面积反推了倍率 —— **但只按面积**。量一遍就看出来了：

```
条数   算出来的画布        面积
 100   860 x 12000       10.3M   ← 面积没超
 200   656 x 18300       12.0M   ← 面积压住了，高度 18300
 400   464 x 25880       12.0M   ← 面积压住了，高度 25880
```

画布除了总面积，**每条边另有一个硬上限**（iOS 上大约 16384，各代不一样）。
而这里的高度是跟着「选了多少条」走的，面积那一道闸把倍率降下来，
高度却从来没被看过一眼。越界的后果不是画糊一点，是画布分配不出来、
渲染进程被系统收走 —— 屏幕白掉，看着就是闪退。

`fitScale` 现在面积与单边各算一个，取最紧的那个，`MAX_SIDE` 取 8192
（比那条线再退一半，赌不起）。

#### 压到看不清就别画了

四百条要压到 0.34 倍才装得下，那时候字已经完全看不清。**交一张糊得没法用的图
比明说画不了更糟** —— 用户会以为功能坏了，而实际上他要的东西本来就不该是一张图。
所以 `MIN_SCALE = 0.5`，低于它回 null，并且说清楚：这张多高、可以分几次保存。

`whyNot()` 单独带这句话出去，没有改 `rasterCard` 的返回类型 ——
「回 null」这件事两处调用方都在依赖，为了带一句话把契约改掉，两边都得跟着改。

#### 顺带：存进去那一下把它压糊了

同一个函数里的第二个毛病。画出来之后是
`db.images.put(file)`，而 `put` 的默认 `maxEdge` 是 1280：

```
原图          500 x 6000
默认存法      107 x 1280     ← 字全糊了
按 MAX_SIDE   500 x 6000
```

**这一项的说明写的是「便于导出」**，而存进去的是一条看不清的细条。
两处调用都改成按 `cardshot.MAX_SIDE` 存 —— 光栅那边已经保证不超过它，
所以照它存就一点都不缩。

#### 还有一处：换图时直接删了旧的

`db.images.remove(p.imageId)` 绕过了引用表。「保存到相册」存的是**同一个 id**，
直接删会把别处那张一起删掉（4.114 那一条）。改成 `purge.releaseImages`，
并且**先挂上新的再放旧的** —— 顺序反了的话，放的那一刻新的还没挂上，
引用表会认为它没人要。

### 4.129 相册里那张照片，得是她自己存的

从前是反过来的：人点进相册，按一下「生成」，看着它画出来。
**那样没有任何意外可言** —— 你已经知道会多出一张，也知道是什么时候。
而相册里那些照片的意思恰恰在于「你没在场的时候她拍了一张」。

所以这件事必须由角色自己挑时候做，人只能事后翻到（`ai/tasks/snap.js`）。

#### 节奏按天算，而且要隔够

跟深夜那一档（`emo`）同一个办法。定成每小时一张，它就从「她今天拍了张照片」
变成一个固定产出，和签到没区别 —— 意外感是这功能的全部，频率一高就没了。

「上次什么时候存的」记在 `localStorage`，和主动消息的落点一样是**设备本地的
临时状态**，不进 IndexedDB。代价是它不在任何数据域里，所以那几张登记表
（备份、角色包）都管不到它；删角色时得在 `purge.dropCharacter` 里单独抹一行，
这一条容易漏。

#### 不跟着「主动消息」的开关走

两个开关各管各的。一个只想让角色偶尔存张照片、但不想被主动搭话的用户是成立的，
所以 `tick` 里这一段排在 `if (!cfg.proactive) continue` **之前**。
第一版写在后面，结果是「不开主动消息就永远不存照片」，而界面上完全看不出来。

#### 弹不弹：跟朋友圈同一条规矩

只有「特别关心」的角色才弹通知。相册本来就是「你想起来才去看」的东西，
每张都弹就成了骚扰，而且弹了也就不是意外了。

#### 花两次，所以默认关着

一次想拍什么（文字），一次真画出来（生图）。按第 15 条登记在 `EXTRA_CALLS` 里，
默认关，开关按第 5 条放在那个角色自己的页面上，不进全局设置。

### 4.130 接口轮循：默认那一档要和从前一模一样

从前只有两档：主用挂了换副用。想配五套轮着用是做不到的。

现在失败之后按 **主用 → 副用 → 列表里其余** 的顺序依次换，换几套由
`failoverMax` 决定。

#### 默认值定成 1，是为了不悄悄多花钱

这一条比功能本身要紧。`chatFallback` 那个开关早就有人开着了，如果这次
改动让它从「换一套」变成「全都试」，那些人的账单会在毫不知情的情况下
翻几倍 —— 而第 13 条的意思是「代码不替用户做省钱的决定」，**同样也不替他
做花钱的决定**。

所以默认 `failoverMax: 1`，行为和从前逐字一致；想轮更多自己往上加。
填 0 是「其余的全试」，不是「一套都不试」—— 关不关归那个开关管，
不归这个数字管，两个都能关的话用户要在两处之间猜谁说了算。

#### 没填全的跳过

缺密钥或模型的预设直接不进队列。换过去只是白撞一次墙，还多一次计费。
`usableChatCount()` 因此只数填全了的。

#### 账要按「真能换出去几套」算

`attemptsPerCall` 从前写 `chatFallback ? 2 : 1`。开着开关但只配了一套时，
它说 2，实际是 1。现在取 `1 + swapCount()`，而 `swapCount` 同时看开关、
`failoverMax` 和填全了的套数 —— 三者取最紧的。**界面上写「最多 4 次」
而实际只有 2 次，和写 1 却打了 4 次一样，都是把账说错。**

#### 报错要点名

从前只把第一套的名字写进报错。配了五套的人看见一句「请求失败」根本没法查。
现在把试过的那几套全列出来，再附最后一个的原文。

#### 取消不往下换

`isAbort` 的那一支直接抛。人都不要了，再换一套是凭空一笔账。

### 4.131 健康一键生成：有限集只能按表收

「身体状态」那一页从前要一格一格点：精力、心情、哪儿不舒服、睡了多久。
它和「角色的一天」一样是**设定**不是测量，所以本来就该能按人设生成一份。

#### 模型给什么都不能直接落库

这一页上几乎每个字段都是**有限集**：精力三档、心情四档、症状九种、
形态七种。模型一定会自己发明 id —— `tired`、`anxious`、`b8` 都见过。
照单收下的话，界面上那一格什么都不显示（它在表里查不到），
而库里躺着一个谁也认不出的值，查起来要一路查到这一层。

所以逐项对着表收，对不上就丢：

| 字段 | 收法 |
|---|---|
| 精力 / 心情 / 形态 | 在表里就留，不在就空 |
| 症状 | 只留表里有的，顺带去重 |
| 睡眠分钟 | 钳在一天之内（模型会把小时当分钟填） |
| 时间 | 认 `HH:MM`，认不出就空 —— 空时间界面本来就允许 |
| 备注 | 截到 200 字 |

**坏值清空，但那一条本身留着**：一条时间和形态都没填的排便记录是合法的，
而整条丢掉会让「今天几次」这个数也跟着错。

#### 覆盖要问，而且要说清楚覆盖的是手填的

生成落库前不问，那是界面的事 —— 只有它知道原来那一格有没有东西。
问的那句话要写明「包括手动填写的部分」：用户一格一格点出来的东西，
被一次生成默默盖掉是最气人的一种。

生成的那一份在 `source` 上标 `ai`，页面底下写一行，改过之后仍然是那一行 ——
这一点没做严格（改一格不会翻回 manual），够用就行。

#### 不进 EXTRA_CALLS

用户按一下才跑。那张表管的是「发一条消息会不会变成两次请求」，
手动触发的生成（生成 NPC、批量食谱、试生成）都不在里面。按第 15 条走副用。

### 4.132 手机密码：依据是随机挑的，但对同一个角色不变

从前是「有生日就用生日」。于是几乎每台手机都是生日 —— **猜中第一台之后，
后面每一台都不用猜了**，这个玩法当场失效。

现在从这几样里挑一个：生日 MMDD、出生年份、设定正文里第一串四位数、
最后一串四位数。实测一百个角色，四种各占两三成。

#### 挑是随机的，但必须是**确定性**的随机

密码并不落库（见 `theirs.js` 开头：「一台手机不会还没有密码」）。
真随机的话，`localLock` 每调一次就换一个，人昨天记住的今天就打不开。
所以拿 `hashOf(char.id + ':lock')` 当种子 —— 对同一个角色永远挑中同一个，
而用户看不出下一个角色会是哪一种。

同一串数字只算一份：生日 `0314` 和设定里的 `0314` 是同一个密码，
不该被当成两种依据去占两个位置。

#### 提示要跟着依据走

这是最容易写错的地方。选中的是年份，提示却说「这是一个日期」，
那条提示就是在骗人。每一档各带自己的三条提示，测试里逐档对过。

#### 代价：已有角色的密码会变

密码是现推的，不是存的。所以这一改，**之前记住的密码不再有效**。
「问提示」和「直接查看密码」那两个口子都还在，打得开 —— 但这件事得说出来，
不能让人以为手机坏了。

#### 有一个测试锁的是旧行为

`theirs.mjs` 里写着「生日就是密码」并硬输 `0314`。那正是要改掉的行为，
所以改成现问现输（`localLock(char).code`），依据分布另由 `lockpick.mjs` 验。
**和 4.128 里 `shot.mjs` 那条同一类**：测试锁住了一个本该改的约定，
改需求的时候它会以「回归」的面目出现。

### 4.133 美化 app：管「有哪些」，不管「长什么样」

美化从前只有一个入口：进一段会话、开右上角菜单、点美化。于是三件事一直做不了 ——
导入别人给的美化包得先随便找一段对话，看不到「我一共有几份」，
删一份之前也不知道会连累哪几段会话。

现在拆出一个独立的 app（`src/apps/skin/`）。**但编辑没有搬过去。**

#### 为什么编辑留在会话里

会话那一页的样板间用的是 chat 自己的 `Bubble` 组件，所见即所得。
第 8 条不许一个 app import 另一个 app，所以编辑搬到新 app 就只能对着一个
**仿制的**样板调样式 —— 那是退步。两边都能编辑则违反第 5 条
「同一个开关只能有一个入口」。

所以分工是一条线：**库那一页管「有哪些」，会话那一页管「长什么样」。**
库里点「挂到一段会话并前往调整」，挂上之后走 Intent 跳进 chat 的美化页，
调样式仍然只有那一个地方。

#### 预览不是样板间，而且必须说出来

库里那张预览用的是卡片那一套静态 HTML（`cardshot.buildHtml`），
class 和真气泡同一套（`.msg` `.bubble` `.msg-col`，都来自 app.css），
所以针对这些选择器写的规则看得出来；但结构不完全一样，
细到伪元素、相邻选择器那一层就可能对不上。页面上因此写明这是静态预览。

#### 令牌挂在 :host 上，不是 :root

预览画在 shadow root 里 —— 这份美化的 CSS 直接塞进页面会把整个 app 一起改了。
而 shadow root 里**没有 `:root`**，那是文档根。整段令牌挂过去不会报错，
只会静静地不生效，人会以为尺寸那几栏坏了。

`skin.compile(skin, { varsOn })` 因此收一个选择器参数：挂进页面时是 `:root`，
画在 shadow 里时是 `:host`。**只有令牌那一段是我们自己生成的，所以换得起**；
用户手写的那一段一个字都不改（理由见 4.111）。

#### 复制一份

`skin.duplicate(id)` 名字后缀「副本」，重名再加数字。调好一份再在它基础上改，
比从头写一遍常见得多，而从前只能导出再导入，绕一大圈还多出一个文件。

#### 删之前先说会影响谁

`skin.chatsUsing(id)` 列出挂着它的会话。删除确认里写明「那几段会话恢复为默认样式」，
`skin.remove` 本身也会把它们一并取下（见 4.111），不留指向空 id 的孤儿。

### 4.134 头像框：一张图，三个字段，存在美化行里

画在头像外围的一圈，不改变头像本身。挂在 `.msg-face` 的 `::after` 上 ——
那个盒子正好就是头像那一块，框按它的百分比放大，头像是圆是方都不受影响。

三个字段一起走：`frame`（图）、`frameWho`（戴给谁）、`frameScale`（多大）。
`create` / `duplicate` / `pack` / `unpack` 四处都要带全。**少带一个，
复制和导入过来的那一份就会戴错人或者错大小** —— 而且不报错，只是看着不对。

#### 为什么图存成 data URL，不进 images 库

`compile` 是同步的，挂载、库里的预览、卡片光栅三处都在同步路径上。
换成按 id 现取，这三处全部要改成异步。代价是美化包会变大，
所以导出前把这张图的字节数摆出来（`frameBytes`）。

顺带一个好处：它不是 images 库里的一条，所以不必登进 `usedImageIds`，
也就不会在「清理无引用图片」时被顺走。

#### 只认 `data:image/...;base64,`

这个字符串会被**原样拼进一段 CSS** 的 `url("...")` 里。放行别的形式就是
两个口子：网络地址让美化包决定往哪台服务器发请求，而带引号的字符串
能从 `url()` 里逃出去，后面接什么都行。所以 `unpack` 和 `create` 各查一道，
正则只认 base64 那一种，不合的一律存空。

`frame.mjs` 里两条测试专门打这两下：网络地址、以及
`data:image/png;base64,AA");}body{display:none}.x{a:url("`。

#### 用 compressFit，不是 compress

`compressFit` 把整张图放进正方形画布、留白透明、**不居中裁切**。
框的四个角正是它最要紧的地方，裁掉就不成其为框了。
它输出 webp（带透明通道），不支持的退回 png，两者都留得住透明。
存 512 见方：头像本身 36 像素，三倍屏上 108，再大只是把包撑起来。

#### 生成顺序

令牌 → 头像框 → 用户手写的 CSS。框和令牌同属「我们生成的那一层」，
手写那一段排在最后，仍然盖得住它（和 4.111 里那条顺序是同一条）。

#### 「尺寸全部恢复默认」不碰它

那个按钮清的是 `tokens` 和 `shape`。框是一张图，不是一个数字，
被一个写着「尺寸」的按钮顺手删掉会很意外 —— 页面上因此写明一句，
移除走它自己的按钮。

### 4.135 挂载点：样板间要盖住类名表列出的每一处

「类名」那一页列了十九条选择器。问题是其中有几条**样板间里根本画不出来**，
于是用户照着写一条规则、盯着样板间、什么也没发生 —— 他没法知道是规则写错了，
还是这一处压根不在眼前。

量过一遍（`probe-sel.mjs`）：样板间里当时缺底栏、输入框、发送键、引用条、
表情气泡、那行小字。**九项令牌里「底栏按钮大小」「底栏内边距」两项，
调了看不出任何动静**，因为样板间里没有底栏。

#### 底栏抽成组件，两边共用

`ComposerBar` 从 `Conversation.js` 里抽出来，会话页和样板间用同一份。
理由和 `Bubble` 那条一样（见这一页开头）：另写一份假的，真页面改了 class
它不跟，在美化页调好的东西回到会话里不生效，而且没人会发现。

样板间那一条传 `frozen`：输入框只读，所有 `onClick` 传 `null`。
它是给人看的，不是给人用的。

引用条同理 —— 样板间中间那条消息带上 `quoteText`，`.quote-ref` 就画出来了。

#### 命中数：不靠人核对那张表

`skin.CLASSES` 是手写的，而页面结构会变。「两张表迟早对不上」在这个项目里
反复发生过。所以类名那一页每次打开都**在样板间里真的查一遍**
（`SkinPage` 的 `useHits`），每一条后面写它此刻命中几处。

三种结果，措辞不一样：

| 命中 | 显示 |
|---|---|
| 大于 0 | 样板间中 N 处 |
| 0，且这一条写了 `needs` | 样板间中暂不可见，需要…… |
| 0，且没写 `needs` | 样板间中未命中，该选择器可能已失效 |

第三种就是这张表该改了。`mounts.mjs` 里有一条测试专门盯它：
**没写 `needs` 的每一条都必须命中**，一条都不许落到第三种上。
以后谁把某个 class 改名，这条测试当场就红。

#### where: 'page' —— 顶栏不在样板间里

顶栏就是这一页自己的顶栏，美化挂着的时候直接看它。对着样板间数命中，
它会被报成「可能已失效」，而它好好的。所以那两条标 `where: 'page'`，
对着整页数，措辞也换成「本页中 N 处」。

#### needs：画不出来不等于表错了

`.msg-stamp` `.msg-read` `.msg-meta` 要先打开消息时刻或已读回执 ——
那两个开关就在同一页下方，开了当场从「暂不可见」变成命中，`mounts.mjs` 里
连这个过渡一起测。`.bubble-sticker` 要会话里真有一条表情消息；
样板间里塞一条假的只会画出「表情已删除」，比不画更糟。

### 4.136 美化契约：一套不许改名的类名，交给外面的人

美化从前只有一条路：在应用里调那几个旋钮，或者在自定义 CSS 框里写内部类名。
两条都走不通「别人自己做一份」这件事 ——

参照过一个现成的例子（rainyxx-rrr.github.io，一个气泡美化的代码生成器）：
它不是聊天应用，是一页表单加一个手机预览，导出的是一段纯 CSS，
针对的是**另一个应用**的类名（`.message-container.sent .message-bubble`、
`.message-avatar::after`、`.transfer-card::before`），每一条都带 `!important`。
用户把那段 CSS 贴进自己的聊天应用里。

这套玩法成立的前提只有一个：**那些类名不会改。** 而它们是那个应用的内部
实现，随时会改 —— 改的那天，所有人写过的美化一起失效，而且是静默失效。

所以这里分两套名字。

#### 内部类名与契约钩子

| | 例子 | 承诺 |
|---|---|---|
| 内部类名 | `.msg` `.bubble` `.composer-bar` | 没有承诺，随时改 |
| 契约钩子 | `.ph-msg` `.ph-bubble` `.ph-composer` | 一经发布永不改名 |

同一个元素上两个都挂：`class="msg is-mine ph-msg ph-msg-mine"`。
多一个字符串的代价，换的是「重构不会毁掉别人几个月的活」。

变量同理：`tokens.css` 里写的是 `--bubble-r: var(--ph-bubble-r, 14px)`，
我们自己的样式照旧读 `--bubble-r`，外面只认 `--ph-bubble-r`。
**应用里那几个旋钮写的也是 `--ph-*`**，不是内部名 —— 只留一条路，
两条路的话，用户调的和作者写的会互相盖，而盖的顺序谁也说不清。

#### 表只有一份，而且是双向核对的

`src/system/skin-contract.js` 里的 `HOOKS` 与 `VARS`。契约文档从它生成，
美化页上「类名」那一页从它生成，`scripts/check-contract.mjs` 拿它核对：

- 表里写了的钩子，源码里必须真的挂着。写了不挂，文档在骗人
- 源码里挂着的 `ph-`，表里必须登记。挂了不写，作者看不到它
- 每个公开变量必须真的有人读，否则作者写了也不生效

手写的表迟早对不上，这个项目已经反复吃过这个亏（4.135 是上一次）。

顺带查出来一处：相册用 `ph-` 当「photo」的缩写（`.ph-grid` `.ph-tile`），
和契约命名空间撞了，一并改成 `photo-`。

#### 第四条检查：挂了钩子就不许再写内联样式

这是最要紧的一条。**内联样式赢过任何选择器**，作者写什么都盖不住，
而且完全看不出为什么 —— 他只会以为「这个应用的美化是坏的」。

头像正是这么一个例子：`Avatar` 从前把 `width` / `height` / `border-radius`
直接写在 `style` 上，于是谁都改不动头像大小。那个生成器里「头像尺寸」
是一个最常用的滑杆，我们这边则根本没有这一项 —— 不是没做，是做不了。

现在 `Avatar` 只喂三个自定义属性（`--av-size` 等），真正那几条声明在 ui.css 里，
从公开变量读起：

```css
.avatar { width: var(--av-size, 44px); }
.ph-face .avatar { width: var(--ph-avatar-size, var(--av-size, 36px)); }
```

`--ph-avatar-size` 只管会话里那一层，否则一份会话美化会把整个应用的头像
一起改大。圆角是全局的，形状本来就该一致。

**只设自定义属性的内联样式不算违规**，检查里放行：`style="--sheet-h:400px"`
喂的是一个变量，作者照样能用选择器改掉真正那条声明；
`style="height:400px"` 是写死，谁都盖不住。两种长得像，后果完全相反。
底部浮层的高度、应用图标的图、壁纸的图都按这个改了。

#### 不必写 `!important`

那个生成器每一条都写 `!important`，因为它在和应用自己的样式表硬碰。
我们这边注入的 `<style>` 挂在 `<head>` 最后，同样特异度下它本来就胜出 ——
所以契约文档里明说不必写。写了不会更保险，只会让后面想覆盖它的人无从下手。

#### 两档范围，由美化自己声明

```json
{ "kind": "phone-skin", "version": 1, "scope": ["chat"], "css": "..." }
```

| scope | 注入在哪儿 | 逃生口 |
|---|---|---|
| `chat` | 挂在一段会话上，进那一页才注入，离开即摘 | 消息列表不上美化，天然存在 |
| `shell` | 一直挂着，主界面与列表都在内 | 人为留两个，见下 |

**老包没有 `scope`，一律按 `chat` 读。** 默认成全局等于替作者把影响面
扩大了一圈，而他当初没这么写。

两层是**两个 style 节点**（`skin-css` 与 `skin-global`），不合并 ——
合了的话，关掉全局的那一下会把会话那一份也摘掉。

#### 全局美化的两个逃生口

会话那一档写坏了只坏一页，回得去。全局这一档写坏了可能整个界面都点不动，
所以人为留两个口子：

1. **设置 app 打开时一律摘掉**（`shell/Root.js` 判断 `appId === 'settings'`）。
   写坏了整个界面的时候，设置那一页仍然是素颜的，进得去、点得到
2. **一个总开关** `settings.skinOff`，关掉两层都不注入

崩溃记号（`skinTrying`）两层共用：全局那一份挂上之前也记，活过 1.2 秒才清。
上次进来就崩在它身上的，这次不注入。

删掉设为全局的那一份时，`settings.globalSkinId` 要一并清掉 ——
少这一行就是一个指向空 id 的全局设置，表现为「删了之后全局美化再也设不回来」。

#### 「写给作者」那一页

`src/apps/skin/ContractPage.js`。三个页签（类名、变量、怎么写），
外加两个复制按钮：整份说明的 Markdown、一份能直接改的 CSS 骨架。

这一页的用处只有一个：**让不装这个应用的人也能写出一份能用的美化。**
他需要的就三样 —— 有哪些类名、有哪些变量、哪些地方够不着。

### 4.137 美化生成器：拖旋钮，出 CSS

写 CSS 的人是少数。想要「气泡换个底色、加个渐变边框、角上贴张图」的人是多数，
让他们为此去学选择器和伪元素不合理。所以有这一页：七组旋钮，生成的 CSS
原样存进那一份美化的自由层。

参照的是 rainyxx-rrr.github.io 那个生成器（见 4.136）。它做对的几件事这里
照搬了：左边旋钮右边实时预览、贴图带位置与旋转、渐变边框、头像框收发各一张、
导出一段能直接交给别人的 CSS。做得更好的两处：

- **预览用的是真组件。** 那边是一套仿造的 HTML，这边是会话页真的 `Bubble`
  与 `ComposerBar`（见 4.135），所见即所得
- **旋钮的值存下来。** 那边靠导出一份配置文件再导回去，这边 `gen` 直接存在
  美化行里，跟着美化包一起走 —— 别人导进去能接着调，不是只拿到一段死 CSS

#### 七组

气泡、边框与阴影、角落贴图一、角落贴图二、头像、顶栏、底栏、整体。
气泡那一组第一项是「改哪一边」，双方 / 角色 / 自己 ——
和那个生成器的 `all / sent / received` 是同一件事。

#### 一张表，两处读

`system/skin-gen.js` 的 `GROUPS`。界面照着它画，`emit` 照着它出 CSS。
**加一项旋钮只改那一个文件**，界面不必跟着改。两处各列一份，迟早对不上。

#### 为什么带 !important

这是做的时候才发现的：应用自己的样式表里有 `.msg.is-mine .bubble`
这种复合选择器，特异度 (0,3,0)，比单个 `.ph-bubble-mine` 的 (0,1,0) 高，
**光靠注入顺序压不住**。那个生成器每条都写 `!important` 正是这个原因。

生成的这一段是机器写的、随时整段重生成，所以在这儿用 `!important` 合适。
手写那一段排在它后面，要盖住它同样写 `!important` —— 界面上说明了。
契约页上原先那句「不必写 `!important`」因此改成了分情况说。

真正干净的解法是拿 `@layer` 把应用自己的样式表整个降一层，
那样任何未分层的美化规则都天然胜出，一个 `!important` 都不用写。
留着以后做，改动面比这一次大。

#### 只认 data: 的图，只认 #rrggbb 的颜色

和头像框同一条（4.134）：这些字符串会被原样拼进 CSS。网络地址让美化包
决定往哪台服务器发请求，别的形式能从 `url()` 或声明里逃出去。
认不出的一律不写进去 —— 不是报错，是当作没填。

#### 没调过的一项都不写

`emit` 跳过等于默认值的项。不然一份只改了底色的美化会生成两百行，
其中一百九十九行和不写一模一样，交给别人看也读不懂哪条是有意的。
一项都没调就回空串，不留一段空壳注释。

### 4.138 生成器改成对着一份复刻页调，分组按页面从上到下排

生成器从前挂在会话的美化页上，顶上是真的 `Bubble` 与 `ComposerBar`。
两个问题：**必须先挑一段会话**才能调；**真页面上很多状态凑不齐** ——
「同一个人连发三条」要正好有那样一段历史，引用、表情、已读各要一个条件。
而「连着的第一条才带头像」这类规则，没有连着的几条根本看不出来。

所以改成对着一份手写的复刻页调（`system/skin-stage.js`），生成器搬进美化
app 自己的一页（`/gen/:id`），不挂任何会话。复刻页一次摆全：连发三条、
一轮拆两个气泡、引用、表情、时刻、已读、双方各几条。

#### 复刻页会走偏，所以有一个测试盯着

`stage.mjs` 把复刻页和真会话页各渲染一遍，取出每个契约钩子**以及它最近的
那个钩子父级**，两边逐条对。真页面有的复刻页必须有；复刻页多出来的必须是
契约里标了 `needs` 的那几种（那正是它存在的理由）；父子关系也要一致。

只有 `page` 不比：真页面外面还套着外壳，复刻页没有。

**同一份复刻页也是库里那张预览。** 从前库里另用卡片那一套静态 HTML，
它没有契约钩子，于是生成器里调好的头像框在库里一点都看不见 ——
两份复刻永远差一截，一份就没这回事。

#### 栽的那一下：样式表少取了一张

复刻页最早只取 `base.css` 与 `app.css`（沿用了卡片那一套）。而 `.navbar`
的 `display: grid` 写在 `ui.css` 里，于是预览里顶栏三样东西竖着排成一摞，
看着像布局坏了 —— 坏的是取样式表那一步。现在取的是 index.html 里那一串，
一张不少。

#### 换图标：藏 svg，给按钮铺图

图标是内联 `<svg>`，CSS 改不了它的路径。办法只有一个：

```css
.ph-back svg { opacity: 0 !important; }
.ph-back { background-image: url("data:image/..."); background-size: 26px; }
```

「设为透明但仍可点击」就是这一招的前半截：svg 藏起来，按钮和点击区一分不动。
为此契约加了四个按钮各自的钩子：`.ph-back` `.ph-nav-action` `.ph-plus`
`.ph-sticker-btn`（`.ph-send` 本来就有），契约版本升到 2。
`IconButton` 因此多收一个 `cls`。

#### 连着的第一条 / 最后一条

纯 CSS，不加标记类：

| | 选择器 | 要求 |
|---|---|---|
| 只有第一条 | `A + A` 的那些藏起来 | 哪儿都跑得动 |
| 只有最后一条 | `A:has(+ A)` 的那些藏起来 | iOS 15.4 以上 |

藏用 `opacity: 0` **不用 `display: none`** —— 抽掉头像那个盒子，那几条的
气泡会整体左移，看着像错位。一轮里「只有第一个气泡」同理，用
`.ph-col > .ph-bubble:not(:first-child)`。

#### 伪元素的位置不够分，所以让用户自己排

一个元素只有 `::before` 与 `::after`。渐变边框、小尾巴、两张贴图都要用。
从前的做法是「渐变边框开着时贴图二不生效」，一条写死的优先级。

现在贴图与小尾巴各自能选挂在哪个元素（气泡 / 这一轮 / 整行）的哪个位置，
撞上了由 `conflictsOf` 数出来，界面上直接写「某某和某某都挂在同一个位置，
只有一个会生效」。**不做隐藏的优先级** —— 那样用户只会看到
「我明明设了却没出来」，而 CSS 不会报任何错。

#### 改写文字

`.ph-read` 的字号压成 0，用 `::after` 的 `content` 放新的。
写进 `content` 的那句话要转义引号与反斜杠，否则一句
`a"}body{display:none}.x{content:"` 就能从声明里逃出去。

#### 预览的缩放

没聚焦时整页看得见：宽高各算一个倍率取小的，再横向居中。展开一组时
**按目标的高度算倍率** —— 顶栏只有几十像素，要放大不少才看得清；
消息区本来就几百像素高，再放大只会看不到全貌，所以那种情况不放大。
横向也跟着挪，不然放大之后只看得见左半边。

### 4.139 0 不是哨兵，负数要能填

生成器里每一个数值旋钮从前都写着 `def: 0`，`emit` 里判的是 `if (num(x))`。
两个后果，用的人一眼就能撞上：

- **调不到 0。** 圆角想调成方的、两条消息之间想调到 0，填进去等于「不改」
- **填不了负数。** `if (num(-14))` 虽然为真，但 `min: 0` 根本不让填

第二条更隐蔽：`.ph-chat-body` 的间距原先用 flex 的 `gap`，而 **`gap` 不接受
负值**。就算放开 `min`，「两条消息叠起来」也写不出来。那个参照的生成器
（4.136）用的是 `margin-bottom`，正是为此。

#### 空串才是「不改」

三十一个覆盖式的旋钮 `def` 从 `0` 换成 `''`，`emit` 里判空的那一步换成
`set()`：空串跳过，`0` 和负数都照发。`Slider` 跟着改：没设过时滑杆压淡、
数字框显示「默认」，填了之后右边出现一个「不改」把它清回去 ——
和取色那一栏同一套（那里早就这么做了，数值这一栏落下了）。

顺带两处从「不生效」变成「生效」：边框填 0 写 `border: none`，
投影填 0 写 `box-shadow: none`。从前这两项填 0 等于没填，
于是没有办法去掉别处继承来的边框。

#### 间距一律从 gap 换成 margin

| 旋钮 | 从前 | 现在 |
|---|---|---|
| 两条消息之间 | `.ph-chat-body { gap }` | `gap: 0` + `.ph-msg { margin-bottom }` |
| 同一轮内两个气泡 | `.ph-col { gap }` | `gap: 0` + `.ph-col > .ph-bubble + .ph-bubble { margin-top }` |
| 头像与气泡的距离 | `.ph-msg { gap }` | `gap: 0` + 两边各一条 margin，方向相反 |

**padding 同样不接受负值**，所以「第一条与顶部的距离」这类按符号分流：
非负走 padding，负的走 margin。往里让开是留白，往外顶出去是负边距，
对用户是同一个旋钮。

#### 顺带一处：底部边距只能往上

「外观」里那条 `bottomLift` 写的是 `min="0"`，只能把底部往上抬。
地址栏不存在时想收回多余的留白就没有办法。放开到 `-40`，
同时换成新的 `Slider`，不再自己写一个裸的 range。

`neg.mjs` 里有一条机器查的断言：**扫一遍全部数值旋钮，不许再有
「`def` 是 0 且 `min` 不小于 0」的那种**，以后新加一项忘了放开就会红。
位移类的另有一条，每一项都必须能填负。

### 4.140 生成器改成左边一条竖栏，以及那一栏为什么滚不动

第一版是「预览钉在上面，十一组折叠条一路往下排」。两个毛病。

#### 一、旋钮那一栏根本滚不动

`.page-body` 是 flex 子项（`flex: 1; min-height: 0`），**但它自己不是 flex
容器**。里面那个 `.scroll` 写的 `flex: 1` 因此等于没写，高度撑成内容那么高，
它自己不会出现滚动条；而 `.page-body` 又没开 `overflow`，于是超出去那一截
既滚不到也点不着。

这一类错的共同点是**中间断了一层**：从 `.page` 到真正要滚的那个盒子，
每一层都得把高度钉住，断一层后面全废。所以这一页现在从上到下是

```
.page  →  .page-body  →  .gen  →  .gen-main  →  .gen-panel
         flex:1        height   flex:1        flex:1
         min-height:0  :100%    min-height:0  min-height:0 + overflow
```

#### 二、找一组要滚半天

十一组折叠条排下去，想改底栏得先滚到底，改完想看效果又要滚回顶上。

改成**左边一条 62 像素的竖栏常驻**，十一组各一格加一个「总样式」，
十二格一屏装得下（`rail.mjs` 里有一条断言盯着这件事，以后加组加到装不下
就会红）。任何一组都是一下点到；右边上半预览、下半那一组的旋钮。
再点一下当前那一格就收起来，预览占满整块。

改过的那几组在竖栏上带一个小点，不必逐个点开找。

#### 放大只能放到「一整页宽刚好装下」为止

第一版按目标的**高度**算倍率：顶栏才 48 像素高，算出来的倍率因此很大。
量了一下，430 宽的页面只看得见中间 240 —— **两边各切掉九十多像素**，
九组里有七组是这样。而顶栏、底栏、消息区、气泡这几样本来就是整行宽的，
横向放大一点好处都没有，全是坏处。

所以只剩两档，没有中间态：

| | 倍率 | 做什么 |
|---|---|---|
| 没聚焦 | 宽高各算一个取小的 | 整页都看得见 |
| 聚焦 | 按宽度铺满 | 纵向滚到那一块 |

`stage.mjs` 里那条断言不再量倍率，改成量**看得见多宽**：逐组点一遍，
只要哪一组 `clientWidth / scale` 小于 430 就红。倍率是手段，
「有没有被切掉」才是要守的东西。

#### 预览的高度是布局给的，不是写死的

第一版把预览写成固定 330 像素。现在它是 `height: 46%`（收起时 `flex: 1`），
所以 `Stage` 里那套倍率计算必须**每次读当下的 `clientHeight`**，
并且把「旋钮开着没有」也放进依赖 —— 少了它，收起之后还按原来那个高度算，
上下空一大片。

同一条也落在测试上：`stage.mjs` 里不再写死 331，而是拿 `clientHeight` 比；
「本来就很高的那一块不再放大」也改成和**当下**的整页基准比，
因为展开旋钮会让预览变矮，基准跟着变。

### 4.141 「看着是 0 其实不是 0」：安全区那一块

底栏的下边不是一个内边距，是**两样加起来**：

```css
.composer-bar { padding-bottom: calc(var(--composer-pad) + var(--safe-bottom)); }
```

`--safe-bottom` 是 `env(safe-area-inset-bottom)`，iPhone 上那条 Home
Indicator 要占 34 像素。所以旋钮上写着 0，屏幕上底下仍然空一大块。

三处都要改，少一处都还是错的。

#### 一、旋钮拆成上下两个，下边那个放开到负

从前一个「上下内边距」同时写 `padding-top` 与 `padding-bottom`，最小 0。
现在拆成两项：上边照旧（padding 本来就不能是负的），
**下边放开到 -80**。写了就整条盖掉那个 `calc`，非负走 padding、
负的部分走 margin 继续往下收。

说明文字要把这件事说出来，不能让人对着一个 0 猜：
「这一块默认等于内边距加上系统安全区，所以看着比数字大。」

#### 二、预览里要摆出那一块，不能抹掉

`STAGE_CSS` 从前写着 `.stage-page .composer-bar { padding-bottom: var(--composer-pad) }`
—— 把安全区抹成 0。于是**预览里根本看不到那一块**，人调完到真机上才发现
下面还空一截。预览抹掉了真机上有的东西，它的意义就没了。

改成 `.stage-page { --safe-bottom: 34px }`，照着真机摆一段。

#### 三、放开了滑杆还要放开写值那一步

「外观」里那条底部边距，上一版把滑杆放开到 -40 就算完了。可
`system/look.js` 里写着

```js
root.style.setProperty('--bottom-lift', Math.max(0, s.bottomLift || 0) + 'px');
```

滑杆动了、值也存进库了，到这一步被抹平 —— 整条链上只要有一处钳成非负，
前面放开多少都没用。`neg.mjs` 因此多一条：不只看滑杆的 `min`，
还要 `applyLook` 之后去读 `--bottom-lift` 的计算值。

#### 按名字查一遍

`neg.mjs` 里加了一条机器查的：凡是标签里带**移动 / 偏移 / 距离 / 之间 /
留白**的数值旋钮，下限都必须小于 0。内边距那几项除外（CSS 本身不接受负的
内边距），下内边距另有一条单查。以后再添这类旋钮忘了放开就会红。

### 4.142 生图「一次都没成功过」：那句 Failed to fetch 底下是跨域

先把整条路在假接口上走了一遍（`img-e2e.mjs`，16 项）：地址拼接、
Authorization、请求体、五种回包形状（纯 base64、带 `data:` 前缀、只给链接、
`images[].image`、最外层就是数组）、200 里带 error、回包里没图 —— 全通。
**代码那一段是对的，断的是真实网络那一层。**

#### 绝大多数生图中转站不让网页直连

`fetch` 失败时抛的永远是同一句 `Failed to fetch`：域名解析不了是它，
连接被拒是它，**跨域被拦也是它**。而生图这类接口多半压根没考虑过浏览器
直连，不会回那个允许跨域的响应头。于是在网页里生图，十有八九第一下就死
在这里，屏幕上只有一句 `Failed to fetch`，既不提跨域，也不提该怎么办。

聊天那几家本来就允许浏览器直连，所以聊天一直好用 —— 这也是为什么
「只有生图不行」。

#### 语音早就有这一套，生图一直没有

`ai/voice.js` 里的 `ask` 与 `testVoice` 把这件事做全了：先看这一次走的是
直连还是外壳，直连失败时再发一次 `mode: 'no-cors'` 问「服务器到底活着没有」
（见 `net.js` 的 `reachable`），据此分出「被跨域拦下」和「压根没连上」。

生图这边三处发请求用的都是裸 `nfetch`，一处诊断都没有。现在补齐：

| 断在哪 | 下一步 |
|---|---|
| 没填密钥 / 没填模型 | 直说没填 |
| 被跨域拦下 | 服务器是通的，但它不让网页直接调。改填允许跨域的中转地址，或装成应用由外壳发 |
| 没连上 | 地址、域名解析、网络 |
| 接口报错 | 已经连上了，是对方拒绝的。密钥、模型名或尺寸不对，并带上对面的原话 |
| 图片链接取不回来 | 把「返回格式」改成 base64 |

`testImage` 放在 `ai/image.js`，「生图」那一页的按钮从「试生成」改成
「自检并试生成」，报告直接列在页面上：断在哪一步、下一步做什么、
这一次走的是直连还是外壳、原始报错。

#### 生图一条都没进抓包，所以谁也看不见发了什么

聊天那边每一次请求都记在抓包里，生图这边**一条都没有**。发出去什么、走的
哪个端点、带没带参考图，屏幕上一点痕迹都没有 —— 于是「画出来的东西不对」
既没法自己查，也没法说给别人听。现在三条路（文生图、带参考图、NovelAI）
都记，成功记取回多少字节，失败记原因。

#### 最终那一串不止「画面描述」，所以逐段标出来源

`compose` 拼的是四到五段：

| 段 | 从哪儿来 |
|---|---|
| 画面描述 | 角色这一轮写的 `[图片：…]` |
| 角色外貌 | 锁脸把脸图读成的那段话，要配了识图才有 |
| 生图世界书 | 标了「只用于生图」的书，按这一张的内容命中 |
| 这个角色的固定提示词 | 角色卡上那一栏 |
| 全局生图提示词 | 设置里那一栏 |

平时这几段一个字都不露，画出来不对劲时十有八九是后面几段里写了什么。
所以 `explain()` 把每一段前面加上来源标出来，抓包里单独记一条
「它由哪几段拼成」。**要找一句自己没写过的话是从哪儿钻进去的，
看这一条就够了。**

#### 装成应用就没有这回事

外壳那层用 `URLSession` 发，跨域是浏览器的规矩，不是 HTTP 的规矩
（见 `system/net.js` 开头）。所以同一套配置，网页里不通、装成 ipa 之后
往往就通了。报告里那一行「这一次走的是外壳转发 / 浏览器直连」就是为了
让这件事一眼看得出来。

### 4.143 「请求超时」：外壳那层写死 120 秒，而生图本来就慢

自检报回来的是「没连上」，原始报错是**请求超时** —— 那四个字是 iOS 把
`NSURLErrorTimedOut` 翻成中文的说法，也就是说：请求是发出去了的，
不是跨域、也不是地址错，是**本机自己掐的**。

`ios/Sources/NetBridge.swift` 里写死 `timeoutIntervalForRequest = 120`。
生图一张跑一两分钟是常事，慢一点的模型每一次都正好死在这条线上。
聊天不受影响，因为它是流式的，第一个字很快就回来。

#### 期限要跟着请求走，不是跟着会话走

会话级那个 `timeoutIntervalForRequest` 是**所有请求共用**的，压不住也放不开
单独某一类。所以改成每个请求自己带 `timeoutInterval`，网页那头按接口给，
不给就用默认的 300 秒。

超时还要**单独标一个记号**交回去（`timedOut`），因为它的下一步和别的都不同：

| | 下一步 |
|---|---|
| 超时 | 多半只是这个模型慢，把期限调大 |
| 被跨域拦下 | 换中转地址，或装成应用 |
| 没连上 | 查地址、域名解析、网络 |

混成一句「连不上」，人只会去反复检查那个本来就没错的地址。

#### 浏览器那条路压根没有期限

`fetch` 自己**永远不超时**。对面收了请求再也不回，这一条就一直挂着，
转圈转到天荒地老，而人分不出「很慢」和「死了」。所以直连那条也接上
`AbortController`，并且把调用方自己的 `signal` 连过去 ——
不然「取消」按钮按不动它。

`reachable()` 同理加了 8 秒的期限：这一问是自检里问「服务器到底活着没有」
的那一下，它挂住的话整个自检跟着挂住，人看到的是「点了没反应」。

#### 等多久由用户填

`settings` 里每套生图接口一项「等待上限」，默认 300 秒，**填 0 表示一直等**
（第 13 条：能力不设上限，真要截断那也是默认值）。老的接口没有这一项，
读到的是 `undefined`，退回 300 秒。

### 4.144 自检能收到、发消息却不行：两条只在消息那条路上的岔子

自检通过、发消息却报超时。两条路不一样的地方只有两处，两处都会出这个症状。

#### 一、自检的是你正在编辑的那一套，发消息用的是「当前生效」的那一套

`Editor` 里那个按钮调的是 `testImage(preset)`，带着你眼前这一套；
而 `reply.js` 走的是 `activeImage()`。**存了好几套接口时，这两个常常不是
同一套** —— 新配了一套、测通了，却忘了把它设为生效，于是发消息仍然走着
那套旧的、早就不通的。屏幕上两边看着一模一样。

现在三处一起说：编辑一套非生效的接口时页面上直接挂一条提示，旁边给一个
「把这一套设为生效」；自检报告里也写明「发消息时用的是某某那一套，
不是这一套」；自检结果对象上带 `isActive` 与 `active`。

#### 二、参考图那条先白等一场

开了「支持参考图」且角色设了脸图时，消息那条路**先试 `images/edits`**，
不认再退回纯文生图。问题是多数中转站没有这个端点，而它们**不回 404，
是把连接挂在那儿** —— 于是一张图先白等满整份超时，再从头画一次。
自检从来不走这条，所以自检永远看不见它。

它本来就是一次「试试看」，有现成的退路，不该花掉整份预算。所以给它一个
单独的、短得多的期限（45 秒），整份填 0「一直等」时这一条仍然有期限。
那个开关的说明也照实写：多数中转站没有这个端点，只想要文生图就关着。

#### 查它的办法已经有了

抓包里那一条会写着「生图 · 带参考图」还是「生图」，以及用的是哪一套接口的
名字（见 4.142）。这两件事各对应上面一条。

### 4.145 那条 400 里有两件事

上游回的是：

> 请上传你希望作为参考的图片，或说明需要生成的具体对象
>（例如「乃木喵」和「调色盘」分别是什么样的形象）

#### 一、只有这段对话里才有的名字，生图那一步不认得

「乃木喵」「调色盘」是这两只宠物在这段对话里的名字。**生图是另一个模型，
它收到的只有那一串提示词，看不到任何对话。** 所以这两个词对它毫无意义，
这一家干脆就拒了。

`skeleton.image` 补一句。这一句是**机制**不是判断（第 16 条）——
「生图那一步收不到对话」是客观事实，和「该怎么描述」无关：

```
The image is produced by a separate model that does not receive this
conversation. A name that exists only here, such as a pet's name or a
nickname, carries no meaning for it: write what the thing looks like.
```

#### 二、整段负面提示词正被当成「要画的东西」发出去

这一条更隐蔽。`images/generations` 的标准里**没有负面提示词这个字段**，
而「负面提示词」那一栏从前只给 NovelAI 那一档。于是照着别处的习惯，
把 Positive 与 Negative 两大段一起贴进「全局生图提示词」——
Negative 那几十行被原样拼进正向提示词发出去。

写着 `selfie, portrait, studio lighting, Ghibli style`，模型就照着画自拍、
棚拍、吉卜力 —— 正好是想排除的那些。而界面上一点痕迹都没有，
画出来只是「不对劲」。

三处一起补。

**最要紧的是：不止提醒，直接摘出来。** 人已经写了「Negative」这三个字，
意思没有任何含糊 —— 照它办不是替谁做决定，是读懂他写的东西。
`cut()` 在那一行处切开：前半留在正向（顺手把单独成行的 `Positive` 抬头也
去掉），后半收进负面。**每一段各切各的** —— 全局提示词、角色固定提示词、
画面描述都可能各带一段。摘出来的那几段在抓包里单独标着
「……里的负面部分（已从正向里摘出来）」。

摘出来之后发不发，仍然由那个开关定：关着就丢掉（那本来就是负面，丢掉是对的，
当成正向发出去才是错的），开着就并进 `negative_prompt`。

- `negativeBlock()` 认出提示词里单独成行的 `Negative` / `负面提示词` /
  `Negative prompt:`，设置页上直接说「这些会被当成**要画的内容**发出去」，
  并数出后面还有几行。句子里出现 negative 这个词不误报，后面没内容的不报。
- 「负面提示词」那一栏对所有档都开，另配一个开关
  **「这套接口认得 negative_prompt」，默认关着**。OpenAI 本身见到不认识的
  字段直接 400，而那个 400 里看不出是哪个键（和 `response_format` 同一个
  坑，见 `FORMATS`）；中转站转给别的模型时常常是认的，所以由用的人自己说。

### 4.146 有些中转站的「生图」其实是一个只会编辑图的对话模型

第二次的报错换了一句：

> 我这边需要先确认可用的图像目标才能继续处理。
> 请上传你希望参考或编辑的图片后，我可以根据你的描述生成对应效果。

**这不是本机发错了。** 请求走的是 `images/generations`，带的是纯文字；
是对面那个模型根本不做纯文生图 —— 有些中转站把这个端点转给一个对话式的
多模态模型，那种模型收到纯文字就回一句「请上传图片」。

原样转出来的话，看的人只会以为是自己少填了什么，于是反复去检查配置。
所以 `asError` 认一下这一类回话（中英文各几种说法），在原文后面接一句
点破它，自检里也单列一档「这个模型不做纯文生图」，下一步写明换一个真正的
生图模型（`gpt-image-1`、`dall-e-3` 一类）或换一套接口。

**模式匹配对面的话是权宜之计**，说法千变万化，认不出来时仍然原样转出。
但这一类太常见，认出一次就省一轮来回。

### 4.147 只改 CSS 的时候不要重建 DOM

生成器与美化列表的预览都画在 shadow root 里，原先是这么搭的：

```js
useEffect(() => {
  root.innerHTML = `<style>${base}</style><style>${skin.compile(row)}</style>` + buildStage();
}, [row?.id, row?.updatedAt]);
```

依赖里带着 `updatedAt`，于是**滑杆每动一格就把整块重搭一遍**：六条消息、
两条栏、十几个图标、每个图标里的内嵌图片全部丢掉重画。浏览器画的是新节点，
布局、图片解码、字体都从头来，屏幕上就是一阵闪。滑杆一秒能发几十次改动，
闪就连成了一片。

拖滑杆改的只有 CSS。**要换的是那段文字，不是那些节点。** 拆成两个 effect：

```js
// 只认 id。换一份美化才重搭
useEffect(() => {
  root.innerHTML = `<style>${base}</style><style class="skin-css"></style>` + buildStage();
  setReady(n => n + 1);
}, [row?.id]);

// 改样式时只写那一个 style 节点的文字，DOM 一个都不动
useEffect(() => {
  const el = root.querySelector('style.skin-css');
  if (el) el.textContent = skin.compile(row, { varsOn: ':host' });
}, [ready, row?.updatedAt]);
```

`ready` 是必须的：第一个 effect 里有一次 `await`（底样式是 fetch 回来的），
第二个 effect 先跑完时 style 节点还不存在。让第一个搭完之后推一下，
第二个才有东西可写。

同一条道理也用在倍率上：`scaler.style.transform` **一次写完**。从前先写
一个基准倍率再写最终值，每次改动都能看见画面弹一下。

`innerHTML` 整个重写是最粗的那一档 DOM 操作，代价随内容线性涨；
`textContent` 换一段样式表只走一次 CSS 重解析，节点全都还在原处。
凡是「界面没变，只是样子变了」的地方都该走后面这条。

`scratchpad/noflicker.mjs` 守着这件事。「闪」测不了，但「那几个节点还是不是
原来那几个」测得了：给气泡打上 `data-mark`，连改十次、再真的用鼠标拖一遍
滑杆，记号都还在就说明没重建；同时量 `getComputedStyle` 的背景色与圆角，
确认样式确实跟着变了。换成另一份美化时记号该消失 —— 那一次本来就要重搭。

### 4.148 「自检能过、发消息就超时」：手机上装的外壳还是 120 秒那一版

4.143 把外壳的期限从写死的 120 秒改成了每一次请求自带的数，可那是一处
**Swift 改动，只有重新打包安装才生效**。打包记录上，最近一只 ipa（第 22 次）
打自那次修复之前的提交 —— 手机上跑的 NetBridge 仍是
`timeoutIntervalForRequest = 120`，网页传过去的 `timeout` 它看都不看。

于是两条路真的不一样长：

| | 提示词 | 对面要跑 | 120 秒够不够 |
|---|---|---|---|
| 自检 | `a single small black circle on white` | 几秒 | 够 |
| 消息 | 画面描述 + 世界书 + 角色提示词 + 全局提示词，几百字，1024 见方 | 两三分钟 | 不够 |

而旧外壳超时时只回一句系统的本地化文案 `请求超时`，没有 `timedOut` 标志，
`viaNative` 把它当成普通错误抛了个 `TypeError`，上层照着报成
**「连不上生图接口」** —— 它明明连上了。「等待上限」调到 900 也一样在 120 秒
被掐，而屏幕上没有一个字说这个旋钮在这一版外壳上是摆设。

三处改：

- **外壳说清自己认不认期限。** `window.phoneNet` 从一个 `true` 改成能力表
  `{ timeout: true }`。`canNative` 只看真值，老代码不受影响；
  `shellTimeoutOk()` 看那一项，旧外壳自然是 false。
- **旧外壳的超时按超时报。** `viaNative` 在没有 `timedOut` 标志时认一下
  `请求超时` / `The request timed out.`（都是 `NSURLErrorTimedOut`），抛
  `TimeoutError(120)` 并带上 `oldShell`。`ask` 看到它就不再叫人去调
  「等待上限」，改说：外壳是旧版，期限写死 120 秒，重新打包安装后才按填的数等。
- **自检通过时就提醒。** 旧外壳上自检是能过的（那一张几秒就回来），
  等到发消息失败再说就晚了。所以 `testImage` 的通过提示、「等待上限」
  那一栏的说明都在旧外壳上带这一句。

`scratchpad/imgoldshell.mjs` 用两只假外壳各跑一遍：旧的
（`phoneNet = true`，超时只回 `{error:'请求超时'}`）与新的
（`{timeout:true}`，回 `timedOut`）。旧代码 8 条挂，新代码 17/17。

**Swift 那半边改了要记得打包，而且打包记录是唯一能证明「手机上跑的是哪一版」
的东西。** 这一回花在查代码上的时间，一句 `git log -- ios/` 对一眼打包记录
就能省下。

### 4.149 主页改成 Instagram 的版式

从前的主页是「封面 + 头像一行 + 一个大按钮 + 动态的纯文字列表」：
一张图都不露，动态只有一列字，而动态里最值得看的正是图。

现在照 Instagram 的个人页排：

```
头像   动态  照片  关系      ← 三个数字，点「动态」切到列表、点「关系」进关系网
名字
签名                        ← 只出 signature，人设一个字不露（CLAUDE.md 第 6 条）
[发消息] [编辑资料]          ← 我的主页是一个「编辑本人人设」
○ ○ ○ +                     ← 精选：一张图配一个名字，挂在这个人身上
▦ 网格 | ≡ 列表
■ ■ ■                       ← 一格一条带图的动态，多图角上有标记，点一格进详情
```

**同一个组件同时管我的主页与角色主页**（`chat/pages/Profile.js`），
所以两边一起换。三个数字：动态条数、动态里的图片张数、关系网里的人数
（我的主页第三个换成联系人数，小号不算）。

几处随之搬家：

- **封面去掉了**，只在朋友圈顶部用。换封面的入口跟着搬到朋友圈顶部
  （第 5 条：开关放在它起作用的地方）。
- **删除角色不再在主页上。** 编辑资料那一页已经有一个，同一个开关只留一处。
- 一条动态的卡片、评论表、删除，从朋友圈页里抽到 `MomentBits.js`：
  朋友圈、主页的列表分栏、详情页三处共用一份。从前只有朋友圈有，
  主页要列动态就得再抄一遍。

**详情页**（`/moment/:id`，`MomentPage.js`）是新的：图整幅按原比例看，
多张左右翻（scroll-snap），正文、时间、点赞、评论都在一屏，评论直接在页里写。
网格里点一格、列表里点一条、朋友圈里点图，都进这里。

**精选**存在 `highlights: [{ id, imageId, title }]`，角色挂在角色行上，
我挂在本人那一行上。不是新数据域，备份与角色包不用改；但它存图，
所以 `purge.usedImageIds` 与 `charpack.collect` 各补了一行 ——
漏了前者是「清理无引用」时删一批，漏了后者是导出的角色包里少一块。

#### 按钮那一行只放按钮

第一版把「原本的头像」那一块也塞进了按钮行。它 47 像素高，比 30 像素的
按钮高出一截，于是：整行被撑到 67 像素，右边被占掉五十多像素，
两个按钮既不满宽也不居中 —— 一眼看过去就是歪的。

**它说的是头像的事，就该贴着头像。** 现在是头像右下角一个 28 像素的圆形
角标，里面正是原本那张脸。角标上没有文字，所以点下去先问一句
「当前头像将被替换为更换之前的那一张」，不再默默换掉。

两个按钮同款同宽同高（`variant="ghost"`，30 像素），不带图标 ——
Instagram 那一行也是两个等重的浅底按钮，主次由文字自己说，不靠填充色压。

**这一类「多塞一个元素进某一行」的走样，量一下就看得出来。**
`igprofile.mjs` 量的是按钮宽度是否相等、两个按钮加起来是否铺满一行、
行里有没有第三个元素 —— 不是截图比对。前提是**测试数据里那个元素真的会出现**：
第一版这几条断言在旧版式上也过，因为种的角色没换过头像，角标根本没渲染。
补上 `avatarBase` 之后，旧版式当场挂 3 条（行高 67、空出 80）。

`scratchpad/igprofile.mjs` 把这一页从头点到尾：数字、网格与多图标记、
详情页评论、列表分栏、精选的建改删与引用登记、角色页的按钮与那一行的尺寸、
朋友圈的封面入口。

### 4.150 「选择语音模型」列出来的全是文本模型

MiniMax 那一页点「拉取并选择」，列表里是 `MiniMax-Text-01`、`abab6.5s-chat`、
`MiniMax-M1` —— 一个能说话的都没有。挑哪个都合成不出声音，而界面上看不出
哪儿不对：名字是真的，接口也是真的，只是这批模型不会说话。

**`/v1/models` 回的是文本模型。** 那个端点列的是这个账号能用的聊天模型；
语音合成走的是另一个端点（MiniMax 是 `/v1/t2a_v2`，OpenAI 兼容是
`/audio/speech`），**那批模型名不在那份列表里**。而这一页从前直接借用了
聊天那一套 `fetchModels({ provider: 'openai' })`，于是问错了地方。

三家的来源各不相同，所以 `voice.js` 里新开一个 `fetchVoiceModels`，按家分：

| | 列表哪儿来 | 为什么 |
|---|---|---|
| MiniMax | 内置一份 `MINIMAX_MODELS` | **它没有「列出语音模型」这个接口。** 不发请求，问了也白问 |
| ElevenLabs | `GET /v1/models`（`xi-api-key` 头） | 这一家的列表接口回的就是语音模型，还带 `can_do_text_to_speech` |
| OpenAI 兼容 | `GET /models`，再挑出像语音的那几条 | 只有一份混在一起的总表，文本、向量、重排、生图都在里头 |

三条配套的讲究：

- **内置的那份是备选，不是上限**（第 13 条）。新型号手填即可，不必等这张表
  更新 —— 所以界面上明写「以下是内置的常用型号」，免得新型号没列出来时
  以为是不支持。
- **挑剩下的不丢掉。** 中转站的命名千奇百怪，`tts` / `speech` / `audio` 这套
  猜法一定有猜漏的，所以给一个「显示全部」，猜错了有路走回去。
- **列表哪儿来的要说出来。** 内置的一份和接口拉回来的一份可信程度不一样，
  用户有权知道自己在看哪一种。

顺带修了 `models.js` 的 `normalize`：从前只认 `id` / `name` / `model`，
而 ElevenLabs 的字段叫 `model_id`。少认这一个，取到的就是它的显示名
（`Eleven Multilingual v2`），而接口要的是 id（`eleven_multilingual_v2`）——
填进去必然报错，且报错里看不出是大小写还是空格的事。

还有一处：这个选择器从前要自己先点一下「拉取列表」才有东西。那一步没有任何
理由让人来做，改成打开就拉，按钮留着当「重新拉取」。

`scratchpad/vmodels.mjs` 用三个假接口各跑一遍。里面留着一条
**「聊天那一套问 MiniMax，回来的全是文本模型」** —— 它不测新代码，
它说明的是为什么不能用旧的那条路。

### 4.151 会话里的短视频：海报加一个播放标记

新增一种消息 `kind: 'clip'`，两份东西：视频本身进 `files`，**开头那一帧另存成图
进 `images` 当海报**。气泡上先摆海报，点一下才换成播放器。

**为什么不是「按住播放」。** iMessage 上实况图是按住播放，而这个项目的长按
是消息菜单（第 12 条）—— 两边抢同一个手势，谁都不好用。所以点一下播，
长按照旧弹菜单。

#### 取帧只能靠浏览器自己解码

`system/clip.js` 的 `probe()` 拿一个 `<video>` 解码、`seek` 到 0.1 秒、
画进 canvas。不用 ffmpeg.wasm —— 那一份三十二兆，为一张海报把它拉起来
不值得（见 4.x 的 system/ffmpeg.js）。

代价是**浏览器不认的编码取不出帧**。那时候 `probe` 不抛异常，回
`poster: null`：海报只是好看，没有它这段视频照样存得下；为它把整条路断掉
不值得。气泡上摆一个占位，点开能不能放同样是浏览器说了算 —— 这两件事
本来就是一回事。

`<video>` 上 `muted` 与 `playsInline` 缺一不可：iOS 上不静音、不内联，
就不许在没有用户点击的情况下解码。另外认不出的编码会一直停着，
既不 `loadeddata` 也不 `error`，所以有一个十秒的兜底。

#### 两份文件要登记四处

漏一处的后果都是安静的（第「备份完整性」条）：

| 哪儿 | 登记什么 | 漏了会怎样 |
|---|---|---|
| `purge.usedImageIds` | `m.posterId` | 点一次「清理无引用」，海报全没 |
| `purge.fileUsers` | `m.clipId` | 同上，视频全没 |
| `purge.dropChat` / `clearHistory` | 两份都删 | 删会话之后留成孤儿，占着空间谁也找不到 |
| `charpack.collect` | 两份都带 | 导出的角色包里视频是空的 |

#### 测试环境解不了 H.264

`scratchpad/clip.mjs` 头一版拿现成的 mp4 去测，海报和时长都是空的。
量了一下：这套 Chromium 的 `canPlayType('video/mp4; codecs="avc1.42E01E"')`
回的是空串 —— **它没有 H.264**，而 iOS 上 H.264 是原生的。所以那不是代码的事。

改成让浏览器自己用 `MediaRecorder` 录一段 VP8 的 webm 来测主路径；
那个 mp4 正好留下来测另一条：**解不出帧的照样发得出去**，海报留空，
气泡上是占位，点得开。两条都测到了，比原来那一版更全。

### 4.152 生成视频：一个异步任务，不是一次请求

MiniMax 的视频生成和生图不是一条路：

```
POST {base}/v2/video_generation                  回 { task_id }
GET  {base}/v2/query/video_generation/{task_id}  回 { task: { status, content: { url }, error } }
status 依次是 queued、running，然后 succeeded 或 failed 或 cancelled
```

整个过程通常一到五分钟。这带来三件现有代码没有的讲究：

**只有「提交」那一下走 AIQueue。** 队列一共两个并发位，把五分钟的轮询塞进去，
聊天那边就排在后面动不了。轮询是几个很便宜的 GET，它不该占位。

**task_id 必须落库。** 提交完立刻写回那条消息（`clipTask`），之后才开始等。
切页面、锁屏、重开应用都不要紧 —— `main.js` 启动时叫一次 `resumeClips()`，
把 `media === 'pending'` 且有 `clipTask` 的接着等下去。**不重新提交**：
任务一直在对方那边跑着，重提一次是再花一份钱换同一段视频。

**产物链接是限时的。** 文档写明「请及时下载或转存，过期后可重新查询获取」，
所以拿到就立刻取回来存进 `files`，不把那个链接当结果记下来。取回来还要看一眼
是不是视频 —— 链接过期或被网关挡住时回的常是一页 HTML，状态码照样 200。

#### 多套配置，一套生效

和生图预设同一个形状（`services.videoPresets()`）。**多套不是为了好看**：
同一套接口有好几个中转站在转，地址和密钥各不相同，换一家试试不该是
「把原来那套改掉」—— 改掉就回不去了。地址可填，所以转这套接口的任何一家
都用得上。

模型那张表（`MiniMax-H3` / `MiniMax-H3-Max`）是**备选不是上限**（第 13 条），
名字自己填得进去。但两档的分辨率限制不一样（H3-Max 不支持 2K），
所以在界面上直接说出来，而不是等接口回一个 400 再让人自己猜是哪一项不对。

#### 等着的时候要说人话

生成一段要一到五分钟，**一个光转圈的气泡在这种长度上说明不了任何事** ——
分不出「还在排队」和「已经卡死」。接口自己会说（`queued` / `running`），
每问一次就把状态写回消息，气泡上照着显示。

超过「最长等待」就不再问了，但报错里要写明**任务仍在对方那边运行、没有被取消、
编号是多少**，别让人以为钱白花了。

#### 自检只提交，不等它跑完

跑完要几分钟，而那一页要回答的是「这套配置对不对」—— 提交成功就说明地址、
密钥、模型、参数都过了关。真去等一遍还要花一次生成的钱。

`scratchpad/vgen.mjs` 用一个假接口跑完整条路：提交、三次轮询
（queued、running、succeeded）、取回、存下、取海报，并且核对发出去的
请求体长什么样。最后一条钉的是**重开之后接着等的那一条一次都没有重新提交**。

#### 角色自己发视频：默认关着

角色写 `[视频：…]` 就生成一段，和 `[图片：…]` 同一套。**默认关着**
（第 15 条）：一条回复里写一个标记就多一次生成，而生成视频按秒计费，
比生图贵得多，还要跑几分钟。

照那一条的做法登记在一处：`cost.js` 的 `EXTRA_CALLS` 多一行 `videoOn`，
于是「用量与上限」那一页自动把它算进账里，`check-calls.mjs` 也会盯着
「新加了一项却默认开着」。开关三层：全局那一个、角色卡上的
`canSendVideo`、以及没配视频接口时压根不给。

`[视频：` 这个方括号标记**留在中文**（第 14 条的三样例外之一）：它是协议，
`reply.js` 按它切分，库里的历史消息也存着它。模板正文照旧是英文。

`scratchpad/vcap.mjs` 钉这几条：默认值确实是关的、确实登记在账上、
标记切得出来且不吞掉前后正文、关着时不进 prompt、落成的消息 content
就是那个标记、以及模板正文里没有中文。

### 4.153 生成之前先把提示词写好

角色在回复里写 `[图片：乃木喵和调色盘]`。这句话在对话里说得通，交给生图接口
就什么都不是 —— **那个模型收不到这段对话**，「乃木喵」对它来说只是三个字。
上游因此回过 400：「请说明需要生成的具体对象」（见 4.145）。

语音同理：一句话该用什么语气，只看那行字是读不出来的。视频更甚，
它还要知道什么在动、怎么动。

所以多一步：**把只有这段对话里成立的说法，翻成一份自带全部信息的描述。**
`system/ai/promptwrite.js`，三个入口：

| | 做什么 | 回什么 |
|---|---|---|
| `forImage` | 改写画面描述 | 一段新的画面描述，后面照旧接世界书与全局提示词 |
| `forVideo` | 改写画面与运动 | 同上，多了「什么在动、怎么动」 |
| `forVoice` | **不改那行字**，只另给一句语气说明 | 十二个词以内：情绪、语速、音量 |

指令存在 `task.image-prompt` / `task.video-prompt` / `task.voice-prompt`，
和别的模板一样可以改（第 11 条），正文是英文（第 14 条）。

#### 「乃木喵是什么」的答案要跟着一起过去

**只给那一句短话，这一步只是把一句短话换成另一句短话**，那个 400 照样会回来。
所以把角色名、角色设定的前六百字、最近十二条对话一起送过去 ——
答案就在里面。

#### 三样各一个开关，一律默认关着

它是**实打实的第二次接口调用**（第 15 条）：角色发一张图，就先写一次提示词、
再生成一次。所以三样各一个开关，默认关着，各登记一行进 `cost.js` 的
`EXTRA_CALLS`，「用量与上限」那一页自动把它算进账里。

走的是**副用接口**（第 15 条那张表：只有对话回复与通话走主用）。
写提示词慢一点没人会发现，不该和聊天抢同一套通常更贵的接口。

#### 写不出来就用原话

这一步是锦上添花。它超时、报错、回一句空话，都只是退回角色本来写的那句，
**不让整张图发不出去**。为了改写而把生成断掉，比不改写更糟。
语音那一路退回的是空串，于是沿用角色卡与全局那一份语气。

模型爱在外面套一层代码围栏或者引号，收回来时剥掉 —— 那一层跟着送进
生图接口，就成了要画的内容的一部分。

#### 只在聊天那条路上

朋友圈配图与角色自拍的描述本来就是模型照一份任务规格写出来的，不是随手一句，
没有这个毛病。这里只接聊天里那三个标记。

`scratchpad/pwrite.mjs` 18/18：默认关着且一次都不调、开了之后确实多调一次、
上下文（角色设定与最近对话）确实带过去了、围栏与引号剥得掉、
失败退回原话、以及真发一张图时**送给生图接口的是改写之后那一句**。

### 4.154 一个分组永远发不出来，以及一个骰子带歪整轮翻译

两桩报上来是两件事，其实一件事的两头：**名单被谁占满了**、
**队伍被谁插队了**。

#### 表情：自己锁死自己的圈

`stickerNames` 从前把所有表情按 `useCount` 从多到少排一遍，取前 N 个
（冷的时候 12 个）。分组一多就出一个闭环：

```
某一组先用起来 -> 计数涨上去 -> 名单被它占满
        ^                              |
        |                              v
   计数永远是 0  <- 永远发不出来 <- 模型不知道有这些名字
```

另一组一次都没被列出来，模型根本不知道有这些名字，也就永远发不出来，
于是永远进不了名单。表现出来正是「有一个分组不能用」。

改成**各组轮流取一个**：每一组都至少露一个名字，组内仍然按用得多的排在前面。
名额一个都不少给，只是换了个分法 —— 12 个名额三个组，仍然是 12 个名字。

#### 骰子：掉一次格式，后面每一句译文都挂错

`[骰子]` 那一类是「整行就是它」的标记，从前的正则要求整行一个字不差。
模型写出来常有三种走形：

| 写成 | 从前 |
|---|---|
| `1. [骰子]` | 认不出 |
| `[骰子]。` | 认不出 |
| `[骰子：谁先说]` | 认不出 |

认不出的后果**不止是多一个丑气泡**。那一行会变成一条普通正文，
于是它排进了「等着配译文」的队伍 —— `attachTrans` 按顺序把第 n 句译文
配给第 n 条还没有译文的正文，队伍里凭空多一个，后面每一句就全挂错一条。
**一个骰子能把整轮的翻译全带歪**，而屏幕上看不出这两件事有什么关系。

两处改：

- **`lineMark(words)` 一处定义**，拍一拍、骰子、开通共同账户、批准、去电
  都用它生成。序号前缀和尾随标点译文那几条早就认了（见 `TRANS_HEAD`），
  这里照同一套认；括号里多写的那一句直接丢掉 —— 这类标记本来就没有正文可带。
- **第二道防线**：整行是一个方括号标记、却没被任何规则认走的那种，
  照旧落成气泡（丢掉的话模型真想说的话也会跟着没），但**不参与配译文**。
  走形的花样认不完，认漏了至少别把整轮翻译带歪。
  只认方括号那两种 —— 圆括号太常出现在正文里（「（笑）」），拿它当标记会误伤。

`scratchpad/stkdice.mjs` 12/12（旧代码挂 7 条）：三个分组在名额只有 12 时
都还露得出名字、骰子那五种写法都认得出、以及认不出的标记自己不占译文。

### 4.155 手动整理：规则认不完，得留一条自己动手的路

4.154 把骰子那几种走形认了进去，但**走形的花样是认不完的**。模型偶尔写出
第六种、第七种，本地一条规则都命中不了 —— 那时候「修正格式」那一页列出来
是一句「未发现格式问题」，人就没有出口了。

所以底下永远留一条手动的：**把原文摆出来让人自己改一个字，再按标记重新分条。**

#### 改的是原文，不是气泡

一旦掉了格式，那一行早就被拆成好几个气泡了，挨个改改不回来 ——
而在**原文**里它还是完整的一行。原文存在整轮第一条上（见 `renderTurn` 的
`raw`），所以从这一轮的任何一条都取得到：`manualSource(msg)` 回
`{ text, scope, count }`，取得到就整轮一起重排，取不到（用户自己发的、
或者是旧数据）就只动这一条。

`previewSplit(text)` 让那一栏一边打字一边说「会分成几条、其中有什么」，
不必先应用再看结果。改过的文字同时存回 `raw`，下次接着这一版改，
而不是又跳回模型最初那一版。

#### 顺带揪出来的：骰子不属于它那一轮

写这一条的测试时发现：手动重排之后骰子那一条不见了。

`materialize` 的骰子分支调的是 `extras.roll({ chatId, role })` ——
**`turnId`、`authorId`、`createdAt` 一个都没带过去**，而它上面那一段注释
恰好写着「用 row 建，不自己拼字段：turnId 在里面，重新生成这一轮时它才跟着
被清掉」。同一个文件里，一条守着，一条没守。

后果不止手动重排：**「重新生成这一轮」按 turnId 删，删不到它**，
它就留成孤儿挂在那儿，点数还是上一轮的。`roll` 加一个 `row` 参数，
角色那条路把整行底子交下去；用户自己点骰子那条路不传，照旧。

`scratchpad/manfix.mjs` 13/13：造一轮本地规则一条都认不出的回复
（`《掷个骰子吧》`），确认「修正格式」确实列不出任何一项，
然后走手动那条路把它改回 `[骰子]`，整轮三条换成 text、dice、text。

### 4.156 角色发的表情认不出名字，气泡就掉成一行字

报上来是「一个分组能渲染，另一个分组全显示文字」。**和图片、和渲染都无关** ——
用户自己从那个分组发照样有图。差别只在一条：

| 谁发的 | 怎么找到那个表情 |
|---|---|
| 用户 | 点面板里的那一格，`stickerId` 直接就是它 |
| 角色 | 写一个名字，`byName` 拿名字去找 |

`byName` 返回空，`stickerId` 就是 null，气泡于是回退成 `表情：名字` 那一行字。
所以这件事从头到尾只是**名字没对上**。

（一度往「远程图取不回来、浏览器把 `alt` 当文字画出来」那边查，方向是错的：
用户自己发得出图，图本身没问题。）

#### 两边看着一样、比起来却不等

`byName` 从前只做 `trim().toLowerCase()`。这几种看着一模一样却不相等：

| | 存的 | 模型写的 |
|---|---|---|
| 全角与半角 | `（笑）` | `(笑)` |
| 全角字母 | `ＯＫ` | `ok` |
| 零宽字符 | `开心\u200b` | `开心` |
| 扩展名 | `开心.png` | `开心` |
| 包裹符号 | `开心` | `「开心」`、`"开心"`、`[开心]` |
| 多一个空格 | `开心  1` | `开心 1` |

`normalizeName` 一次抹平：NFKC、去零宽、去扩展名、剥掉包裹的符号、
收拢空白。四级匹配：精确、关键词、去掉所有空白再对、最后才是包含匹配。

#### 名单里的顿号会把一个名字劈成两个

`stickerNames` 从前用 `、` 把名字连起来，**而名字里本来就可能有 `、`** ——
`开心、大笑` 在模型眼里就是两个名字，写出来哪个都对不上。改成一行一个。

#### 认不出的时候，让人一键指认

走形的花样认不完（和 4.155 同一个道理）。所以认不出时那一行字现在可以点：
点开是表情面板，选一个，这条消息就挂上它，**并且把模型写的那个名字记成
这个表情的关键词** —— 下次它再写同一个名字，自己就对上了，不必每次来指认。

`scratchpad/stkname.mjs` 19/19（旧代码挂 5 条）：十种看着一样的写法都认得出、
名单一行一个、认不出时点得开、指认之后记住了名字、再问一次自己就对上了。

#### 真正的分组毛病：两个组里有重名

上面那些是名字**走形**，而「一个分组能用、另一个不能用」是另一回事：
**两个组里有同名的表情。**

两套表情包都带「开心 / 生气 / 难过」很常见。于是：

```
名单：  - 开心      <- 日常组的
        - 开心      <- 猫猫组的，字一模一样
模型写：[表情：开心]
byName：返回 all() 里的第一个，也就是先建的那一组
```

**后加的那一组里凡是重名的，角色一个都发不出来** —— 名字写对了也没用，
指向的永远是另一组那一张。把两组的名字列出来一对，正是「一个分组能用，
另一个不能用」。

改法是让名单本身不含糊：**重名的带上分组**（`开心（猫猫）`），
`byName` 认这个形状，先按「名字 + 分组」找，找不到再走原来那四级。
不重名的照旧只写名字 —— 绝大多数表情不重名，给每一个都缀上分组
只是把名单撑长，而名单是要占 token 的。

#### 管理页上直接说「这几个角色发不出来」

判据只有一条，也是这件事的充要条件：**拿它自己的名字（名单上的那个写法）
去找，找不找得回它自己。** 找不回来的，角色写对了也发不出去。

`unreachable()` 把这些摆到表情管理页顶上，连原因一起写：是重名（会发成
哪一个），还是压根没有名称。这样下一次不必再靠猜。

`scratchpad/stkgroup.mjs` 9/9：重名的带分组、不重名的不带、两组都列得出、
照名单的写法每一个都找得回自己、以及从前那几个确实是发不出来的。

### 4.157 让它发视频，结果调了生图

三件事凑在一起，看上去像是「视频请求被路由到了生图」。

#### 一、开关默认关着，角色就去发图片

`videoOn` 默认是关的（第 15 条：会多花一次钱的一律默认关）。关着时视频这项
能力压根不进 prompt —— 于是被要求发视频时，角色做的是它手上有的那件最接近的事：
**发一张图**。这不是路由错了，是它没有这项能力。

从界面上看不出来，所以「生成视频」那一页现在会说：接口配好了但开关是关的，
角色不会主动发视频、被要求时通常改发图片，开关在「用量与上限」。
**不在那一页再放一个开关** —— 同一个开关只能有一个入口（第 5 条），
这里只是把状态和去处说出来。

#### 二、`[video：…]` 掉进了语音分支

`MARK` 那张表里认了 `视频|video` 两个词，而 `CLIP_KINDS` 只收了 `视频`。
落 part 那一段是一串 if：

```js
push(IMAGE_KINDS.has(kind) ? { type: 'image', … }
  : CLIP_KINDS.has(kind)  ? { type: 'clip', … }
  : STICKER_KINDS.has(kind) ? { type: 'sticker', … }
  : { type: 'voice', … });          // <- 最后这一档兜底
```

少收一个词，那一条就一路掉到**最后的 voice 分支**，变成一段语音。
**MARK 里认了几个词，这几个 Set 就要收几个**，否则多出来的那个词有一个
静悄悄的去处。

#### 三、base 带着版本号时，端点被带歪

`/v2/video_generation` 这个路径**自带版本号**。而生图那边的路径
（`/images/generations`）不带，它的拼法是「base 没有版本号就补一个」。
照抄过来就成了「base 有版本号就把路径上的摘掉」——

```
base = https://relay/v1   path = /v2/video_generation
从前  ->  https://relay/v1/video_generation     不是 V2 的端点
现在  ->  https://relay/v2/video_generation
```

照着生图那套配置把 `.../v1` 抄进视频接口是很自然的事，所以这条一定会被踩到。
改成：**先把 base 尾巴上的 `/vN` 摘掉，再接完整路径**，带不带都对。

`scratchpad/vroute.mjs` 11/11（旧代码挂 5 条）：四种写法都走视频、图片语音
各走各的、四种 base 都打到 `/v2/video_generation`、开关关着时那一页说得出原因。

### 4.158 名字翻成外貌：本地的、免费的、默认就开着的那一道

角色写 `[图片：乃木绿实在窗边]`。**生图与视频模型收不到这段对话**，
「乃木绿实」对它来说只是四个字，画出来当然不是她。宠物同理：
`乃木喵` 是什么猫，只有这边知道。

这件事从前有两道防线，**两道都不顶用**：

| 防线 | 为什么不顶用 |
|---|---|
| 提示词里叫角色自己写清楚长什么样（4.145 给 `skeleton.image` 加的那句） | 它记不住，照样写名字 |
| `promptwrite` 另调一次接口改写（4.153） | **默认关着**，而且要多花一次钱 |

于是默认情况下，**没有任何一步把名字翻成外貌**。

#### 第三道：角色卡上写，本地拼

角色卡新增**外貌**字段（`appearance`）。`imageprompt.js` 的 `appearanceOf`
拿画面描述扫一遍：**哪张卡的名字出现在里面，就把那张卡的外貌接在后面。**

不调接口、不花钱、默认就开着。**也不改用户写的那句话** —— 描述原样排在最前面，
后面只是多了一段「这个名字长这样」。

**不止当前这个角色。** 扫的是所有角色卡：另一个人、一只做成卡片的猫，
点到名字都带上。只修主角那一半没有意义 —— 「乃木喵」和「乃木绿实」
是同一个问题。

两条边界：**一个字的名字不参与匹配**（那种名字在任何一句话里都撞得上）；
**没手写外貌时退回 `faceDesc`**（锁脸那条路读出来的那一段，见 4.x）。

#### 视频那条路从前什么都没拼

`generateClip` 把那句话原样发出去 —— 没有外貌、没有生图世界书、
没有角色固定提示词、没有全局提示词。那几段说的都是「这东西长什么样」，
对视频一样成立，没有理由只给图片用。现在走 `imgPrompt.compose` 同一套，
抓包里也逐段标出来。

#### 导入角色卡时顺手抽出来

`task.card-import` 多一个 `appearance` 字段，规则里写明它发给的是一个
收不到对话的模型：只写画得出来的东西，不写名字、不写性格、不写来历。
不然每张导进来的卡都要手填一遍，这条防线就形同虚设。

`scratchpad/lookinj.mjs` 11/11（旧代码挂 6 条）：点名的带上、别人的卡也带上、
没点名的一个不带、一个字的名字不参与、没手写时退回锁脸那段、
以及生图与视频两条路发出去的都确实带上了外貌。

### 4.159 内置生图预设，以及从人设里一键抽外貌

#### 一个预设分两段，因为「画得好」落在两个地方

`style.daily`（日常拍照分享）：

| | 加在哪儿 | 说什么 |
|---|---|---|
| `ask` | **写描述的那一方**：角色写 `[图片：…]` 时那份说明，以及 `promptwrite` 改写那一步 | 场景、环境、构图、光线；画面里有人时写明穿着，并与场景相称 |
| `tail` | **最终提示词**末尾，和全局提示词并排 | 成片像随手拍下来分享的照片，不像棚拍、不像海报 |

只做 `tail` 不够：**光有画风没有内容，画出来的还是一句话那么空。**
只做 `ask` 也不够：描述再全，成片还是会往「作品照」上跑。

正文存在模板里（第 11 条），可以改；英文（第 14 条）。
用户自己的全局提示词仍然排在最后 —— 那是他写的，让它有最后一句。

**默认一个都不开**，开关在「设置 - 生图 - 内置预设」。开预设**不增加接口调用**，
所以不进 `EXTRA_CALLS`：它只是往已经要发的那串文字里多接一段。

这和第 16 条不冲突：那一条管的是**内置提示词不替角色作判断**，
而这里是用户自己打开的一档画风，默认关着，开不开是他的事。

#### 从人设里一键抽外貌

4.158 给角色卡加了「外貌」，但外貌本来多半已经写在人设里了 ——
让人再抄一遍没道理。编辑资料页那一栏下面加一个按钮：读上方的人设，
调一次接口整理出外貌，覆盖这一栏。

`task.appearance` 的规格与导入角色卡那一份同源：**只留画得出来的**，
不写名字、不写性格、不写来历 —— 它发给的是一个收不到对话的模型。

**手动点出去的一次调用，不进 `EXTRA_CALLS`**（第 15 条管的是「发一条消息
变成两次请求」）。和「核心设定」那个按钮同样的道理。

`EditPage` 里那个 `useState` 放在提前 return 之前 —— 角色被删掉那一次
组件会走另一条路，hook 少调一个，数量就对不上（doctor 的 hook 顺序那一项）。

`scratchpad/imgstyle.mjs` 17/17：默认关着时两头都干净、开了之后两头都有、
全局提示词仍在最后、界面开关存得下、以及那个按钮真的把外貌填了进去。

### 4.160 通话：念原文、看译文、声音留下来、挂断有总结

#### 翻译：台词只管说，翻译另翻一道

从前通话里的翻译两档都不对：

| 这段对话的翻译走 | 从前在通话里 |
|---|---|
| 随回复给出（inline） | 模型在台词后面写 `[译文：…]`，这一行被**原样念出来**、原样混进字幕 |
| 单独的翻译接口（api） | 一个字都不翻 |

inline 那一档还有一层矛盾：`skeleton.call` 明文写着「不要用任何方括号标记」，
而翻译那一段又叫它写 `[译文：…]` —— **两句话在同一份提示词里打架**。

改成：**通话里一律另翻一道。** `call.js` 拼提示词时用 `plain(chat)` 把这段对话的
翻译设置摘掉，模型只管说原文；角色说完一整轮，`translate.runAny` 单独翻 ——
配了翻译接口走翻译接口，没配走副用，**两条路用的是同一份 `payload()`**：
只送要翻的那几行和规则，人设、记忆、对话一个字都不送。

**送进语音接口的永远是原文。** 字幕在原文下面另起一行显示译文。

开关就是这段对话自己的「翻译」设置（第 5 条），没开就一次都不翻。
普通聊天在 inline 档不另花钱，而通话里每一轮多一次，所以另登记一行
`callTranslate`，翻译设置页上也写明了这一点。

翻译比挂断慢一步是常事：挂断之后才翻回来的那一句，写回刚落下的那条通话记录，
不然最后一句在记录里永远没有译文。

#### 声音：从前播完就扔了

`playOne` 从前 `voice.speak` 拿到 URL、播完就 `revokeObjectURL` —— **声音直接扔了**，
通话结束之后回不去听、也下载不了。现在每一句合成出来先存进 `files`，
挂在那一轮的 `audio` 清单上（按句切的，所以一轮可能好几段）。

只有**走语音接口**的才有得存。浏览器自带那档是现念的，自己说的话是现场识别的，
都没有文件 —— 通话记录里照实说，不假装有。

整通电话下载时按顺序接成一个 mp3：各家语音接口回的都是 mp3，按帧存，
首尾直接相接大多数播放器都认。

存下来的声音登记了四处（`fileUsers`、`dropChat`/`clearHistory`、`charpack.collect`），
`purge.callAudio(m)` 一处定义三处用。

`finish` 里**先抄一份 callLog 再存**：每一行的 audio 是那一轮的 bucket 数组，
挂断时还可能有一句在合成、回来往里 push，不抄的话那一下改的是库里那条记录的
内存副本，重开应用就没了，文件却留成孤儿。

#### 顺带修的：挂断之后手机还在说话

挂断时正在合成的那一句，合成完照样 `new Audio` 播出来。`hush` 本来会换代
（`era++`），可 `playOne` 从没看过那个代号。现在合成回来先看一眼，代号变了就作罢，
那一句也不存 —— 它没被听见，存下来反而对不上。

#### 总结

`call.summarize(msgId)`：`task.call-summary`，写成**这段对话设置的翻译语言**，
没设就跟通话本身同一种语言 —— 那是写给人看的，而人已经在这段对话里说过
自己要看什么语言了。

自动那一档（`callSummary`）登记在 `EXTRA_CALLS`，**默认开着** —— 用户点名要的，
是第 15 条一个登记在案的例外：`scripts/check-calls.mjs` 的 `ALLOW_ON` 里写着
它和理由，没登记的新项照样拦。老数据没写过这一项的按开着读（`!== false`）。
通话记录最上面另有按钮，随时手动生成。

`scratchpad/calltr.mjs` 21/21：用一个说日语的角色打一通 inline 档的电话，
看着语音接口到底收到了什么（全是日语，一句中文都没有），字幕有译文，
声音存进了每一句，挂断后有总结，整通能接成一个文件，删会话时声音跟着删。

### 4.161 语音台本：语音世界书，以及句子里的停顿与情绪

从前「合成语音前先标一遍语气」只回一句不超过十二个词的语气说明，整句一个腔调。
用户要的是配音台本那种：**哪里停、哪里换情绪，标在句子里面**，而且标法由
自己写的世界书决定。

#### 标记（协议，留中文）

```
<情绪 平静>你回来了。<停顿 0.8><声音 叹气><情绪 生气>今天怎么这么晚？
```

- `<停顿 秒数>`：不写秒数按 0.5
- `<情绪 词>`：从这里起用这个情绪，直到下一个情绪标记
- `<声音 词>`：这里发出一声不是字的声音（叹气、笑、吸气、咳嗽）

尖括号不用方括号：台本常写在 `[语音：…]` 里，方括号会把外面那一层提前收口。

#### `ai/voicescript.js`：只管翻成各家的写法

2026-09 查过各家文档（MiniMax 官网被这边的网络挡住，看的是 GitHub 上的转载），
`dialectOf(kind, model)` 按型号分：

| | 停顿 | 情绪 | 声音 |
|---|---|---|---|
| MiniMax | `<#0.8#>`，0.01 到 99.99，两个之间必须有字 | `voice_setting.emotion`，一次请求一个 | 只有 speech-2.8 认，`(sighs)`，固定十九个 |
| OpenAI 兼容 | 没有写法，写省略号 | `instructions`，自由文本 | 丢掉 |
| ElevenLabs | `<break time="0.8s" />`，最长 3 秒 | 没有，丢掉 | 丢掉 |
| ElevenLabs v3 | **不认 `<break>`**，写 `[pause]` 等 | 写在句子里 `[angry]` | `[sighs]` |

MiniMax 的 emotion 只认 happy sad angry fearful disgusted surprised calm，
fluent 与 whisper 只有 speech-2.6 认。**从前的词表里有一个 neutral，它不在枚举里**，
角色卡风格填「中性」「平淡」的人整个请求都被退回来。现在「平淡」算 calm，
认不出的一律不送（`mmEmotion`）。

情绪走参数的那几家，一句台本里换几次情绪就**切几段**，一段一次，
回来的音频按顺序接成一条。都按字数计费，切段不多花钱。
段首段尾的停顿丢掉（MiniMax 不收两边没字的间隔），连着的停顿合成一个。
不走参数的整句一段。

**没有标记时 `speak` 一个字都不变**，还是从前那一次请求。

#### 世界书多一个用途

`forImage` 旁边加一个 `forVoice`，折成 `purposeOf(b)`：`chat` / `image` / `voice`
三选一。书页面上原来那个「只用于生图」开关换成三段的「用途」。
老数据照读，两个都没有就是对话。改用途时两个字段一起写（`purposePatch`），
不留一个残着的 true。

语音书和生图书同一个理由要拆出来：「这里停顿半秒」漏进对话，
角色开口就是一股导演笔记的味道。所以它**不进对话、不进生图**。

#### 谁来写台本

**语音消息**：`promptwrite.scriptFor`。拿这一句加前四条消息扫语音书的关键词，
命中的与常驻的一起交给 `task.voice-script`。**只许加标记，不许改台词** ——
去掉标记之后与原句比（去空白与标点），对不上就不用，照原句念。
台本存在消息的 `voiceScript` 上，正文 `[语音：…]` 仍是原句。
开关是原来那个 `writeVoicePrompt`，改名「合成语音前先写成台本」，默认关，
每条语音多一次调用。

**通话**：台词是边说边念的，另写一遍来不及，所以**不另调接口**，
让通话模型自己在台词里标（`skeleton.call-script`，同一个开关）。
语音书整本给出去（`voiceBookText`）：开口之前不知道这句会说什么，没法先扫，
带关键词的条目把关键词写在前面，由它自己对。

- 字幕、历史、翻译、通话记录用去掉标记的那句（`vs.plain`，半个标记也去掉）
- 带标记的原文存在那一行的 `script` 上，下一轮原样还给模型 ——
  历史里一个标记都没有，等于每轮都在示范「不必标」
- 按句送进语音接口时情绪要跨句带着走：上一句换成「生气」，下一句没写就补一个
  `<情绪 生气>`，否则每句都回到默认
- 浏览器自带的朗读不认标记，念的是去掉标记的那句

`scratchpad/vscript.mjs` 54/54：拆段与各家各型号的写法、MiniMax 情绪枚举、三种用途互不串、
写台本关着原样开着带语音书、改了台词退回原句、MiniMax 按情绪分两次请求
且音频接成一条、通话字幕无标记而历史带标记、情绪跨句、关着时通话提示词里没有那一段。

### 4.162 群聊

4.7 的设计定于第 15 条之前，默认是「每个角色单独调用」。落地时按第 15 条改了：
**默认一次调用写整轮**，单独调用做成开关。

#### 数据：群就是一段会话

`chats` 上 `group: true`、`characterIds` 放两人以上、`title` 可空（空时用成员名拼）。
消息、分页、搜索、美化、备份全按会话算，不用改。`system/group.js` 管成员、群名、@。

**要 `group` 标记，不只看人数**：全库十几处「找这个角色的私聊」写的是
`characterIds.length === 1`。一个群剩一人就会被认成那人的私聊，于是有两段私聊。
所以成员不少于两人：界面上移不到一人，删角色删到只剩一人时整个群删掉（`purge.dropCharacter`）。

#### 生成：一次调用，按名字拆回

`engine.buildGroupSystem`：

- 全群一份：世界书（成员各自激活后取并集）、`user`、`time`
- 每人一份，写在 `[成员：名字]` 下：人设、情境、说话示例、核心设定、关系底色、
  钉住的、最近记下的、本轮召回
- 一对一才有的块（空间、距离、一起听、账本、出行、日程……）不进群
- 能力只给群里说得通的几样：图片、视频、语音、表情、引用、骰子、时间、译文
  （`capabilities.js` 的 `GROUP_CAPS`；某一样只要有一个成员开着就给）
- 召回每人算一份，**只走本地那一档**，不走重排：重排按成员算就是每轮 N 次调用

格式（`skeleton.group-rules`）：每行 `名字：台词`，没名字的行接上一个人。
历史里成员说过的话都是 assistant，前面带名字，和要它写的格式一样。

`reply.splitSpeakers` 把一整段拆回各人：名字加粗、套括号都认；写成用户名字的
几行整段丢掉；一个名字都没有就整段算第一个被点名的，再不行算第一个成员。
然后每段走原来的 `splitReply` 与 `materialize`，图片、语音、表情照旧生成，
只是 authorId 是那一段的人。一对一的 `renderTurn` 与群的 `renderGroupTurn`
共用 `renderPlan`，译文、通知、停顿都是同一套。

#### @

发出去那一下由 `group.mentionsIn` 认名字、存 id（`mentions`），改名不影响。
长名字先认，免得「小林」吃掉「小林子」的一半。这一轮被 @ 的人写进末尾的
`[本轮点名]`：那是用户的指令，不是替角色作判断。输入框里打 `@` 出成员条，点一下补全。

#### 每个角色单独调用（`groupPerChar`，默认关）

登记在 `EXTRA_CALLS`，「用量与上限」里一个开关，`cost.perTurn` 按人头算。
开口的是被 @ 的那几个；没 @ 人就全体按顺序各说一次，后说的看得见先说的。
每人走一对一的 `streamReply`，只看自己的角色卡（`skeleton.group` 那一段告诉它群里还有谁）。

入口只有 `ai/group.js` 的 `run`：会话页、「过一会儿才回」的后台那一路（`pace.runDue`）都走它。

#### 记忆

提取时（`memory-extract`）群里记下的事**每个成员各存一份**（他们都在场），
同一件事的几份共用一个 `twin`，`updateId` 指到任何一份就几份一起改。

群资料里的开关「群里的事带进私聊」（`chat.groupMemory`，默认带进）。
关掉以后新记下的几份带上 `scopeChat = 群 id`：

- `listFor(charId, personaId, chatId)` 只在 `chatId` 对上时给，不给 chatId 一律不给
  —— 私聊、朋友圈、日程这些地方都拿不到
- 不进关系底色（`bond.sourceOf`）
- 不和范围不同的记忆互相顶替（`memcheck.settleNew`）
- 删群时一并删（别处再也用不上它），导出角色包时不带
- 记忆 app 里标着「仅限群「…」」

#### 群里没有的

转账、礼物、通话、一起听、一起看、外卖、申请、共享位置、线下、心声、拍一拍、
自动回复、主动找你、导出角色、关系底色、「我们」。面板里不出现，菜单里不出现。
这些都是对着一个人的。「清空这个角色的聊天记录」只清它自己的会话，不动群。

群不进角色包：其他成员不一定在包里，装到别处是一个缺人的群。

`scratchpad/group.mjs` 46/46。

#### 群里主动开口

群自己的开关（`chat.groupProactive`：on、平均间隔、免打扰起止），在群资料里，默认关。
和一对一那套各管各的：一对一的在角色卡上（会话菜单「这个角色 - 主动发起对话」），
管的是那个角色找你；这里管的是群冷了一段时间之后有人先说话。

调度搭在 `proactive.tick` 上，落点和角色的存在同一张表里，键是 `g:群 id`。
到点、不在免打扰、当前账号、没在生成、未读没堆满，才发。
一次触发一次调用：`streamGroupReply({ opening })`，末尾补一段 `task.group-proactive`
（多久没人说话、现在几点）和一条 user —— 历史以成员的话收尾时，有的接口会把它当成
要续写的半句。落下来走 `ai/group.js` 的 `replay`，和平常一轮同一条路。
「每个角色单独调用」那个开关不管这里：主动开口始终一次调用。

#### 群头像

群资料里上传（`chat.avatar`，按头像尺寸压），没有就拼成员的。换下来的、
「改用成员头像」清掉的、删群时的那张走 `releaseImages`；`usedImageIds` 里登记了。

`scratchpad/group2.mjs` 22/22。

### 4.163 花钱排查：重排缓存、后台自己跑的几项上账

用户账单上重排占了九成八（Qwen3-VL-Reranker-8B，一天二十多万 token）。
实测一轮是向量 1 次、重排 1 次（送 50 条候选），闲着一次都不调 —— 按设计在花，
没有漏。但有两处该修：

**重排没有缓存。** 重新生成、命中禁写词重掷、失败重试，送的查询一字不差，
每次都再付一次。现在 `rerank.rankQueued` 按「模型 + 查询」存**每一条候选的分数**：
回复完之后召回过的那几条会疲劳，重新生成时候选大半相同、顺序变了、进出几条，
整份列表对不上；按条存的话，打过分的不再送，只送换进来的那几条，合起来排。
为此不再向接口要 `top_n` —— 它只缩短回来的列表，不省钱（按送进去的字数计），
却让排在后面的没有分数。实测重新生成那一次从送 50 条变成送 1 条。

**后台自己跑的几项不在「用量与上限」上。** 角色主动发消息（含深夜那一档）、
群里主动开口、发给角色的图写成描述（识图方式为「交给聊天模型」时，每张图在回复之后
多一次），现在都登记在 `EXTRA_CALLS`（`setting: null`，开关各在各的对象上，这里只摆账）。

顺带：`bondAuto` 在 engine 里写的是 `!== false`，和 cost.js、「用量与上限」的
`=== true` 不一致，改成一致。

`scratchpad/costfix.mjs` 14/14。`scratchpad/rerank.mjs` 过期了一处：近期那一档
（`memoryRecent`）会把它仅有的三条记忆常驻、从召回里排掉，于是重排没东西可排，
补上 `memoryRecent: 0`。

### 4.164 互动标识

参照 QQ 的互动标识（聊天火花、友谊的小船），做成这个项目的样子：
**全部在本地按聊天记录算，不调接口**；单色 SVG，花哨放在形态与动效上。
内核是 `system/badges.js`，界面在 `apps/chat/pages/BadgeBits.js`。

#### 存在哪儿、怎么算

挂在会话上，不另开数据域：`chat.stats`（统计）、`chat.unlocked`（成就与档位，id -> 时刻）、
`chat.limited`（`节日:年份` -> 时刻）、`chat.awards`（互相颁发的）。离了那段会话没有意义，
备份、删除跟着会话走。

**增量**：`upTo` 记着算到哪条，二分找起点，只看新的；同一毫秒的几条靠 `edge` 那几个 id 分开。
两万条的会话首次整段算约 25 毫秒，之后每次同步 0.1 毫秒。删消息、改消息不回头改，
标识页有「重新统计」。**不在渲染里算**：会话页在条数变化时、`proactive.tick` 每一轮、
拼提示词之前各同步一次，界面只读。

一天按设备本地日期，双方都至少发过一条才算互发。提示行与报错不算。

#### 有哪些

- **连续互发**：3 天火苗、7 火焰、30 火把、100 星、365 太阳。状态四种：点亮、快熄灭
  （昨天互发、今天还没有，图形一明一暗）、余烬（断了正好一天）、熄灭。
  **余烬第二天由对方先开口并互发，火花接上**，同时解锁「救火」—— 和「角色主动发消息」连起来
- **成长**：相识天数、消息量（小船、巨轮）、通话时长、一起听时长，写明下一档要多少
- **隐藏成就**：凌晨三点、连珠炮、撞车、第一通电话、长谈、守约、五二零、早安晚安、
  书信往来、救火、夜谈。没解锁只显示「？？？」和一个方向
- **限定**：新年、情人节、圣诞、双方生日（取自角色卡与我的资料，格式宽松）、
  相识纪念日，当天互发得当年那一枚
- **亲密度**：几项加权成分数分六级，头像外那一圈画多满；满级有个亮点绕着转。
  **每一级叫什么由用户填**（`chat.levelNames`），系统只写「第 n 级」
- **群里的周榜**：话痨、团宠、夜猫子，按自然周现算不存，跟在群消息的名字后面
- **年度回顾**：数字本地算；「最常说的词」用 `Intl.Segmenter` 分词，慢，页面先出来再补。
  「请角色写一段」点了才调一次（`ai/tasks/year-note.js`，`task.year-note`），不是每轮的开销，
  所以不进 `EXTRA_CALLS`

#### 颁发

标记 `[授予：名字｜理由]`（协议，留中文），能力 `award`，群里也能用。角色写了就落一条
`kind: 'award'` 的消息，同时记进 `chat.awards`；那一条删了，那一枚收回（重新生成时不留旧的）。
你颁给角色：标识页里填名字与理由，同样落一条消息，角色在历史里看得见。

#### 这段对话自己的开关（第 5 条）

都在标识页：显示标识（默认开）、**让角色知道**（默认关，开了在末尾加一段 `[互动标识]`：
天数、今天的状态、等级、上一次角色开口之后才解锁的）、火花快熄灭时提醒（默认开，
几点之后提醒可填，本地通知，一天一次）。入口有两个：会话菜单「互动标识」与角色主页那一排，
进的是同一页。

契约新增三个钩子：`ph-badge`、`ph-award`、`ph-level-ring`。
系统开了「减弱动态效果」时动效全停。

`scratchpad/badges.mjs` 43/43。

### 4.165 防丢、测试进仓库、关系入口合并

#### 防丢（`system/safekeep.js`、`system/ghbackup.js`）

所有数据只在这台设备的浏览器里。三件不需要后端的事：

- **申请持久存储**（`navigator.storage.persist`）。从前一次都没申请过：空间紧张时浏览器可以
  自己清掉这个站的数据。第一次 tick 时申请，存储页上看得到结果，没通过可以再申请
- **备份提醒**：`lastBackupAt` 由导出文件与 GitHub 备份两条路记下。超过 `backupRemindDays`
  （默认 7，填 0 不提醒）天就发一条本地通知，一天最多一次。从没备份过的从最早那条消息算
- **备份到 GitHub**：网页直接调 GitHub 的接口（带 CORS），不需要后端 —— 路线图 N4 一直卡在
  「要一个后端」上，其实不用。
  - 用 Git Data 接口：传 blob、拼一棵树、提交、挪分支。**一次备份一次提交**，提交历史就是版本历史
  - 文件名和导出的 ZIP 一样（`backup.json`、`images/<id>.<ext>`、`files/<id>.<ext>`），
    恢复时拼回 ZIP 交给 `backup.restore`，不另写恢复逻辑
  - 图片按 id 存，传过的不再传；本机删掉的在新树里 `sha: null` 删去
  - **只认私有仓库**，每次都查；公开的直接拒绝 —— 里面是全部聊天记录
  - 令牌存在 `settings.githubBackup`，`backup.build` 不带密钥时把它剥掉；
    从 GitHub 恢复之后把令牌与仓库设置放回去，不用重填
  - 自动备份：应用开着、到了间隔就在后台传一次，失败发通知，一天最多重试一次
  - 音视频默认不传（体积大），单个超过 95 MB 的跳过

#### 测试进仓库（`tests/`、`scripts/test.mjs`）

从前一百多个回归测试全在一次性的临时目录里，容器一回收就没了。现在：

- `tests/*.test.mjs`：每个一个独立进程，退出码非 0 算没过
- `tests/_env.mjs`：页面地址、浏览器、截图目录、夹具，全部从环境变量来，不写死路径
- `scripts/test.mjs`：自己起一个静态服务器（不依赖 python），并行跑，只在没过时打印输出。
  `node scripts/test.mjs group badges` 只跑名字里带这些词的
- 和冒烟一样，找不到 playwright 就跳过，不拦提交

搬进来的是当时能过的那些；过不了的（多半是界面改过、测试没跟上）没搬，
要用时照着现在的界面重写。

#### 关系入口合并

情侣空间与互动标识从前各算各的：空间里有「在一起的日子」与自己加的纪念日，
标识里有「相识天数」与「相识纪念日」，两边互不知道对方。现在：

- 设了在一起那一天：标识的成长里多一档「在一起」（100、365、1000 天），
  每年那一天互发得一枚「在一起纪念日」
- 情侣空间里每年重复的纪念日：那一天互发得一枚限定，名字取纪念日的标题（改名跟着变，
  id 是 `day-<纪念日 id>:<年份>`）
- 这几处日子改了，标识整段重算一遍，从前那几年的那一天回头算上
- 情侣空间里有「互动标识」与「年度回顾」两个入口（跨 app，走 intent）；
  标识页里没设在一起那一天时，有一行提示去情侣空间设定

顺带：仓库根目录八十八张开发截图删掉了（它们跟着 GitHub Pages 一起发布），
`.gitignore` 里拦住根目录的 png，图标除外。

### 4.166 会话菜单瘦身、后台任务、那年今天

#### 会话菜单瘦身（`apps/chat/pages/ChatMorePage.js`，路由 `/more/<chatId>`）

会话右上角菜单从前二十几行，按「多久点一次」拆开：

- 留在菜单里：这个角色（角色卡、主页、主动发起对话、在听什么）、这段对话（搜索、互动标识、
  那年今天、多选、节奏、美化、翻译、互动、共享位置）、记忆（立即总结、标记为已总结、关系底色、我们）
- 收进最底下的「更多」：所有角色通用的四页（上下文与记忆、表情包、能力开关、Prompt 模板）、
  每轮的接口调用、数据（导出、导入、三种清空）

入口没换地方，仍从这段对话的菜单进（第 5 条），只是深了一层。清空之后退回会话。
顺带把「看看她在听什么」改成「查看角色在听什么」（第 7 条）。

#### 后台任务（`apps/settings/BackgroundPage.js`，设置 - 后台 - 后台任务）

只读总览，开关一个都不放（各在各的对象上，第 5 条）。每一行点进去就是那一项自己的页面：

- **正在进行**：队列里进行中与排队的请求数
- **定时执行 · 调用接口**：逐个对象列出开着的主动发起对话、深夜消息、自己存照片、当日日程、
  群里主动开口、待发的延迟回复，写明间隔与下一次的时刻；接口没配、生图没配时写明不会执行
- **定时执行 · 不产生模型费用**：定时寄出的信、自动备份到 GitHub、备份提醒
- **每条消息顺带的调用**：`EXTRA_CALLS` 里开着的那几项（按对象挂的已在上面逐个列出，这里跳过）
- **实际调用次数**：24 小时 / 7 天 / 30 天，按任务合计，标出哪些是后台自动发生的

实际次数来自新的调用账本 `system/ai/usage.js`。从前只能算「会」打几次，算不出「已经」打了几次，
账单多出来只能对着代码猜。账本记在每个真正发请求的出口：文字模型在 `engine.send`
（唯一的门，重试与换套每次都记），向量、重排、识图、语音识别、语音合成（台本拆段按段记）、
生图、生成视频各在自己的出口。按小时汇总存在 localStorage，保留 30 天，不进备份。
任务 id 到界面名字的对照在 `usage.TASKS`，没登记的照记，显示 id 本身。

#### 那年今天（`system/onthisday.js`、`apps/chat/pages/OnThisDay.js`，路由 `/onthisday/<chatId>`）

往年的同一天这段对话里说过什么，本地翻，不调接口。

- 同一天按月日算；2 月 29 日只对上闰年那一天
- 只算你和角色都开过口的日子
- 页面按年列出，每年先露 6 条（默认值，可展开全部），可以前后翻日期，「在聊天中查看」跳到那天第一条
- 入口：会话菜单里（**只在往年今天有记录时出现**，平时不占一行）、互动标识页的「回顾」
- 每天 9 点之后提醒一次（本地通知），开关在那年今天页上，按会话存 `chat.onThisDay`，默认开

### 4.167 语音里的译文、通话里被打断的那半句

**语音把译文也念出来。** 行内译文开着时，模型常把译文写进 `[语音：…]` 的括号里
（`[语音：原话（译文：…）]`、`[语音：原话 [译文：…]]`，或者括号没收口、下一行的
`[译文：…]` 被正则当成语音的后半截），整段被送去语音接口。现在：

- `reply.splitVoiceText`：带「译文：」标签的那一截切出去，收进这条语音的 `translation`；
  用户配的行内形状同样认；都不像就原样念
- 语音下一行的 `[译文：…]` 挂在这条语音上 —— **只认紧跟在它下面的**，
  语音没带译文时，后面那条正文的译文不会被它截走
- `[语音：原话 [译文：…]]` 外层落单的那个右括号不再自成一个气泡
- 单独翻译接口那条路也把语音一起翻
- `generateVoice` 念之前再过一遍，旧消息重新生成语音时同样只念原话，`regenMedia` 顺手把记录拆干净
- 语音气泡展开文字时，原话下面显示译文
- `skeleton.translate` 改成：语音括号里只放原话，下一行照常写译文（给译文一个地方放，
  它就不往括号里塞了）

**通话里一开口，角色刚才的话就没了。** 从前 `say` 掐掉正在说的那一轮，整轮作废：
字幕上那半句当场消失，通话记录与下一轮的历史里都没有。现在把字幕上已经出现的那一截落成一句
（`cut: true`），排在我这一句前面，并照常翻译；被掐掉的那一轮用 `seq` 判断，
就算随后收了尾也不再补一条、不再写字幕、不再报错。

### 4.168 界面按常见聊天软件的习惯走一遍

照着真人用聊天软件的习惯过了一遍主要几屏，改的都是「一眼看过去哪里别扭」的地方：

**会话页**
- **时间分隔**（`receipt.sepOf` / `needSep`）：相邻两条相隔五分钟以上，中间居中写一行时间。
  今天写时分，昨天写「昨天」，一周内写星期，今年写月日，往年带年份。
  「消息时刻」多了这一档「按间隔」并设为默认；从前默认「不显示」，迁移 9 把落在库里的 off 挪过来，
  逐条显示的两种不动。挂契约钩子 `ph-time-sep`
- **同一人连发收紧**：同一个人三分钟内接着说的那一条带 `is-cont`，间距收到
  「同一轮里两个气泡之间」的两倍，跟着美化里的间距变。头像照留
- **面板打开仍贴底**：消息区变矮（加号面板、表情面板、引用条、输入框变高）时，
  本来贴着底部看的跟着贴住。从前点开加号，最新那几条被压到面板后面

**列表**（设置首页、会话菜单）
- 状态写在右边（「未配置」「关闭」「按按钮才回」），副标题只在有内容时写一行；
  说明文字留在点进去的那一页。`ListItem` 的 `right` 给字符串时包一层 `.li-value`，太长省略
- 没标 `multiline` 的行，副标题一律单行省略 —— 那个标记本来就是「这一行要换行」的意思

**主屏与聊天列表**
- 最近会话：只列当前身份的会话；群显示群名（从前显示第一个成员的名字）；
  两格高只放两段（第三段被切掉半截）；点哪一行直接进那段对话
- dock 的空位只在编辑时画虚线框
- 还没填内容的播放器横条、文字块，写一句怎么填
- 群头像拼图改成圆角方形、每格小方块，和单人的圆头像分得开，也不再被圆框切掉边角

**联系**：卡片墙一行三张（两张一行时一屏只见四个角色）；没写签名时留空（第 6 条）

### 4.169 主屏拖动（`screens/home/drag.js`）

从前挪一个图标要四步：长按进整理、点它、在弹出的表里点「移动到别处」、再点目标，
手指离开屏幕之前看不出会落在哪。现在按住就拖：

- 整理模式下按住一个格子（应用、文件夹、挂件、dock 里的应用），挪动超过 6 像素就拿起来
- 平时长按进整理模式，**手指不松开**接着挪就拿起来；平时直接划过去仍是翻页，不会拿起图标
- 拿起来的那一格原处变淡；一个跟手的影子贴着手指走（克隆出来挂在 body 上，
  翻页时原来那一格已不在屏幕上，影子要活得比它久）；网格上用虚线框画出落点，
  dock 上亮出落在哪一格
- 落点按影子的左上角算，不按手指：抓着大挂件右下角拖，落点仍是它左上角那一格
- 贴着屏幕左右边缘停半秒翻页，可以一路拖到别的页；拖到最后一页右边，这一页有东西就新开一页
- 松手：落在网格上走 `moveTo`（同尺寸对调，否则挤开），落在 dock 上走 `movePicked`；
  从 dock 拖下来落在应用上对调、落在空处放下；落在两者之外回原处
- 落下之后用 FLIP 过渡：拖的那一格从影子最后的位置飞进新格子，被挤走、被对调的
  从各自的老位置滑过去。换页、格子边长变了不算挪动，不飞；系统要求减少动效时不飞
- 拖完那一下浏览器补发的 click 吞掉，不会误开菜单；点一下仍是原来的菜单，
  「移动到别处」那条点选的路也留着
- 格子上 `touch-action: none`：主屏本来不滚动，不让出来的话浏览器会接管 pointermove

**让位**：拖着在一处停下 150 毫秒，其他格子先按「松手会变成什么样」挪好 —— 同样大小的
滑到空出来的那一格，被大挂件压住的就近让开；挪到别处它们再滑回去。预览用的是
`layout.planMove`（只算不存），松手时 `moveTo` 走的是同一段，看到的就是落下的样子。
拖着的那一格在预览里已经排进落点、隐身，那里空出来的就是它会落的地方。

被挤走的格子从前放到「这一页从左上往右下数第一个空位」，现在放到**离它原来位置最近**的空位
（`nearestSpot`，竖着挪略加分量），只在这一页已用到的行数里找；找不到才退回原来的办法。
拖动时它们是当场滑过去的，跳得远了看不出去了哪儿。

过渡的起点在渲染时量（那时 DOM 还是上一版），正在滑的格子量到的是它半路的位置，目标又变了
也从半路接着滑；只有格子真的换了位置（`offsetLeft/Top`，不含 transform）才重新起步，
手指每挪一下都会重渲染一次，不这样的话正在滑的格子会被一遍遍重新起步、滑不动。

### 4.170 打给角色默认总是接

从前「接不接」一律按「主动发起对话」里的免打扰时段掷骰子：时段内 15% 接，其余时间 90%。
可那个时段是给角色主动发消息用的，默认 0 点到 8 点，哪怕从没打开过主动消息也在 ——
于是夜里打过去几乎回回不接，而界面上看不出为什么，看起来就是「只能等角色打给我」。

现在角色卡「通话」下面多一个「接听你的来电」：**总是接听**（默认）/ 按作息。
按作息才走原来那套（`call.answerOf`、`willAnswer`），说明里写出当前的免打扰时段。
角色打给你的那一路不受影响。

### 4.171 生成视频接上 OpenAI 兼容的中转站（`ai/video.js`）

从前只有海螺那一套（`/v2/video_generation`）。现在「类型」有四种，对外仍是 submit / look / wait 三个：

| 类型 | 提交 | 查询 | 取回 |
|---|---|---|---|
| MiniMax（海螺） | `POST /v2/video_generation` | `GET /v2/query/video_generation/{id}` | 结果里的链接 |
| OpenAI 视频格式 | `POST /v1/videos`（model、prompt、seconds 字符串、size「宽x高」，首帧放 `input_reference.image_url`） | `GET /v1/videos/{id}` | `GET /v1/videos/{id}/content`，**要带密钥**；查询结果里直接给了链接就用链接 |
| 中转站统一格式 | `POST /v1/video/generations`（model、prompt、duration、width、height，首帧放 image） | `GET /v1/video/generations/{task_id}` | 结果里的 url |
| 聊天接口出视频 | `POST /v1/chat/completions`，描述当成一句话，不流式 | —— | 回复里挑出的链接（优先 .mp4/.webm/.mov） |

- 各家状态词翻成三个：queued / running / succeeded，失败另算；气泡上那一行只认这三个
- 后两种没有默认地址，必须填中转站地址，没填不算配好（`isVideoReady`）
- **聊天接口那一种是一次长请求，不进 AIQueue**（会占着并发位几分钟，聊天排在后面动不了），
  编号是本地造的、请求挂在内存里，所以关掉应用就接不回来 —— 类型说明与设置页底下都写明了；
  超时就是「最长等待」。自检不提交生成（那要花一段视频的钱），只问 `/v1/models`，看密钥与模型名
- 设置页按类型只显示用得上的字段：海螺是分辨率、时长、宽高比；OpenAI 与统一格式是尺寸与时长；
  聊天那一种只有最长等待

格式按 OpenAI Videos API 与 new-api 文档核对过（2026-09）。

### 4.172 音乐：正在播放与歌词（`apps/music/NowPage.js`，路由 `/now`）

点底部那条（除了播放、下一首两个键与进度条）进「正在播放」，和网易云同一个样子：

- 封面那一面：大封面，下面一行是正唱到的那一句（有译文带上译文）
- 歌词那一面：整首滚动，正唱的那句加粗放大、停在正中；点哪一句就从哪一句放；
  手指自己划过歌词，三秒内不抢着滚回去；点歌词以外的空白回封面
- 外文歌有译文时，译文排在原文下面，右上角可以关
- 底下是可拖的进度条、上一首、播放暂停、下一首

歌词从哪儿来（`player.lyricOf`）：曲库里自己粘过 LRC 的用那一份；网易云的现取 `/lyric`，
原文与译文按时间戳对上，取过的记在内存里。纯音乐、未收录、自己传的歌没有歌词，各说各的话。
`netease.lyric` 从只回原文改成回 `{ lrc, tlrc, pure, none }`（此前没有调用方）。

高亮按新的 `player.position()`（audio.currentTime，带小数）算，四分之一秒取一次；
player 里那个 seconds 一秒一更、还取了整，拿它对歌词每句都慢半拍。

**一起听也从这一页看**（`/now/listen`）。.84 只接了音乐 app 自己的播放条，而会话里「一起听」
那一条点进去是一起听的歌单页，没有大封面也没有歌词 —— 在会话里听歌的人找不到入口。现在：
点会话顶上那一条中间进「正在播放」；一起听歌单页顶上多一行「正在播放」也能进；
那一页右上角回一起听的歌单。页面用一个小适配层对齐两套引擎（`useSource`），
`listen.js` 补上 `position` / `duration` / `seek`。

### 4.173 角色自己的歌单与分享歌曲

从前角色能做的音乐动作只有「一起听」「点歌」和一个空的「建歌单」—— 建出来是空的，
而且会话里看不到。现在三个标记：

```
[分享歌曲：晴天 - 周杰伦]                   会话里落一张歌曲卡片，点开放、进「正在播放」
[加入歌单：夜里听 | 晚风 - 林晚；晴天 - 周杰伦]  放进角色名下的歌单，没有就建
[建歌单：夜里听]                             建一个空的（也可以带竖线后面那一串）
```

**找歌**（`music.resolveSong`）：`splitQuery` 先把角色写的那串拆成歌名与歌手
（横线、破折号、括号、书名号几种写法都认），先在曲库里找；找不到而且配了网易云，
就搜「歌名 歌手」，挑歌名正好对上的那首，没有就第一首，收进曲库（封面网址存在 `cover`）。
两处都没有回 null，卡片标成没找到 —— 不凭空造一首放不出来的歌。

**卡片先落、歌后补**：网易云那一搜要一两秒，不该拦着这一轮后面的话。
消息 `kind: 'song'`，`songState` 从 `pending` 到 `done`（带 `songId`）或 `missing`。

**歌单挂在角色名下**（`owner` 是角色 id），同名的往里追加，已有的不重复收。
做完落一行提示（`notice` 带 `playlistId`）写明放了哪几首、哪几首已经在、哪几首没找到，
点它进一起听那一页 —— 那一页本来就列着角色的歌单。

能力的开关仍是角色卡上那一个「一起听」（第 5 条），条件从「曲库非空」放宽成
「曲库非空或配了网易云」。

**角色包**带上角色名下的歌单、歌单里的歌、会话里分享过的歌（连封面与音频文件）；
装回来时歌按 id 已有就不重放，歌单换新 id、owner 指到新角色。
**删角色**删它的歌单，歌留在曲库 —— 歌可能已经被用户收进自己的歌单里。

**歌单提示当场落**（.87）：从前找完歌才落那一行，网易云一搜一两秒，这一轮后面的话早落下去了，
提示排到了后面。现在解析到就落一行「正在整理歌单」，找完改字，顺序和角色写的一致。

### 4.174 朋友圈分享音乐

卡片与选歌抽成 `apps/chat/pages/SongCard.js`：`SongCard`（会话里的分享、动态里带的歌共用）、
`SongPicker`（曲库按歌名歌手筛；配了网易云的，输入后可搜，选中的收进曲库）。

- **自己发**：发布表里「分享音乐」挑一首，动态存 `songId`。只带一首歌、不写字也能发。
- **角色发**：`task.moment-create` 后面接一段 `task.moment-song`，告诉它 JSON 里多一个
  `"song":"歌名 - 歌手"`。只在该角色能用音乐时接（与聊天里 listen 能力同一个条件），
  否则一个字都不提。找歌同 `resolveSong`，先挂 `songState: 'pending'`，找不到就摘掉 ——
  动态正文照样成立，不挂一张放不出来的卡片。
- **评论与回复**：动态正文后面附一行 `[分享歌曲：歌名 - 歌手]`，评论的角色看得到是哪首。
- **角色包**：动态里挂的歌跟着走（`charpack.collect` 的 songIds）。

### 4.175 会话里自己分享歌曲

「+」面板多一格「分享音乐」（`panel.js` 的 `song`，默认在面板上，群聊也可用），挑歌用
`SongPicker`，落消息走 `music.share`：`kind: 'song'`，正文写成与角色分享时同一个标记
`[分享歌曲：歌名 - 歌手]` —— 历史直接进上下文，角色看到的就是这一句，不需要另写 prompt。
卡片与角色那张是同一个 `SongCard`，自己这一侧在强调色底上，副标题与封面底子跟着前景色走。

### 4.176 歌曲卡片照 Apple Music 的分享卡片重做

- **会话里（tile）**：上面整幅方形封面，右下角圆形播放键；下面一栏歌名、歌手、来源
  （「歌曲 · 网易云音乐 / 本机曲库」）。不再挂 `.bubble`：卡片自己就是那一块，
  自己这一侧不跟着强调色变黑。
- **朋友圈（row）**：封面在左、文字居中、播放键在右。
- **底色取封面主色**（`useTint`）：缩成 16×16 按饱和度加权平均，太白太黑的像素不算，
  再用 `color-mix` 按 26% 混进 `--surface-2` —— 每首歌颜色不同，仍是低饱和底色，
  深浅两套主题自动跟着走。网易云的图若不许跨域读像素，取不到就用面板底色。
  颜色只经 `--song-tint` 自定义属性传进去，声明写在样式表里。
- **播放键就地放、就地停**，不跳页；正在放的这首外面一圈进度（每半秒取一次位置，
  只有这一首在取）。点卡片其余地方进「正在播放」看歌词。

### 4.177 音乐的几处补齐

- **会话列表预览**：歌曲消息写成「[歌曲] 歌名」，不露方括号标记。
- **卡片不再每秒重画**：`PlayKey` 不整个订阅播放器，只取「是不是这首、放没放」三态
  （`usePlayState`），同一个值不触发重画。播放器每秒更新一次秒数，从前聊天记录里
  有几十张歌曲卡片就每秒几十张一起重画。进度圈只在正在放的那一张上取位置。
- **分享的歌附上歌词**：`music.share` 取回歌词存在消息上（`songLyric` / `songPure`，
  取歌词不调模型）；`engine.buildHistory` 与群聊历史在那一条后面附 `[歌词]` 一段
  （`music.lyricBlock`）。**只附用户那一侧** —— 角色自己分享的那条带着歌词回到历史里，
  就是在示范「分享完接着贴歌词」。评论、回复带歌的动态同样附上。
  「用量与上限」里一个开关（`songLyric`，默认开）与行数（`songLyricLines`，默认 20，0 = 整首）：
  不多调接口，但仍在上下文里的每一轮都带着这几行。
- **歌曲消息的长按菜单**：从这首开始一起听（单聊、角色没关一起听时）、加入我的歌单
  （可当场新建）、复制歌名（复制「歌名 - 歌手」，不是标记）。歌曲消息进了
  `repair.STRUCTURED`，不给编辑 —— 改了正文，卡片上的歌不会跟着变。
- **本机歌单详情**（音乐 `/local/:id[/:chatId]`，`LocalListPage`）：查看曲目、播放、
  移除一首（`music.removeTrack`，歌留在曲库）、删除歌单；带着会话 id 进来时多一个「一起听」。
  入口：一起听页里角色的歌单、角色放歌之后那一行提示、曲库页新增的「歌单」一节。

### 4.178 只写文字发一张图

「图片」来源表里多一项「输入图片内容」（`Conversation` 的 `typePhoto`）：不选图，写下画面发出去。
和「改为输入」发语音同一个道理。消息仍是 `kind: 'image'`，`imageId: null`、`media: 'text'`，
描述存 `imageDesc`，正文 `[图片：…]` —— 角色收到的与识图读出来的是同一种东西，
所以 `vision: 'done'`，不走识图，`imagesFor` 也不会去读一张不存在的图。
气泡是一张文字图（小标「图片」＋描述）；`ImageStack` 只叠有图的那几张；
长按「编辑」走原来那条改描述的路，描述与正文一起改。

### 4.179 MCP：角色调用外部工具

**连接**（`system/mcp.js`）。浏览器里只能走 HTTP，stdio 服务器用不了；服务器还得允许跨域。
三代服务器按从新到旧试：

| 代 | 怎么认 | 之后每个请求 |
|---|---|---|
| 2026-07-28 | `server/discover` 回来的 `supportedVersions` 里有它 | 头里 `MCP-Protocol-Version` / `Mcp-Method` / `Mcp-Name`，`params._meta` 里带版本、客户端信息与能力；没有会话 |
| ≤2025-11-25 Streamable HTTP | `initialize` 握手成功 | 带 `Mcp-Session-Id`；协商出 ≥2025-06-18 时带 `MCP-Protocol-Version`；404 视为会话过期，重新握手再发一次 |
| 2024-11-05 旧版 SSE | POST 被 400/404/405 拒掉 | GET 开事件流，`endpoint` 事件给出 POST 地址，回复从流里按 id 对回来 |

新一代的探测带着 `Mcp-Method` 这类自定义头，老服务器的 CORS 白名单里没有它，预检直接失败 ——
所以探测的**网络错误也当成「退一档」**，只有 401/403 不退。回复可以是 JSON 也可以是 SSE，
SSE 里夹着的服务器通知跳过，只认 id 对得上的那条。结果读成文字（`resultText`），
图片、音频这类角色读不了的写明有这么一个东西。

**配置**（`services.mcp.servers`，MCP app，见 4.180）：地址、请求头（「Key: value」一行一个，
和密钥同样只在连同密钥备份时进备份）、超时、调用前确认（每次 / 只读免确认 / 直接）、
逐个工具的开关（`toolsOff`）。工具清单连接时取回来存下 —— 拼 prompt 是同步的，不能每轮现取。

**哪个角色能用哪几台**在角色卡上勾（`char.mcpServers`，第 5 条）。

**提示词**：能力 `mcp` 一直给整段（`skeleton.mcp` + 工具清单与参数 schema），不走冷热：
不知道有哪些工具、参数长什么样就调不对。两台服务器有同名工具时写成「服务器名.工具名」。

**解析**：`[调用：名字 {JSON}]`。参数里常有方括号与换行，MARK 那条正则会在第一个 `]` 收口，
所以 `liftCalls` 先按花括号配对（认得字符串与转义）把整段取出来，换成占位再交给 MARK。
名字对不上、参数不是 JSON 的照样落一条失败 —— 丢掉的话角色以为调成了，会接着编一个结果。

**执行**（`system/mcptools.js`）：一次调用一条消息（`kind: 'tool'`），状态 ask → running → done /
error / denied。要确认的停在 ask，卡片上点允许或拒绝。页面重开时正在调的那一次不自动重调
（工具可能有副作用），卡片上给「重试」。

**进历史**：调用那一句在角色那一侧，紧跟一条对方那一侧的 `[工具结果：名字]`（`resultFor`）：
状态说明是英文，结果原样给，按「用量与上限」里的字数截断（默认 4000，0 = 整段）。

**接着回复**（`mcpFollowUp`，登记在 `EXTRA_CALLS`，默认关，第 15 条）：这一轮的调用都有了结果，
就把会话记成「该回复了」，走 pace 那套到点回复（会话页开着当场回，不在就由全局定时器回并通知）。
从你上一条消息往后，角色调用过工具的轮数超过 `mcpFollowMax`（默认 1，0 = 不限）就停。

群聊不给这项能力（`GROUP_CAPS` 里没有它）。

### 4.180 MCP 拆成独立的 app（`src/apps/mcp/`）

从设置里搬出来，设置那一行删掉（同一件事只留一个入口）。两个页签：

- **服务器**：添加、连接、请求头、超时、调用前确认、逐个工具的开关；每个工具多一个「试用」
  （`/server/:id/tool/:name`）：按参数 schema 先填一个样子，直接调一次看结果，
  不经过角色、不落消息（`mcptools.tryTool`）。
- **调用记录**：所有会话里的工具调用（`mcptools.calls`），等待允许的排最前 ——
  人在别处时角色发起、停在 ask 的那一次，要有个地方找得到。点一条回到那段会话。

「用量与上限」仍在设置里（全局的账），服务器页底下给一个跳过去的按钮。
角色卡上没有服务器时那一行跳到 MCP app。

### 4.181 和风天气；身体状态每天自动生成

**天气**（`system/weather.js`，「设置 - 和风天气」）。按角色卡上的「所在地区」（城市名）查，
不按 IP：和风只认城市名、经纬度或它自己的城市编号，而角色本来就没有一个真的 IP。
2026 起和风每个账号一个专属 API Host，旧的公共域名逐步停用，所以地址由用户填；
密钥放在请求头 `X-QW-Api-Key`。城市名第一次查到的编号记在 `services.qweather.cities`，以后不再查。

排当日日程时（`tasks/day.makeToday`）先查当天预报，和大运、随机事件、三顿一起落在当天记录上
（`weather`），再带进 `task.day-plan`（`{{weather}}`）；聊天时「你今天」那一段也带一句
（`weather.promptLine`）。只陈述预报，不写「所以该带伞」（第 16 条）。查不到是 null，不拦着日程。
查天气不调模型，不进 `EXTRA_CALLS`。「日常 - 今天」上显示，可单独刷新（不重排日程）。

**身体状态每天自动生成**（`char.healthAuto`，开关在健康 app 里那个角色的页面上，第 5 条）。
每天一次模型调用，默认关，登记在 `EXTRA_CALLS`（`healthAuto`，setting 为 null，同 dayOn）。
两个入口：和这个角色聊天之前（这一轮就读得到），以及全局定时器（不聊天也每天填上）。
一天只试一次，失败也算试过（`healthAutoAt`）—— 和当日日程同一个教训；失败原因记在
`healthAutoError`，页面上写明，手动点「按人设生成今天」重试。当天已有内容（包括手填的）不碰。

### 4.182 ins 风小组件（`screens/home/insWidgets.js`）

八个：拍立得、胶片、快拍、主页卡、九宫格、一句、在一起、天气。仍守 tokens.css 的「黑白」：
ins 的样子靠留白、发丝线、衬线字与小写英文标注，不靠颜色。拍立得的相纸与胶片的片基
深浅两套主题都一样（`--paper` / `--film`），和应用图标深色下仍是白底同一个道理。

- 数据全是本地现成的：朋友圈照片、相册、聊天记录、当日日程里存下的天气。**主屏不发任何请求**
  —— 天气读的是排日程时存下的那一份，每次重画都去查的话额度一会儿就没了。
- 「一句」按日期从角色的文字消息里挑一句（6 到 60 字，不挑标记），同一天是同一句。
- 主页卡只露签名，不露人设（第 6 条）。
- **可调项写在 spec.fields 里**（image / text / date / segmented / switch / char），
  `WidgetEditor` 照着生成表单，不再每加一个组件去那边手写一段。默认值在 spec.defaults。
- 点一下就跳走的那几个 `stopPropagation`：格子本身那一下是「打开编辑面板」，两件事一起发生，
  回到主屏时面前摆着一张没人要的编辑表。改内容走整理模式里新加的「编辑内容」。
- 挑小组件的列表按 `group` 分成「基础」与「ins 风」，每项带一句 `desc`。

### 4.183 网易云扫码不用先找 cookie

从前扫码的第一步（取 key、生成二维码）常被网易云风控拦下（-462：接口所在服务器的出口 IP
在名单上），于是只能先去浏览器里找一个已登录的 cookie 粘进来，二维码才出得来 —— 为了登录先得已经登录。

现在被拦下时自动向接口要一个**游客身份**（`/register/anonimous`，不需要账号），带着它重新取 key、
生成二维码、查扫码状态（`viaGuest`）。一张二维码从头到尾用同一个身份（`qrGuest` 按 key 记着）。
游客身份只在内存里，不落库。游客身份也过不去时写明是出口 IP 的问题、该填 realIP。
「测试这个地址」里扫码那两步走同一个路子（`qrProbe`），探出来的结果与真正登录时一致。

另有短信验证码（`ui/qrlogin.js` 的 `SmsLogin`，同样自动换游客身份），和扫码同一个信任级别。
**不做密码登录**：曾经加过（先转 MD5、走 POST），又删了 —— MD5 对网易云来说和密码一样能登，
接口一方拿到就能用，而且泄露了收不回来；扫码与短信泄露的只是会话，退出或改密码就失效。
网页替人从 music.163.com 读 cookie 做不到（别家网站的 cookie 不给读，MUSIC_U 还是 HttpOnly）。

### 4.184 本站提供的网易云接口（`src/site.js`）

用户多半不懂技术、没有任何平台账号，自己部署接口做不到；而网页直连网易云做不到
（music.163.com 不允许跨域，这一道墙网页绕不过去），中间必须有一台服务器。所以由运营方部署一个，
写进 `SITE.neteaseApi`：用户什么都不填就用它；在「设置 - 音乐服务」里自己填了的以自己的为准，清空即回到本站的。

生效地址一律走 `services.neteaseBase()` / `neteaseIP()`（用户填的优先，其次本站的）；
`neteaseConfig()` 仍是用户自己填的那一份，设置页输入框绑它，灰字显示本站的地址。

安全上不多出一方：用户从本站打开应用，应用代码由本站提供，本来就完全信任本站。
这和让用户填陌生人的公共实例是两回事。

**运营方部署要点**（一次性）：

1. 部署一份 NeteaseCloudMusicApi（或仍在维护的分支），例如 `npx NeteaseCloudMusicApi@latest`，默认监听 3000 端口。
2. **尽量放在中国大陆的服务器上。** 境外机房的出口 IP 常被网易云拦截（-462）；放不了就在
   `SITE.neteaseRealIP` 填一个中国大陆 IP。扫码时的游客身份兜底（4.183）只能缓解，不能保证。
3. **用 HTTPS。** 本应用跑在 https 上时，http 的接口会被浏览器当混合内容直接拦掉。
4. **允许本站跨域。** 接口要对本站的来源返回 CORS 头（该项目有 `CORS_ALLOW_ORIGIN` 一类设置，填本站域名）。
5. **关掉或脱敏访问日志里的查询串。** 本应用把 cookie 放在网址参数里发给接口（`call` 的 `cookie=`），
   反向代理默认会把整条网址记进日志，那等于把登录过的用户的账号凭据写进了日志文件。
6. 填 `src/site.js`，同时改构建号（`version.js` 与 `index.html` 两处），用「设置 - 音乐服务 - 测试这个地址」验一遍。

### 4.185 没有服务器：网易云经 Cloudflare Worker 转发（`worker/`、`src/system/ne/`）

4.184 要运营方跑一份 NeteaseCloudMusicApi，那需要一台服务器。没有服务器时，把那份 Node 服务
拆成两半：**加密与拼参数搬进浏览器**，**只剩「替网页发请求」这一件事留给一个 Cloudflare Worker**。

```
页面  system/netease.js  callRaw(path, params, cookie)
        |  SITE.neteaseWorker 有值、用户没填自己的接口
        v
      system/ne/routes.js   22 个接口，照原项目 module/ 同名文件：网址、参数、加密方式、回复包装
      system/ne/client.js   照原项目 util/request.js：cookie 补全、请求头、状态码
      system/ne/crypto.js   weapi（AES-CBC 两层 + 无填充 RSA）、eapi（AES-ECB），WebCrypto + BigInt
        |  POST {url, body, cookie, ua, referer, ip}
        v
      worker/netease.js     只转发到 music.163.com / interface(3).music.163.com，设上请求头，
        |                   把回复原文与 Set-Cookie（去掉 Domain）装进 JSON 交回
        v
      网易云
```

回复与接口服务器给的一模一样，所以 `netease.js` 其余部分（搜歌、歌单、扫码、短信、一起听）一行没改。
加密与原项目逐字节对照过（`tests/necrypto.test.mjs`，夹具由原项目的 crypto-js 与 node-forge 生成）。

与原项目有意不同的几处：

- **游客身份每台设备各注册一个**（`device.anon`），原项目是服务器启动时注册一个给所有人共用。
- **每台设备固定一个随机国内 IP**（`device.ip`，原项目 `randomCNIP` 的那批网段），随请求放进
  `X-Real-IP`。几百个用户分散在不同 IP 上，同一个账号又始终是同一个 IP。用户或本站填了 realIP 以那个为准。
  三样东西存在 `services.netease.device`，随备份走。
- 二维码在本机生成（`vendor/qrcode-generator.mjs`），不发请求。
- 扫码状态出错时照实交回（原项目吞掉错误回一个空的成功），否则看不出被风控拦下。

Worker 不懂网易云的业务，网易云改接口时改的是 `system/ne/`，Worker 一般不动。
部署步骤写给不懂技术的运营方，在 `worker/README.md`。

「设置 - 音乐服务 - 测试这个地址」在 Worker 模式下跑同一套七项（行 id 不变，总结照旧按 id 认），
每项经 `callRaw` 走真正用起来的那条路；「连得上」改为 GET Worker 看它自报 `mini-phone-netease`。
「按次传 cookie」在这条路上恒为可用（cookie 在本机拼进请求）。

测试：`necrypto`（加密对照）、`neworker`（Worker 单测）、`neworker-e2e`（页面 -> 真 Worker -> 假网易云，
假网易云用已知密钥解开 eapi 核对网址与参数；覆盖游客注册一次、设备 IP 重开不变、逐项测试、扫码登录、Worker 不通）。

**这里测不到真的网易云。** Cloudflare 的出口 IP 会不会被网易云拦、`X-Real-IP` 在 Cloudflare 上能不能
原样送到，只有部署之后用「测试这个地址」才知道。

### 4.186 上传的图回到前台后变白（`system/db/blobs.js`）

图与语音以 Blob 存库，画出来用 `images.url` / `files.url` 换出的 blob: 地址，缓存着反复用。两处让这些地址作废：

- `db/index.js` 从第一版起在 `pagehide` 时把全部地址 revoke。页面只是被藏起来（切走、进后退缓存）再回来时，
  屏幕上的头像、图标、壁纸全指着作废的地址。整页销毁时浏览器自己收回这些地址，这一句只有坏处，已删掉。
- iOS 放在后台久了会收走 blob 背后的数据，缓存里的地址读不出来。库里的数据还在，重读一遍换新地址即可。

`blobs.js` 在回到前台（`visibilitychange`、`pageshow`）时抽查一个缓存着的地址；一张 img / audio / video
加载失败时查它那一个（只查缓存里还在用的，故意删掉的图不算）。读不出来就清空 images、files 的地址缓存，
代数加一；`useImage` / `useThumb` / `useFile` 以代数为依赖重新取，新地址到手前留着旧的。
所有显示都经这三个 hook，背景图（图标、壁纸、角色卡）同样跟着换。

测试：`bloburls`（藏起来再回来、后台被收走、单张加载失败、删图不触发）。

### 4.187 还有别处在用的图不删

排查「图没了」时查出的另外三处，与 4.186 的地址失效无关，是真删了：

- **共用 id 的图被当成旧图删掉。** 外观预设存的是当时的壁纸、图标 id；角色头像从头像池里挑；
  「保存到相册」存的是消息那一张。四十来处「换图」都是写上新 id、`images.remove(旧 id)`。
  存过预设再换壁纸，预设里那张就没了，套回预设时悄悄当作没设过。
  现在 `images.remove` 只登记，下一个宏任务按 `purge.usedImageIds` 核一遍，没人用才删；
  同一轮的一起核，引用表只算一次；引用表没登记上时一张都不删。
  确定要删的（清空全部、清理无引用、`releaseImages` 已核过的）用 `images.destroy`。
- **ins 风挂件的照片存在 `config.cover`**，引用表与预设只认 `config.imageId`，「清理无引用」会删它。
  现在挂件配置里的字符串一律算引用（`looks.configStrings`），不按字段名认。
- **开机的 `heal` 删掉不认识的挂件并存回去。** 更新时若有一次跑的是缓存里的旧代码，新版加的挂件连同照片的引用
  就被抹掉了。现在留着，主界面画成「挂件缺失」，长按照样能移走。

测试：`imagekeep`（旧代码 2/11）。

### 4.188 接口的站点与充值链接；模型栏一律能拉取

**充值链接。** 聊天预设多两项 `siteUrl`、`topupUrl`（`services.newChatPreset`），只用来给链接，不参与请求。
填在「设置 - 接口」各套自己的编辑页（第 5 条：站点跟着那一套走，不在设置列表里另加一行）。

用到它的地方是会话里失败的那一条（`chat/pages/FailNote.js`）：`engine.runWith` 失败时在错误上挂
`failedPresets: [{ id, balance }]`，会话存进那条 `status: 'error'` 的消息。气泡上写出报错原文，
余额不足（402，或报错里有 insufficient / quota / balance / 余额 等）给「去充值」，别的错误给「打开站点」，
「重试」照旧。主用副用都失败时各给一个并标上名字。余额不足却没填链接时，写明去哪儿填。
链接一律 `target=_blank`：加到主屏幕的应用里会在上层弹一个浏览器页，关掉即回到原会话。

识图、语音识别等单项接口的「接口来源」暂未加这两项：它们失败时不落消息，没有地方放这个链接。

**模型栏。** `ModelPicker.js` 的 `ModelField`：输入框加「拉取并选择」（拉列表、可搜索、点了即填）。
识图、语音识别、记忆、翻译、联网搜索、视频六页原先只有输入框，现在都用它；以后新加接口页的模型栏也用它，
不要各自再接一遍 `ModelPicker`。

测试：`topup`（旧代码找不到失败提示，直接超时）。

### 4.189 改名 Eira；猫的图标

界面上的名字（清单、页面标题、主屏幕名称、通知默认标题、设置页脚、备份文件名、通知权限的指引）改为 Eira。
**这几样不改**，改了旧数据就认不出来：备份格式标识 `mini-phone-backup`、GitHub 备份目录默认的 `phone-backup`。
代码注释与开发用脚本里的「小手机」不动。

图标由用户给的线稿生成：底色拉成纯白，猫占图标宽度约 64%（`icon-192.png`、`icon-512.png`、`apple-touch-icon.png` 180）；
安卓自适应图标会被裁成圆，另出一张 50% 的 `icon-maskable-512.png`。

已经加到主屏幕的，iOS 不会自动换名字和图标：要删掉主屏幕上那个，再从浏览器「添加到主屏幕」一次。
数据存在浏览器里，按同一个网址加回来，数据都还在。

### 4.190 外壳启动：先等网络，再载网页（`ios/Sources/LaunchView.swift`）

从前外壳一启动就载网页。国行 iPhone 第一次打开时系统正弹着「是否允许使用网络」，那一下必然失败，
弹窗后面已经是一整页「无法载入」加网址。点了「允许」也不会自己重试。

现在先盖启动页；`reportFailure` 把网络类错误（-1009、-1005、-1003、-1004、-1006、-1001、-1018、-1019、-1020）
当作「等」：`NWPathMonitor` 报网络通了、`CTCellularData` 报权限刚放开，都马上重试；另按 1、2、4、8 秒退避重试。
网络是通的却 20 秒连不上才换失败页。`didFinish` 时揭开启动页。失败页正文不再写网址。

安卓那一边不在这个仓库里，同样的问题要在它的外壳里照这个思路改。

### 4.191 登录账号（`system/auth.js`、`shell/Login.js`、Worker 的 `/auth/`）

**账号只管进门，不管数据。** 角色卡、聊天记录仍只在本机，登录不同步任何东西。

服务端就是网易云那个 Worker（`worker/netease.js`），多了 `/auth/login`、`/auth/check`、`/auth/logout`、`/auth/admin`。
绑了 KV（`ACCOUNTS`）并设了 `ADMIN_PASSWORD`（至少 12 位）才开启；否则 GET 报 `accounts:false`，一切照旧。
- 账号：KV 每人一条 `user:<名>`，PBKDF2 一万轮（免费版每次请求 10 毫秒 CPU），列表要的几项放在 metadata。
- 凭证：`{名, 设备, 过期}` 做 HMAC，密钥由管理员密码推出（不存 KV：各地同步有延迟，随机生成会出现两把）。
  改管理员密码等于全员退出。有效期 30 天，每次开机检查时换新。
- 设备：每人最多 2 台，第 3 台登录挤掉最早那台（检查时发现自己不在名单里即退出）。
- 管理员：账号名 `admin` 加管理员密码直接登录（保留名，不占设备名额），管理页再输一次管理员密码。
  「管理账号」入口只在 `admin` 登录时出现；普通账号直接打开 `/signin/admin` 只看到「仅管理员可用」。
- 密码：账号由管理员手动添加（名字必填），初始密码统一为 `Eira2026`（可用 Worker 变量 `INITIAL_PASSWORD` 换），
  重置也回到它。账号带 `initial` 标记，自己改过（`/auth/password`，要原密码，至少 6 位，不能等于初始密码）即清掉；
  改完之后别的设备退出。用初始密码登录时登录页接着给「修改初始密码」，可以跳过。
- 开启后网易云转发要 `Authorization: Bearer`，只验签名不读 KV（听歌请求多，读 KV 会用光额度）。

应用：`SITE.accounts` 填账号服务地址。`main.js` 在自愈之后、`boot()` 之前 `gate()`：
有凭证就检查（离线放行）；没凭证就问服务端开没开（没开放行并记住；连不上又不知道时停在登录页，给「重新连接」）。
回前台距上次检查超过 30 分钟再查，被挤下线、停用、重置密码时退出并在登录页说明原因。
凭证与设备号存 localStorage，不进数据库、不进备份。设置首页「登录」一组 → 修改密码、退出登录、
管理账号（添加、批量添加（一行一个名字）、重置密码、全部下线、停用、删除）。

**测试**：`scripts/test.mjs` 与 `scripts/smoke.mjs` 在给出 `src/site.js` 时抹掉 `accounts`，
否则每个测试一打开应用都停在登录页。登录本身见 `accounts`（Worker 单测）与 `login`（端到端）。

前端这道门拦得住普通用户，拦不住会改网页代码的人（网页代码是公开的）；真正拦得住的是网易云转发那一层。

### 4.192 安卓外壳（`android/`）

照 `ios/` 写：一个 WebView 从网络地址载入网页。启动页先等网络（同 4.190）；在文档一开始注入 `assets/bridge.js`，
给出与 iOS 同名的 `window.webkit.messageHandlers.net`（底下接 Kotlin 的 `EiraNative`），网页一行不改。
导出备份：拦下 `<a download href=blob:>` 的点击（包括不放进页面就 `click()` 的那种），分块交给外壳写进「下载」。
返回键交给 `window.phoneBack`（`shell/goback.js`，现在给回有没有退），无处可退时把应用切到后台。
还没有：系统通知、后台保活、健康、系统闹钟。

打包：`.github/workflows/android-apk.yml`，签名钥匙取仓库机密 `ANDROID_SIGNING`（PKCS12 的 base64）。
没有它就用临时签名，文件名标明 —— 签名一变，装新版要先卸载，卸载即清空数据。

### 4.193 返回方式默认改为左上角的返回键

很多人注意不到底部横条能回去。`navStyle` 默认从 `bar` 改为 `back`，迁移 10 把库里还是 `bar` 的一并挪过来
（从前 `bar` 就是默认，分不出谁是自己选的；想要横条的在「设置 - 外观 - 返回方式」换回）。

改默认时查出放映厅全屏的两处：全屏时 `Page` 没有 `onBack`，按悬浮返回键直接离开放映页；
画面横过来时右上角的关闭键转到左上角，被悬浮键盖住。现在全屏时 `onBack` 是 `leaveFull`（`hideBar` 不画导航栏），
悬浮键在全屏里加半透明深色圆底，页面自己的关闭键在返回键模式下让位。

### 4.194 对话中间的设定：OpenAI 兼容接口上默认转成 user 轮（`providers/midsystem.js`）

设了深度的世界书条目、本轮召回、时间与状态这些每轮都变的块，都以 `role: system` 插在历史中间（`engine.insertLore`）。
Anthropic 那边早就转成 user（它根本不收）；OpenAI 兼容那边原样发出 —— 中转转给 Claude、Gemini 时
常被丢掉或只留第一条，于是「请求记录里有，模型没看见」。

现在两边共用 `asUserTurns`：中间的 system 改成 user、`<context>` 包住、合并相邻同角色（带图的不合）。
OpenAI 兼容的接口编辑页可改回「原样 system」（`preset.midSystem`），给认中途 system 的接口用。
请求记录（trace）记的是经 `provider.shape` 转过之后、真正发出去的样子。

测试：`midsystem`（旧代码 0/1：中间有 system，时间状态也在其中）。

### 4.195 注入预算默认不限

`contextBudget` 默认从 6000 改为 0（不限）；迁移 11 把库里仍是 6000 的改成 0，自己填过别的数的不动。
6000 时世界书只能占约 2400 token（四成），条目一多就按优先级悄悄跳过。
这个数同时截断对话历史（`takeLatestWithin`），设置页从前只写「世界书与记忆」，现在写全。
不限之后历史仍按条数（默认 20 轮）或轮数取，不会无限增长。测试：`budget`（旧代码 12 条只进 4 条）。

### 4.196 整页被推开后归位；安卓全屏；角色看得见用户的朋友圈

**整页归位（`system/viewport.js`）。** 手机浏览器点输入框时会自己把整页往上推（overflow: hidden 拦不住），
键盘收起后常常不推回来：聊天页上面少一截、下面空一大片。现在没有输入框激活时，整页一旦不在原位
（`scroll`、`focusout`、`visualViewport` 的 `resize`、转屏）就归位；正在输入时不动。
外壳那一串 100vh 与 iOS 的 `black-translucent` 都不动（4.x 的两次踩坑：灰带与底部白边）。

**安卓全屏。** 加到主屏幕时 standalone 模式下系统状态栏用主题色填死，壁纸画不上去。manifest 改为
`display: fullscreen`（`display_override` 退回 standalone；iOS 不受影响，它看 apple 那条 meta）。
安卓安装包同样藏起系统状态栏、注入 `window.phoneFullscreen`；安卓 15 退出强制铺到系统栏底下
（`values-v35` 的 `windowOptOutEdgeToEdgeEnforcement`），免得底部导航条盖住输入框。
这两种情况下 `StatusBar` 的「自动」改为显示网页自己画的那条，否则看不到时间与电量。

**用户的朋友圈（`context/moments.js`）。** 从前发布只存进库，没有任何区块读它，角色一条都看不到。
现在带上最近 `momentsCount` 条（默认 3，0 为全给，在「用量与上限」）：时间、文字、几张图、配的歌、
这个角色点过赞与评论过什么。图片本身不给。自己的动态上，评论弹窗里可「请角色评论」：
点一位调一次接口，写一条评论并点赞；不在发布时自动叫所有角色（第 15 条）。

测试：`viewport`（旧代码：整页停在推上去的位置，顶栏在 -200）、`mymoments`。

### 4.197 iPhone 上会话页上下两块空白；朋友圈的可见范围

**顶上多一条。** 网页自己画的状态栏（26px）在 iPhone 上出现了：上一版为安卓加了「全屏显示时画它」，
iPhone 上系统状态栏一直都在，画出来就是顶栏被压下 26px，开着壁纸时字是白的，看起来是一条空白。
现在苹果触屏设备上「自动」一律不画（`StatusBar.shouldShow` 的 `apple()`）。

**底下多一块。** `.page > .page-body:last-child` 按通用规则让出一次 `--safe-bottom`，会话的输入栏
自己又让一次（`calc(--composer-pad + --safe-bottom)`），iPhone 上输入栏下面平白空 34px。
现在内容区里有 `.composer-bar` 或 `.gen-panel`（自己算了安全区的两种）时不再让。放映厅、书页这些
底栏不算安全区的，照旧靠通用规则。外壳 100vh 与 `black-translucent` 都没动。

模拟 iPhone（UA、安全区上 59 下 34）：旧代码顶栏在 85、输入栏离底边 34，与用户截图一致；新代码 59 与贴底。

**朋友圈可见范围。** 发布时选「所有角色 / 选择角色 / 仅自己」，存在 `visibleTo`（`'all'`、id 数组、空数组；
老动态没有这个字段算所有人）。发布后看得见的角色逐个评论并点赞（`tasks/moments.reactToMine`，一位一次调用，
发布页写明共几次）；聊天上下文只给看得见的角色（`context/moments.js`）。上一次的选择记在设置里。

### 13.2 接下来

按「用户能不能感觉到」排序，不按实现难度。

**N1 小菜单的格子**：已做完。来电、转账、礼物、位置、外卖、申请、骰子、一起听、一起看、
线下、生成视频都在面板上，顺序由用户自己排（见 4.85）。

**N2 群聊**
第一版已做（见 4.162）：建群、@、一次调用写整轮、每人单独调用的开关、
群自己的记忆开关、图片与语音、译文、群里主动开口、群头像上传。
还没做的：群里的线下。

**N3 Web Push 服务端**
客户端半边（SW 注册、权限、订阅）已经好了，差一个能存 subscription
并发 VAPID 的后端。等你先挑一个托管的地方再说，没定之前不动。

**N4 后台中转 / GitHub 备份**
GitHub 备份已做（见 4.165）：不需要后端，网页直接调 GitHub 的接口，存进用户自己的私有仓库。
剩下的是「后台中转」那一半：密钥仍然暴露在页面里。等 N3 的托管决定。

**N5 下一批（2026-09 整理时列的）**
已做：会话菜单瘦身、「后台在做什么」总览、那年今天（见 4.166）。
还没做：主屏上的火花与纪念日小组件、群里的线下、全局搜索、角色之间私下来往（花钱，默认关）、
小游戏（真心话大冒险、你画我猜、猜歌）、新手引导。

**长期**
- 更多 app 按同一套契约扩展（`stub` 里已经占着位）
- MiniMax T2A 语音端点从没拿真 key 验证过，哪天配上了要先测

---

## 14. 从 rainyword 继承的设计

来源: `yu7705423-cell/rainyword`(单词学习陪伴 app,单文件 index.html)。
其 V2 的 prompt 架构与记忆系统已在真实使用中验证过,直接作为本项目的基础。

### 14.1 原样继承

| 设计 | 说明 |
|---|---|
| 可排序 prompt 区块 | 每个上下文一个 `build()`,按用户可配顺序拼接;顺序清洗 + 逐块 try/catch |
| 固定开场 | 身份声明: "你不是AI助手...你就是X,正在用手机发消息" |
| 记忆 rank S/A/B/C | S/A 全注入,B 关键词命中,C 仅存档。可解释,用户在管理页能看懂为什么某条没生效 |
| 记忆六分类 | fact / emotion / pending / pattern / relation / profile |
| `updateId` 更新机制 | 模型指定更新已有条目而非重复新增,防记忆膨胀 |
| 提取时带入已有档案 | 配合"重复的不要输出",构成去重的另一半 |
| 记忆缓冲区独立存储 | 未总结的对话与 messages 分开,总结后清空 |
| 时间情境注入 | 现在几点 + 距上次聊天多久,转自然语言表述 |

### 14.2 搬运时必须修正

| 问题 | 原实现 | 本项目 |
|---|---|---|
| 记忆无 scope 分层 | `charMemories[charId]` 一个角色一份 | 加 `global` / `character` / `chat` 三层 |
| B 级扫描窗口过窄 | 只扫最后一条用户消息 | 扫最近 N 条消息,与世界书共用窗口 |
| 无 token 预算 | S/A 无条件全注入 | 按 rank 与新近度排序后截断,管理页显示占用 |
| 世界书过于简陋 | 仅 enabled + global/local | 补齐关键词触发、优先级、插入位置、预算(见 3.2 / 4.4) |
| JSON 解析不稳 | 贪婪正则 `/\{[\s\S]*\}/` | 整体 parse -> 括号平衡扫描 -> 报错重试 |
| 硬编码 OpenAI 格式 | 直接拼 `/chat/completions` | 走 provider 适配层 |
| 无请求队列 | 直接 fetch,总结与回复可能并发 | 统一走 AIQueue |
| 回复风格收尾 | "一条消息就是一个念头" | 改为强调节奏不均匀与不周到,见 4.2 B3 |
| prompt 写死在代码里 | 骨架与分析提示词均为字符串字面量 | 全部移入可编辑模板,代码只留默认值 |

### 14.3 改造复用

`generateScenarioSeeds` 在原项目中为目标词生成"不含该词的话题线索",
其**两步法**(先单独生成角色近况,再由角色自然聊起)正是朋友圈动态该用的套路:

```
第一步  生成角色最近经历了什么(独立调用,产出若干条近况)
第二步  以近况为素材产出朋友圈动态 / 主动发起的聊天话题
```

对应 `tasks/moment-create.js` 与后续的"角色主动发消息"能力。

---

## 15. 已确认的设计决定

1. **主页**指"我的"主页(我的人设、我发的动态);
   角色主页从联系人进入。两者复用同一个 Profile 组件,subject 不同。
   **我的人设只在这里编辑**,设置里不再重复一份入口。
2. **群聊**要做。见 4.7,默认每角色独立调用 + 发言调度器。
3. **朋友圈图片**全部由用户本地上传并本地存储,不接图片生成 API。
   这决定了主存储必须是 IndexedDB,见 3.7 / 3.8。
4. **回复风格 prompt** 不采用 rainyword 的"一条消息就是一个念头",
   改为强调打字节奏的不均匀与不周到,见 4.2 B3。
5. **所有 prompt 模板运行时可编辑**,代码里只留默认值,见 4.2 B4。
6. **全屏,不画机身外壳**,桌面与移动端一致。
7. **视口单位只用 `vh`**,禁止 dvh/svh/lvh 及混用。全项目只在根容器用一次,
   其余走 flex 与 grid。见 `CLAUDE.md` 与 8.1。
8. **主界面为横条 + 方形挂件混排**,4 列显式坐标网格,不是等距图标海。见 8.4。
9. **底部是 Dock,放四个 app 图标**,不是 tab 栏。它只是快捷方式,
   不构成导航层级,因此系统层仍是单一栈。见 7 / 8.5。
10. **主界面支持多页横滑翻页**,空位以 placeholder 单元占住,
    版式从第一天就是完整的,以后填入真 app 只替换 `ref`。见 8.4。

### 4.198 聊天背景与上下栏

会话右上角菜单「这段对话」里一行「聊天背景」，点开是半屏面板（`chat/pages/ChatLook.js`），
遮罩不压暗（`Sheet` 的 `cls="is-preview"`），会话就在上半屏，每拧一下都看得见结果。

**是这段会话自己的设置，不是美化包。** 美化是一份能挂在几段会话上、能导出分享的样式；
这里存在会话记录上（`chat.look`），换一段不影响另一段。面板底部「应用到全部会话」照搬一份
到其余全部会话（群聊在内），背景图共用同一个 id。

```
look: { bg, veil, split, top: Bar, bottom: Bar }
Bar:  { style: solid | clear | glass, color: '' | '#rrggbb', alpha: 不透明度, blur, fg: '' | light | dark }
```

- 默认上下一起（`split: false` 时 bottom 就是 top）；打开「上下栏分开设置」时输入栏从顶栏抄一份起步。
- 界面上写「透明度」，存的是不透明度（`alpha`），两者相加为 100。
- 下面那一栏是输入框连同压在它上面的几条（引用、待办、表情联想、@ 成员）、展开的面板、
  多选栏：看上去是同一块，一起换。

**怎么落到页面上。** `chatlook.pageOf` 给出类名与 `--ph-*` 变量，经 `Page` 新增的 `cls` / `vars`
挂在 `.page` 上（第 18 条：只传自定义属性，声明写在 app.css）。没设过的那一栏不挂类名也不发变量，
那一栏就还是应用本来的样子，美化照样盖得住；设过的那一栏 `.page.look-top > .navbar` 比作者常写的
`.ph-navbar` 更具体，同一段会话里用户自己设的为准。公开变量六个，登记在契约表：
`--ph-chat-bg` `--ph-chat-veil` `--ph-bar-top(-blur)` `--ph-bar-bottom(-blur)`。

- 背景画在整页上，顶栏、输入栏底下也是它。遮罩朝**当前主题的底色**淡过去（浅色主题是一层白，
  深色是一层黑），压在图上的时间分隔、提示行另垫一块底。
- 字色：深色栏配浅字时，就地把栏里的 `--text` 几档与输入框底色、描边换掉，里面的元素照旧读那几个变量。
  顶栏字色同时经 `statusBarStyle` 交给网页自己画的状态栏与悬浮返回键。
- 网页自己画状态栏的时候（安卓全屏、安卓安装包、电脑），那一行在 `.page` 外面读不到变量：
  会话页把顶栏颜色写到根上的 `--look-status-bg`，离开时摘掉。
- 栏不是实色（半透明、毛玻璃）时，那一栏浮在消息列表上（类名 `float-top` / `float-bottom`），
  消息从底下滚过去；实色照旧排在列表外面。
  - 顶栏：负的下外边距让出位置，列表顶上补同样高的内边距（`--navbar-h + --top-inset`，定值）。
    会话顶上那几条（一起听、一起看、节奏）照旧排在顶栏下面，没有它们时列表才伸到顶栏底下
    （`.conv-main:first-child`）。
  - 输入栏那一组包进 `.conv-foot`，高度会变（输入框长高、面板展开、引用条出现），
    会话页用 ResizeObserver 量出来挂成 `--foot-h`。列表本身没有变矮，原来那个「列表变矮就贴底」
    的观察器不会来，所以量的时候本来贴着底部的一并贴住。

**图。** 背景图存 images 域，登记在 `purge.usedImageIds` 与 `charpack.collect`。换图、清除、
恢复默认都先改会话再 `images.remove` 旧图 —— remove 会按引用表核一遍，另一段会话或相册还在用
就不删。从相册选的直接引用同一个 id，不另存一份。

### 4.199 正式版与测试版

已经有用户在用，每推一次都直接落到他们手上。分成两份（规则见 CLAUDE.md 第 19 条）：
正式版是 `release` 分支，GitHub Pages 从它发布，网址不变；测试版是开发分支，
Cloudflare Pages 在后台连着仓库，每推一次自动部署到 `*.pages.dev`（无构建命令，输出目录 `/`）。
只有维护者说「推正式版」才合进 `release`。

Pages 单个文件上限 25 MB，ffmpeg 的 wasm（30.7 MB）因此整站部署失败过：现在切成两块放，
`system/ffmpeg.js` 取回来拼好经 `wasmBinary` 交给核心。同一次还查出 ffmpeg 的路径写成了 `/vendor/...`，
正式版挂在 `github.io/phone/` 下面，从根算的路径指到 `github.io/vendor/` 去了，线上一直加载失败。
改成从模块自己的位置算；`tests/subpath.test.mjs` 把应用挂到 `/phone/` 下、拦掉根目录，专查这一类。

测试版不放在 `github.io` 下另一个路径，是因为那和正式版同一个域名，同一台手机上共用
IndexedDB 与 localStorage：测试版的数据迁移会改掉正式数据，`DB_VERSION` 被测试版升上去之后，
旧代码的正式版打开同一个库会直接失败。换一个域名，两边天然隔开。代价是测试版一开始是空的，
要用真实数据测就从正式版导出备份、在测试版导入 —— 数据迁移这类改动恰恰要这样测。

`system/channel.js` 按网址（`*.pages.dev`）认出测试版：左边缘挂一枚「测试版」（点不到，不挡边缘返回）、
标题改成「Eira 测试版」、设置页构建号后面写明。测试里用 localStorage 的 `eira-channel=test` 代替那个域名。

### 4.200 互相改备注

两个方向，各存一处（`system/remark.js`）：

| | 存在 | 谁改 | 显示在 |
|---|---|---|---|
| 我给角色的备注 | `char.remark` | 会话菜单「这个角色 - 备注」 | 会话列表、会话标题、联系人列表 |
| 角色给我的备注 | `chat.charRemark` | 角色在回复里写 `[备注：…]` | 会话里一行提示；查它手机时「与你」那一栏与点进去的标题 |

- 我给的只是换个叫法：prompt 里照旧用角色本名，另告诉角色一句「对方把你存成了什么」。
- 角色给的挂在会话上而不是角色上：同一个角色对不同身份的你各是各的。
- 能力 `remark`（目录一行 + `skeleton.remark` 细则），行里带上当前两边的备注，是数据不是判断。
  角色卡里「改备注」关掉时能力不注入，写了也不生效；群聊不接（群里那个「你」在谁的通讯录里说不清）。
- 名字没变不重复落提示。提示行带着这一轮的 `turnId`，重新生成这一轮时一起换掉。
- `MARK` 那条总正则里要同时认 `改备注|备注`，漏了那一行会被当成普通文字发出去（测试里栽过一次）。

### 4.201 各类卡片的美化钩子；头像整边隐藏；会话里的美化页精简

**钩子。** 契约表新增六个（加钩子不动契约版本）：`ph-trans`（文字与语音转写下的译文）、
`ph-transfer`、`ph-voice`、`ph-call`（通话结束后的那条记录）、`ph-location`、`ph-gift`，
都带 `needs`。生成器复刻页（`skin-stage.js`）里摆了这几种卡片与一行 `ph-time-sep`，
结构照真组件抄，`stage.mjs` 照旧核对钩子那一层。

**生成器。** 译文、引用条、语音、转账、礼物、位置、通话记录各一组（`CARD_GROUPS`，排在贴图之后、
底栏之前；带 `card: true`）。存的时候互不相干，**竖栏上合成一格「卡片」**，点进去先选改哪一种 ——
一种一格的话竖栏十九格，一屏装不下（`rail.mjs` 查着「一屏装得下」）。
旋钮一样：改哪一边、底色、文字与图标颜色、圆角、描边（译文与引用多一个字号）。
卡片里几行小字各有颜色，文字颜色一并写到 `卡片 *` 上。「时刻与已读」一组补了居中那行时间的
颜色、字号、底色、圆角。分组可以带 `desc`，生成器在标题下显示，用来写出类名。

**头像显示。** 头像一组多了「显示头像：双方 / 仅角色 / 仅自己 / 都不显示」（`gen.avatar.show`），
不显示的那一边 `display: none`，连位置一起收掉。和原来「连着的几条里哪几条有头像」
（`opacity: 0`，留位置）不是一回事，两项并存。

**分工。** 会话里的美化页只管「这段会话挂哪一份」：挂上、换一份、取下、新建、导入，
外加头像形状与显示头像、消息时刻与已读回执，上方仍是真气泡的样板间。改一份美化本身的
（生成器、尺寸、自定义 CSS、复制 CSS、导出、改名、删除、暂停后恢复）都在「美化」app 那一份的页面上，
尺寸与自定义 CSS 各一个子页（`/size/:id`、`/css/:id`）。改的是那一份，挂着它的每段会话都跟着变，
放在某一段会话里容易让人以为只改了那一段。

- 「复制 CSS」复制 `skin.compile` 的整段（尺寸变量、生成器、头像框、自定义 CSS），JSON 导出保留。
- 原来会话美化页里的「类名」一栏与它背后的 `skin.CLASSES`（内部类名表）一并删掉：内部类名不对外
  承诺（第 18 条），对外的是 `ph-` 那一套，在「写给作者」里。
- `scripts/test.mjs` 加了 `TEST_VERBOSE=1`：失败时打出输出末尾 40 行，超时类的错看得出卡在哪一步。

### 4.202 角色卡展示页（拍立得）；在会话里换头像

**三张图，三样用处，各存各的：**

| 字段 | 是什么 | 在哪儿用 |
|---|---|---|
| `avatar` | 聊天头像 | 气泡旁、列表、主页 |
| `faceImage` | 锁脸照片 | 给生图对脸，不给人看 |
| `portrait` | 形象照 | 角色卡展示页上那张拍立得 |

会话菜单「角色卡」进 `/card/:charId`（`chat/pages/CharCard.js`）：形象照铺在相纸上，白边写名字
（有备注写备注，本名另起一行）与签名；点相纸或下面的按钮换图。**人设一个字都不露**（第 6 条），
改设定走「编辑资料」。形象照不发送给模型。存取在 `avatar.js` 的 `setPortrait` / `clearPortrait`，
换图时删旧图（`images.remove` 先核引用）。

**会话里换头像。** 会话菜单多了一组「头像」：更换角色的头像、更换我的头像（群聊只有后者）。
都走 `avatar.setFace`：换下来的那张留在 `avatarBase`（主页上换得回去，与 `restoreFace` 同一条规矩）；
换自己的顺带 `rememberMine`，角色下一轮知道你换了头像。

**顺带修的。** `avatarBase` 从来没登记在 `purge.usedImageIds` 里（角色的、自己的都没有），
「清理无引用」会把换下来的原图当成没人用的删掉，之后就换不回原本的头像。现在角色、身份两处都登记，
`dropCharacter` 与 `charpack.collect` 也一并带上 `portrait`、`avatarBase`。

### 4.203 悬浮返回键：单击、双击、长按

「返回键」那一档（`navStyle: 'back'`）：点一下退回上一级，**双击回主界面**，**长按打开多任务**。
主界面上不再显示它（从前在主界面上它是「打开多任务」的按钮，多出一块东西）。

单击等 240 毫秒没有第二下才办。立刻就退的话，退到主界面这个键就没了，双击的第二下落在底下的
应用图标上，平白打开一个应用。`navback2.mjs` 专查这一条。

### 4.204 拍立得的签名；手写体字体槽；按网址添加字体

拍立得白边上是**个性签名**（`signature`），用手写体放大写；下面一行小小的 `@名字`（有备注写备注）。
没有签名时白边上写名字，不再重复 `@`。

**手写体是第三个字体槽**（`settings.fontHand` → `--font-hand`，和正文、衬线同一套 `fonts.apply`）。
默认 `--font-hand-base`：系统里的行楷、楷体，没有就回落 cursive。在角色卡展示页「签名字体」里选，
所有角色卡共用一种（第 5 条：要改它的时候人正看着拍立得）。

**字体库能按网址添加**（`fonts.addUrl`）：
- 字体文件网址：下载下来走 `fonts.add`，存进 files 域，离线也能用，记下 `source`。
- 样式表网址（Google Fonts 那种）：存 `{ css, family }`，用的时候挂一个 `<link>`。中文字体被切成上百片，
  浏览器只取用得到的那几片；整份下载下来反而几十兆。`familyOf` 对它返回样式表里写死的 family。
- 读不到（不允许跨域、网址错）时抛出说得清的错，不存一条用不了的记录；只收 https。
- 展示页给了四款 Google Fonts 的中文手写体（`HAND_PRESETS`），点一下加进字体库；需要联网才显示。
- 删掉正在用的字体时手写槽一并清空，在线字体的 `<link>` 一并摘掉。

### 4.205 组件式浮层渲染到所在那一层的最后面

`Sheet`、`FullSheet` 在哪个组件里写，从前就在那个组件的 DOM 里画。iPhone 上可滚动的那一块
（`.page-body.scroll`）自成一层，里面的东西 z-index 写得再高也盖不过它后面的兄弟：朋友圈的评论表
被底部标签栏的图标压在了输入框上，写不了字。电脑上的 Chromium 不会叠错，所以一直没发现。

现在 `ui/overlay.js` 的 `Portal` 在原地只留一个隐藏的占位，内容 `render` 到所在那一层
（`.app-layer`、`.home-layer`，都没有就 `.screen`）最后面的一个 `.sheet-host` 里。
`.sheet-host` 是 `display: contents`，不占位；`.overlay`、`.fullsheet` 仍按应用层绝对定位。
- 同一个容器里每次重画都是比对着改，表里的组件状态不丢；关掉时占位与容器一起收掉。
- 后开的浮层 append 在后面，自然叠在先开的上面（整屏浮层里再弹一张表也一样）。
- 整屏浮层从应用层顶上盖起，顶栏照应用页的规矩让出刘海那一条（`--top-inset`）。
- 项目里没有用 context，所以换一棵渲染树不丢东西。以后要用 context，这里要改成带 context 的 portal。

**测试里按 DOM 顺序取第一个输入框的会受影响**：表现在排在页面后面。`apisource` 就是这么挂的，
选择器限定在 `.sheet` 里。

评论表另外把输入框挪到最上面、表占大半屏，已有的评论排在下面：键盘起来只盖得住下半截。
`momentcomment.test.mjs` 查的是浮层挂在哪儿（不在滚动容器里、在应用层最后面），不是截图。

### 4.206 启动画面

首次打开要下几十个模块，慢网上白屏好几秒。从前只有 `#app` 里一个小圈圈，而且要等 `main.js`
下完才画得出来，有人以为打不开就退了。

现在 `index.html` 里直接写着一块 `#splash`：**Eira** 字样，下面一排 13 根竖条按错开的相位起伏，
像音波在流动。
- **写在 HTML 里，样式内联、颜色写死**：它不等 js，也不依赖 tokens.css。
- **五个样式表不许挪进 body。** 挪过去启动画面能再早一点露面（head 里的样式表挡整页第一帧），
  但美化、自定义 CSS、字体都往 head 末尾追加 `<style>`，靠排在基础样式表后面才压得过；
  样式表进了 body 顺序就反过来，所有美化一起失效。`viewport`、`fullscreen` 两个测试
  （往 head 里垫安全区）当时就是这么挂的。样式表多半在缓存里，挡的那一下很短；慢的是几十个模块。
- **主题跟着上一次**：`Root` 应用主题时顺带记进 `localStorage['eira-theme']`，下次启动画面直接是那一套。
  没记过按默认浅色。
- 等过 6 秒底下淡出一行「正在载入。网络较慢时，首次打开需要更长时间。」
- `main.js` 画出第一屏（`Root`、登录页、启动失败）之后 `dropSplash()`：淡出 0.28 秒再摘掉。
  代码版本对不上、正在自愈重载的那一段，启动画面留着，那行字换成「正在更新到最新版本」。
- `pointer-events: none`：它只是盖着，不接点击。
- `prefers-reduced-motion` 下波动放慢一半。

`splash.test.mjs` 把 `main.js` 拖住几秒来查。注意 `type="module"` 的脚本会一起拖住 DOMContentLoaded，
等这个事件就看不到启动画面了，测试里只等到 `commit`。

### 4.207 撤回：消息与角色的动态；角色的动态可以删

**撤回不是删除。** 删除把整条从库里抹掉；撤回是在那一条上记一笔 `recalled`，界面折成一行，
点一下还看得到原文。规则都在 `system/recall.js`。

**消息** `msg.recalled = { at, by, seen }`
- 我撤回：长按自己那一条「撤回」（提示行、线下那一场不能撤）。`seen` 记的是撤回时角色回过话没有。
- 角色撤回：回复里在那一条下面写一行 `[撤回]`，和心声一样挂到上一条上。`at` 写成发出后 2.5 秒：
  先照常显示，到点折起来。气泡组件是 memo 过的，到点没有别的东西让它重画，所以自己掐一个表。
- 会话里居中一行「你撤回了一条消息 / 某某撤回了一条消息」，右边「查看」展开原文（压淡），再点收起。
  长按这一行照样弹菜单。契约钩子 `.ph-recall`。
- 会话列表预览直接写「…撤回了一条消息」，不等那 2.5 秒（列表那一行不会为了到点再重画）。
- 进上下文（`engine.buildHistory` / `buildGroupHistory`）：
  - 我撤回、角色没读到：`[撤回了一条消息]`，图片不发、歌词不附。
  - 我撤回、角色读到过：`[撤回了一条消息：原文]`。它已经回过这句，拿掉原文它的回话就没了着落。
  - 角色自己撤回的：原文照进，末尾补 `[撤回]`，和它该写的格式一模一样。历史就是示范。
- 角色卡「撤回」开关（`canRecall`，默认开），能力目录里一项 `recall`。群聊不给。

**动态** `mo.recalled = { at }`，只有角色的动态会被撤回。
- 聊天里写一行 `[撤回动态]`：撤回它最近一条还挂着的。能力说明里带上那一条的时间与正文，
  不然它不知道自己最近发过什么（聊天上下文里没有它自己的动态）。
- 回我的评论时（`task.moment-reply`）可以在 JSON 里交回 `"recall": true`。是同一次调用顺带的一个字段，
  不另调接口（第 15 条）。
- 朋友圈、主页列表里折成一行「某某撤回了一条动态」，点开是原来那张卡片，压淡，只剩删除按钮；
  详情页顶上写明已撤回，不收赞、不收评论。主屏小组件、主页照片格、照片数都不算撤回了的。
- 进上下文的「对方的朋友圈」只有我自己的动态，所以撤回不影响它。

**角色的动态可以手动删。** 卡片和详情页都有删除按钮，确认后连同评论与点赞一起删。

### 4.208 引用放在哪儿：纯 CSS 的四种版式

从前引用条在 DOM 里排在气泡前面，只能画在上方；想放到下面、包进气泡，CSS 都做不到。现在：
- `.ph-quote`（气泡外面那一条）在 DOM 里排在气泡**后面**，默认 `order: -1` 画到上方
  （`.msg-col` 是纵向 flex，群里那行名字 `order: -2` 在它更上面）。美化写 `order: 0` 就回到下方，
  `display: none` 就不显示。
- `.ph-quote-in`：第一个文字气泡里面另有一份，默认 `display: none`。要「包在气泡里」：
  `.ph-col:has(.ph-quote-in) > .ph-quote { display: none }` 再把 `.ph-quote-in` 放出来。
  图片、转账这类没有文字气泡的，找不到里面那一份，外面那条照旧显示。
- 生成器「引用」一组多一项「引用显示在哪里」：气泡上方 / 气泡下方 / 包在气泡里 / 不显示。
  包在气泡里时，底色、字色那几项改的是里面那一份。
- 写给作者的类名清单（契约表的 note）写明了这几种写法。样板间同样两份都有。

### 4.209 角色说完之后再按「回复」；主动发起带上聊天记录

**症状**：角色回完一轮，我不说话直接再按一次回复，它把上一轮换个说法又说一遍。

**原因**：请求以角色自己那几句（assistant）收尾，前面最近的一条 user 还是上一轮我那句。
模型就当作还在回那一句。有的接口还会把收尾的 assistant 当成要续写的半句。

**改法**：`buildHistory` / `buildGroupHistory` 收 `followUp: true` 时（`engine.withFollowUp`），
最后一条对话是 assistant 就补一条 user：`skeleton.follow-up`（对方还没回，这次写的排在那几句后面）。只陈述事实（第 16 条）。
**在按深度插世界书、现在几点、本轮召回之前补**：深度数的是离末尾几条，补在后面的话本该贴着
最后一条的那几块就隔了一条（`lore`、`midsystem` 两个测试当时就是这么挂的）。
单聊 `streamReply`、群聊 `streamGroupReply`（不是「成员先开口」那一种时）都传。我刚说完的正常情况下末尾就是我那句，不补。

**主动发起**（`proactive.sendProactive`，含「情绪来了」那一档）从前**一条聊天记录都不带**：
只有设定区加一句「这次由你开口」。角色不知道聊到哪儿，也不记得自己上次主动发过什么，
隔几小时把同一句问候再发一遍。现在和平时回复一样走 `buildHistory`（条数、字数上限照「用量与上限」），
末尾一条「这次由你开口」；每轮都变的那几块照旧插在对话末尾。仍然是一次调用，只是带的字多了。
`runTextTask` 为此多收一个 `messages`。

`followup.test.mjs` 截下真正发出去的请求来查。

### 4.210 滑一下引用；接口页切换模型；表情面板写出描述词

**滑一下引用**（`Conversation.js` 的 Bubble）。长按菜单里的「引用」要两下，回话最常用的却是它。
在一条消息上朝左或朝右滑，横着先走够 10 像素才算归气泡（竖着走得更多的是翻消息，让开），
跟着手指最多平移 72，滑够 56 松手即引用，没滑够弹回。起手落在左右边缘 40 像素里的归页面的
边缘返回（`ui/page.js` 的 `EDGE`），气泡不接。位移写进 `--swipe-x`，`.msg.is-swiping` 读它，
不在契约钩子上写内联 transform（第 18 条）。长按菜单里的「引用」保留，说明里写了可以滑。
一旦横着归了气泡，长按的计时就取消。

**切换模型**（接口页顶上一组）。主用、副用各一行，点开直接是模型列表，只换模型，地址与密钥不动
（`services.switchModel`）。换下来的那个记进这套接口的 `recentModels`（最多 8 个），模型列表顶上
「最近用过」一排，拉列表慢或拉不到时也能直接切回去。编辑页里「获取并选择」也走同一个函数。

**表情面板写出描述词**。发出去之后角色读到的只有 `[表情：名称]`，图片从不给模型。
所以面板上每个表情下面写着名称，编辑页的「名称」一栏改叫「名称（描述词）」并说明这一点。

### 4.211 引用掉格式；主屏图标名称颜色

**引用为什么老掉格式**，三个原因叠在一起：
1. **历史里的写法和要它写的不一样。** `engine.withQuote` 从前把引用过的消息写成
   `(in reply to 「…」) 正文`，而能力说明要它写单独一行 `[引用：…]`。模型最听自己前几轮的样子，
   照着历史抄，解析不认，括号原样漏进气泡。现在历史里写成 `[引用：摘录]` 单独一行、正文在下一行。
2. **摘录里不许有括号。** 从前那条 `QUOTE_LINE` 的摘录部分排除了 `)` `）`，
   而本项目的动作正是写在（）里 —— `[引用：（摸摸头）好乖]` 整行认不出来。
3. **只认整行。** 标记和正文写在同一行（`[引用：…] 好啊`）就不认。

现在 `reply.quoteOfLine` 逐个认：按深度数括号找收口（摘录里的（动作）不算），给回 `{ quote, rest }`，
同一行后面的正文照常往下走。另外认 `(in reply to 「…」)`（旧历史的写法）、`【引用】「…」`、
`> 摘录`（`>` 后要有空格，`>_<` 不算）。摘录外面包的引号、尾巴上的省略号在认领出处前剥掉。

**「修正格式」也修得动了**：它的「按标记重新整理」用的就是 `splitReply`，认得出来就修得回来。
认领出处时排除正在写的那一轮（`resolveQuote` 的 `turnId`）：重排一条旧消息时，那条消息的原文里
正带着这段摘录，不排除就认领到它自己，修完就是一个点不动的引用。

**主屏图标名称颜色**。「外观 - 图标」里多一项「图标名称颜色」：自动 / 几个色块 / 自选。
存 `settings.iconLabelColor`（空为自动），`look.applyLook` 写到 `--ph-tile-name-color`，
`.app-name` 有没有壁纸都读它，读不到才回落原来的颜色（无壁纸跟主题、有壁纸白字带投影）。
登记为契约变量（`on: ['shell']`），美化包也能写；外观预设（looks.js）一并保存这一项。
文件夹展开后那一层底色是浅的，照旧用自己的字色。

### 4.212 图标四周的透明边；每个图标的图片大小

网上下的 png 图标四周常留一大圈透明。从前整张图原样放进格子（`background-size: contain`），
格子的边界就是那张图的边界，图标本体只占中间一小块，看着空了一大圈。

**上传时自动裁掉。** `images.compressFit` 多一个 `trim`：先按不透明像素（alpha > 10，抗锯齿那一圈不算）
找出内容的外框，只把这一块放进正方形画布。四边都贴边、或整张透明的不裁。
自己主屏的图标（单个、批量）、角色手机的图标都开着它；头像池不开。

**早先传的**：换图标那张表里「去掉四周的透明边」，外观页「去掉所有图片四周的透明边」一次处理全部。
`look.trimAppIcon` 读出存着的那张重新裁，换成新 id、删旧的；本来就贴边的不动（`images.hasPadding`）。

**图片大小**：换图标那张表里一条滑杆，50% 到 160%，存 `appIcons[id].scale`（100 不存）。
格子读 `--tile-img-size`（`background-size`），不在契约钩子上写内联声明。调大时超出格子的部分被裁掉。
裁透明边是自动的那一步，滑杆给裁完还想再调的人。

**不要底板（异形图标）**。换图标那张表里「显示底板」，每个图标各自一项，存 `appIcons[id].bare`。
关掉后格子加 `is-bare`：底色、阴影、毛玻璃、圆角全去掉，描边改成透明（不去掉，格子大小不变），
只剩图片本身。选择器前面挂 `:root`，压过有壁纸、开毛玻璃那两条。只有自己主屏的图标有这一项
（`service.bare`），角色手机那边的格子是另一套画法。

**上传时裁不裁透明边**也能关：同一张表里「上传时裁掉四周的透明边」，全局一项（`settings.iconTrim`，
默认开），自己主屏、批量更换、角色手机的图标都照它。本身带留白、想原样保留的图标关掉它再上传。

### 4.213 衣帽间（第一期）

一个新 app（`apps/closet`），衣橱与妆台两个标签，可以在「我 / 某个角色」之间切换。

**层次**：大类、小类、单品三层，分类表只有一份（`system/closet-kinds.js`）。
- 衣橱九个大类（上装、下装、连身、外套、鞋、包、首饰、配饰、家居），妆台八个（护肤、底妆、眼妆、唇妆、
  修容、香氛、发与身体、工具）。每个大类下的小类可以自己加（`settings.closetSubs`），用不到的大类可以隐藏。
- 妆台的小类带默认值：每次用量的单位与数量（粉底 2 泵、香水 2 喷）、开封后可用几个月（PAO）、是否主要记色号。
  单位换成毫升的比例在 `DOSE`，单品页上可以改。工具一类不算余量。
- 横切标签只用来筛选，不决定位置：颜色（色块值只写在分类表里，页面走 `--sw`）、季节、场合（运动是场合，
  不单列大类）、来源（自己买的 / 收到的礼物 / 一起做的，另记 giver、with）、状态（在用、收藏、闲置、已用完、已送出）。
  已用完、已送出的不删、默认不显示、也不告诉角色（`closet.live`）。

**页面**：总览（三列大类卡片，九个一屏看全；衣橱顶上「今天穿的」，妆台顶上「快用完或快过期」；
「未整理」放没分类的）、大类页（小类一排、筛选、搜索）、「全部」（筛选加排序）、单品页（每一栏改了就存）。
添加两条路：拍照或选图（可多选，每张一件，落在「未整理」或当前大类），手动（大类、小类、名字）。

**数据**：`closet` 域，按 `owner` 建索引（'me' 或角色 id；自己的另记 personaId）。
登记齐了：`db/index`、`schema.STORES`（DB_VERSION 24）、`backup.COLLECTIONS`、`purge.dropCharacter`
（删角色连它的衣帽间，我收着的它送的东西不动）、`purge.usedImageIds`、`charpack.collect/install`
（角色的衣帽间算角色的一部分，不看要不要带聊天记录）。

**估算**（全在本地）：
- 余量：容量、每次用量、每天几次三样都填了才算，从开封那天（没有就购入、再没有就放进来那天）起按天扣；
  「按实际剩余校准」记一个 level，从那一刻重新扣。
- 保质期三种依据，前面的有就用前面的：填了的保质日期、开封日期加 PAO、只有购入日期时加未开封保质期（默认 36 个月）。
- 快用完：余量低于 15% 或按当前用量不足 14 天；快过期：离过期不足 30 天。都在衣帽间设置里改。

**给角色看的**（上下文「衣帽间」一块，可在上下文页关掉，属每轮会变的那一类）：
- 今天穿的：对方勾了的那几件带名字、分类、颜色、描述，礼物写明「谁送的、哪天」（按读它的角色的视角：
  它送的写 you）。角色自己今天穿的（用户替它勾的）另列一段。
- 清单：**只在最近几句话里出现穿搭、化妆的词时才带**，按大类分行，只写名字，每类几件在「用量与上限」里改
  （默认 8，填 0 全带）。和能力目录的冷热同一个做法，本地词表认，不花钱。
- 快用完、快过期：**频率是代码管的，不靠提示词去劝**（第 16 条）。一件东西满足条件后，只在第一次递出去的
  那一天出现（`alertAt`），之后要过了冷却期（默认 30 天）才再出现一次。所以不会天天问「是不是快用完了」。
- 结尾一句只说事实：清单里没有的就是不知道，不要编。

**调接口的两样都是点了才调**，一件一次，按钮上写着次数：识图（`ai/tasks/closet.js` 的 recognize，
`task.closet-vision`，只填空着的几项，认不出的颜色丢掉；多张时先问一句「共 N 次」）、按描述生图（`task.closet-image`）。

**礼物联动**：拆开了的礼物气泡上多一个「收进衣帽间」，经 intent 打开 `/gift/:msgId`：角色送的收进我的、
我送的收进那段会话里那位角色的，名字取拆开后的东西，记上谁送的、哪天、哪段会话（单品页上能跳回去）。
同一条礼物收过就直接打开那一件。

**我送的礼物自动收**：角色拆开我送的礼物时（`gift.settle`，open 且 role 为 user），`closet.keepGift`
按名字（认不出再按外包装）用本地词表 `guessKind` 猜分类：先认小类名（「针织 / 毛衣」拆开认，长的先认），
再认一份别名表（耳环、手镯、毛衣、高跟……）。太泛的几个小类名（`LOOSE`：造型、平底、套装、手提……）
单独出现时不认，免得「平底锅」进了鞋柜。认得出就直接收进那位角色的衣帽间，记 `auto: true`；
认不出（一盒糖、一束花）就不收，气泡上的按钮照旧可以手动点。整轮重新生成把礼物退回待拆时
（`gift.unsettle`）`dropAutoGift` 撤掉那一件；用户动过它（走 `closet.update`）就清掉 auto，不再撤。
角色送我的仍然手动收。不调接口。

第二期见 4.214。

### 4.214 衣帽间（第二期）：套装、角色搭配、生成角色衣帽间、好久没穿、生图参考

**套装**和单品存在同一个 `closet` 域里，`side: 'outfit'`，`items` 存单品 id。这样备份、删角色、
角色包都跟着单品走，不用另登记一个域；单品那边的查询全按 side 筛（wear / beauty），套装自然不会混进
清单、今天穿的、快用完。单品删了，套装里那一格读的时候跳过（`outfitItems`），不回头改套装。
角色包复制安装时单品换了新 id，`charpack.install` 先放单品、记下新旧 id，再放套装并换掉 `items`。

- 「今天穿这一套」（`wearOutfit`）：今天穿的换成这几件，不在这套里的取消；同一天穿两次只记一次。
- 「今天穿的」那一条上多一个「存为套装」（`outfitFromToday`）。
- 衣帽间首页衣橱那一侧多一行「套装」，进去是套装列表、套装页（改名、调整单品、删除）。

**角色搭配**：新能力 `outfit`，标记 `[搭配：名字 | 甲、乙、丙]`（`reply.js` 的 OUTFIT_KINDS）。
- 只在我这边衣橱里有分好类的东西、并且是一对一的会话时才给（`hasWardrobe`）；聊到穿搭（与上下文带清单
  同一份词表 `closet.WEAR_TOPIC`）或最近用过时细则 `skeleton.outfit` 跟着到。细则只写格式与「按名字对」。
- 落成 `kind: 'outfit'` 的一条消息（`postOutfit`）：`outfitItems` 是按名字认出来的单品（先认一模一样的，
  再认互相包含的，同一件不认两次），认不出的留名字、id 为空，卡片上画成灰的。正文就是那一行标记，
  历史里角色读到的也是它。
- 卡片（`chat/pages/OutfitBits.js`，契约钩子 `ph-outfit`）上「存为套装」经 intent 打开衣帽间的 `/fit/:msgId`：
  存过就打开存过的那一套（`fromMsgId` 认），一件都认不出就不存。套装上记 `by`（谁搭的）。

**我替角色挑一套**：在角色的衣帽间里存好一套，套装页上「发给某某」（`sendOutfit`）落一张我发的卡片到和它
最近的一对一会话，认的是它的衣帽间。角色下一轮在历史里读到那一行。

**按角色设定生成**（`ai/tasks/closet.js` 的 `wardrobe`，模板 `task.closet-wardrobe`，用量记 `closet.wardrobe`）：
角色的衣帽间首页一行「按角色设定生成」，一次调用，件数自己填、不设上限；只生成文字。结果先列出来勾选
（第 6 条的例外），勾了的才入库（`keepWardrobe`）。已有同名的、大类不属于这一侧的丢掉，认不出的小类留空。

**好久没穿**（`takeIdle`，上下文「衣帽间」一块）：穿过至少一次、上次穿着超过 `closetIdleDays`（默认 60，
填 0 关掉）天的衣物。和快用完同一个办法，**频率是代码管的**：一次只递一件、当天整天都在；两次之间至少隔
`closetIdleGap`（默认 7）天；同一件一段闲置只递一次（`idleAt > lastWorn` 即递过），再穿一次才重新算。
只写事实：名字、分类、上次穿的日期、隔了几天。

**生图参考今天穿的**（`imageprompt.js` 的 `wearOf`，衣帽间设置里的开关 `closetInImage`，默认关）：
画面描述点了角色或我的名字、或写着合照、我们、一起这类词时，把此人今天穿着的名称与描述接在提示词后面，
抓包的分段里标成「今天穿的（衣帽间）」。不增加调用。


### 4.215 衣服照片上的模特：脸只能是角色的

存衣服常用网店图，照片上是模特穿着。那张脸**绝不能**跑到角色（或我）身上。能漏进来的路有三条，各堵一道：

1. **衣物照片永远不当参考图发给生图接口。** 生图只拿文字（`imageprompt.js` 的 `wearOf`）。
   以后要做「按衣服原图生成」时，必须先把人裁掉或只发平铺图，不许直接把这张图塞进 edits。
2. **识图只记衣服。** `task.closet-vision` 写明照片可能是模特穿着的，name 与 desc 不提穿的人、
   不描述脸、头发、皮肤、身形、姿势、表情；按角色设定生成（`task.closet-wardrobe`）同样写明。
3. **描述拿去给别人读之前先过 `closet.garmentOnly`**：按句切开，提到模特、人台、脸、发型、肤色、身材、
   表情、姿势（中英文，英文按整词认，surface 不算 face）的句子整句去掉。识图回来存之前过一遍，
   生图接今天穿的、上下文里给角色看的今天穿的，读的时候再过一遍（用户自己写的描述也可能混进模特）。
   宁可多删一句衣服的细节，不能让别人的脸混进来。

生图那一段末尾另接一句：衣物那几行只描述衣服，脸、头发、身形按各人的外貌来，不是拍衣服的模特。
抓包分段里标作「衣帽间：只取衣物」。测试 `tests/closetface.test.mjs`。

### 4.216 衣帽间（第三期）：随身、借穿、回忆、动作卡片、穿搭盲盒

**随身**：衣橱多一个大类「随身」（`carry`：伞、耳机、相机、钥匙、零食、小物）。勾上记在同一个 `wornOn` 上，
意思是「今天带着」。首页「今天穿的」下面多一条「今天带着」（`TodaySheet` 的 `carry`）。用 `isCarry` 分开说：
给角色看另起一段 `In …'s bag today`；生图不画它；「好久没穿」不算它；`wearOutfit` 换一套衣服不动它。
配饰多一个小类「领带」。别名表认「雨伞」「拍立得」。

**借穿**：`item.lent = { to, at, personaId }`，`lend` / `giveBack`。借出去的东西还在原主人的衣帽间里
（格子上一个借出的记号），今天穿不穿算在借的人身上：`wornToday` 去掉自己借出去的、加上借来的
（`borrowedBy`）。首页多一条「借来的」，今天穿的那张表里借来的另列一组。给角色看时借来的写明是谁的、哪天借的；
两人之间借了没还的（`lentBetween`）单列一段，今天没穿也在：东西在谁手上是一件持续的事。
入口在单品页「借穿」一段：自己的「借给角色」，角色的「借来穿」，借出期间「收回 / 归还」。

**回忆**：`item.memories = [{ id, at, text, chatId, msgId }]`。**只由用户记，不自动编**：会话里长按一条消息
「记到衣帽间」经 intent 打开 `/remember/:msgId`，挑一件东西（我的与这段会话里那位角色的）；或者单品页手写。
穿着、带着它的那天，上下文里这一件后面带最近几条 `remembered 日期: 「原话」`，条数 `closetMemMax`
（「用量与上限」，默认 2，0 为全部）。原话是数据，不翻译。

**动作卡片**：单品页「在会话中使用」，挑一段一对一会话、一个动作（`actionsFor` 按大类给几个现成的，
可自己写），落一条我发的 `kind: 'groom'`，正文 `[动作：请你帮我喷上「木质香水」（雪松与檀香）]`，
角色的东西后面带「（你的）」，描述先过 `garmentOnly`。不调接口，角色下一次回复时读到；怎么回应由角色卡决定。
契约钩子 `ph-groom`。

**穿搭盲盒**：角色的套装页「作为穿搭盲盒发出」，填一个主题，落一条 `kind: 'dresscode'`：
**正文与卡片上只有主题**，我挑的那几件存在消息上但不进历史。这期间角色写的 `[搭配：…]` 由 `postOutfit`
看到这段会话里有没揭晓的盲盒（`openDresscode`）就封上（`sealed`、`dresscodeId`），卡片只露「已为你挑好一套」。
我在主题卡片上点「揭晓」（`revealDresscode`）：盲盒标成已揭晓、角色那几张解封，另落一行提示
`[穿搭揭晓：主题｜我为你挑的：…｜你为我挑的：…]`，角色下一轮在历史末尾读到两边各挑了什么。
搭配能力在「穿搭主题」出现后是热的。契约钩子 `ph-dresscode`，封着的搭配卡片另带 `is-sealed`。

测试 `tests/closet3.test.mjs`。

### 4.217 剧情里的衣帽间：线上线下自动认、塞进包里

衣帽间不是单独的一个 app，它为主线剧情服务：角色在聊天或线下剧情里换了衣服、借走、借出、归还、
往对方包里塞了东西，衣帽间都跟着变。全在 `system/closet-story.js`，本地认标记，不多调接口。

**标记**（角色写；认不出是哪一件就当没写过）：
- `[换上：名字]` 该角色换上自己的、或借来的一件（记今天穿着）
- `[借走：名字]` 该角色借走我的一件；`[借给你：名字]` 该角色把自己的一件借给我；`[归还：名字]` 还回一件
- `[塞进包里：东西]` 只在线下：偷偷放进我的包里，可以是衣帽间里没有的东西

**线上**：能力 `closetact`（能力开关里可关，群里不给），细则 `skeleton.closet-act`。`reply.js` 认前四个，
做完落一行提示（`[阿岚借走了你的红色围巾]`，历史里角色也读得到），提示上存着那一件改之前的样子
（`closetUndo`）；删掉这一行（多半是重新生成那一轮，`dropMessage`）照着改回去。

**线下**：`buildSceneSystem` 接 `skeleton.scene-closet`（同一个能力开关管，群戏不给）。标记在 `scene.js` 的
`addBeat` / `appendBeat` / `addSwipe` 里从正文摘掉、照做（只认角色写的、只认真的一场戏，「我们」的章节不认），
做了什么记在那一版上（`acts`，另存摘标记之前的原文 `marked`）。重写、翻到另一版、删掉那一版或那一段时，
先按 `acts` 撤回，再按新的那一版做一遍。写完一段有借还、换上时提示一句；塞进包里的不提示：那是秘密。

**塞进包里**：记在这一场上 `scene.slips = [{ id, from: 'char' | 'me', authorId, what, beatId, at, arrived }]`。
- 角色放的：标记从正文摘掉，我读的正文里没有；它自己的线下提示词里接一段「这一场你放进对方包里、对方还没发现的」
  （`secretsFor`），免得它自己忘了。
- 我放的：走界面，线下菜单「偷偷放进对方包里」、聊天里演的那种在「放进包里」。**不写进正文**，
  所以角色的提示词、历史里都没有。
- **收场**（`arrive`，聊天里演的「收场」、单开一页的「收场」都会调，只落一次）：
  角色放的落成一条 `kind: 'slip'` 的卡片（`ph-slip`），点开之前只写「包里多了一样东西」，会话列表的预览、
  通知也只写这一句；点开（`openSlip`）落一行 `[打开包，发现了：…]`，角色下一轮读到；
  打开后可以「收进衣帽间」（`keepSlip`，认得出分类就放进那一类，认不出放进随身）。
  我放的落一行 `[阿岚回到家打开包，发现了你偷偷放进去的：…]`，并收进它的衣帽间，记着是我送的。

测试 `tests/closetstory.test.mjs`。

### 4.218 一起听接网易云；主屏角标不裁

**一起听**。只用网易云的人本机曲库常常是空的，从前三处都卡在这里：
- `listen.start` 挑不出第一首就报错，角色写 `[一起听]` 起不来，同一轮后面的 `[点歌]` 也跟着落空；
- 一起听页只列本机曲库，没有搜索，看不到自己的网易云歌单，「加歌」也只能从曲库里挑。

现在：
- `listen.startAnywhere`：本机挑不出歌时去网易云拿一首（刚听过的；没有就取自己前几个歌单的第一首）。
  角色的 `[一起听]` 走它。
- `reply.js` 的一起听与点歌**按轮排队**（`inTurn`，键是会话加 turnId）：同一轮先起播再点歌；不同轮互不等，
  上一轮去网易云搜歌慢，不拖住下一轮。连网易云都拿不到第一首（没登录）时记下这一轮想一起听（`listenWanted`），
  同一轮的 `[点歌]` 就拿它点的那首起播。
- 一起听页：「搜索网易云」（回车才搜），点一首就一起听这一首（先 `fromNetease` 收进曲库，只存歌名与 id）；
  每一首可以「加入歌单」，放进我的或角色的歌单。登录了网易云再列「我的网易云歌单」，点开
  `listen.startNeteaseList`：歌单里的歌收进曲库、本机建同名歌单（已有就补齐），从第一首一起听；
  正在一起听就只换歌单与这一首。「加歌」那张表顶上也能搜网易云；角色的歌单同样有「加歌」。
- 正在和这一位一起听时，点单曲只换这一首，不重开一场。

**主屏角标**：`.cell-app` 从前是 `overflow: hidden`，未读角标挂在图标右上角外面 4px，被裁成半个。
改成 `visible`，名字自己带 ellipsis，横向不会溢出。

测试 `tests/listenne.test.mjs`。

### 4.219 点歌：歌名加歌手，原唱放不了就放翻唱

`[点歌：歌名 - 歌手]`。提示词里从前那句「从你知道的曲目里挑歌名」删掉了：配了网易云之后，不在曲库里的歌
照样点得到，那句话等于替角色把范围收窄了。现在 `skeleton.listen` 只陈述事实：先查曲库，接了音乐服务再上网搜；
原唱放不了时换成别人唱的版本，有一行提示说放的是谁的。

`music.playableSong(query)`：
- 曲库里歌名、歌手都对得上的先用（没写歌手时照旧用 `findSong` 的宽松认法）；
- 否则搜网易云（写了歌手就先按「歌名 歌手」搜一次，再按歌名搜一次），歌名对得上的分两堆：
  歌手也对得上的是原唱（没写歌手时取第一个同名的），其余是别人唱的；
- 原唱逐个问一次播放地址，放得出来就用；都放不了，依次试别人唱的版本。
  问播放地址是网易云的接口，不是模型，不算多调一次（第 15 条）。

换了版本、或一个能放的都没有时，`reply.js` 落一行提示（带 turnId，重新生成那一轮时跟着清掉）：
- `[周杰伦的《晴天》无法播放（需要会员或没有版权），换成了路人演唱的版本]`
- `[没有找到周杰伦的《夜曲》，换成了路人演唱的版本]`
- `[没有找到可以播放的……]`（不换歌）

提示是事实，角色下一轮读到，要不要说「这首没版权」、怎么说，由角色卡决定。

### 4.220 换网址：搬家与本机缓存

正式版要从 GitHub Pages 换到自己的域名（接 Cloudflare），大陆打开会快得多。两件事先做好：

**一、本机缓存**（`sw.js`、`system/offline.js`）

从前 Service Worker 故意不缓存（怕新旧文件混着，见 `system/refresh.js`）。现在服务器在海外、四百多个 js，
每次打开都一个个取太慢，所以缓存，但做成**整份按构建号换**：
- 注册地址带构建号 `sw.js?b=<index.html 的 meta>`，缓存名 `eira-<构建号>`。构建号一变地址就变，
  浏览器装一个新的 Service Worker；别的构建号的缓存在它激活后放到后台整份删掉。
- 页面本身（导航）永远先问网络：index.html 里写着真实版本，启动时的构建号比对（main.js）靠它。断网退回缓存。
- 同源静态文件先看缓存；带 `cache:'reload'` 的（强制更新那一遍）走网络并覆盖缓存；跨域的一律不碰。
- 启动完把这一次已加载的文件报给**这一版的**那个 Service Worker 补进缓存（第一次打开时它还没接管）。
- 激活时只 `clients.claim()`，不等任何事：激活期间页面请求全挂起，等慢事整页就白屏。
- 发了新版：用户打开时拿到新 index.html，缓存里的旧代码和它对不上，照旧走自愈（清缓存、`cache:'reload'`
  重取、重开），拿到的是新代码；新版的 Service Worker 等上一版手上的事做完才接管，最晚下一次打开。
- 通知那边（`push.js`）注册的是同一个地址，两处不一样会互相换掉。
- 自动化测试里默认不缓存（地址带 `cache=0`）：好几个测试靠拦截本站 js 造场景，缓存接管后拦截失效。
  `tests/offline.test.mjs` 与 `tests/swupdate.test.mjs`（自带一台能中途改构建号的服务器，模拟发版）专门测缓存。

**二、搬家**（`system/move.js`、根目录 `move.html`）

浏览器数据按网址分开存，换了网址新网址上是空的。`site.js` 填 `moveFrom`（旧网址）与 `moveTo`（新网址）后：
- 两边跑的是同一份代码（都从 release 部署），各自认得出自己是旧的还是新的（`isOld` / `isNew`）。
- **推**：旧网址「设置 - 存储与备份 - 搬到新网址」，打开新网址 `?move=in`；新网址说好了，旧网址把整份备份
  （`backup.build`，含图片、文件与接口设置）用 postMessage 发过去，新网址 `backup.restore` 后去掉查询串重开。
- **拉**：新网址「从旧网址搬过来」，打开旧网址的 `move.html`，它打好包发回来。
- **只和 site.js 写死的那个网址说话**：发时 postMessage 的目标源写死成对方，收时核对 `event.origin` 与 `event.source`。
  `move.html` 被别的地址叫开、或没有开它的窗口时，一个字节都不发。不经过任何服务器。
- 启动时一天提醒一次：旧网址上问要不要搬，新网址上还没数据时问要不要搬过来。
- iPhone 主屏幕上的 PWA 与 Safari 各存各的：从 PWA 推过去，新网址在 Safari 里打开、数据落在 Safari，
  之后在 Safari 里把新网址加到主屏幕；不行就走备份文件导出、恢复。
测试 `tests/move.test.mjs`（127.0.0.1 当旧网址、localhost 当新网址）。

**换网址时要做的**：新域名的 Cloudflare Pages 项目连 `release`；`site.js` 填好 `moveFrom` / `moveTo`；
账号服务与网易云转发也绑到新域名的子域名上（`workers.dev` 在大陆常连不上），改 `site.js` 的 `accounts` / `neteaseWorker`。
测试版仍在 `*.pages.dev`，不能和正式版同一个域名（CLAUDE.md 第 19 条）。

### 4.221 线上旁白

会话「互动」里的开关（`chat.narration`，`extras.narrationOn` / `setNarration`），每段会话单独，默认关。
开着时能力 `narration` 常驻（它是这一段会话的写法，不是想用再用的功能），细则 `skeleton.narration`
只说格式：单独一行 `[旁白：…]` 显示为消息之间的旁白，第三人称写此刻看得见的神态、动作、环境；一次回复可以
有好几行，各自落在写它的位置，一行里可以有几句；旁白不算作回复里的消息条数。写不写、写几行由角色卡决定。
不多调接口。

`reply.js` 认 `旁白` / `narration`，落一条 `kind: 'narration'`（正文照原样留着标记，历史里角色读到的就是它；
`narration` 字段存摘掉标记的那一句）。不弹通知；会话列表预览写「[旁白] …」。

会话里画成居中的一行小字（`.msg-narration.ph-narration`），没有头像、没有气泡。颜色走契约变量
`--ph-narration-color`，默认 `--text-3`；会话「聊天背景」里多一排「旁白文字颜色」（`chat.look.narr`，
`chatlook.pageOf` 挂到页面上），有聊天背景图时另加一层底色的淡阴影，换了壁纸也读得清。
测试 `tests/narration.test.mjs`。

### 4.222 角色自己改锁屏密码

能力 `lockcode`，标记 `[改密码：数字｜这串数字是什么]`（`reply.js` 的 LOCK_KINDS）。4 到 6 位，和现在一样就不换。
`theirs.changeLock` 存成 `{ code, why, src: 'char', changedAt, hints: [「这串密码是某天新换的，一共几位」] }`，
手机重新锁上（`relock`）；`why` 仍是猜不出来时「直接查看密码」给的答案。

**不要太频繁，由代码管**：离上一次改不满 `lockGapDays` 天（角色卡里改，默认 30，0 为不限），这项能力整个不给，
`reply.js` 那边写了也不认；**这一轮刚聊到密码时不受此限**（`theirs.askedLock`：最近两句我说的话里有密码、锁屏、
解锁，或「改……生日／纪念日」），所以「你怎么没有改我的生日」照样能改。角色卡「能力」里另有总开关 `canChangeLock`。

不把现在的密码告诉角色：它知道了多半会说出来，猜密码那件事就没了。会话里落一行
`[阿岚改了手机的锁屏密码]`，不写数字；这一行存着换之前那一份（`lockUndo`），删掉它（重新生成那一轮）时
`theirs.restoreLock` 放回去。测试 `tests/lockcode.test.mjs`。

### 4.223 通话缩小：悬浮球与桌面悬浮窗

通话原来只有全屏一种样子，打着电话就什么别的都做不了。现在左上角两个按钮：

**缩小为悬浮球**（`call.shrink` / `call.expand`，状态是 `call.mini`）。通话照常进行：回合、声音、
麦克风、秒表都不停，屏幕让给别的页面。球显示头像（视频通话是那张画面）与时长，对方正在说时头像
外面一圈一明一暗。可以拖，松手贴到近的那一边；点一下展开。响铃时不给缩：接不接要当场决定。

缩起来时**全屏那一层不拆**，只是 `opacity: 0` 加 `pointer-events: none` 加 `inert`：
摄像头取帧靠的是那一层里的小窗（`camera.attach`），拆掉的话角色在缩小期间就看不见你了；
也不用 `display: none`，浏览器会把看不见的静音视频停掉。打了一半的字同样留着。

**桌面悬浮窗**（`system/callfloat.js`）。出了 Eira 网页画不了东西，只能借系统的两条路，
哪条都没有就不给这个按钮：

| | 怎么做 | 什么时候出现 |
|---|---|---|
| 安卓 app | 外壳的 `EiraNative.setFloat` / `floatAllowed` / `askFloat`，原生小窗（`android/.../CallFloat.kt`），要「显示在其他应用上层」权限 | 开着（`settings.callDesk`）且 Eira 退到后台时；回到 Eira 收起 |
| iPhone app | iOS 不许画在别的应用上面，只有系统画中画。外壳 `CallFloatBridge.swift` 把同样那几样画成视频帧交给 `AVPictureInPictureController`；网页经 `callfloat` 通道每有变化就交一次，外壳回一句小窗还在不在 | 点按钮当场弹出，同浏览器那条 |
| 浏览器、PWA | 系统画中画。canvas 画出头像、名字、时长与最近一句，`captureStream` 成视频流给一个藏起来的 `<video>`，再请系统弹成小窗 | 点按钮当场弹出，应用里同时缩起来（不再另画一个球）；关掉小窗回到悬浮球 |

安卓那边第一次点是去系统设置要权限，**不记为开**：记成开却画不出来，按钮亮着、桌面上什么都没有。
画中画必须在点击那一刻同步进去，所以那段视频在通话一开始就备好（`prepare`），不在点击里现建。
字幕是一个字一个字流进来的，交给外壳最多半秒一次；头像只在换了的时候带一次。

**小窗里只能看，不能说。** 系统不许后台的网页用麦克风，安卓 11 以后原生应用在后台也一样；
回到 Eira 才能打字或开麦克风。角色说的话照常进字幕、照常出声。

旧版安卓 app 没有 `setFloat`、旧版 iPhone app 没有 `phoneCallFloat`，按钮自然不出现；要用得装新打的包。测试 `tests/callmini.test.mjs`（安卓、iPhone 两条用假的
`EiraNative` / `callfloat` 通道测网页这一侧；原生小窗本身靠打包流程编译、真机上看）。

同一批顺带：**app 外壳里不提醒搬家**（`move.js` 的 `inShell`）。外壳是一个 WebView，开不出第二个
窗口，点了「搬家」新网址会被当成站外链接扔给系统浏览器，数据落到浏览器那边。外壳的网址另外换
（改打包参数重新打），换之前旧网址照常能用。

### 4.224 角色包导入成副本：「已总结」标记跟着换 id

会话上记着总结到了哪一条：`memoryUpTo` 指一条消息，`memoryUpToBeat` 指一段线下正文（两条水位线，
见 `ai/tasks/memory-extract.js`）。本机已经有这个角色时，导入角色包是副本，会话、消息、正文全换一套
新 id，而这两条标记从前照抄旧 id —— 在副本里找不到那一条，`after` 落空，整段历史又全算成没总结，
「总结记忆」一按就是从头再来一遍（也就是再付一遍钱）。

现在 `charpack.install` 在消息与正文都放好之后，按两张 id 表把会话上的 `memoryUpTo`、`memoryUpToBeat`、
`memoryTriedId` 换到新 id；包里没带那一条（导出时不带聊天记录）就清空。整份备份、GitHub 备份、搬家
一律原 id 恢复，本来就不受影响。测试 `tests/packmark.test.mjs`。

### 4.225 app 外壳改开新网址

安卓与 iPhone 外壳默认打开的网址从 GitHub Pages 换成 `https://eiraphone.cn/`（`android/app/build.gradle.kts`、
`ios/Info.plist` 与 `ios/build.sh`、两份打包流程的默认值；iPhone 的 app-bound 域名跟着换成 `eiraphone.cn`）。

**不做 app 里的自动搬家。** 外壳里开不出第二个窗口，自动搬要外壳当中转、两边各跑一套，换来的只是省一次
导出与恢复。改为让用户在旧版里导出完整备份、装新版后恢复（两份 README 里写明）。外壳里的新网址照旧不提醒
搬家（4.223 末尾的 `inShell`），提醒了也搬不成。

### 4.226 后台消息：app 关着，角色照样发消息来

网页关掉就什么都跑不了，「主动找你」的定时器也跟着停。所以找一台服务器值班：Cloudflare Worker
（`worker/push.js`）加 Supabase（`worker/push.sql`），部署教程 `worker/PUSH.md`；地址填在 `site.js` 的
`pushServer`，没填就没有这一项。应用一侧是 `system/bgpush.js`，开关在「设置 - 通知 - 后台消息」，默认关。

**离开时交任务。** 每个开了「主动找你」、有单人会话的角色一条：接下来几次开口的时间（`proactive.upcoming`，
头一次就是本机排好的落点，落进免打扰的挪到结束那一刻；次数 `bgPush.perChar`，默认 2，不超过未读上限），
加上到时候要发给模型的那一次**完整请求**。请求与平时 `sendProactive` 是同一份：`proactivePayload` 拼内容，
`engine.prepareTextTask` 按这个任务平时那套接口备好地址、头、正文（各 provider 的 `prepare`，与 `complete`
同一份）。只有两处留成记号 `{{bg_time}}` `{{bg_gap}}`，服务器到点按真正发出的那一刻填 —— 几个小时之后
才发，「现在几点」不能写成交任务的这一刻。聊天上下文里其余按时间变的块（「今天」那些）仍按交任务那一刻算。

**服务器到点替你发。** 每分钟一次（Cron Trigger）：发模型请求、取出文字、Web Push 推出去（通知标题是角色名，
正文是去掉方括号标记的第一句，点开进那段会话）。还有下一次就把这一句接进对话、末尾再放「由你开口」那一句，
排下一条。加密（RFC 8291 aes128gcm）与 VAPID 签名（RFC 8292）用 Workers 自带的 WebCrypto 写，不依赖 npm；
测试里拿现成的 `http_ece` 解过、拿 `web-push` 生成的密钥签过。

**回来时取结果。** 服务器写好的那几条照平时的样子落进会话（`renderTurn`，分条、标记都照旧），时间改成
服务器发出的那一刻，未读加上；随后请服务器删掉，并给这个角色重新掷一次本机落点，免得一打开又立刻开口。
取回的每一次都记进用量（`usage.note`），「后台任务」页上的次数不少算。

**不多花一次调用（第 15 条）。** 这是本来就会发生的「主动找你」换了地方发。应用开着时每 4 分钟向服务器报到
（顺便交一份最新的任务），服务器看到 6 分钟内报过到就不发，由本机发；退到后台时报一句「走了」，从这一刻起
到点就发。报到没来得及（iPhone 退后台冻结得快）最多迟 6 分钟。

**密钥与上下文交给了服务器。** 任务里带着用户的接口密钥与聊天上下文。Worker 收下即用自己的 `DATA_KEY`
加密再存，订阅与结果同样加密，Supabase 里只有密文；两张表开着 RLS 且不写策略，只有 service_role 读写得了。
开关的说明里写明了这一条。设备口令只存哈希；本机的登记存在 localStorage（`eira-bgpush-device`），不进备份 ——
换台设备恢复备份，不该把推送接到旧手机上。

只有浏览器与「添加到主屏幕」的网页有 Web Push；apk、ipa 外壳里没有，那里不显示这一项（`push.pushSupported`）。
有 `pushServer` 时，原来那几栏手填 VAPID 公钥、上报地址的「Web Push」不再显示（同一件事只留一个入口）。
测试 `tests/bgpush.test.mjs`（应用一侧，推送服务器用路由顶上）、`tests/pushworker.test.mjs`（Worker 本身，
Supabase、模型、推送服务用假的顶上，推送按 RFC 解开、签名验过）。
