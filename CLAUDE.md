# 小手机 项目规约

架构细节见 `ARCHITECTURE.md`。本文件只放**不可违反的硬性规则**。
写任何代码前先读完这里。

---

## 1. 单位：只用 vh，禁止 dvh

**所有视口高度一律使用 `vh`。禁止使用 `dvh` / `svh` / `lvh`。**

原因：`dvh` 会随移动端浏览器地址栏的显隐而变化，滚动时整个界面跟着抽动重排。
本项目是固定高度的手机外壳，这种抖动不可接受。

**更禁止混用。** 两个容器一个用 `vh` 一个用 `dvh`，地址栏一收就直接错位。

```css
/* 正确 */
.app-root { height: 100vh; }

/* 禁止 */
.app-root { height: 100dvh; }
.panel    { height: 50svh; }
```

由 `scripts/check-units.mjs` 扫描拦截。

### 外壳那一串容器必须整整齐齐都是 100vh

```
html, body   height: 100vh
#app         height: 100vh
.root        height: 100vh
```

**一层都不许换成 `100%`，更不许换成量出来的像素值。**

只要中间有一层不是 `100vh`，它就只撑到 WebKit 认为的可视区，撑不到屏幕底部。
底下空出来那条，iOS 会拿页面底色去填，于是永远有一条白边；
换壁纸、切页面都盖不掉它，因为那根本不是页面画的。

`100vh` 在 iOS 上算的是地址栏收起后的高度。加到主屏幕当 PWA 用时没有地址栏，
这就是整块屏幕，正好。配合 `index.html` 里的 `viewport-fit=cover`，
内容能一直铺到 Home Indicator 底下。

**代价**：在普通 Safari 标签页里（没加到主屏幕），`100vh` 比可视区高一截，
底部那排会被地址栏压住。这个项目按 PWA 用，接受这个代价 —— 别为了标签页
再去量高度，量出来的像素值就是上面说的「不是 100vh 的那一层」。

壁纸另外还用了 `position: fixed`，不管外壳怎样都铺满，纯背景不参与布局。

由 `scripts/check-units.mjs` 拦截：dvh/svh/lvh 一律报错；`vh` 只允许出现在
`styles/base.css` 的这三条规则里，而且必须是 `100vh`；少写一层也报错。

## 2. 零 emoji

**源码、界面文案、图标，任何位置都不允许出现 emoji。**
所有图标一律使用 SVG，统一走 `<Icon name="..." />`。

由 `scripts/check-no-emoji.mjs` 扫描拦截。

## 3. 全屏，没有机身外壳

**不画手机外框、不画刘海、不做机身投影。** 默认界面直接铺满整个视口，
无论桌面还是移动端。

用户可以在「设置 - 外观 - 全屏显示」关掉全屏（用户要求，见 ARCHITECTURE 4.232）：
手机上内容让出系统状态栏，电脑与平板上变成手机宽度居中的一栏。**关掉之后也不画机身** ——
只有两侧留白与两条细边，外框、刘海、投影照样不许有。

## 4. 设计取向：简约

- 不做拟物，不堆毛玻璃，不用彩色渐变图标
- 单色 SVG 图标 + 低饱和底色
- 留白充足，克制装饰
- 颜色、圆角、间距、字号、动效一律来自 `styles/tokens.css`，**禁止硬编码**

## 5. 设置放在它起作用的地方

**不要动不动就往「设置」列表里加一行。**

设置列表只放**全局的、跟具体对象无关的**东西：接口、主题、存储。

凡是属于某个对象的开关，就放在那个对象自己的页面里：

| 这件事属于 | 开关就放在 |
|---|---|
| 某个角色怎么说话、发不发语音、会不会主动找你 | 那个角色的会话菜单 / 角色卡 |
| 某段对话的上下文、记忆、模板 | 那段对话右上角的菜单 |
| 某个 app 的图标和名字 | 主界面长按那个图标 |

判断标准只有一条：**用户想改这件事的时候，人在哪儿？** 就把开关放在那儿。
不要让他退出去、翻进设置、找一遍、再原路走回来。

同一个开关只能有一个入口。别在两个地方各放一份。

## 6. 人设只在「编辑资料」里出现

**人设正文（`persona` / `description` / `scenario` / 开场白 / 说话示例）
只允许出现在编辑页的输入框里。** 列表、卡片、资料页、副标题、预览行，
一个字都不许露。

已经因为这条来回改过三次。人设是一大段长文，塞进任何列表都会把那一屏
挤得只剩它，这正是当初要拆出「联系」app 的原因。

列表里要显示点什么，用 `signature`（一句话签名）。没有就空着，
不要拿人设去顶。

**唯一的例外**：生成结果的确认页 —— 导入角色卡、批量生成 NPC
那种「先看一眼再决定存不存」的界面。那里不看内容就没法判断，
而且东西还没入库。

## 7. 界面文案用书面语

**界面上的一切文字都用书面语。** 标题、副标题、说明、空状态、toast，
一律陈述句。

代码注释和本文件不受此限 —— 那是写给开发者看的，可以口语。
但凡是用户在屏幕上读到的，都要严肃。

不允许：

- 语气词：呀、吧、嘛、呢、啦、哦、咯
- 口头连接：就、而已、反正、本来、干脆、顺手
- 打比方、讲段子、描述使用场景的小故事
- 对用户说教或调侃

角色一律称「角色」，不用「她」「他」—— 角色的性别由角色卡决定，
界面不替用户预设。

```
错：开了她就会自己挑时间发消息来
对：开启后，角色会在设定的间隔内主动发起对话

错：适合一场不该天亮的戏
对：时间固定在设定的时刻，不再推进

错：挑几条一起删
对：选择多条消息后一并删除
```

说明文字要写清楚三件事：**这是什么、什么条件下生效、关掉会怎样**。
不写使用场景的故事。

由 `scripts/check-ui-tone.mjs` 扫描拦截。它只扫 `src/apps`、`src/ui`、
`src/shell` 里的字符串字面量，注释不管。拦的是一份口语词表：语气词、
儿化、破折号插入语，以及反复犯的那几个（「已说好」「还差」「够了」
「哪儿」）。

**它拦不住整句的语气。**「这件事让你不痛快」一个词都不违规，
那一层仍然要自己把关。但反复犯的就是那几个词，所以列一份表是值的。

## 8. 模块边界

```
apps/*  只能 import:  sdk/, ui/, icons/
apps/a  永远不能 import  apps/b
apps/*  永远不能 import  system/
```

跨 app 的数据一律放在 `system/db/` 的数据域里，app 只是它的视图。
跨 app 的调用一律走 Intent。

由 `scripts/check-boundaries.mjs` 扫描拦截。

## 9. 无构建

原生 ES Modules，不引入打包器，不需要 `npm install`。
第三方库手工放进 `vendor/`，import 本地文件。

ES Modules 在 `file://` 下受 CORS 限制，需静态服务器运行：

```
python3 -m http.server 8000
```

## 10. AI 请求

不允许在组件里直接 `fetch` 模型接口。一律走 `phone.ai.*`，
底下经过 AIQueue（并发限制、去重、取消、重试）与 provider 适配层。

## 11. Prompt 不写死

骨架开场、收尾、各任务的 instruction 全部存在 `settings.promptTemplates`，
代码里只保留 `DEFAULT_TEMPLATES` 作为回落。改语气不应该需要改代码。

## 12. 有长按的地方，禁掉系统的选中与复制

**只要一个元素挂了长按手势，它就必须带上 `.no-callout` 类。**

不加的话，iOS 会在长按时弹出系统自己的选中与「拷贝」浮层，
和本项目的长按菜单同时出现 —— 两边抢同一个手势，谁都不好用。

```html
<!-- 正确 -->
<div class="msg no-callout" onTouchStart=...>

<!-- 禁止：挂了长按却没禁掉系统浮层 -->
<div class="msg" onTouchStart=...>
```

`.no-callout` 定义在 `styles/ui.css`，一处定义，各处引用。
不要在各自的样式里重抄那三行属性。

由 `scripts/check-longpress.mjs` 粗扫：文件里既有 `onTouchStart`
又有 300 到 1200 毫秒的定时器，却找不到 `no-callout`，就报错。

**要复制就在自己的菜单里给一个「复制」。** 禁掉系统的选中不等于不能复制，
只是复制的入口要归自己管 —— 系统那套选的是「一段文字」，
本项目要的是「这一条消息」，本来就不是一回事。

目前挂着长按的：会话气泡 `.msg`、消息列表与联系人列表的 `.msg-row`、
主界面 `.home`（长按进编辑模式）。

## 13. 能力不设上限，花钱的必须能关

**凡是「一次能做多少」的，一律不设上限。** 一次回复分几条、一次带几张图、
一次搜出多少结果、一次读几条历史 —— 想要多少是用户的事，代码不替他封顶。
真有性能上的理由要截断（比如一屏画多少条气泡），那也是**默认值**，不是上限，
必须能改，也必须能改到「全都要」。

**凡是要花钱、或者可能多跑几轮接口的，一律给开关，数目让用户自己填。**
默认值往保守了给，但必须能改、能关。界面上写清楚：开了会多花什么，关了会少什么。

这两条合起来的意思是：**代码不替用户做省钱的决定，只把账算清楚摆出来。**

所有这类旋钮集中在「设置 - 用量与上限」一页 —— 「花钱的必须能关」的前提是
用户找得到它们在哪儿。跟某个对象绑死的那种（这个角色要不要主动找我）仍然按
第 5 条放在那个对象身上。

## 14. prompt 正文一律英文

**凡是会进 prompt 的句子，一律用英文写。** 骨架、各能力的说明、各任务的
instruction、上下文区块里那几句提示，全都算。

原因：中文指令会把自己的措辞漏进输出里。让模型翻译，它交回来一口中式英文；
让它聊天，它一股说明书味 —— 因为整份说明书就在它眼前，用的正是同一种语言。
英文指令与中文输出之间隔着一层，措辞不会直接串过去。

**角色说什么语言，由 `skeleton.opening` 里单独一句规定，不靠 prompt 本身
的语言去暗示。** 默认中文，角色卡或世界书写了别的就按那个来。

### 有三样东西留在中文，它们不是文案

| 留中文的 | 为什么 |
|---|---|
| 方括号标记 `[图片：…]` `[约定完成：…]` | 是协议。`reply.js` 按它切分，库里几万条历史消息也存着它。改了旧数据全废 |
| 区块抬头 `[相关记忆]` `[你今天]` | 同上，prompt 内部互相引用时靠它对位 |
| 「」里引的原话、`《》`里的片名 | 那是被举例、被禁止的**内容**，本来就该是中文 |

注入进去的**数据**同样不翻译：人设正文、记忆条目、字幕、歌名、地名、
时段名。那些是内容，不是指令。

### 留在 prompt 里的中文，仍按原来的标准

书面语、中性、祈使。是**说明书**，不是聊天。

不允许（与第 7 条同一份清单，外加两项）：

- 语气词：呀、吧、嘛、呢、啦、哦、咯
- 口头连接：就、而已、反正、本来、干脆、顺手、省得、一上来
- 「别…」这种口语祈使 —— 否定祈使一律写「不要…」
- 破折号插入语

**称角色一律用「该角色」，不用「她」「他」。** 和第 7 条同一个理由：
性别由角色卡决定。prompt 里写「她」，模型会照着认下来。

由 `scripts/check-prompt-tone.mjs` 扫描拦截，只扫字符串字面量，注释不管：

- **英文那一条**只在 `templates.js`、`capabilities.js`、`context/*.js` 上查 ——
  只有这几个文件敢说「里面的字符串就是 prompt 正文」。`engine.js`、`tasks/*`
  里报错文案和 prompt 混在一起，机器分不出来，那一层要自己把关。
  查法是「连着四个以上汉字」，标记名和区块抬头都比它短，先摘掉再查。
- **书面语那几条**照旧全扫。它拦的是机械可查的痕迹，
  「这件事让你不痛快」这种查不出来，同样要自己把关。


## 15. 一次回复只调一次接口

**默认情况下，用户发一条消息，只允许打出去一次模型请求。**

凡是会让它变成两次以上的 —— 自动重试、失败换一套再试、顺带整理记忆、
顺带压关系底色、顺带排日程、单独生成心声、单独翻译 —— 一律给开关，
**一律默认关着**。开关上写清楚：开了会在什么条件下多花一次。

**以后新加的东西也按这条办。** 判断只有一句话：这个功能会不会让「发一条消息」
变成两次请求？会，就默认关着。

### 会多调接口的东西只在一处登记

`src/system/ai/cost.js` 的 `EXTRA_CALLS`。四个地方读它：
「用量与上限」那一页把账摆出来、「后台任务」那一页列出开着的几项、`scripts/check-calls.mjs` 拦「新加了一项却
默认开着」、以后再加这类功能时照着加一行。

漏登记的后果是安静的：功能照跑，账单照涨，而界面上那个「这一轮 1 次」
仍然写着 1。

已经打出去几次，另有一本账：`src/system/ai/usage.js`，每个真正发请求的出口记一笔。
新加一个直连接口的出口（不走 `engine.send` 的那种），要在出口处补一句 `note()`，
否则「后台任务」页上的实际次数少算它。

### 自动重试上限 3 次，默认 0

`queue.enqueue` 收的 `retries` 只是「这件事最多值得重试几次」，
真正试几次取它与用户设的 `retryMax` 里较小的那个。`retryMax` 钳在 0 到 3，
默认 0。再多也救不回来，只是把同一个错误的账单乘以四。

### 谁走哪套接口

| 任务 | 接口 |
|---|---|
| 对话回复、通话 | 主用 |
| 记忆提取、关系底色、记忆导入、历史压缩 | 记忆接口，没单独配就跟副用 |
| 其余全部 | 副用，没配就退回主用 |

**只有你正盯着屏幕等的那两件走主用。** 整理记忆、排日程、生成 NPC、
写朋友圈这些慢一点没人会发现，不该和聊天抢同一个（通常更贵的）接口。

「用哪一套」不是「多打一次」，所以这条路由本身不需要开关。
失败之后换下一套再试才是第二次调用，那个默认关着。

失败之后**按「主用、副用、其余」的顺序依次换**，最多换几套由用户填
（`failoverMax`，默认 1 —— 正好是从前「改用副用」的花法）。填 0 表示
其余的全试（第 13 条）。没填全密钥或模型的接口直接跳过：换过去也是
白撞一次墙，还多一次计费。

由 `scripts/check-calls.mjs` 扫描拦截：`EXTRA_CALLS` 里登记的每一项，
在 `DEFAULT_SETTINGS` 里必须是关着的；`enqueue` 的 `retries` 字面量不得大于 3。



## 16. 内置提示词只陈述客观事实，不替角色作判断

**只告诉模型规则，规则是客观事实。任何判断都不替它做。**

| | 写 | 不写 |
|---|---|---|
| **客观事实** | 标记怎么写才生效、分几条发、哪些数字不许改、不知道的不能说 | |
| **判断** | | 说什么语言、该有多大反应、该不该反驳、话题怎么接、什么时候该沉默、几份设定冲突时听谁的 |

**角色是什么样，由角色卡、世界书和用户自己写的模板决定。** 内置那份只负责
让机器能把输出解析出来。

线上聊天的全部规则就一条：**每轮回复分 3 到 5 条发出。**

### 三类最容易伪装成规则的判断

**一、替它定语言。** 从前开场白写着「默认用中文，除非角色卡另有规定」。
角色来自各个国家，这一句是在替用户拿主意。现在一个字都不写 ——
角色卡、世界书、对话历史都在上下文里，语言由它们自己说明。

图片描述那两处没有语言线索（图上没有字），所以给它一段**实际的原文**
当样本照着走，不是给它一个我们选的语言。

**二、替它排优先级。** 从前有一段「冲突时的取舍」：人设 > 世界设定 > 其余。
哪条该让哪条，是用户写这几份设定时自己的事。

**三、替它决定什么时候用某个功能。** 「用之前先想想合不合适」「多数时候
不必说话」「这一段台词密集，此时少说」—— 这些都是判断，不是规则。

### 判断是长出来的

没有人会一次写一整章「该怎么做人」，都是一句一句添的。每一句单看都有道理，
攒够几十句，所有角色就开始像同一个人说话，而那个人是写规则的人。
**这一整套已经删过两次了**（九章消息规则、两段示例、十四项自检、
各能力里的「什么时候该用」、注入块里的「该不该开口」、默认语言、取舍顺序）。

### 字数预算

`scripts/check-prompt-tone.mjs` 给 `skeleton.*` 定了上限：
`skeleton.rules` 200 字，其余每段 900 字。

字数拦不住内容，但拦得住这种生长 —— 一条格式规则用不了那么多字，
超了就说明混进了不是规则的东西。`task.*` 不受此限：那是生成任务的规格书，
本来就该写长，而且它们不注入聊天。

## 17. 角色卡与世界书不做 JSON 导入

**不要给角色卡、世界书加「导入 JSON」。** 角色卡只从散文读：`.txt`、`.md`、
`.docx` 交给模型读成一张卡；世界书就在应用里写。

理由不是懒。JSON 导入意味着去认别家的字段名，而那份 schema 不归我们管：
它改一次，我们这边就多一个分支；它有十个方言，我们就要认十份。更要紧的是
**导进来的东西长什么样由那份 schema 决定**，本项目在人设、记忆、世界书上
那些自己的约定（第 6 条、第 16 条）会被它一路带歪。

散文那条路反过来：模型读完按**我们自己的**结构填，不认识的东西自然落不进来。

美化包是 JSON（见 ARCHITECTURE 4.127），那是**我们自己导出的自己的格式**，
两回事 —— 别拿它当先例。

## 18. 美化契约：`ph-` 那套名字一经发布永不改名

**外部作者写的美化只认 `ph-` 前缀的类名和 `--ph-` 前缀的变量。**
它们登记在 `src/system/skin-contract.js`，那是唯一的一份表。

| | 例子 | 承诺 |
|---|---|---|
| 内部类名 | `.msg` `.bubble` `.composer-bar` | 没有承诺，随时改 |
| 契约钩子 | `.ph-msg` `.ph-bubble` `.ph-composer` | 一经发布永不改名 |

同一个元素上两个都挂：`class="msg is-mine ph-msg ph-msg-mine"`。

**加钩子可以，改名和删名不可以。** 加一个是向后兼容的；改名或去掉一个，
等于把已经发出去的美化包作废 —— 那是一次契约版本号的事，不是一次重构的事。

### 挂了契约钩子的元素，不许再写内联样式

内联样式赢过任何选择器。写了，作者写什么都盖不住，而且看不出为什么。
要传值就传自定义属性，真正那条声明写在样式表里：

```js
// 正确
style=${`--av-size:${size}px`}

// 禁止
style=${`width:${size}px`}
```

应用自己那几个旋钮也写 `--ph-*`，不写内部变量名。**只留一条路** ——
两条路的话，用户调的和作者写的会互相盖，而盖的顺序谁也说不清。

由 `scripts/check-contract.mjs` 双向核对：表里写了的必须真的挂着，
挂着的必须登记，每个公开变量必须真的有人读，钩子上不许写死声明。

### 美化包自己声明改到哪一层

`scope` 填 `chat`（挂在一段会话上）或 `shell`（整个应用），可以两档都要。
**没写这一项的老包一律按 `chat` 读** —— 默认成全局等于替作者把影响面
扩大了一圈，而他当初没这么写。

全局那一档有两个逃生口，改代码时不许动：**设置 app 永不注入美化**，
以及「外观」里那个总开关。少了它们，一份写坏的全局美化没有任何办法关掉。

## 19. 正式版与测试版：没有用户那句话，不碰 release

已经有用户在用。推上去的每一行都会在他们下次打开时生效，而数据只在他们自己的设备上，
坏了没有后台能替他们救回来。所以分两份：

| | 分支 | 发布到 | 谁在用 |
|---|---|---|---|
| 正式版 | `release` | `eiraphone.cn`（Cloudflare Pages，连 `release`）；旧网址 GitHub Pages（`yu7705423-cell.github.io/phone/`）保留着，只用来让老用户搬家 | 用户 |
| 测试版 | `claude/mini-phone-architecture-tqzvig` | Cloudflare Pages（`*.pages.dev`），在 Cloudflare 后台连着仓库自动部署 | 只有维护者 |

- **日常改动只推开发分支。** 测试版随之自动更新，正式版不动。
- **只有用户明确说「推正式版」，才把开发分支合进 `release`。** 合之前全部回归测试、
  doctor、冒烟都要过。只用 fast-forward 或 merge，不改写 `release` 的历史。
- 正式版出了急事也照这条：在开发分支修，用户同意后再合。

**测试版不能和正式版放在同一个域名下。** 浏览器的数据按域名分，
`yu7705423-cell.github.io/phone-test/` 和正式版是同一个域名，同一台手机上两边共用一份数据：
测试版的数据升级会直接改掉正式数据，数据库版本一旦被测试版升上去，正式版在那台手机上打不开。
测试版在 `*.pages.dev`，天然隔开。它由 `system/channel.js` 按网址认出来，挂一枚「测试版」标记。

**Cloudflare Pages 单个文件上限 25 MB。** 超了整站部署失败，测试版停在上一版。
大文件要切块放（ffmpeg 的 wasm 就是切成两块的，见 `vendor/ffmpeg/README.md`）。

**换正式版网址时**：`src/site.js` 填 `moveFrom` / `moveTo`，旧网址上的用户才能把数据搬过去（见 ARCHITECTURE 4.220）。
不填就换，用户打开新网址是空的。

测试版连的是同一个账号服务，普通账号在那边登录会占一个设备名额（每个账号 2 台，满了挤掉最早那台）。
测试版上用 `admin` 登录，管理员不占名额。

---

## 20. 图标和图片、聊天记录不许丢：已经丢过五次，每次原因都不一样

用户最常报、也最伤人的一类事故：「更新之后 / 闪退之后 / 从后台回来，图标和图片全没了」「刚聊的切个后台就没了」。
数据有时还在库里、有时根本没写进去，用户看到的都是「东西没了」。**已经五次，五条不同的路**，
碰到下面任何一处，都要先想这一条：

| 路 | 怎么丢的 | 守住它的 | 测试 |
|---|---|---|---|
| 开机回写 | 读 kv 瞬时失败回落成默认值，开机的自愈 / 迁移把默认值写回去，盖掉真的图标与壁纸设置 | 开机时跑的任何写入先问 `stored()`，没读到的记录不许写（ARCHITECTURE 4.124） | `iconloss` `layoutloss` |
| blob 地址失效 | iOS 后台久了收走 blob 背后的数据；从前 pagehide 还主动 revoke | 所有图一律经 `useImage` / `useThumb` / `useFile` 取地址，不许自己 `createObjectURL` 存着用；不许在 pagehide 里 revoke（4.186） | `bloburls` |
| 删图不问引用 | 一张图几处共用，换一处就删了 | 一律 `images.remove`（先核引用表），新存图的地方登记进 `usedImageIds`（4.114、4.187） | `imagekeep` |
| 数据库连接断了 | iPhone 从后台回来，网页与数据库的连接已断；一直拿着旧连接，读写全失败，失败只记在控制台。刚聊的只在内存里，重开全没 | `idb.js` 连接被关就丢掉、像连接问题的失败换新连接再做一次；真写不进去在屏幕上说（4.233）。**任何写入失败都不许只记在控制台** | `idblost` |
| 新旧版本混着 | 发版后样式表或点开才载入的 js 还是上一版：新代码设变量、旧样式表不读，图一起消失；旧的一份还被存进新版缓存，每次打开都是它 | 构建号三处一起改（meta、version.js、样式表 `?v=`）；`sw.js` 从网络取来要存的一律 `no-cache`，页面比 Service Worker 新时不从旧缓存拿（4.230） | `stalemix` `swupdate` `heal` |

**特别是第四条。** 把内联 `background-image` 改成 CSS 变量（第 18 条要求这么做）之后，
图能不能画出来取决于 JS 和 CSS 是不是同一版。今后动 `sw.js`、`refresh.js`、`main.js` 的自愈、
`index.html` 里的构建号与样式表，或者又把一处图改成变量，**上面那几份测试全部重跑一遍**。

用户再报「图没了」「记录没了」时，不要先猜是哪一条：五条逐一对照，找到是哪一条再改。

## 提交前自检

```
node scripts/doctor.mjs
```

一次性跑完十四项检查：视口单位、零 emoji、导入导出、hook 顺序、依赖边界、硬编码颜色、长按禁选、提示词书面语、界面文案书面语、接口调用次数、备份完整性、美化契约、死导出、构建号。

其中五项是无构建方案必须自己补的：

- **导入导出**：没有打包器替我们校验，引用一个已删掉的导出要到运行时打开
  那个页面才会炸。
- **hook 顺序**：hook 必须每次渲染都以相同顺序调用。在提前 return 之后再调
  hook，组件关闭再打开时数量就对不上，表现为状态串味或界面不刷新，而且
  不报异常，只会安静地出怪事。
- **死导出**：没有 tree-shaking 替我们发现「写了没人调」。一个函数写完没接上
  调用方，会一直躺在那里，直到某次改 API 把它改坏了都没人知道。明知没调用方
  但故意留着的，写进 `scripts/check-dead.mjs` 的 `KEEP` 名单，一行一个理由。
- **备份完整性**：`system/db/index.js` 里注册的数据域，必须在
  `system/backup.js` 的 `COLLECTIONS` 里都列上。漏一个不会报错、不会变慢，
  导出看着也成功 —— 只有换台设备恢复时才发现那一域是空的，而那时候原始
  数据往往已经没了。已经因此丢过九个数据域。
  同一条还查第三处：数据域也必须在 `system/db/schema.js` 的 `STORES` 里，
  并提升 `DB_VERSION`。那一处漏了整个应用起不来，而报错里看不出是哪个域。
  第四处机器查不到：按 chatId / charId 挂着的域，要在 `system/purge.js` 的
  `dropChat` / `dropCharacter` 里各补一行，否则删会话、删角色时它留成孤儿；
  同一域还要在 `system/charpack.js` 的 `collect` / `install` 里各补一行，
  否则导出的角色包里少一块（见 ARCHITECTURE 4.116）。
  存图片、存文件的地方同理要登进 `usedImageIds` / `fileUsers`，
  漏一处就是「清理无引用」时删一批。
- **构建号**：`index.html` 的 `<meta name="build">` 必须和 `src/version.js`
  一致。启动时拿这两个值比对，判断浏览器缓存里的 js 是不是旧的（见下）。

### 改代码就要同时改两处构建号

没有文件指纹，浏览器会把 js 一直留在 HTTP 缓存里，而且是**新旧混着**的：
这次新加的文件从没被缓存过，一定是新的；旧文件却可能还是上一版。
于是新页面调用了旧模块里还不存在的函数，直接炸在运行时。

`src/main.js` 启动时拿 `index.html` 的 meta 和 `version.js` 的 `BUILD` 比对，
对不上就把代码全换一遍再重开。HTML 是导航请求，浏览器对它的重新验证
比对子资源积极得多，所以拿它当「真实版本」。

**两处必须一起改**，否则要么永远检测不出旧代码，要么每次启动都重开一遍。

`index.html` 里五张样式表的 `?v=` 也是这个构建号（第三处，同一个文件里），
样式表因此跟着页面换版。一起改，`check-build` 拦着（见 ARCHITECTURE 4.230）。

## 改完界面再跑一次冒烟

```
python3 -m http.server 8000 &
SMOKE_PW=<某个装了 playwright 的目录> node scripts/smoke.mjs
```

把每个 app 的每条路由都打开一遍，看有没有页面炸掉。

doctor 的「导入导出」只查模块之间的引用。**文件内部引用了一个不存在的函数
它查不到** —— 那种错要到运行时打开那个页面才炸，而且 ErrorBoundary 会把它
兜住，只留下一句「XX 已停止」。已经因此漏出去过两次。

本项目不装 npm 依赖（第 9 条），所以 playwright 从别处借，用 `SMOKE_PW` 指过去。
找不到就跳过，不拦提交。

## 改了哪一块，就跑那一块的回归测试

```
SMOKE_PW=<某个装了 playwright 的目录> node scripts/test.mjs            全部
SMOKE_PW=... node scripts/test.mjs group badges                         名字里带这些词的
```

测试在 `tests/*.test.mjs`，自己起静态服务器，不用先开 python。
**单独跑一个也走 `scripts/test.mjs <名字>`**：`src/site.js` 填了登录服务（`accounts`）时，
直接用 python 起的服务器打开应用会停在登录页；`scripts/test.mjs` 给出 site.js 时会把它抹掉。
**新写的测试放进 `tests/`，不要放在临时目录** —— 从前一百多个测试都在临时目录里，
容器一回收全没了。路径一律从 `tests/_env.mjs` 取，不写死 `/tmp`、`/opt`。

修 bug 的测试要先在旧代码上跑一遍，确认它真的会失败，再修。
